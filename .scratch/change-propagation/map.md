# Incremental Change Propagation

Label: `wayfinder:map`

## Destination

An implementation-ready, decision-complete plan for propagating only new,
modified, or retired source facts—and their correctness-required Affected-Key
Closure—from bronze to silver, silver to MDM and gold, and MDM to the hosted
Neo4j graph, with deterministic replay and one aligned Decision Watermark.

## Notes

- Repo: `edgartools-platform`. Originally decision-spec only; superseded
  2026-08-22 (Tickets 13-17 landed real, merged implementation, not just
  decisions) — this map now also carries implementation and a first prod
  dry run through to Ticket 29. Per-source-family production cutover and
  ongoing operation still happen in a later effort.
- AWS-only. Keep the current SEC EDGAR → warehouse CLI → S3/Snowflake → dbt →
  hosted graph architecture. Do not introduce another cloud, registry,
  workflow engine, storage target, or secret-management path.
- Scope locked during destination grilling and revised by Ticket 03 grilling on
  2026-08-21:
  - SEC is the external Source Authority; Bronze is the mandatory immutable
    evidence store for every successful relevant response; PostgreSQL is the
    sole local authority for acquisition and processing state; and Silver
    remains the authoritative Runtime System of Engagement for published
    business state.
  - Every SEC request requires a prior PostgreSQL Source Fetch Decision. A
    Logical Source Revision materializes only after verified Bronze capture,
    and PostgreSQL exposes one joined download/processing status per candidate.
  - A change includes `UPSERT`, `RETIRE`, and scoped replacement completion;
    history is preserved rather than physically deleted.
  - Delivery is at-least-once with content-addressed idempotency and
    deterministic convergence, not an infrastructure-level exactly-once claim.
  - Work is bounded by the Affected-Key Closure: directly changed inputs plus
    the entity, survivorship, relationship, and derived-table dependents needed
    for correctness—never unrelated full-universe work.
  - A Change Propagation Run is immutable and binds source versions/hashes,
    parser/schema versions, operation types, affected keys, expected producers,
    stage outcomes, and publication identities.
  - Silver, MDM, gold, and graph publish independently. A composite Decision
    Watermark becomes agent-grade only after every affected stage is complete
    and aligned.
  - MDM uses targeted re-resolution plus bounded match/survivorship closure and
    a periodic full-universe reconciliation backstop.
  - Gold refreshes the affected dbt dependency closure and records whether
    Snowflake performed incremental or full work.
  - The graph is an immutable candidate generation assembled from changed
    partitions plus content-addressed reuse of unchanged partitions, fully
    verified before atomic pointer activation.
  - Immutable manifests/deltas live in S3; cursors, leases, outbox entries, and
    outcome ledgers live in Snowflake Postgres, outside the silver artifact.
  - Initial bootstrap and recovery after ledger loss use an explicitly
    authorized Hybrid Source Baseline, complete non-serving Silver candidate,
    catch-up barrier, verification, and atomic activation into a new epoch.
  - Migration performs read-only reconciliation and cuts over
    boundary-by-boundary without concurrent canonical writers.
- Settled predecessor maps are inputs, not questions to reopen:
  - [Decoupled bronze pipeline](../decoupled-bronze-pipeline/map.md):
    S3→SNS→two SQS queues, per-accession completion events, N-or-timer batching,
    on-demand compute, retry/DLQ policy, and migration principles.
  - [Silver-on-Snowflake Migration](../silver-snowflake-migration/map.md) and
    [DuckDB Retirement](../duckdb-retirement/map.md): append-only Snowflake
    landing, dbt-native silver, Snowflake Postgres operational bookkeeping, and
    retirement of DuckDB readers/writers.
  - [MDM Entity Resolution Ahead of Silver](../mdm-ahead-of-silver/map.md):
    two-phase Snowflake backfill and independent sweep semantics.
  - [dbt Gold Silver Rewiring tickets](../dbt-gold-silver-rewiring/issues/):
    existing vertical migrations away from Python full-snapshot gold builders.
- Current-head facts established before charting:
  - Bronze replay selects complete CIK directories and has no durable consumed
    object/version cursor; an intact old checkpoint can mask a newer bronze
    snapshot.
  - Silver landing objects use table/date/run paths that collide when multiple
    windows share one execution run ID; current landing rows also lack
    tombstones and scope-completion records.
  - MDM change detection is company-only by content hash; other entity and
    relationship paths rescan broadly or fail to requeue modified rows.
  - The legacy gold path still builds full snapshots, and normal graph workflow
    tails can sync a new generation while verifying the previously active one.
- Use `/grilling` and `/domain-modeling` for grilling tickets, `/prototype` for
  concrete contract artifacts, and a `/research` subagent for research tickets.
  Before any later code change or restructuring proposal, use
  `/gof-refactor-reviewer` against the relevant code and git history; leave the
  design alone unless current costs justify a pattern.
- Acceptance must cover no-op replay, modification, retirement, scoped
  replacement, concurrent producers, partial failure/resume, out-of-order
  delivery, unchanged graph-partition reuse, bounded reconciliation, and an
  aligned Decision Watermark—with measured proof that unrelated rows were not
  processed.

## Decisions so far

<!-- Closed ticket decisions: one-line gist and link; detail stays in the ticket. -->

- [Verify Snowflake incremental change-processing primitives](issues/01-verify-snowflake-incremental-primitives.md) — Snowflake supports bounded incremental publication and aligned refresh evidence, but the application run ledger must own the non-atomic cross-stage barrier.
- [Inventory table-specific change and dependency semantics](issues/02-inventory-table-change-semantics.md) — All 31 landing tables are mapped; current writers lack a shared retirement/no-op contract, local replacement deletes do not propagate, and MDM/graph closure is dispersed rather than registry-owned.
- [Decide the Bronze capture, consumption ledger, and source cursor contract](issues/03-decide-bronze-consumption-ledger.md) — SEC owns source truth, mandatory verified Bronze owns raw evidence, PostgreSQL owns local acquisition/processing state, and Silver owns published business state; ledger-gated capture uses a narrow Facade, executable family policies, and bundled acquisition handlers while scope proof, status, and epoch recovery fail closed.
- [04 — Prototype the Change Propagation Run contract](issues/04-prototype-change-run-contract.md) — Re-grilled against 13 tickets of shipped code: no new schema needed. A Run is a derived identity (`cause_reference`, already populated by every family); Change Envelope = the existing Source Change term, UPSERT/RETIRE/SCOPE_COMPLETE = the existing Lifecycle Diff taxonomy, replay/repair linkage = existing idempotency + Ticket 25's REPAIR revisions. Publication identity is `cause_reference` as one input to the composite Decision Watermark CONTEXT.md already defines (Ticket 09 assembles the full tuple). Stage manifest/outcome-ledger shape for MDM/gold/graph and Affected-Key Closure expansion deliberately deferred to Tickets 06/07/08. Worked-example trace: [`assets/change-propagation-run-contract.md`](assets/change-propagation-run-contract.md).
- [05 — Decide silver delta publication and scope-completion semantics](issues/05-decide-silver-delta-publication.md) — Cross-checked against the settled Silver-on-Snowflake Migration map: producer/window/attempt/file identities, immutable landing paths, and concurrent producer ordering were already decided there, not reopened. Two genuine gaps found and decided: a new `Silver Landing Retirement Record` (CONTEXT.md term added) — a shared companion landing table the source family's own acquisition code writes into on a proved Scope Completion shrink, since the existing append-only window-function collapse structurally cannot represent retirement by omission alone; and a real completion barrier generalizing Ticket 19's `ExpectedProducerSet`/`SilverFinalizer` mechanism to Snowflake landing counts, layered on top of (not replacing) the already-live `target_lag=6h` refresh. Concrete DDL and wiring deferred to new [Ticket 35](issues/35-build-silver-landing-retirement-and-completion-barrier.md).
- [06 — Decide MDM affected-key closure and publication outbox](issues/06-decide-mdm-closure-and-outbox.md) — The "existing publication outbox" (`publication.py`) turned out fully built but completely dormant — zero production callers on either the enqueue or drain side. Decided: enqueue once per upstream `cause_reference`; bounded candidate-neighbor expansion is 1-hop (direct relationship edges only); domain-content-hash skip-if-unchanged generalizes to all 6 entity types; a periodic full-universe reconciliation backstop is needed to catch what 1-hop misses. Concrete work split across new [Ticket 36](issues/36-wire-mdm-publication-outbox.md) (outbox wiring), [Ticket 37](issues/37-generalize-mdm-skip-if-unchanged.md) (hash generalization), [Ticket 38](issues/38-design-mdm-full-universe-reconciliation-backstop.md) (backstop design). A drafted fix for merge/relationship-propagation was reverted before commit after primary-source evidence (`snowflake_graph.py`'s own comments) showed it was already correctly handled by an existing read-side mechanism (`GRAPH_ENTITY_MERGE_LINEAGE`), not a gap — see the ticket's own Answer for the full near-miss writeup.
- [07 — Decide gold affected-DAG refresh and status semantics](issues/07-decide-gold-affected-dag-refresh.md) — Corrected the ticket's own premise: `EDGARTOOLS_SOURCE`'s Python export is legitimate/permanent (per `single-path-per-layer`), only `gold_models.py`'s DuckDB-read dependency retires — deferred to `dbt-gold-silver-rewiring`'s own unblocked Ticket 07, not re-decided here. Snowflake refresh (confirmed live: `target_lag='DOWNSTREAM'`, not `SNOWFLAKE_RUN_MANIFEST_TASK` as CLAUDE.md's Phased Pipeline doc still says) is authoritative, mirroring silver. Affected-table mapping and the three external Explore models' isolation are already structurally solved (Snowflake's own DAG-reactive refresh; Explore models are already an isolated DAG branch per ADR 0001). Gold's Decision Watermark contribution reuses Snowflake's native per-table refresh version. One real open risk flagged, not resolved: whether `DOWNSTREAM` lag actually refreshes gold's mostly-leaf tables with acceptable freshness was never verified live — deferred, with the completion barrier, to new [Ticket 39](issues/39-verify-gold-downstream-lag-and-build-completion-barrier.md).
- [08 — Decide graph partition reuse and candidate-generation publication](issues/08-decide-graph-partition-reuse.md) — Nearly everything already fully built: `MdmGraphGeneration`/`MdmGraphPartition` already implement content-addressed cross-generation partition reuse, generation-scoped CLI commands, and a full lifecycle; retirement/eligibility/parity map onto the already-decided release-readiness vocabulary. The one genuinely real, still-open gap is this map's own charted fact from before any ticket existed — no mutual-exclusion mechanism prevents concurrent generation builds. Decided a partial unique index limiting non-terminal generations to one at a time, mirroring `uq_source_fetch_work_active_key`'s existing shape. Concrete migration deferred to new [Ticket 40](issues/40-serialize-concurrent-graph-generation-builds.md).
- [09 — Decide the cross-stage coordinator and composite watermark contract](issues/09-decide-cross-stage-coordinator.md) — The composite Decision Watermark already exists at the validation-logic layer (`decision_contract.py`'s `DecisionWatermark`/`evaluate_agent_grade`, real callers in the Agent Decision Surface read path) — this ticket's job was reconciling its `business_date` grain against everything Tickets 04-08 built at `cause_reference` grain (fine-grained signals feed the coarse one) and deciding how to wire it real data. Topology: an aggregator, not an orchestrator — several independently-triggered stage machines, unchanged, plus one new lightweight, scheduled (not event-driven) reconciler that only watches each stage's existing completion signal, never drives them. `graph_parity_ok` deliberately stands in for MDM completeness (no new field). No new retry/DLQ/repair on the aggregator itself — observe-only, repair happens through whichever stage's own existing mechanism is stuck. Concrete build deferred to new [Ticket 41](issues/41-build-cross-stage-watermark-aggregator.md), blocked on Tickets 35/36/39/40 all existing first. Closes this session's originally-requested Tickets 04-09 chain.
- [36 — Wire the MDM publication outbox into real MDM commits](issues/36-wire-mdm-publication-outbox.md) — Deviated from Ticket 06's literal "once per `cause_reference`" (that field doesn't exist under `edgar_warehouse/mdm/`): `MDMPipeline.run_all()` now enqueues one `request_publication` per invocation, keyed on `run_id`, gated on non-trivial output. New `mdm publication-drain` CLI command (+ off-by-default EventBridge schedule on the consolidated `mdm-utility` machine, mirroring Ticket 44's precedent) is the real coordinator, wired to the same `SnowflakeGraphSyncExecutor`/`SnowflakeGraphVerifier` machinery `mdm sync-graph`/`mdm verify-graph` already use in prod — deliberately *not* wired to Ticket 08's `generation.py` pipeline, which turned out to be pure Postgres bookkeeping with zero real Snowflake writes behind it (a significant, previously-undocumented gap, flagged as new fog rather than fixed here).
- [13 — Expand acquisition command registration](issues/13-expand-acquisition-command-registration.md) — Behavior-preserving Command registration seam landed; `capture-filing-artifact` and `drive-filing-discovery-for-date` are both migrated through it, unregistered commands untouched (PR #445, merged `d30cbfd8`).
- [14 — Establish the acquisition ledger and status spine](issues/14-establish-acquisition-ledger-status-spine.md) — PostgreSQL fenced ledger (`AcquisitionLedger`, role-owned transitions, monotonic observation positions) live; a no-download candidate resolves without network access (PR #445, merged `d30cbfd8`).
- [15 — Capture one filing-artifact family through the gated Facade](issues/15-capture-filing-artifact-through-gated-facade.md) — Non-bypassable capture Facade plus the `filing_artifact` Source Family Registry Strategy prove content-addressed Bronze capture with durable artifact-reference finalization; manually smoke-tested against real Postgres + a real live SEC filing (PR #446, merged `6e4079cc`).
- [16 — Drive filing capture from SEC change discovery](issues/16-drive-filing-capture-from-sec-change-discovery.md) — `drive-filing-discovery-for-date` seals the daily index into a digested Discovery Manifest and issues one Fetch Decision per candidate, replay-safe and per-candidate fault-isolated (PR #447, merged `bae5637e`).
- [17 — Make Bronze capture retry-safe and recoverable](issues/17-make-bronze-capture-retry-safe.md) — Retry-after-failure, durable Fetch Attempt evidence, and lease-gated orphan quarantine all land; a real Postgres-only stale-fencing-token exception-translation gap (found by Spec review) is fixed and regression-tested. 304/conditional-GET linking is deliberately deferred to [Ticket 28](issues/28-add-conditional-fetch-and-not-modified-linking.md) — no live caller exists yet for it.
- [18 — Materialize ordered logical source revisions](issues/18-materialize-ordered-logical-source-revisions.md) — New `SourceRevisionLedger` and `source_revision` table (own `edgartools_acquisition_processor` role) materialize immutable, ordered Logical Source Revisions from CAPTURED decisions or from parser/schema reinterpretation of already-verified Bronze evidence, with real-thread concurrency proof and behavioral identity tests. Ticket 04's blocking edge was stale for this subset — Ticket 03's own answer already named the three hashes, completeness declaration, and revision-identity composition; Ticket 04 stays open for the run-manifest/expected-producer-set/replay-linkage portions later stages still need.
- [19 — Complete the filing-to-Silver acceptance seam](issues/19-complete-filing-to-silver-acceptance-seam.md) — New `ProcessingLedger`/`SilverFinalizer` (generic, own `edgartools_acquisition_silver_finalizer` role) seal expected Silver producers per revision and record read-back-verified outcomes; `filing_artifact`'s bounded first slice writes/reads back DuckDB's `sec_raw_object`. Same-key ordering is DB-backed (partial unique index) plus a plain committed read — `SELECT ... FOR UPDATE` was tried and rejected live (PostgreSQL requires UPDATE privilege the processor role deliberately lacks). Ticket 05's blocking edge was stale for this DuckDB-targeted subset; stays open for the real Snowflake delta-publication contract.
- [31 — `EXCLUDED_OPERATIONAL_TABLES` content never reaches canonical silver once canonical exists](issues/31-excluded-operational-tables-never-reach-canonical-silver.md) — Two compounding bugs in `silver_protection.py` (a skip-if-unchanged fingerprint blind to `EXCLUDED_OPERATIONAL_TABLES`, and `merge_candidate_into_canonical` never copying those tables' content once canonical exists) meant daily-index checkpoint/staging writes never durably reached canonical. Fixed via a new `PUBLICATION_SIGNIFICANT_OPERATIONAL_TABLES` fingerprint+merge pass (PR #454, merged), verified live in prod.
- [29 — Deploy the gated acquisition path to prod and dry-run it](issues/29-deploy-and-dry-run-gated-acquisition-path.md) — Migration 013 applied to prod (two genuine provisioning bugs fixed, PR #453); once Ticket 31 unblocked canonical silver publication, a full-scope `drive-filing-discovery-for-date 2026-08-21` dry run (3,719 candidates, 614 captured) and its no-op replay both completed cleanly against real prod infrastructure — identical row_counts, zero re-fetches, zero errors on replay, proving idempotent convergence live.
- [20 — Version and activate the Acquisition Universe](issues/20-version-and-activate-acquisition-universe.md) — `SourceRegistryLedger`, migration 014, and `mdm registry-*` CLI make the Source Family Registry a versioned, Postgres-backed ledger proving catch-up before activation (PR #455); a real-Postgres integration suite found and fixed two genuine bugs (a flush-ordering race, a migration-rerun idempotency gap). Fully resolved once Ticket 32 closed its two remaining partial bullets.
- [32 — Wire Ticket 20's remaining registry policy fields, enforce the removal boundary, and bootstrap the first active version](issues/32-wire-remaining-registry-policy-fields-and-boundary-enforcement.md) — All four registry policy fields are now real dispatch/validation gates (validate-against-the-one-known-value, not speculative dispatch); `coverage_end_date` genuinely gates future acquisition, including a `business_date`-not-wall-clock correctness fix `/code-review` found and fixed live; `infra/scripts/bootstrap-source-family-registry.sh` is the committed, idempotent bootstrap path, smoke-tested against real Postgres. This fully closes Ticket 20.
- [21 — Migrate submissions snapshots and pagination](issues/21-migrate-submissions-and-pagination.md) — New `submissions` Source Family (Strategy, two-phase CIK-driven discovery, Silver acceptance sealing `sec_company`/`sec_company_filing`) proves inventory-aware completeness: a main candidate's scopes retire only once every discovered pagination file has independently settled. Bootstrap script now activates both families in one draft. TDD caught and fixed a genuine replay-safety bug (candidate IDs were keyed by run-derived data); `/code-review` found and fixed a missing `UnsupportedRequiredProducers` gate (Ticket 32's pattern hadn't been ported to this sibling) plus two Duplicated Code smells. Merged as `0160e168` via PR #458.
- [22 — Migrate company-facts snapshots](issues/22-migrate-company-facts-snapshots.md) — New `company_facts` Source Family (single-phase this time — one JSON response per CIK, no pagination inventory), sealing `sec_financial_fact`/`sec_accounting_flag`. Advisor consultation before coding ruled out DELETE-based retirement (`sec_financial_fact` has no validity-interval column, and spec.md forbids physical deletion) — Scope Completion is a recording requirement (count+digest in `scope_reference`) instead. Three bullets left honestly partial rather than silently claimed complete: conditional-fetch (bullet 1, same gap as submissions, closes via Ticket 28), the "retired" outcome kind (bullet 4, needs a schema change — graduated to [Ticket 33](issues/33-add-validity-interval-retirement-to-financial-facts.md)), and full Facade purity (bullet 5, blocked on Ticket 27's legacy-bypass removal). Merged (PR #460).
- [23 — Migrate reference catalogs](issues/23-migrate-reference-catalogs.md) — New `reference_catalog` Source Family, per-source-name over a fixed 2-item set (`company_tickers`/`company_tickers_exchange`), sealing `sec_company_ticker`. Unlike company-facts, this table already supports retirement via the legacy `replace_company_tickers`'s delete-then-insert -- reused unmodified, gated behind CAPTURED-and-complete. Surfaces (does not fix) a real pre-existing gap this ticket confirmed at a new layer: `merge_candidate_into_canonical` never shrinks canonical, so that local retirement doesn't propagate there -- Ticket 02's inventory already knew this for the Snowflake-export layer. `/code-review` found and fixed a naming-convention break plus a real false-FAILED verification bug (a blank-ticker catalog row silently dropped by the writer but still counted in the expected set). Bullet 1 (conditional-fetch) stays the same honest partial as its siblings, closing via Ticket 28. Merged as `f9c5cc5d` via PR #462.
- [24 — Migrate ADV sources](issues/24-migrate-adv-sources.md) — Two Strategies, not three: `adv_bulk_dataset` (`AdvBulkDatasetPolicy`, one Strategy over both IAPD ADV bulk archives and Firm Roster archives -- both complete-snapshot-per-`dataset_period`, same shape as Ticket 23's `reference_catalog`) and `adv_filing` (accession identity, reuses `FilingArtifactPolicy` verbatim under a distinct family/coverage). Resolving *which* archive URL to fetch (`reports_metadata.json`/the Firm Roster listing) is treated as a discovery input, not a Fetch Decision, reusing `adv_bulk_fetch.py`/`firm_roster_fetch.py`'s own period-selection logic unmodified. Silver writes reuse `ingest_adv_bulk_archive`/`ingest_firm_roster_archive` unmodified (preserving the real `fund_index` SMALLINT-overflow fix), verified via parse-count-vs-write-count reconciliation. `adv_bulk_dataset`'s half merged via PR #464; bullet 4's remaining `adv_filing` driver gap closed in a follow-up pass: `drive_filing_discovery.py` generalized to a shared `source_family`-parameterized body (`run_drive_filing_discovery_for_date`/`run_drive_adv_filing_discovery_for_date` are now thin wrappers over it), plus a real, previously-unexercised bug found and fixed along the way -- `discovery_candidate_id` had no `source_family` component, so two families' drivers sharing one sealed daily index collided on the same accession's Fetch Decision; fixed while deliberately preserving `filing_artifact`'s exact legacy id format (Ticket 29 already has real prod ledger rows keyed on it) so the fix doesn't break replay-idempotency for the one family with live history. `/code-review`'s Spec pass also caught an overclaim in this ticket's first draft: the new driver adds a ledger-gated path, it does not retire `warehouse_orchestrator.py`'s pre-existing `fetch_filing_artifacts` artifact-fetch pipeline (which still independently captures `ADV_FORMS`, same as it always has for `filing_artifact`'s own `OWNERSHIP_FORMS` since Tickets 16/17/29 -- a pre-existing, undisclosed overlap, not new here) -- retiring either is Ticket 27's job, unchanged.

- [25 — Add conflict, repair, exclusion, and evidence-import workflows](issues/25-add-conflict-repair-and-evidence-import.md) — Resolved conflict/repair/role-separation (bullets 1, 2, 5); exclusion/evidence-import split to [34 — Add exclusion and evidence-import workflows](issues/34-add-exclusion-and-evidence-import-workflows.md). `object_storage.write_immutable_bytes` now quarantines a conflicting payload to a content-addressed sibling and raises `ImmutableContentConflictError` instead of discarding it on a bare error (traced to the real trigger: only the legacy identity-keyed Bronze writers can hit this -- the new Facade's own capture path is content-hash-keyed, so a mismatch there is structurally impossible). New `SourceRevisionLedger.materialize_repair` fills `RevisionRelationship.REPAIR`, the slot Ticket 18 reserved but never built; new `acquisition/conflict.py`'s `ConflictLedger` records conflicts and resolves them (race-safe via a conditional `UPDATE ... WHERE status='PENDING'`, not read-then-write). New migration `015_source_evidence_conflict.sql` -- caught and fixed a real bug live against Postgres before committing: the table must be owned by 013's `edgartools_acquisition_owner` (same as `source_revision`), not `edgartools_acquisition_processor` -- `CREATE` on schema `public` was never granted to any of the five *operational* roles, only the owner role; the first draft's wrong ownership choice reproduced `permission denied for schema public` running as `application`. Bullet 5 (role separation) was already built by 013 -- added the missing proof at the GRANT level (real Postgres, not SQLite) rather than trusting the happy-path tests already in place.
- [33 — Add validity-interval retirement to financial facts](issues/33-add-validity-interval-retirement-to-financial-facts.md) — Both `sec_financial_fact` and `sec_accounting_flag` gained `valid_from`/`valid_to`/`is_current`; retirement is a per-CIK full-scope `UPDATE` closing the interval (never a `DELETE`), keyed off each table's own `is_current=TRUE` rows as the prior-snapshot proxy; reinstatement runs through the existing merge methods' `ON CONFLICT` branch. Found and fixed two real bugs along the way: a NOT-NULL backfill break in the existing `_backup_and_recreate_table` schema-migration path, and a Snowflake landing-export gap where ordinary (non-retiring) writes never populated `is_current`/`valid_to` at all. `/code-review` found zero missing/wrong requirements; one documented, non-blocking semantic-drift note (`valid_from` is last-write-wins in the Snowflake landing/dbt collapse vs. genuinely first-insert-wins in DuckDB).
- [37 — Generalize the domain-content-hash skip-if-unchanged path to every MDM entity type](issues/37-generalize-mdm-skip-if-unchanged.md) — No new wiring needed: a separate, already-merged [`mdm-resolver-skip-unchanged`](../mdm-resolver-skip-unchanged/map.md) map had already audited and closed this for company/person/security (content-hash) and adviser/fund (existence-based dedup in `adv_bulk.py`, the classes' dead-code equivalents deleted); audit_firm has no row-loop resolver, not applicable. Writing this ticket's own required per-entity-type test surfaced a real, different gap that audit-by-reading-code had missed: fund's dedup keys on `private_fund_id`, so a later accession amending a known fund updates the golden record but emits no `MdmChangeLog` row, silently starving `MDMExporter.export_pending` — filed separately as [42](issues/42-fund-dedup-keyed-on-pfid-starves-mdm-export.md) rather than fixed inline (different bug shape, unsized backlog cost).
- [42 — Fund bulk resolution's pfid-keyed dedup starves MDM export of change signal](issues/42-fund-dedup-keyed-on-pfid-starves-mdm-export.md) — Re-keyed `resolve_funds_bulk`'s dedup to always be `accession_number:fund_index` (matching adviser), never `private_fund_id`. Live prod sizing (read-only query, no destructive action): 130,614 of 130,615 fund `MdmSourceRef` rows are pfid-keyed, so the fix causes a real, exactly-once ~130,614-fund re-stage backlog on its first post-deploy `mdm run` — sized at ~185 additional batched inserts under the module's existing 5,000-row chunking, and absorbed on the export side by `MDMExporter.export_all_pending`'s existing batch loop. Bounded and within the code's designed capacity, so shipped without a user check-in. Real production code change (`edgar_warehouse/mdm/adv_bulk.py`) — needs a warehouse image rebuild + deploy before live, unlike Ticket 37's test-only diff.
- [30 — Fence `application` from acquisition ledger tables under Snowflake Postgres's `snowflake_write` role](issues/30-fence-application-from-acquisition-tables-under-snowflake-write.md) — Confirmed live: `application` ambiently inherits full DML on acquisition/registry tables via Snowflake Postgres's managed `snowflake_write` role (a standing `pg_default_acl` rule for new tables), bypassing migration 013's `application`-scoped `REVOKE`s. Deploying the fix live surfaced a second, more severe platform behavior: `ALTER POSTGRES INSTANCE ... RESET ACCESS FOR '<role>'` re-grants `snowflake_write`'s access to *already-fenced* tables as a side effect, for both `snowflake_admin` and `application` — meaning `bootstrap-prod-mdm.sh`'s own normal run order was silently reopening the fence on every run. Also found migration 014 (Ticket 20) had the identical original gap 013 once had. Both fixed (PR #475): `bootstrap-prod-mdm.sh` now re-applies the REVOKE as its true last step; 014 got 013's `snowflake_write` block. Verified live: zero leaks across all 11 objects × both roles × SELECT/INSERT/UPDATE/DELETE. Monitoring the fence against future out-of-band rotations is deliberately deferred to new [44](issues/44-monitor-snowflake-write-privilege-drift.md).
- [43 — Migration 013's owner-gated statements have no live deploy path via the `application` DSN](issues/43-acquisition-owner-migrations-unreappliable-via-application-dsn.md) — `application` (the only credential the standard `mdm migrate` deploy path uses) has no membership in `edgartools_acquisition_owner` by design, so a rerun of an already-installed owner-gated migration silently no-ops. The already-existing privileged path is `bootstrap-prod-mdm.sh`'s `snowflake_admin`-driven `mdm migrate` call — no new script or role-grant needed; rejected granting `application` a narrow owner membership as undermining migration 013's own deliberate security boundary. Also made the silent skip loud (`mdm_migration_privileged_rerun_skipped` event, all three owner-gated migrations) with a real-Postgres test proof. The live prod run itself (rotates real credentials) is explicitly deferred pending the user's go-ahead — completing it also finishes Ticket 30's own remaining live-verification step.
- [44 — Monitor for `snowflake_write` privilege drift on fenced acquisition tables](issues/44-monitor-snowflake-write-privilege-drift.md) — `mdm check-fence` (new `edgar_warehouse/mdm/fence_monitor.py`) discovers the fenced-table set live rather than hardcoding it, joins the `mdm_utility` state machine as a new mode, and adds an off-by-default EventBridge schedule + two CloudWatch alarms (leak/access-gap, both `treat-missing-data=breaching`). Checks both sides deliberately — the deny side Ticket 30 found, and a new `OperationalAccessGap` allow-side check (a table's owner losing its own SELECT) added after review flagged a deny-only monitor would miss the manifest-pipeline-ownership incident's shape applied to this table set. Live-verified through the real `application` DSN (no elevated credential needed) and caught a genuine third instance of Ticket 30's exact gap along the way — migration 015's `source_evidence_conflict` table had the same missing `snowflake_write` REVOKE 013 and 014 once had, found only because this ticket's dynamic table discovery surfaced it automatically. Schedule/alarms committed off by default, not yet enabled in prod.
- [40 — Serialize concurrent graph generation builds](issues/40-serialize-concurrent-graph-generation-builds.md) — A partial unique index (`uq_graph_generation_single_non_terminal`, mirroring `uq_source_registry_version_single_active`) limits `mdm_graph_generation` to at most one non-terminal row at a time; a rejected build raises a new `ConcurrentGenerationBuildRejected` and fails outright. Proven both via SQLite unit tests and a real-Postgres genuine-concurrent-threads integration test. Found while implementing: `mdm sync-graph`'s live CLI path doesn't actually call `create_generation()` at all (it tags Snowflake rows with a bare UUID instead) — `mdm_graph_generation` is confirmed empty in prod today, so this guard is correctly built but not yet a live production caller; wiring `sync-graph` onto it remains open and unticketed. PR [#485](https://github.com/paulananth/edgartools-platform/pull/485).
- [34 — Add exclusion and evidence-import workflows](issues/34-add-exclusion-and-evidence-import-workflows.md) — Resolved the ticket's own open design question: `exclusion_reason` is a column on `source_fetch_decision` (CHECK-enforced), not a new table — an exclusion has no independent lifecycle, unlike a conflict. New `EvidenceImportLedger`/`source_evidence_import` mirrors `ConflictLedger`'s shape; checksum-verified, fails closed, reuses the Facade's own content-hash-keyed Bronze path, and needs no new `FetchDisposition`/Facade path since the resulting local reference is directly usable as an ordinary `ALREADY_CAPTURED_VERIFIED` `verified_evidence_reference`. `/code-review` caught a real gap (the actual `source_change_status` operator-facing view, not just the Python dataclass, needed widening) and a real structural bug (widening that view broke 013's own unconditional rerun once an owner-privileged `migrate()` runs again — a genuine disaster-recovery scenario this repo relies on — fixed with a defensive exception-swallowing wrap in 013 itself, proven by a real double-rerun regression test).
- [10 — Decide baseline, migration, cutover, and rollback sequencing](issues/10-decide-migration-cutover-rollback.md) — Grounded against live state before deciding: the new ledger-gated drivers have zero scheduled presence in prod today (confirmed via `deploy-aws-application.sh`), and in-flight-execution handling turned out to already be a non-issue (AWS Step Functions pins an execution to its start-time definition). Decided: cutover is per-family and independent, not one coordinated platform-wide epoch; a family's legacy capture call retires only after a real side-by-side production window proves the new path an equal-or-superset with zero silent gaps; no Ticket 26 baseline rebuild is needed for ordinary cutover (reserved for genuine ledger loss only); cutover mechanically means wiring the new driver into the existing `daily_incremental`/`load_history` state machines, replacing the legacy call. Consequence: Ticket 26 was never a genuine prerequisite for Ticket 27 and is removed from its blockers — both are now open and unblocked. Data retention (DuckDB/mutable landing/legacy SOURCE) is out of this map's scope entirely, deferred to the DuckDB Retirement map; coordination between the two cutovers tracked as new [Ticket 45](issues/45-coordinate-with-duckdb-retirement-cutover.md).
- [45 — Coordinate with the DuckDB Retirement map's cutover](issues/45-coordinate-with-duckdb-retirement-cutover.md) — A real, one-directional constraint: DuckDB Retirement's plan (decided 2026-08-16) predates and never accounts for `silver_acceptance.py` (a hard `SilverDatabase` dependency this map's own Ticket 46 introduced on 2026-08-27), so its atomic write-path cutover would silently orphan or break `filing_artifact`'s Silver-acceptance verification if it landed first. Nothing on this map is blocked by it — Ticket 27's own critical path doesn't touch storage-layer choice — so the fix routes entirely to the other map: filed as [DuckDB Retirement's Ticket 09](../duckdb-retirement/issues/09-account-for-silver-acceptance-in-write-path-cutover.md) — **resolved 2026-08-28**: `silver_acceptance.py` plus four dormant sibling `*_silver_acceptance.py` modules found by sweeping this map's Domain (`reference_catalog_`/`company_facts_`/`submissions_`/`adv_bulk_dataset_silver_acceptance.py`, all created 2026-08-23 to 2026-08-25) all join that map's Ticket 01 atomic cutover bundle. DuckDB Retirement's map is ready to hand off for implementation again.
- [48 — Add a per-candidate progress counter to gated discovery](issues/48-add-gated-discovery-progress-counter.md) — `gated_discovery_started` logs the sealed-manifest total once; `gated_discovery_progress` logs `processed`/`candidate_count` every 100 candidates, every 30s, and on the last candidate, via `_emit_pipeline_event`. `drive_discovery_manifest` only gained an optional `on_progress` callback; it does not emit events itself.
- [35 — Build the Silver Landing Retirement Record and the Snowflake completion barrier](issues/35-build-silver-landing-retirement-and-completion-barrier.md) — Companion landing table + `silver_not_retired` anti-join on every collapsed dbt silver model; `reference_catalog` writes retirement rows on a proved shrink; Snowflake-landing producer kind settles via injected COUNT(*) and does not change `target_lag = '6 hours'`.
- [28 — Add conditional-fetch validators and not-modified linking](issues/28-add-conditional-fetch-and-not-modified-linking.md) — `download_sec_conditionally` + `latest_verified_capture` + `execute_due_repoll` (`DUE_POLICY`). A `304` links the prior Bronze reference with no write; a `200` uses Ticket 15 capture. Validators persist in the same fenced `finalize_source_fetch` write (9-arg; the 7-arg sidecar UPDATE would permission-deny). First DUE_POLICY poll is unconditional because `SourceFamilyPolicy.fetch()` still returns bytes.
- [38 — Design the periodic MDM full-universe reconciliation backstop](issues/38-design-mdm-full-universe-reconciliation-backstop.md) — **MDM Reconciliation Backstop**: full `run_all()` with skip-if-unchanged off and no `--limit`; catches multi-hop, near-miss, and hash-skip; not `verify-graph` and not Identity Backstop Sweep. Monthly, off-by-default, exclusive lease vs ordinary `mdm run`. Review-band writes `MdmMatchReview`. Follow-ups: [49](issues/49-implement-one-hop-mdm-candidate-neighbor-expansion.md) (1-hop) and [50](issues/50-build-mdm-reconciliation-backstop.md) (this pass).
- [39 — Verify gold's DOWNSTREAM refresh lag live, and build its completion barrier](issues/39-verify-gold-downstream-lag-and-build-completion-barrier.md) — Live prod: all 21 gold DTs were DOWNSTREAM but refresh history is MANUAL-only. `gold_model_config()` now `target_lag='6 hours'`. `verify_gold_dynamic_table_producer` fail-closes on a stale `data_timestamp`. `gold_watermark_contribution` exposes per-table timestamps for Ticket 41.
- [41 — Build the cross-stage watermark aggregator](issues/41-build-cross-stage-watermark-aggregator.md) — Observe-only `reconcile_cause_reference` + `rollup_business_date` → `evaluate_agent_grade`. 5/15-minute SLO names `stuck_stage`. SQL sketches: MDM_COMPANY_ENTITY universe; BUNDLE_AUDITOR reads EDGARTOOLS_SOURCE. CLI `reconcile-decision-watermark`.
- [51 — Build the 1-CIK then 100-CIK filing_artifact capture-parity harness](issues/51-build-filing-artifact-capture-parity-harness.md) — Ticket 10 Decision 2 compare: gated equal-or-superset of legacy on Logical Source Keys / Verified Source Evidence / Source Fetch Decision rows. Stage 1 default Apple CIK 320193; stage 2 `--limit 100`. CLI `compare-filing-artifact-capture`. A CIK-scoped `drive-filing-discovery-for-date` does not record family catch-up. Does not retire legacy capture (Ticket 27) and does not mix Identity Backstop Sweep or MDM Reconciliation Backstop.
- [52 — Check whether the capture-parity harness exercises the legacy path](issues/52-check-harness-exercises-legacy-capture.md) — Confirmed: Ticket 51 never runs legacy capture. "Legacy" snapshots are hand-built fixtures; the only live driver added is gated `--cik-list`. Ticket 46 still *invokes* both paths in `daily-incremental` when the flag is on, but nothing diffs their outputs. Follow-up [53](issues/53-drive-legacy-and-gated-capture-into-parity-diff.md).

## Ticket 03 implementation tickets

<!-- Agent-grabbable tracer bullets; blocking edges live in each ticket. -->

- [19 — Complete the filing-to-Silver acceptance seam](issues/19-complete-filing-to-silver-acceptance-seam.md) — Verify publication or explicit non-publication while protecting prior Silver authority.
- [20 — Version and activate the Acquisition Universe](issues/20-version-and-activate-acquisition-universe.md) — **Resolved** — see Decisions so far above.
- [32 — Wire Ticket 20's remaining registry policy fields, enforce the removal boundary, and bootstrap the first active version](issues/32-wire-remaining-registry-policy-fields-and-boundary-enforcement.md) — **Resolved** — see Decisions so far above.
- [21 — Migrate submissions snapshots and pagination](issues/21-migrate-submissions-and-pagination.md) — **Resolved** — see Decisions so far above.
- [22 — Migrate company-facts snapshots](issues/22-migrate-company-facts-snapshots.md) — **Resolved (partially)** — see Decisions so far above.
- [23 — Migrate reference catalogs](issues/23-migrate-reference-catalogs.md) — **Resolved (partially)** — see Decisions so far above.
- [24 — Migrate ADV sources](issues/24-migrate-adv-sources.md) — **Resolved** — see Decisions so far above.
- [25 — Add conflict, repair, exclusion, and evidence-import workflows](issues/25-add-conflict-repair-and-evidence-import.md) — **Resolved (partially)** — see Decisions so far above.
- [34 — Add exclusion and evidence-import workflows](issues/34-add-exclusion-and-evidence-import-workflows.md) — **Resolved** — see Decisions so far above.
- [26 — Rebuild and activate a ledger epoch](issues/26-rebuild-and-activate-ledger-epoch.md) — Recover authority through a Hybrid Source Baseline and atomic activation. Ready-for-agent; per Ticket 10, reserved for genuine ledger-loss disaster recovery only, no longer on Ticket 27's critical path.
- [27 — Contract legacy acquisition bypasses](issues/27-contract-legacy-acquisition-bypasses.md) — Remove bypasses only after every source family proves the authoritative path. Ticket 26 removed from its blockers, but now additionally blocked on new Ticket 46 — its removal-evidence bullets need a real Decision-2 side-by-side window to exist for at least one family first, and none has run yet.
- [45 — Coordinate with the DuckDB Retirement map's cutover](issues/45-coordinate-with-duckdb-retirement-cutover.md) — **Resolved** — see Decisions so far above.
- [46 — Wire `filing_artifact`'s gated driver into `daily_incremental`](issues/46-wire-filing-artifact-into-daily-incremental.md) — **Resolved**: in-process chosen over a separate Step Functions state (the load-bearing reason is the shared `sec_fetch_active` lease `daily_incremental`'s ASL already holds around the whole task — see the ticket's Answer), gated behind an off-by-default `--enable-filing-artifact-gated-capture` flag, zero ASL changes needed. Live-prod verification completed 2026-08-28: gated-capture path ran clean against all 4,491 sealed daily-index candidates for 2026-08-26, exit code 0, zero errors, both paths diffable as designed.
- [47 — `bootstrap-prod-mdm.sh`'s follow-up REVOKE fails deterministically on a freshly owner-transferred table](issues/47-bootstrap-prod-mdm-revoke-fails-on-fresh-owner-transfer.md) — Found while unblocking Ticket 46: a reproducible (2/2) script bug, low impact (the real privilege state was already correct both times), root cause not yet confirmed.
- [48 — Add a per-candidate progress counter to gated discovery](issues/48-add-gated-discovery-progress-counter.md) — **Resolved** — see Decisions so far above.
- [28 — Add conditional-fetch validators and not-modified linking](issues/28-add-conditional-fetch-and-not-modified-linking.md) — **Resolved** — see Decisions so far above.
- [29 — Deploy the gated acquisition path to prod and dry-run it](issues/29-deploy-and-dry-run-gated-acquisition-path.md) — **Resolved** — see Decisions so far above.
- [30 — Fence `application` from acquisition ledger tables under Snowflake Postgres's `snowflake_write` role](issues/30-fence-application-from-acquisition-tables-under-snowflake-write.md) — **Resolved** — see Decisions so far above.
- [31 — `EXCLUDED_OPERATIONAL_TABLES` content never reaches canonical silver once canonical exists](issues/31-excluded-operational-tables-never-reach-canonical-silver.md) — **Resolved** — see Decisions so far above.
- [33 — Add validity-interval retirement to financial facts](issues/33-add-validity-interval-retirement-to-financial-facts.md) — **Resolved** — see Decisions so far above.
- [35 — Build the Silver Landing Retirement Record and the Snowflake completion barrier](issues/35-build-silver-landing-retirement-and-completion-barrier.md) — **Resolved** — see Decisions so far above.
- [36 — Wire the MDM publication outbox into real MDM commits](issues/36-wire-mdm-publication-outbox.md) — **Resolved** — see Decisions so far above.
- [37 — Generalize the domain-content-hash skip-if-unchanged path to every MDM entity type](issues/37-generalize-mdm-skip-if-unchanged.md) — **Resolved** — see Decisions so far above.
- [42 — Fund bulk resolution's pfid-keyed dedup starves MDM export of change signal](issues/42-fund-dedup-keyed-on-pfid-starves-mdm-export.md) — **Resolved** — see Decisions so far above.
- [38 — Design the periodic MDM full-universe reconciliation backstop](issues/38-design-mdm-full-universe-reconciliation-backstop.md) — **Resolved** — see Decisions so far above.
- [49 — Implement 1-hop MDM candidate-neighbor expansion](issues/49-implement-one-hop-mdm-candidate-neighbor-expansion.md) — Ticket 06's incremental pass, still unbuilt. Ready-for-agent; does not block Ticket 50.
- [50 — Build the MDM Reconciliation Backstop](issues/50-build-mdm-reconciliation-backstop.md) — Ticket 38's pass. Ready-for-agent; does not wait on Ticket 49.
- [51 — Build the 1-CIK then 100-CIK filing_artifact capture-parity harness](issues/51-build-filing-artifact-capture-parity-harness.md) — **Resolved** — see Decisions so far above.
- [52 — Check whether the capture-parity harness exercises the legacy path](issues/52-check-harness-exercises-legacy-capture.md) — **Resolved** — see Decisions so far above.
- [53 — Drive both legacy and gated capture into the parity diff](issues/53-drive-legacy-and-gated-capture-into-parity-diff.md) — Ticket 52 follow-up: actually run both producers, then Ticket 51's compare. Ready-for-agent.
- [39 — Verify gold's DOWNSTREAM refresh lag live, and build its completion barrier](issues/39-verify-gold-downstream-lag-and-build-completion-barrier.md) — **Resolved** — see Decisions so far above.
- [40 — Serialize concurrent graph generation builds](issues/40-serialize-concurrent-graph-generation-builds.md) — **Resolved** — see Decisions so far above.
- [41 — Build the cross-stage watermark aggregator](issues/41-build-cross-stage-watermark-aggregator.md) — **Resolved** — see Decisions so far above.
- [43 — Migration 013's owner-gated statements have no live deploy path via the `application` DSN](issues/43-acquisition-owner-migrations-unreappliable-via-application-dsn.md) — **Resolved** — see Decisions so far above.
- [44 — Monitor for `snowflake_write` privilege drift on fenced acquisition tables](issues/44-monitor-snowflake-write-privilege-drift.md) — **Resolved** — see Decisions so far above.

## Not yet specified

- Exact production canary thresholds and live rollout gates; these become
  specifiable after the acceptance artifact and migration sequence are locked.
- Whether/how Ticket 08's generation-builder pipeline (`generation.py`) ever
  gets wired to a real Snowflake writer — found while resolving Ticket 36
  that `build_partition` is pure Postgres bookkeeping with zero Snowflake
  writes behind it, so `mdm_graph_generation` staying empty in prod (also
  independently noted by Ticket 40) isn't a bug in either of those two
  tickets, it's this. Not yet sharp enough to ticket: needs a decision on
  whether to wire it to `SnowflakeGraphSyncExecutor` (Ticket 36's
  `publication-drain` coordinator's own approach), repurpose it, or retire
  it in favor of the simpler `sync-graph`/`verify-graph` pair.

## Out of scope

- Replacing Silver as the Runtime System of Engagement or treating Bronze as a
  competing processing-state authority beside PostgreSQL.
- Re-deciding the settled AWS messaging substrate, per-accession event grain,
  on-demand compute stance, Snowflake-native silver target, or DuckDB retirement.
- Physical deletion of SEC history, best-effort partial graph activation, or
  exposing a misaligned watermark as agent-grade.
- Non-AWS deployment/storage paths, broker execution, portfolio management, or
  unrelated dashboard/product work.
- Full-universe production cutover for every source family, and ongoing
  production operation of the gated path — Ticket 29 covers only a first,
  bounded, single-family prod dry run; broader rollout is a later effort.
