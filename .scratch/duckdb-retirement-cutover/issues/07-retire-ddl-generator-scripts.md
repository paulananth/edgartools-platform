# 07 — Retire the DuckDB-Backed DDL Generator Scripts

**What to build:** DuckDB Retirement's Ticket 06 corrected its own premise
first: `infra/scripts/generate_silver_landing_ddl.py` never introspected a
live DuckDB instance — it only ever used an ephemeral `:memory:` DuckDB
connection, seeded from the `silver_store._DDL` string, purely as a
SQL-parsing engine to emit Snowflake DDL. A second caller with the identical
pattern was found: `generate_silver_dbt_models.py`.

Delete both scripts entirely — no successor generator. 13 of the 14
Snowflake bootstrap SQL files in `infra/snowflake/sql/bootstrap/` are
already hand-maintained; these two generators were the one exception.
Future schema changes to the bookkeeping/landing tables become a direct
hand-edit, matching every other bootstrap SQL file in this repo.

This is distinct from Ticket 02's bookkeeping-table SQLAlchemy models
(different tables — the 11 tables moving to Snowflake Postgres — different
platform, and a genuine ORM use case there, unlike this ticket's
ephemeral-parser use case).

**Blocked by:** None — can start immediately.

**Status:** done

- [x] `infra/scripts/generate_silver_landing_ddl.py` is deleted (along with
      its dedicated regression test, `tests/unit/test_generate_silver_landing_ddl.py`
      — the only test that exercised the deleted script directly, so it has
      nothing left to import).
- [x] `infra/scripts/generate_silver_dbt_models.py` is deleted (no dedicated
      test existed for it).
- [x] `infra/snowflake/sql/bootstrap/11_silver_landing_schema.sql` and the
      31 committed `infra/snowflake/dbt/edgartools_gold/models/silver/*.sql`
      files (the two generators' most recent outputs) are confirmed still
      correct and are now the hand-maintained sources of truth. Their
      headers ("Generated by ... regenerate with ...", "Auto-generated by
      ... do not hand-edit") were stale the moment the generators were
      deleted, so all 32 files got a rewritten header stating they're
      hand-maintained now, with no regeneration step. `sources.yml`'s
      comment above the `edgartools_silver_landing` source (pointed at the
      deleted generator for "keep in lockstep") was corrected the same way.
      No CLAUDE.md/docs reference existed to begin with (grepped, zero
      hits) — item below was already trivially satisfied.
- [x] Beyond CLAUDE.md (already clean): found and fixed 6 additional stale
      references the checklist's own wording didn't name but its spirit
      clearly covers — `infra/snowflake/sql/bootstrap/13_silver_landing_ingest.sql`
      and `14_silver_landing_mdm_entity_id.sql` both cited the deleted
      generator as "the source of truth"/schema origin; 4 of the 31 silver
      dbt models (`sec_accounting_flag.sql`, `sec_company_filing.sql`,
      `sec_financial_derived.sql`, `sec_financial_fact.sql`) cited the
      deleted generator's internal `_SPLIT_TABLES`/`_COALESCE_PRESERVING_COLUMNS`
      dicts "for the citation" backing their first-insert-wins/last-write-wins
      column splits — real design rationale that would have been lost
      outright, not just a dangling pointer. Recovered the actual citations
      from the deleted script's `git show HEAD:...` content, verified each
      one's mutable/immutable column list against `silver_store.py`'s
      current `merge_filings`/`merge_financial_facts`/`merge_financial_derived`/
      `merge_accounting_flags` `ON CONFLICT ... DO UPDATE SET` clauses
      directly (not trusted from the old comment) before inlining —
      deliberately cited by function name only, not line numbers: the
      deleted script's own line-number citations (e.g. `silver_store.py:1581-1680`
      for `merge_filings`, now actually at line 1799) had already drifted
      stale even before this ticket, confirming line numbers are the wrong
      thing to copy forward. One more (a gold-layer model,
      `accounting_flags.sql`) cited the same deleted dict by name for an
      already-fixed-upstream note; corrected the same way. Two more
      (`edgar_warehouse/serving/silver_landing_export.py`,
      `tests/unit/test_silver_landing_export.py`) cited the deleted
      generator only for a stable fact (table-naming convention, table
      scope) that doesn't depend on the generator's continued existence —
      repointed at the enduring hand-maintained artifacts instead
      (`11_silver_landing_schema.sql`) rather than a script that no longer
      exists.
- [x] Grep confirms no other script or CI step invokes either deleted file
      — checked both plain filename references and actual invocation
      patterns (`uv run python infra/scripts/generate_silver...`,
      `python3 infra/scripts/generate_silver...`) across every `.py`/`.sh`/
      `.yml`/`.yaml`/`.sql`/`.md` file outside `.scratch/` (left untouched
      as historical record, not live references). Zero hits beyond the
      intentional "originally generated by ... deleted by Ticket 07"
      headers this ticket itself wrote.
- [x] Full test suite green (see commit message for the exact count) —
      including a live rerun of `tests/unit/test_silver_landing_export.py`
      and `tests/unit/test_silver_landing_retirement.py`/
      `tests/acquisition/test_processing.py` (the tests that reference the
      dbt/silver model paths touched above) to confirm the edited
      docstrings/comments didn't break anything they describe.

**Three-axis review (Standards/Spec/GoF, CLAUDE.md hard rule):** Spec —
clean pass, all 5 checklist items verified independently against the code,
the ~38-file breadth beyond the two literal script deletions judged
in-scope (serves the checklist's own "no stale reference to a deleted
generator anywhere" intent, confirmed comment-only with zero SQL logic
changes across all 31 dbt models). GoF — no findings; the "one generator
vs. N hand-maintained files" tradeoff was already deliberately adjudicated
by this map's own wayfinder ticket, so re-litigating it here would reopen a
closed decision, not fix a fresh smell. Standards — one real finding, fixed:
deleting `tests/unit/test_generate_silver_landing_ddl.py` correctly retires
coverage for the *generator*, but the *invariant* it locked in (Snowflake
implicitly forces `NOT NULL` on any `PRIMARY KEY` column, so every table
needs its own `ALTER TABLE ... ALTER COLUMN parse_sequence DROP NOT NULL`
immediately after its `CREATE TABLE`) doesn't disappear along with the
generator — it now lives in a file this same ticket makes hand-editable,
with nothing left to catch a future hand-edit silently dropping one table's
`ALTER`. Fixed by adding `tests/unit/test_silver_landing_schema_sql.py`,
which asserts the same invariant directly against the static
`11_silver_landing_schema.sql` file (4 tests, including a new
create-vs-alter set-equality check the original didn't have). Verified
red-capable per TDD discipline: temporarily stripped one table's `ALTER`
statement from a copy of the real file, confirmed 3 of 4 tests failed with
the exact expected assertion, then restored the file and confirmed a clean
`git diff`.
