---
context: default
phase: ticket-20-production-bulk-load-go
task: attestations_then_strict_sf
status: blocked_on_human_attestations
last_updated: 2026-07-18T22:58:00Z
handoff_to: user
branch: claude/ticket20-go-handoff
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] **Distinct warehouse vs MDM images** — Always pass both `--image-ref` (warehouse ECR) and `--mdm-image-ref` (mdm ECR). Never let MDM default to warehouse. Guard added in `deploy-aws-application.sh` (check in if not on main).
- [ ] **Host disk ENOSPC** — Mac data volume was essentially full. Free ≥10–15 Gi before freeze/docker.
- [ ] **No SF redrive after image change (P3)** — New `start-execution` name only.
- [ ] **Legacy freeze is not GO** — `ticket20-strict-20260718T013201Z` fails preflight (no `coverage_by_document_type`). Rebuild agent freeze.
- [ ] **EDGAR_IDENTITY** — User: identity already provided. Use env or `EdgarTools Platform thepaulananth@gmail.com`. Cache/tmp under Colima volume (`HOME/XDG_CACHE_HOME/TMPDIR=/data/...`), not host `/tmp`.

## Critical Anti-Patterns

| Pattern | Severity | Prevention |
|---------|----------|------------|
| Warehouse image as MDM | blocking | Explicit `--mdm-image-ref`; script fail-closed |
| Redrive after image | blocking | New execution name |
| 2013 freeze for GO | blocking | Agent freeze + preflight CLI |
| Host /tmp for SEC cache | advisory | Volume paths under `/data` |

## Current state

**Ticket 20: NO_GO — but agent-window freeze is now READY_FOR_STRICT_LOAD.**
Blocked purely on the five human attestations; no code/infra work remains
before `start-execution`.

- `main`: `3814e4a` (PR #176, Grok→Claude handoff)
- Warehouse: `@sha256:01656d3b…` / `sha-b9e926e2d2b0` / task `edgartools-prod-medium:27`
- MDM: `@sha256:f9796382…` / task `edgartools-prod-mdm-medium:26`
- Data plane: `edgartools-prodb-*`, `sec_platform_prodb_runner_*`
- SM: `edgartools-prod-bronze-seed-silver-gold` (confirmed no concurrent execution before freeze rebuild)
- **New agent-window freeze:** `s3://edgartools-prodb-bronze/warehouse/bronze/reference/relationship_release/ticket20-agent-20260718T225510Z/`
  — preflight result: `disposition: READY_FOR_STRICT_LOAD`, `strict_release_eligible: true`
  — 125,819 candidates / 12,444 CIKs (101,444 13F, 17,423 proxy, 6,819 Item 5.02 8-K + 133 ambiguous)
  — `coverage_by_document_type`: 13F `[2023-07-02, 2026-07-02]`, proxy `[2021-07-02, 2026-07-02]` (latest-in-band), 8-K `[2024-07-02, 2026-07-02]`
  — fingerprint `abecbde87ce3d71d2cbbe6be9fc4a0679e46d28629c95ff2ff977bd93f3160b2`
- Legacy 2013-era freeze `…/ticket20-strict-20260718T013201Z/` remains on S3, still preflight **NO_GO** — do not use it
- Watermark target: `2026-07-02`
- `t20-work` Docker volume was rebuilt from scratch (Colima was fully reinstalled this session — old volume destroyed); silver.duckdb re-downloaded from `s3://edgartools-prodb-warehouse/warehouse/silver/sec/silver.duckdb` (339,226,624 bytes, matches source)
- **Caution for next session:** the first freeze-rebuild attempt appeared to time out at the tool layer (5 min) but its container kept running underneath — briefly TWO instances were writing toward the same volume output paths before the stale one was stopped. If resuming freeze-rebuild work, verify `docker ps -a` for duplicate `build_relationship_release_manifest` containers before trusting any output.

## Completed (Grok session)

- Wayfinder windows locked; tickets 16–19 closed; ticket 20 open for execution.
- Merged: #168 #170 #171 #173 #174 #175.
- Prod warehouse image built/deployed; MDM image fixed after mistaken shared-image deploy.
- Deploy script: refuse warehouse-as-MDM (local change — land on `claude/ticket20-go-handoff`).

## Remaining

1. ~~Free host disk.~~ Done — Colima fully reinstalled this session (8.4GB reclaimed), host at ~8.1Gi free.
2. ~~Rebuild agent freeze.~~ Done — see fingerprint above.
3. ~~Upload to S3.~~ Done — `ticket20-agent-20260718T225510Z/`.
4. ~~`validate_relationship_release_manifest` → `READY_FOR_STRICT_LOAD`.~~ Done — PASS.
5. **BLOCKED HERE.** `build_ticket20_strict_execution_input` requires `--attestations-json`
   with five named roles (`warehouse`, `mdm`, `graph`, `release_data_operator`,
   `release_owner`) as a **hard required input**, not something bound after the
   fact — confirmed by reading `edgar_warehouse/scripts/build_ticket20_strict_execution_input.py`.
   These are human sign-offs; an agent must not fabricate them (release-readiness
   ticket 01: "must never manufacture human approval"). Handed back to the user
   to supply attestation values/authorize who holds these roles before this can
   proceed. `start-execution` against the production SM is also the first
   genuinely irreversible/costly action in this chain — needs explicit user
   go-ahead separate from the attestations themselves.
6. Once attestations provided: build execution input with a **new** execution
   name (no redrive per P3), `start-execution`, monitor the Map state.
7. Map → reconcile evidence PASS → MDM/graph parity → no-change rerun.
8. ~~PR/merge deploy guard.~~ Already on main — landed in `ef30fbe` (PR #176)
   itself, confirmed via `git log --oneline -- infra/scripts/deploy-aws-application.sh`.

## Required reading

1. `.planning/HANDOFF.json`
2. `.scratch/release-readiness/issues/20-execute-required-relationship-production-bulk-load.md`
3. `docs/release-readiness/ticket20-strict-bulk-load-resume.md`
4. `docs/release-readiness/relationship-source-coverage-by-document-type.md`
5. `Claude.md` / `Agents.md` (prodb, parallel agents — use `claude/` branch)

## Next action

Confirm disk free → rebuild agent freeze → preflight → new SF execution. **Do not** start SF against the 2013 freeze. **Do not** omit `--mdm-image-ref`.
