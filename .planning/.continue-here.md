---
context: default
phase: ticket-20-production-bulk-load-go
task: strict_sf_execution_root_cause_fix
status: sf_failed_unresolved_item502_candidate
last_updated: 2026-07-19T01:55:00Z
handoff_to: claude
branch: claude/ticket20-go-handoff
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] **Distinct warehouse vs MDM images** — Always pass both `--image-ref` (warehouse ECR) and `--mdm-image-ref` (mdm ECR). Never let MDM default to warehouse. Guard added in `deploy-aws-application.sh` (check in if not on main).
- [ ] **Host disk ENOSPC** — Mac data volume was essentially full. Free ≥10–15 Gi before freeze/docker.
- [ ] **No SF redrive after image change (P3)** — New `start-execution` name only.
- [ ] **Legacy freeze is not GO** — `ticket20-strict-20260718T013201Z` fails preflight (no `coverage_by_document_type`). Rebuild agent freeze.
- [ ] **EDGAR_IDENTITY** — User: identity already provided. Use env or `EdgarTools Platform thepaulananth@gmail.com`. Cache/tmp under Colima volume (`HOME/XDG_CACHE_HOME/TMPDIR=/data/...`), not host `/tmp`.

## Critical Anti-Patterns

| Pattern | Severity | Prevention |
|---------|----------|------------|
| Warehouse image as MDM | blocking | Explicit `--mdm-image-ref`; script fail-closed |
| Redrive after image | blocking | New execution name |
| 2013 freeze for GO | blocking | Agent freeze + preflight CLI |
| Host /tmp for SEC cache | advisory | Volume paths under `/data` |

## Current state

**Ticket 20: STRICT SF EXECUTION FAILED (2026-07-19T01:46:51Z UTC) — real
data-quality gate hit, not infra.** Do not redrive this execution name (P3).
Do not fire a new execution until the root cause below is actually fixed or
a sanctioned repair path is used — a bare retry will hit the same candidate
and fail again.

- **Execution ARN:** `arn:aws:states:us-east-1:690839588395:execution:edgartools-prod-bronze-seed-silver-gold:ticket20-strict-agent-20260718T225510Z`
- **Started:** 2026-07-19T00:56:27Z UTC — **Stopped:** 2026-07-19T01:46:51Z UTC (~50 min)
- **Status:** `FAILED` — `States.ExceedToleratedFailureThreshold` (Map: 1 failed, 4 aborted, 120 pending, 0 succeeded out of 125)
- **Root cause (ECS task `007706eed542423baba8435c482347f9`, exit code 2, task def `edgartools-prod-medium:27`):**
  bronze/silver capture and the filing-artifact pipeline both completed cleanly (100 CIKs,
  28,408 silver rows, 1,152 accessions fetched, 0 errors) — the batch then hit the
  `release_mode` fail-closed check in `edgar_warehouse/application/workflows/fundamentals_ingest.py:213`,
  which hard-raises whenever `parse_item_502()` returns `applicability="unresolved"`
  for a required candidate:
  `"required candidate 0000950170-24-098502 has an unresolved Item 5.02 event"`.
  `Item502Result("unresolved", "unclassified_named_event", ())` is returned by
  `edgar_warehouse/parsers/item_502.py:519` when the spaCy extractor finds an
  appointment/departure verb in a non-boilerplate sentence but cannot extract a
  structured person/role/date from it (`ambiguous_verb_seen`).
- **No sanctioned bypass exists today:** `repair_manifest` (passed as `null` this
  run) only gates the `force` re-fetch flag in `warehouse_orchestrator.py`
  (`repair_manifest_accessions`) — it does not affect the hard-raise in
  `fundamentals_ingest.py:213`. Either the Item 5.02 parser needs to handle this
  filing's sentence structure, or a deliberate, attested override path needs to
  be designed — do not silently downgrade `release_mode` or catch-and-skip this
  raise as a shortcut.
- **Root cause confirmed (filing pulled directly):** accession `0000950170-24-098502`
  is CIK 88000, Horizon Kinetics Holding Corp, 8-K filed 2024-08-19. The Item 5.02
  text is a legitimate, clearly-written compound sentence: "...appointed Daniel J.
  Roller to serve as lead independent director **and** appointed the following
  directors to the specified Board committees: Audit Committee: ... / Compensation
  Committee: ... / Nominating and Governance Committee: ...". The extractor handles
  single "appointed PERSON to ROLE" clauses but has no logic for a colon-delimited
  multi-committee roster list, so it falls through to `ambiguous_verb_seen`. Not a
  corrupt/garbled filing, not an infra bug — real parser coverage debt.
- **BULLET-LIST FIX LANDED (PR #185, `1044be1`):** `edgar_warehouse/parsers/item_502.py`
  now normalizes bullet-point list markers (•◦▪●) to sentence-ending punctuation
  in `_text()` before spaCy parses, fixing the exact accession that failed
  production (`0000950170-24-098502`, verified via regression test).
  `PARSER_VERSION` bumped 3→4.
- **SCOPE CHECK, REFINED (2026-07-19, 2,000-sample random scan post-fix, 0 fetch
  errors across both passes):** `unresolved: 211/2000 (10.6%)`, 95% CI
  9.2%–12.0%, projecting **~640–834 unresolved candidates** (point estimate
  ~737) across the full 6,952-candidate freeze. **The bullet-list fix did not
  move this number** — none of the 2,000 sampled unresolved cases happened to
  be the bullet pattern; it's real but apparently rare in random sampling
  (it's exactly what broke the actual production batch, so it does occur).
  **The 9.5-10.6% gap is NOT one pattern — it's at least 6 distinct root
  causes**, broken down by triggering verb lemma across the full 2,000 sample
  (a document can trigger more than one lemma; % is share of the full 2,000,
  not share of unresolved-only):
  - `appoint` 104 (~5.2%) — two known sub-patterns: (a) backward references
    to an appointment already disclosed in a prior Form 8-K ("had appointed...
    as previously disclosed on Form 8-K filed Sept 9"), (b) appositive names
    where the direct object is a common noun and the actual name is an
    appositive ("appointed Mr. X's son, Roberto Castellazzi, as President" —
    `_person_name()` requires the head token itself to be `PROPN`, but the
    dobj here is "son").
  - `join` 71 (~3.55%) — biographical background prose unrelated to the
    disclosed event ("Prior to joining SAB BIO, she was...", "joined Apple in
    2010 and assumed his current position in 2026" — the real new event verb
    "assumed" isn't a recognized appointment lemma at all).
  - `terminate` 62 (~3.1%) — conditional/hypothetical employment-agreement
    boilerplate ("The Agreement will continue until terminated by...") not
    caught by `_is_hypothetical`, which only checks `mark` children like
    if/unless/whether, not this "will/shall ... until terminated" construction.
  - `elect` 23 (~1.15%), `name` 18 (~0.9%, "Named Executive Officer" defined-term
    heading in comp tables mistagged as an appointment verb), `resign` 16
    (~0.8%), `promote` 12 (~0.6%, "was promoted to [ROLE]" direct-object role
    pattern not recognized by `_find_role`, which only handles "as ROLE" /
    "position of ROLE" / "to the board"), plus small tails (`retire`, `step`,
    `separate`, `hire`, `leave` each <=4 occurrences).
  - Several of these (backward-reference, bio-background) are genuine semantic
    judgment calls — getting them wrong risks introducing **false-positive**
    relationships into production MDM data, not just under-counting. Treat
    with more care than the mechanical bullet-list fix.
  - Scripts (all scratchpad-local, not committed): `item502_scope_fast.py`
    (base fetch), `item502_scope_big.py` (2,000-sample run + trigger diagnosis
    in one pass), results in `item502_scope_big_results.jsonl` +
    `item502_scope_fast_results_400_postfix.jsonl`.
  - **Awaiting user prioritization on which of the remaining ~6 patterns to
    fix before the next strict execution attempt** — not yet decided as of
    this write.
- **P3 constraint still applies:** `ticket20-strict-agent-20260718T225510Z` is
  consumed — any retry needs a brand-new execution name.

- `main`: `3814e4a` (PR #176, Grok→Claude handoff)
- Warehouse: `@sha256:01656d3b…` / `sha-b9e926e2d2b0` / task `edgartools-prod-medium:27`
- MDM: `@sha256:f9796382…` / task `edgartools-prod-mdm-medium:26`
- Data plane: `edgartools-prodb-*`, `sec_platform_prodb_runner_*`
- SM: `edgartools-prod-bronze-seed-silver-gold` (confirmed no concurrent execution before freeze rebuild)
- **New agent-window freeze:** `s3://edgartools-prodb-bronze/warehouse/bronze/reference/relationship_release/ticket20-agent-20260718T225510Z/`
  — preflight result: `disposition: READY_FOR_STRICT_LOAD`, `strict_release_eligible: true`
  — 125,819 candidates / 12,444 CIKs (101,444 13F, 17,423 proxy, 6,819 Item 5.02 8-K + 133 ambiguous)
  — `coverage_by_document_type`: 13F `[2023-07-02, 2026-07-02]`, proxy `[2021-07-02, 2026-07-02]` (latest-in-band), 8-K `[2024-07-02, 2026-07-02]`
  — fingerprint `abecbde87ce3d71d2cbbe6be9fc4a0679e46d28629c95ff2ff977bd93f3160b2`
- Legacy 2013-era freeze `…/ticket20-strict-20260718T013201Z/` remains on S3, still preflight **NO_GO** — do not use it
- Watermark target: `2026-07-02`
- `t20-work` Docker volume was rebuilt from scratch (Colima was fully reinstalled this session — old volume destroyed); silver.duckdb re-downloaded from `s3://edgartools-prodb-warehouse/warehouse/silver/sec/silver.duckdb` (339,226,624 bytes, matches source)
- **Caution for next session:** the first freeze-rebuild attempt appeared to time out at the tool layer (5 min) but its container kept running underneath — briefly TWO instances were writing toward the same volume output paths before the stale one was stopped. If resuming freeze-rebuild work, verify `docker ps -a` for duplicate `build_relationship_release_manifest` containers before trusting any output.

## Completed (Grok session)

- Wayfinder windows locked; tickets 16–19 closed; ticket 20 open for execution.
- Merged: #168 #170 #171 #173 #174 #175.
- Prod warehouse image built/deployed; MDM image fixed after mistaken shared-image deploy.
- Deploy script: refuse warehouse-as-MDM (local change — land on `claude/ticket20-go-handoff`).

## Remaining

1. ~~Free host disk.~~ Done — Colima fully reinstalled this session (8.4GB reclaimed), host at ~8.1Gi free.
2. ~~Rebuild agent freeze.~~ Done — see fingerprint above.
3. ~~Upload to S3.~~ Done — `ticket20-agent-20260718T225510Z/`.
4. ~~`validate_relationship_release_manifest` → `READY_FOR_STRICT_LOAD`.~~ Done — PASS.
5. ~~Obtain five attestations.~~ Done — user approved all five (2026-07-18T23:42:59Z).
6. ~~Build execution input + start-execution.~~ Done — running, see ARN above.
7. ~~Monitor the Map state (125 batches) to completion.~~ **FAILED** — see
   "Current state" above. 1/125 batches failed on an unresolved Item 5.02
   candidate (`0000950170-24-098502`); 4 aborted as a consequence.
7a. **NEW, BLOCKING.** Extend `edgar_warehouse/parsers/item_502.py` to handle
    the compound "appointed X to role AND appointed the following directors to
    committees: A: ..., B: ..., C: ..." sentence pattern (confirmed systemic:
    ~9.5% of the 6,952-candidate Item 5.02 universe, not a one-off). Add a
    regression test using the Horizon Kinetics filing
    (0000950170-24-098502, CIK 88000) as a fixture. Do not attempt a
    per-accession manual override — not viable at ~660-candidate scale.
8. Once a new execution's Map finishes clean: reconcile ledger, check for zero
   failure/unresolved/quarantine/circuit-breaker-leftover/unapproved-force
   counts, `required_relationship_bulk_load_evidence.json` PASS.
9. MDM run/backfill/export/sync/verify — exact `EMPLOYED_BY` +
   `INSTITUTIONAL_HOLDS` MDM↔graph parity.
10. No-change rerun: zero SEC network, identical semantic digests.
11. Final GO disposition is a separate Release Owner action (signed evidence
    tag per release-readiness doctrine) — do not self-declare GO even if the
    ledger reconciles clean; report PASS evidence to the user and let them
    make that final call.
12. ~~PR/merge deploy guard.~~ Already on main — landed in `ef30fbe` (PR #176)
    itself, confirmed via `git log --oneline -- infra/scripts/deploy-aws-application.sh`.

## Required reading

1. `.planning/HANDOFF.json`
2. `.scratch/release-readiness/issues/20-execute-required-relationship-production-bulk-load.md`
3. `docs/release-readiness/ticket20-strict-bulk-load-resume.md`
4. `docs/release-readiness/relationship-source-coverage-by-document-type.md`
5. `Claude.md` / `Agents.md` (prodb, parallel agents — use `claude/` branch)

## Next action

Confirm disk free → rebuild agent freeze → preflight → new SF execution. **Do not** start SF against the 2013 freeze. **Do not** omit `--mdm-image-ref`.
