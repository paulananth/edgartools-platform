---
context: default
phase: period-end-pk-collision-fix
status: complete
last_updated: 2026-06-12T17:30:00.000Z
branch: codex/main-sync
---

# sec_financial_fact period_end/period_start PK-collision fix — ALL STAGES COMPLETE

The entire "sec_financial_fact PK omits period_end" effort (TODOS.md) is DONE
and MERGED. Nothing to resume here.

- Stage 1 (PR #61, merged): `period_end` added to the silver PK of
  `sec_financial_fact`/`sec_financial_derived`, plus
  `_migrate_financial_period_end_pk()` schema-migration method in
  `silver_store.py` for already-deployed silver DuckDB files.
- Stage 2 (PR #62, merged): `period_start DATE NOT NULL` (sentinel
  `0001-01-01` for instant facts) added to `sec_financial_fact`'s silver PK
  so QTD/YTD pairs no longer collide.
- Stage 3 (PR #63, merged as `c3c582b33359ffa3d62764b0e15b8a12d1a1f291`):
  propagated `period_start` through the export/Snowflake/dbt path —
  `_SEC_FINANCIAL_FACT_SCHEMA`, `_build_sec_financial_fact()`,
  `mergeKeys.SEC_FINANCIAL_FACT` in `06_fundamentals_load_wrapper.sql`,
  `01_source_stage.sql` DDL + idempotent `ALTER TABLE ... ADD COLUMN IF NOT
  EXISTS`, `financial_facts.sql`/`gold.yml`, plus new
  `tests/unit/test_gold_models_financial_fact.py` (2 tests, passing). Full
  suite: 122 passed, 6 pre-existing failures unchanged.

## Remaining follow-ups (documented in TODOS.md, not started)

1. **Live validation**: re-export Apple CIK 320193 against real Snowflake to
   confirm the QTD/YTD pair survives the MERGE as two rows — needs live
   Snowflake creds (not available in agent sessions so far).
2. **Stage-1-era `PERIOD_END` nullability**:
   `EDGARTOOLS_SOURCE.SEC_FINANCIAL_FACT.period_end` is still nullable in the
   DDL. Making it `NOT NULL` needs confirming no existing rows have
   `NULL period_end` first.
3. **`SEC_FINANCIAL_DERIVED` mergeKeys**: same shape of gap as Stage 3 but for
   the derived table — Stage 1 added `period_end` to its silver PK, but its
   Snowflake `mergeKeys` (`[CIK, ACCESSION_NUMBER, FISCAL_PERIOD]`) still omit
   it.

None of these are blocking or in progress; pick any of them up fresh
(each needs its own `claude/<topic>` branch/worktree per the CLAUDE.md HARD
RULE).

## Notes on this worktree

This worktree (`agent-ae8fd4a23a6d30872`) is on the shared `codex/main-sync`
branch at a stale commit (`1da0349`, predating PR #62/#63 — pull to catch
up). Per CLAUDE.md HARD RULE, Claude must not commit to this shared branch,
so this file is left **uncommitted** intentionally.

`.planning/workstreams/neo4j-pipe/STATE.md` has a pre-existing uncommitted
change — that's Codex's active neo4j-pipe workstream. DO NOT edit, stage,
or commit it.

`.planning/HANDOFF.json` documents a separate, unrelated, currently-active
handoff (MDM Snowflake Postgres Cutover, task 5/8) — DO NOT overwrite it.

<next_action>
Nothing queued. If picking up one of the three follow-ups above, start a new
`claude/<topic>` branch/worktree first.
</next_action>
