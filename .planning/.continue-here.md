---
context: default
phase: ticket-20-production-bulk-load-go
task: 6
total_tasks: 8
status: blocked
last_updated: 2026-07-25T12:32:33.921Z
handoff_to: grok
branch: agent/grok-ticket20-handoff
---

# Grok Handoff: Ticket 20 Production Completion

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] **Do not weaken graph verification** — `StrictMdmVerifyCandidate` found real
  `EMPLOYED_BY` edges with missing source person nodes. Fix the projection or
  identity defect; do not catch, skip, exclude, or activate around it.
- [ ] **Never redrive after an image change** — execution
  `ticket20-strict-q1y-resume-eef3f5b-20260725T0156Z` is consumed. Deploy exact
  new image digests and use a brand-new execution name.
- [ ] **Do not trust premature resume markers** — a prior failed publisher wrote
  markers before canonical silver promotion. The exact invalid marker set was
  quarantined. Completion evidence must be downstream of durable publication.
- [ ] **Do not touch the dirty original checkout** —
  `/Users/aneenaananth/projects/edgartools-platform` contains protected,
  unrelated ER/data-plane and dashboard research work. Use a clean worktree.
- [ ] **Do not claim PASS early** — require the complete strict chain, graph
  activation and active verification, gold refresh, and unchanged-watermark
  no-change evidence.

**Do not proceed until all boxes are acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
| --- | --- | --- | --- |
| Bypass candidate verification | Missing graph endpoints are hidden or treated as non-blocking | blocking | Keep `StrictMdmVerifyCandidate` fail-closed and repair node/edge publication |
| Redrive with new code | A failed execution is redriven after task definitions change | blocking | Always deploy exact digests and start a new execution name |
| Marker equals publication | Batch/accession marker is treated as proof that canonical silver contains the mutation | blocking | Bind marker trust to successful publication; quarantine any pre-publication marker from a failed task |
| Dirty-checkout handoff work | Unrelated protected changes are staged with Ticket 20 fixes | blocking | Create a clean sibling worktree from current `main` |
| Batch-only PASS | Successful map or clean logs are presented as Ticket 20 completion | blocking | Demand full-chain and integrity/no-change evidence |

<current_state>

Ticket 20 is **blocked after candidate graph publication**, not in BatchSilver.

Execution:

`arn:aws:states:us-east-1:690839588395:execution:edgartools-prod-bronze-seed-silver-gold:ticket20-strict-q1y-resume-eef3f5b-20260725T0156Z`

Terminal status: `FAILED` at `2026-07-25T05:00:15-04:00`.

Succeeded stages:

1. `ReleaseModeCheck`
2. `StrictManifestCheck`
3. `StrictBatchSilver` — 8/8 resume batches succeeded; all 108 frozen batches
   now have trusted completion after invalid-marker cleanup.
4. `ReconcileRelationshipRelease`
5. `StrictMdmRun`
6. `StrictMdmBackfill`
7. `StrictMdmIdempotency`
8. `StrictMdmExport`
9. `StrictMdmSync`
10. `StrictMdmSyncIdempotency`

Failed stage: `StrictMdmVerifyCandidate`.

Failed task:

- Task ID: `28027be0fdb8486bb7d9276ef6a2a468`
- Task definition: `edgartools-prod-mdm-small:57`
- Command: `mdm verify-graph --generation-id
  ticket20-strict-q1y-resume-eef3f5b-20260725T0156Z --skip-native-app`
- Exit code: `1`
- Log stream:
  `mdm-mdm-small/edgar-warehouse/28027be0fdb8486bb7d9276ef6a2a468`

Observed failure:

- `missing_graph_edge_endpoints` contains bounded samples of `EMPLOYED_BY`
  edges.
- Every sampled failure has `missing_source_node=true` and
  `missing_target_node=false`.
- Source IDs are person endpoints; target company nodes are present.
- `missing_graph_nodes`, `missing_graph_edges`, `extra_graph_nodes`, and
  `extra_graph_edges` were empty in the emitted bounded diagnostics.
- Exact identity/property match reported OK, but endpoint integrity failed.
- The task retried at the state-machine level and failed consistently.

The candidate generation was **not activated**. `StrictMdmActivate`,
`StrictMdmVerify` against the active pointer, and `StrictGoldRefresh` were not
reached. The protected active generation remains
`adv-mdm-07efab6-20260724t2348z`.

</current_state>

<completed_work>

- Production ADV ingestion repaired and completed:
  - 55,436 ADV filings
  - 374,299 private-fund rows
  - 24,201 adviser entities
  - 129,992 fund entities including the seed
- Active ADV graph completed and verified:
  - 154,303 nodes
  - 138,582 relationships
  - `MANAGES_FUND` rerun inserted zero new edges
  - Native App `GRAPH_INFO`, BFS, and WCC passed
- Ticket 20 prior-ledger fan-in, full MDM export drain, active-generation BFS
  sampling, and production role/image hardening landed in PRs #241–#245.
- PR #255 (`eef3f5bc714f2a6fb4e4c043eb22f2f08d01bfc4`) changed expected silver
  CAS contention to retry until eventual promotion by default. Validation:
  69 focused publication tests, 513 unit/architecture tests, 35 subtests, and
  all GitHub CI jobs passed.
- Exact production deployment for `eef3f5b`:
  - Warehouse:
    `sha256:650878cb25e7a99259d6e4e882c48a61b6c46c0586f63759fe17a4e1885ab371`
  - MDM:
    `sha256:e38db60bd72e5878ec045b6e9625a4be12c5f22bcf2027a1a705e195b2dfbbca`
  - Task definitions: small `64`, medium `69`, large `64`, mdm-small `57`,
    mdm-medium `57`
  - Roles verified as `sec_platform_prod_runner_*`
  - Strict BatchSilver concurrency remains `2`, failure tolerance remains `0`
- Premature marker repair:
  - Failed batch identity: `ca800f9f06951b76`
  - 107 accession markers plus one batch marker copied to:
    `s3://edgartools-prod-bronze-690839588395/warehouse/bronze/reference/relationship_release/ticket20-agent-q1y-20260724T003912Z/invalidated_markers/ticket20-strict-q1y-resume-20260725T0003Z/ca800f9f06951b76/`
  - Original active keys received recoverable versioned delete markers.
  - Corrected remaining artifact contained 8 batches; all 8 later succeeded.
- Dashboard buildout research exists at
  `docs/research/dashboard-buildout-2026-07-24.md` in the dirty original
  checkout, and issues #246–#254 were created. That is separate work; do not
  stage it into Ticket 20.

</completed_work>

<remaining_work>

1. Reproduce and diagnose missing `EMPLOYED_BY` source person nodes for candidate
   generation `ticket20-strict-q1y-resume-eef3f5b-20260725T0156Z`.
2. Determine whether:
   - active MDM person entities were omitted from export;
   - node export drained incorrectly;
   - graph sync filtered person nodes;
   - relationship endpoints reference stale/pre-canonical entity IDs; or
   - canonical remapping occurred after edge construction.
3. Add a focused regression at the real export/sync seam before changing code.
4. Fix without synthetic nodes, endpoint rewriting without authority, or
   relaxing verification.
5. Run focused MDM tests, then `uv run pytest tests/unit tests/architecture` and
   `uv run pytest tests/mdm`.
6. Commit, open a PR, wait for CI, merge, promote byte-identical production
   warehouse/MDM images, and verify task-definition roles/digests.
7. Build a new strict input from the unchanged freeze. The completed-batch
   builder should now report zero remaining batches; verify the state machine's
   empty-map behavior before launch. Do not redrive the failed execution.
8. Require candidate parity, activation, active Native App verification, and
   gold refresh.
9. Execute the required unchanged-watermark no-change validation and assemble
   secret-free Ticket 20 completion evidence.
10. Report evidence to the user. Do not self-declare production GO.

</remaining_work>

<decisions_made>

- Expected ETag/CAS loss is contention, not a batch failure. Default retries are
  unbounded with capped jitter; semantic merge conflicts remain fatal.
- A marker written before canonical publication is not completion evidence.
- Candidate verification stays fail-closed.
- The frozen Ticket 20 identity remains unchanged:
  - Manifest:
    `s3://edgartools-prod-bronze-690839588395/warehouse/bronze/reference/relationship_release/ticket20-agent-q1y-20260724T003912Z/candidate_manifest.json`
  - Fingerprint:
    `61be5eaeebc99c7eb9bf1e5a5e2c67076619bcc28b19ece715a7c2ebb175d852`
  - Watermark: `2026-07-02`
  - 20,833 candidates, 10,792 CIKs, 108 batches
- No failed Step Functions execution name may be reused after a deployment.

</decisions_made>

<blockers>

- Candidate graph endpoint integrity: `EMPLOYED_BY` edges point from person IDs
  absent in the same candidate generation.
- There is no human-action blocker and no approval needed for read-only
  diagnosis or an in-scope corrective PR.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current state.
2. `.planning/.continue-here.md` — this handoff and blocking constraints.
3. `docs/release-readiness/ticket20-strict-bulk-load-resume.md` — P0/P1/P3
   resume contract.
4. `docs/release-readiness/required-relationship-bulk-load-completion-gate.md`
   — exact PASS requirements.
5. `docs/release-readiness/relationship-source-coverage-by-document-type.md` —
   immutable freeze windows.
6. `edgar_warehouse/mdm/snowflake_graph.py` — generation sync and verification.
7. `edgar_warehouse/mdm/export.py` — pending export drain.
8. `edgar_warehouse/mdm/graph.py` — relationship endpoint construction.
9. `.planning/METHODOLOGY.md` if present.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat aggregate node/edge counts as endpoint parity.
- Do not create placeholder person nodes to make edges pass.
- Do not delete or rewrite the protected active generation.
- Do not use the dirty original checkout.
- Do not trust historical GO evidence or the failed candidate as current PASS.
- Do not expose attestation values, DSNs, passwords, or raw secret payloads.

## Infrastructure State

- AWS account: `690839588395`
- Region: `us-east-1`
- Operator profile: `sec_platform_deployer`
- Production data plane uses canonical `edgartools-prod-*` resources and
  `sec_platform_prod_runner_*` roles.
- Failed candidate generation:
  `ticket20-strict-q1y-resume-eef3f5b-20260725T0156Z`
- Protected active generation:
  `adv-mdm-07efab6-20260724t2348z`
- No watcher or background process remains active.

<context>

The strict resume proved the silver concurrency fix: all eight remaining
batches completed under concurrency two with zero map failures, reconciliation
passed, and the full MDM export/sync chain ran twice. The current problem is now
narrowly in graph endpoint completeness. Treat it as a source-to-node identity
or publication defect. Begin with read-only candidate-generation queries and a
deterministic regression; do not mutate production while forming a theory.

</context>

<next_action>

Start with: acknowledge every blocking constraint, then inspect the candidate
generation's missing `EMPLOYED_BY` source IDs against active MDM person entities,
export rows, and generation-scoped graph nodes to identify the first boundary
where they disappear.

</next_action>
