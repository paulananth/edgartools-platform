---
context: default
phase: period-end-pk-collision-fix
status: in_progress
last_updated: 2026-06-10T20:35:00.000Z
branch: main
---

# sec_financial_fact period_end/period_start PK-collision fix — IN PROGRESS

Stage 1 item 2 (DDL changes) is DONE but UNCOMMITTED. Items 3-6 remain.
PRs #55 and #56 (merge-perf + BinderException fix) are both already merged
to main (commits d640929, 57d10ba) — not part of this phase.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---|---|---|---|
| Heredoc commit/PR messages | `git commit -m "$(cat <<'EOF' ... EOF)"` and `gh pr create --body "$(cat <<'EOF' ...)"` both throw `unexpected EOF while looking for matching` in this shell. | advisory | Write message to `/tmp/*.txt`/`*.md` via Write tool, then `git commit -F <file>` / `gh pr create --body-file <file>`. |
| `gh pr merge --delete-branch` deletes local branch + unpushed commits | If you make local WIP commits on a PR branch, push them before merging, or the local branch+commits get deleted (recoverable via `git reflog` + `git fsck --unreachable`). | advisory | Push WIP commits before merging, or omit `--delete-branch`. |

<current_state>
On branch `main`. Uncommitted changes:
- `edgar_warehouse/silver_store.py` — Stage 1 item 2 DDL changes (see below),
  NOT YET COMMITTED, NOT YET TESTED (merge functions not yet updated to match
  — DO NOT run merges against this schema until item 3/4 are done, the
  ON CONFLICT clauses currently reference the OLD PK columns and will error
  against the new PK).
- `.planning/workstreams/neo4j-pipe/STATE.md` — OUT OF SCOPE (neo4j-pipe
  workstream, likely Codex). DO NOT edit/stage/commit/revert.

TODOS.md (committed, `09bb962`) has the full audit writeup for this finding.
</current_state>

<completed_work>
- Audit (committed `09bb962`): confirmed PK collision causes Frankenstein
  (period_end, value) pairs in 96.8% of colliding groups (8500/8784, Apple
  CIK 320193). Adding `period_end` to PK fixes 89% (8784 -> 2440 collisions).
- Stage 1 item 2 (UNCOMMITTED, in `edgar_warehouse/silver_store.py`):
  - `sec_financial_fact` PK (~line 404-410) changed from
    `(cik, accession_number, concept, fiscal_period, segment)` to
    `(cik, accession_number, concept, fiscal_period, segment, period_end)`,
    with an explanatory comment.
  - `sec_financial_derived` PK (~line 449-455) changed from
    `(cik, accession_number, fiscal_period)` to
    `(cik, accession_number, fiscal_period, period_end)`, with an
    explanatory comment.
</completed_work>

<remaining_work>

### Stage 1 (continue)

3. **`merge_financial_facts`** (`edgar_warehouse/silver_store.py`, was
   ~line 2020-2077, now shifted ~+8 lines due to the 2 comment blocks added
   in item 2 — search for `def merge_financial_facts`): update BOTH
   `insert_first_sql` and `insert_last_sql`:
   - `QUALIFY ROW_NUMBER() OVER (PARTITION BY cik, accession_number, concept,
     fiscal_period, segment ORDER BY seq ...)` -> add `, period_end` to the
     PARTITION BY list in both.
   - `ON CONFLICT (cik, accession_number, concept, fiscal_period, segment)` ->
     add `, period_end` in both `DO NOTHING` and `DO UPDATE SET` clauses.
   - IMPORTANT: `period_end` must be REMOVED from any `DO UPDATE SET` list
     (it's now part of the conflict key, can't be in SET). Check
     `insert_last_sql`'s `DO UPDATE SET value = excluded.value, decimals =
     excluded.decimals, parser_version = excluded.parser_version` — verify
     `period_end` is not in that list (it currently isn't, per the version
     read in this session, so likely no change needed there beyond the
     PARTITION BY / ON CONFLICT column lists).

4. **`merge_financial_derived`** (`def merge_financial_derived`, was
   ~line 2079+): same pattern — add `period_end` to `PARTITION BY` and
   `ON CONFLICT (cik, accession_number, fiscal_period, ...)` in both
   first/last insert SQL blocks.

5. **DuckDB migration for existing tables**: `CREATE TABLE IF NOT EXISTS`
   won't alter an existing table's PK — any pre-existing local/dev
   `silver.duckdb` with the old schema will keep the old PK and the new merge
   SQL's `ON CONFLICT (..., period_end)` will fail with a "no matching unique
   constraint" error. STILL UNDECIDED / NEEDS USER CONFIRMATION before
   implementing: drop+recreate `sec_financial_fact`/`sec_financial_derived`
   on schema-version mismatch, then re-bootstrap via
   `bootstrap-fundamentals --mode entity-facts --force` (idempotent SEC
   re-fetch). For a fresh/empty test DB (e.g. differential test in item 6),
   `CREATE TABLE IF NOT EXISTS` with the new DDL just works — no migration
   needed there.

6. **Differential test**: write a throwaway `/tmp/*.py` script (delete after)
   — fetch real Apple (CIK 320193) companyfacts via
   `https://data.sec.gov/api/xbrl/companyfacts/CIK0000320193.json` (header
   `User-Agent: $EDGAR_IDENTITY`), run `parse_entity_facts`, merge into a
   FRESH `SilverDatabase(<tmpdir>/silver.duckdb)` (new DB picks up new DDL
   automatically), confirm `SELECT COUNT(*) FROM sec_financial_fact` ==
   21755 (not 10227). Spot-check the `AccountsPayable`/
   `AccountsPayableCurrent` Frankenstein examples from TODOS.md now produce
   TWO correct rows with correct (period_end, value) pairings, e.g.
   `accn=0001193125-09-153165 concept=AccountsPayable fp=Q3` should yield
   `(period_end=2008-09-27, value=5.52e9)` AND
   `(period_end=2009-06-27, value=4.854e9)` as separate rows.

### Stage 2 — period_start for residual 2,440 collisions (deferred, has open
design questions — see prior plan in git history commit `00d33a9` for full
detail: nullable period_start in PK sentinel-vs-separate-column question, and
QTD-vs-YTD fact selection for sec_financial_derived question)

### After Stage 1 lands
- Update TODOS.md "sec_financial_fact PK omits period_end" entry to mark
  Stage 1 RESOLVED with PR number.
- Per earlier handoff (still valid): rebuild+push warehouse Docker image
  (Colima), re-run 100-CIK benchmark, then resume the 3,706-CIK
  `bootstrap-fundamentals --mode entity-facts` run.
</remaining_work>

<decisions_made>
- Stage 1 (period_end in PK only) lands as its own PR before Stage 2.
- fiscal_year not added to either PK (0/8784 collisions differ on it).
- Existing corrupted silver data: drop+recreate tables + re-bootstrap rather
  than in-place migration — NOT YET CONFIRMED with user (item 5).
</decisions_made>

<blockers>
None blocking continued work on items 3-4 (straightforward SQL edits). Item 5
(migration approach for existing duckdb files) needs user confirmation before
implementing — but is NOT required to complete item 6 (differential test uses
a fresh DB).
</blockers>

## Required Reading (in order)
1. `TODOS.md` — "sec_financial_fact PK omits period_end" entry — full audit.
2. `edgar_warehouse/silver_store.py` — search `def merge_financial_facts` and
   `def merge_financial_derived` for the SQL to edit (item 3-4).
3. Git commit `00d33a9` (`.planning/.continue-here.md` at that revision) for
   the full original Stage 1 + Stage 2 plan if more detail is needed.

<context>
Continuing directly from the prior pause-work handoff (commit `00d33a9`).
The user said "start with Stage 1, item 2" and that DDL change is now done
(uncommitted). Context ran critical (76%+) immediately after, so pausing
again before items 3-4 (merge function SQL updates) which are mechanical but
need a clear head + the differential test to verify.
</context>

<next_action>
1. `git diff edgar_warehouse/silver_store.py` to review the uncommitted DDL
   changes (sanity check before building on them).
2. Find `def merge_financial_facts` and `def merge_financial_derived` in
   `silver_store.py`, update PARTITION BY / ON CONFLICT column lists per
   item 3-4 above.
3. Write and run the differential test (item 6) against a fresh tmpdir
   SilverDatabase — confirm 21755 rows and correct Frankenstein-fix examples.
4. Once test passes: `git add -- edgar_warehouse/silver_store.py` (NOT the
   neo4j-pipe STATE.md), commit, create PR for Stage 1.
5. THEN raise item 5 (migration for existing duckdb files) with the user
   before merging/deploying.
</next_action>
