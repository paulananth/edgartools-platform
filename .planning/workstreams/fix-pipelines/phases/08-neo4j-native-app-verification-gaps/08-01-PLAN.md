---
phase: 08
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - edgar_warehouse/mdm/snowflake_graph.py
  - tests/mdm/test_snowflake_graph_migration.py
  - tests/mdm/test_cli_snowflake_graph.py
autonomous: true
requirements: [GVER-01, GVER-02]
must_haves:
  truths:
    - GRAPH_INFO and BFS use the installed current API contract.
    - LIST_GRAPHS uses its installed schema and app-internal failure is classified as capability.
    - Operators can distinguish readiness, parity, and capability failures from one payload.
---

<objective>
Restore current Native App capability probes and make verify-graph failure domains explicit.
</objective>

<tasks>
<task type="execute">
  <name>Task 1: Add current GRAPH_INFO, BFS, and LIST_GRAPHS renderers</name>
  <read_first>
    <file>edgar_warehouse/mdm/snowflake_graph.py</file>
    <file>tests/mdm/test_snowflake_graph_migration.py</file>
    <file>.planning/workstreams/fix-pipelines/phases/08-neo4j-native-app-verification-gaps/08-RESEARCH.md</file>
  </read_first>
  <action>Factor the fully-qualified GRAPH_APP_NODES/GRAPH_APP_EDGES project mapping used by WCC. Render GRAPH_INFO as `GRAPH_INFO(compute_pool, {'project': ..., 'compute': {}})`. Render BFS with the same project plus camelCase `sourceNodeTable`, `sourceNode`, `maxDepth`, and a unique governed output table in `write`. Render LIST_GRAPHS at `EXPERIMENTAL.LIST_GRAPHS()`. Run all three after readiness prerequisites and preserve JOB_STATUS=ERROR detection.</action>
  <acceptance_criteria>
    <criterion>Rendered GRAPH_INFO/BFS contain sectioned current API config and no obsolete snake_case keys.</criterion>
    <criterion>LIST_GRAPHS SQL references `EXPERIMENTAL.LIST_GRAPHS`.</criterion>
    <criterion>Job result rows containing ERROR fail their named capability check.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_snowflake_graph_migration.py -k 'graph_info or bfs or list_graphs or native_app'</verify>
</task>

<task type="execute">
  <name>Task 2: Separate readiness, parity, and capability domains</name>
  <read_first>
    <file>edgar_warehouse/mdm/snowflake_graph.py</file>
    <file>tests/mdm/test_cli_snowflake_graph.py</file>
  </read_first>
  <action>Assign each check a domain. App installation, roles, grants, pool, and source sample are readiness. Node/relationship identities and endpoints are parity. GRAPH_INFO/BFS/WCC/LIST_GRAPHS are capability. Return independent domain statuses, `failure_domains`, and concise operator remediation. Keep nonzero exit for any required failed domain, but never label readiness as parity or vice versa.</action>
  <acceptance_criteria>
    <criterion>Missing compute pool produces failure_domains containing readiness but not parity.</criterion>
    <criterion>Identity mismatch produces parity but not readiness.</criterion>
    <criterion>Native App job error produces capability.</criterion>
    <criterion>Combined failures report every failed domain.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_snowflake_graph_migration.py tests/mdm/test_cli_snowflake_graph.py</verify>
</task>
</tasks>

<verification>
Run focused and full MDM graph tests; inspect payload fixtures for domain clarity.
</verification>

<success_criteria>
- Current Native App procedure contracts are encoded and regression tested.
- Operator output distinguishes readiness, parity, and app capability failures.
</success_criteria>

<threat_model>
- Threat: algorithm smoke writes uncontrolled output. Mitigation: governed unique output and cleanup.
- Threat: SQL exit 0 masks app job failure. Mitigation: JOB_STATUS result inspection.
</threat_model>

