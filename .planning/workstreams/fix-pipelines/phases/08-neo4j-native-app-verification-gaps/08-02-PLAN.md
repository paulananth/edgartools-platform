---
phase: 08
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - scripts/ops/verify-neo4j-phase8-capabilities.sh
  - tests/integration/test_neo4j_phase8_capabilities.py
  - .planning/workstreams/fix-pipelines/phases/08-neo4j-native-app-verification-gaps/08-LIVE-DEV-RUN.md
  - .planning/workstreams/fix-pipelines/phases/08-neo4j-native-app-verification-gaps/08-VERIFICATION.md
autonomous: false
requirements: [GVER-01, GVER-02]
must_haves:
  truths:
    - Every required procedure has dated live evidence on the installed app.
    - External blockers are exact and platform failures are not misclassified.
---

<objective>
Run the corrected live capability matrix and close Phase 8 with exact pass or external-blocker evidence.
</objective>

<tasks>
<task type="execute">
  <name>Task 1: Add repeatable live Phase 8 capability runner</name>
  <read_first>
    <file>scripts/ops/verify-neo4j-phase7-capabilities.sh</file>
    <file>edgar_warehouse/mdm/snowflake_graph.py</file>
  </read_first>
  <action>Create a dev-only runner guarded by `SNOW_CONNECTION=snowconn` that records app version, readiness, corrected GRAPH_INFO/BFS/WCC, exact EXPERIMENTAL.LIST_GRAPHS reproduction, output cleanup, and verify-graph failure domains. Emit machine-readable results and redact secrets.</action>
  <acceptance_criteria>
    <criterion>Wrong connection fails before SQL.</criterion>
    <criterion>Every capability records PASS, FAIL, or EXTERNAL_BLOCKER.</criterion>
    <criterion>Unique BFS output is cleaned on success and failure.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/integration/test_neo4j_phase8_capabilities.py</verify>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Execute live dev validation and close evidence</name>
  <read_first>
    <file>scripts/ops/verify-neo4j-phase8-capabilities.sh</file>
    <file>.planning/workstreams/fix-pipelines/phases/08-neo4j-native-app-verification-gaps/08-CONTEXT.md</file>
  </read_first>
  <action>Run with snowconn, capture exact version/commands/errors and write 08-LIVE-DEV-RUN.md. Write 08-VERIFICATION.md mapping GVER-01/02 to automated/live evidence. A confirmed LIST_GRAPHS app-internal failure may close GVER-02 as an external blocker, exactly as the requirement allows; platform-owned failures may not.</action>
  <acceptance_criteria>
    <criterion>GRAPH_INFO and BFS each have live current-API evidence.</criterion>
    <criterion>LIST_GRAPHS is either live PASS or a dated external blocker with installed app patch and exact error.</criterion>
    <criterion>Readiness and parity failure examples are visibly distinct.</criterion>
  </acceptance_criteria>
  <verify>Human reviews evidence before Phase 8 status becomes passed.</verify>
</task>
</tasks>

<verification>
Automated tests plus live snowconn evidence; no warning-only platform failures.
</verification>

<success_criteria>
- Phase 8 closes GVER-01 and GVER-02 with corrected platform code and exact external evidence.
</success_criteria>

<threat_model>
- Threat: live smoke collides with canonical outputs. Mitigation: unique output names and cleanup traps.
- Threat: external app bug is hidden as success. Mitigation: EXTERNAL_BLOCKER is explicit and retains non-success capability status.
</threat_model>
