---
phase: 10-cash-conversion-cycle
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - edgar_warehouse/parsers/financials_derived.py
  - edgar_warehouse/silver_store.py
  - edgar_warehouse/serving/gold_models.py
  - tests/unit/test_fundamentals_modules.py
  - infra/terraform/snowflake/modules/native_pull/main.tf
  - infra/snowflake/dbt/edgartools_gold/models/gold/financial_derived.sql
  - infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql
  - infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml
  - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_derived_unit_tests.yml
  - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml
autonomous: false
requirements: [CCC-01, CCC-02]
user_setup:
  - service: snowflake
    why: "terraform apply against the dev Snowflake account adds two physical columns to EDGARTOOLS_SOURCE.SEC_FINANCIAL_DERIVED; SHOW COLUMNS verifies them (Pitfall 1)"
    dashboard_config:
      - task: "Confirm a working SnowCLI connection (snowconn for dev) exists in ~/.snowflake/config.toml and Terraform Snowflake provider creds are set, before running Task 2's terraform apply + SHOW COLUMNS"
        location: "~/.snowflake/config.toml and infra/terraform/snowflake env"
must_haves:
  truths:
    - "cost_of_revenue and accounts_payable are extracted from already-fetched companyfacts XBRL facts by financials_derived.py (first-non-null concept preference), with no new SEC fetch path or loader."
    - "Both new columns physically exist in Snowflake EDGARTOOLS_SOURCE.SEC_FINANCIAL_DERIVED after terraform apply, confirmed by SHOW COLUMNS (not assumed as a side effect of the file edit)."
    - "A consumer querying FINANCIAL_FACTORS receives days_inventory_outstanding (inventory / cost_of_revenue * 365) and days_payable_outstanding (accounts_payable / cost_of_revenue * 365), null for the structurally-inapplicable population (banks/insurers/REITs/SaaS) exactly like gross_margin."
    - "CCC-02 coverage evidence (51.5-63.3% of economically-applicable filers, D-01 framing) is documented in gold.yml, not just in planning docs."
  artifacts:
    - "cost_of_revenue and accounts_payable columns end-to-end: parser -> DuckDB silver -> PyArrow export -> Terraform Snowflake table -> financial_derived.sql -> financial_factors.sql"
    - "days_inventory_outstanding and days_payable_outstanding columns in financial_factors.sql"
    - "gold.yml doc blocks for both new factors citing D-01 coverage framing (CCC-02 evidence)"
    - "Python unit test asserting cost_of_revenue/accounts_payable extraction; dbt DIO/DPO happy-path + explicit-null-path unit tests"
  key_links:
    - "financials_derived.py _pick() -> row dict -> silver_store merge (5 places) -> gold_models export -> Terraform column -> financial_derived.sql passthrough -> financial_factors.sql days_outstanding() invocation"
    - "days_outstanding() macro (created in plan 03-01) -> reused for DIO and DPO with cost_of_revenue as the flow denominator"
    - "Terraform SEC_FINANCIAL_DERIVED column addition -> SHOW COLUMNS verification (the single highest-risk step, per Pitfall 1 and the live Phase 1/2 current_assets blocker on this exact table)"
---

<objective>
Ship Days Inventory Outstanding (DIO) and Days Payable Outstanding (DPO) to FINANCIAL_FACTORS. This requires two NET-NEW silver parser fields — `cost_of_revenue` (for DIO and DPO's flow denominator) and `accounts_payable` (for DPO's balance) — each extracted from the already-fetched `companyfacts` JSON and threaded through the full 9-touchpoint pattern (Python parser -> DuckDB -> PyArrow export -> Terraform Snowflake DDL -> two dbt models -> docs -> tests). `inventory` (DIO's balance) already exists.

Purpose: Completes CCC-01/CCC-02. Per CONTEXT.md D-01 the coverage question is already resolved acceptable (51.5-63.3% of economically-applicable filers), so DIO/DPO are NOT out of scope — they ship. Per D-04, `accounts_payable` is an in-scope second silver field (a scope-gap fix found in research, not scope creep — REQUIREMENTS.md's own "no new loader" constraint explicitly permits parser-field additions from already-fetched JSON). Per D-02, this is Wave 2, depending on Wave 1's (plan 03-01) `days_outstanding()` macro.

Output: cost_of_revenue/accounts_payable end-to-end; days_inventory_outstanding/days_payable_outstanding columns; gold.yml docs carrying the CCC-02 coverage evidence; Python + dbt tests including an explicit cost_of_revenue-null path.

Constraint (milestone-wide, locked): No new loader, no new SEC fetch path, no bronze change. Both fields are silver PARSER additions reading the already-fetched companyfacts facts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-CONTEXT.md
@.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-RESEARCH.md
@.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-PATTERNS.md
@.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-VALIDATION.md
@edgar_warehouse/parsers/financials_derived.py
@edgar_warehouse/silver_store.py
@edgar_warehouse/serving/gold_models.py
@infra/terraform/snowflake/modules/native_pull/main.tf
@infra/snowflake/dbt/edgartools_gold/models/gold/financial_derived.sql
@infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql
@infra/snowflake/dbt/edgartools_gold/models/gold/_financial_derived_unit_tests.yml
@tests/unit/test_fundamentals_modules.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cost_of_revenue and accounts_payable to the silver parser, DuckDB store, PyArrow export, and Python unit test</name>
  <files>edgar_warehouse/parsers/financials_derived.py, edgar_warehouse/silver_store.py, edgar_warehouse/serving/gold_models.py, tests/unit/test_fundamentals_modules.py</files>
  <read_first>
    - edgar_warehouse/parsers/financials_derived.py — the `_*_CONCEPTS` list section (lines 41-157, especially _INVENTORY_CONCEPTS at 138 and _RECEIVABLES_CONCEPTS at 133), the balance-sheet `_pick()` block (lines 244-259, especially `inventory = _pick(...)` at 257), the row dict (lines 297-334, especially `"inventory": inventory` at 318), and the `_pick()` signature at line 160 (returns float | None, first non-null concept wins).
    - edgar_warehouse/silver_store.py — the sec_financial_derived CREATE TABLE DDL (lines 418-469, `inventory DOUBLE` at 441), the `_SEC_FINANCIAL_DERIVED_FACTOR_COLUMNS` migration dict (lines 590-600, drives ALTER TABLE ADD COLUMN IF NOT EXISTS), and the FIVE column-list locations inside `merge_financial_derived()` (staging DDL ~2295-2296, insert_first_sql column+select lists ~2319-2333, insert_last_sql column+select lists ~2346-2360, ON CONFLICT DO UPDATE SET ~2380-2381, and the positional `values_fn` lambda ~2407).
    - edgar_warehouse/serving/gold_models.py — `_SEC_FINANCIAL_DERIVED_SCHEMA` PyArrow schema (`pa.field("inventory", ...)` at 309) and `_build_sec_financial_derived()`'s SELECT list (`inventory,` at 1204). Order MUST match between schema and SELECT.
    - tests/unit/test_fundamentals_modules.py — `_basic_fact_rows()` (lines 95-113) and `test_basic_derivation_produces_row` (lines 123-153, the `self.assertEqual(row[...], ...)` style).
    - 03-RESEARCH.md Pattern 1 (the full 9-touchpoint checklist) and 03-PATTERNS.md `financials_derived.py`/`silver_store.py`/`gold_models.py` sections for exact insertion positions.
  </read_first>
  <behavior>
    - Parser extraction: given fact rows including {"concept": "CostOfGoodsAndServicesSold", "value": 40_000_000_000} and {"concept": "AccountsPayableCurrent", "value": 8_000_000_000}, the derived row has row["cost_of_revenue"] == 40_000_000_000 and row["accounts_payable"] == 8_000_000_000.
    - Fallback preference: given both CostOfGoodsAndServicesSold and CostOfRevenue present, _pick prefers CostOfGoodsAndServicesSold (first in list); given both AccountsPayableCurrent and AccountsPayableTradeCurrent, _pick prefers AccountsPayableCurrent.
    - Absence: given fact rows with no cost-of-revenue / accounts-payable concept, row["cost_of_revenue"] and row["accounts_payable"] are None (no exception).
  </behavior>
  <action>
    Add two module-level concept lists near the other `_*_CONCEPTS` constants (after _INVENTORY_CONCEPTS ~line 141), following the exact multi-concept fallback-list style of _REVENUE_CONCEPTS: `_COST_OF_REVENUE_CONCEPTS = ["CostOfGoodsAndServicesSold", "CostOfRevenue"]` and `_ACCOUNTS_PAYABLE_CONCEPTS = ["AccountsPayableCurrent", "AccountsPayableTradeCurrent"]`. The primary concepts (CostOfGoodsAndServicesSold, CostOfRevenue, AccountsPayableCurrent) are live-verified against the SEC Frames API per D-01/RESEARCH.md; AccountsPayableTradeCurrent is an unverified secondary fallback (RESEARCH.md A2) — Claude's Discretion per CONTEXT.md allows verifying it with one Frames API call or proceeding with it as a starting fallback. Do NOT include `CostOfGoodsSold` (no "AndServices") — it returned 404 from the Frames API for two years and is treated as a legacy/non-existent element (RESEARCH.md A3, anti-pattern).

    In the balance-sheet block (after `inventory = _pick(fact_map, _INVENTORY_CONCEPTS)` at line 257), add `cost_of_revenue = _pick(fact_map, _COST_OF_REVENUE_CONCEPTS)` and `accounts_payable = _pick(fact_map, _ACCOUNTS_PAYABLE_CONCEPTS)`. In the returned row dict (after `"inventory": inventory,` at line 318), add `"cost_of_revenue": cost_of_revenue,` and `"accounts_payable": accounts_payable,`. Use the identical no-try/except style every other _pick() call uses.

    In silver_store.py, thread both columns through ALL of these locations (missing any one silently drops the column from the merge — no exception): (1) sec_financial_derived CREATE TABLE DDL — add `cost_of_revenue DOUBLE,` and `accounts_payable DOUBLE,` after `inventory DOUBLE,` (line 441); (2) `_SEC_FINANCIAL_DERIVED_FACTOR_COLUMNS` dict — add `"cost_of_revenue": "DOUBLE",` and `"accounts_payable": "DOUBLE",` after `"inventory": "DOUBLE",` (line 594); (3) merge_financial_derived staging temp-table DDL — after `inventory DOUBLE,` (~2296); (4) insert_first_sql — add both names to the INSERT column list AND the SELECT list, after `inventory,` (~2320, ~2329); (5) insert_last_sql — same addition mirrored (~2347, ~2356); (6) ON CONFLICT DO UPDATE SET — add `cost_of_revenue = excluded.cost_of_revenue,` and `accounts_payable = excluded.accounts_payable,` after `inventory = excluded.inventory,` (~2381); (7) the positional `values_fn` lambda list — add `r.get("cost_of_revenue"), r.get("accounts_payable"),` after `r.get("inventory")` (~2407), keeping positional alignment with the INSERT column order.

    In gold_models.py, add `pa.field("cost_of_revenue", pa.float64()),` and `pa.field("accounts_payable", pa.float64()),` to `_SEC_FINANCIAL_DERIVED_SCHEMA` after `pa.field("inventory", ...)` (line 309), and add `cost_of_revenue,` and `accounts_payable,` to `_build_sec_financial_derived()`'s SELECT after `inventory,` (line 1204). The schema field order and SELECT column order MUST match (this file enforces identical ordering).

    In tests/unit/test_fundamentals_modules.py, add `{"concept": "CostOfGoodsAndServicesSold", "value": 40_000_000_000},` and `{"concept": "AccountsPayableCurrent", "value": 8_000_000_000},` to `_basic_fact_rows()`, and add `self.assertEqual(row["cost_of_revenue"], 40_000_000_000)` and `self.assertEqual(row["accounts_payable"], 8_000_000_000)` to `test_basic_derivation_produces_row`. Additionally add a new test method (e.g. `test_cost_of_revenue_and_ap_fallback_preference`) building a fact-row list with BOTH CostOfGoodsAndServicesSold and CostOfRevenue present (different values) and BOTH AccountsPayableCurrent and AccountsPayableTradeCurrent present, asserting _pick prefers the first-listed concept for each — mirroring the `_basic_fact_rows()` dict-list shape.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_fundamentals_modules.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_COST_OF_REVENUE_CONCEPTS\|_ACCOUNTS_PAYABLE_CONCEPTS' edgar_warehouse/parsers/financials_derived.py` returns at least 4 (2 list definitions + 2 _pick calls).
    - The parser does NOT reference the legacy element: `grep -c 'CostOfGoodsSold' edgar_warehouse/parsers/financials_derived.py` returns 0 (only `CostOfGoodsAndServicesSold` appears).
    - `grep -c 'cost_of_revenue\|accounts_payable' edgar_warehouse/silver_store.py` returns at least 9 (DDL x2, migration dict x2, staging DDL x2, insert_first column+select, insert_last column+select, ON CONFLICT x2, values_fn x2 — the count crossing all five merge locations plus DDL and dict).
    - `grep -c 'cost_of_revenue\|accounts_payable' edgar_warehouse/serving/gold_models.py` returns at least 4 (schema x2 + SELECT x2).
    - `uv run pytest tests/unit/test_fundamentals_modules.py -x` passes, including the new extraction assertions and the fallback-preference test.
  </acceptance_criteria>
  <done>cost_of_revenue and accounts_payable are extracted by the parser (with fallback preference), threaded through all silver_store merge locations and the DuckDB DDL/migration dict, present in the PyArrow schema and export SELECT in matching order, and asserted by passing Python unit tests including a fallback-preference test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Add Terraform Snowflake columns, apply against dev, and verify via SHOW COLUMNS (Pitfall 1)</name>
  <files>infra/terraform/snowflake/modules/native_pull/main.tf</files>
  <read_first>
    - infra/terraform/snowflake/modules/native_pull/main.tf — the `SEC_FINANCIAL_DERIVED.columns` block (lines 206-247, `{ name = "INVENTORY", type = "FLOAT" },` at 229). Match the all-caps snake_case + `type = "FLOAT"` convention used for every numeric column in this table.
    - 03-RESEARCH.md Pitfall 1 and 03-PATTERNS.md `native_pull/main.tf` section — why terraform apply + SHOW COLUMNS is a REQUIRED explicit step, not a side effect of the file edit; the live Phase 1/2 `current_assets` drift blocker on this exact table.
    - .planning/workstreams/fundamental-factors-v2/STATE.md — the current status of the pre-existing dev source-schema gap.
  </read_first>
  <action>
    Edit `infra/terraform/snowflake/modules/native_pull/main.tf`: add `{ name = "COST_OF_REVENUE", type = "FLOAT" },` and `{ name = "ACCOUNTS_PAYABLE", type = "FLOAT" },` to the `SEC_FINANCIAL_DERIVED.columns` list, immediately after `{ name = "INVENTORY", type = "FLOAT" },` (line 229). Use all-caps snake_case and `type = "FLOAT"` per the existing convention (no precision/scale). Then run `terraform plan` against the dev Snowflake account and confirm the ONLY change is the two new columns on SEC_FINANCIAL_DERIVED. If Snowflake provider credentials are available, run `terraform apply` against dev and then `snow sql --connection snowconn -q "SHOW COLUMNS IN TABLE EDGARTOOLS_SOURCE.SEC_FINANCIAL_DERIVED"`. This is the single highest-risk step in the phase (Pitfall 1) — the file edit alone does NOT create the physical column, and dbt compile/parse cannot detect a missing source column. If the pre-existing `current_assets` drift blocker prevents apply, document the phase as held-open (Phase 1/2 precedent) rather than claiming the columns exist unconfirmed. Then hand off to the human-verify gate below.
  </action>
  <verify>
    <automated>grep -c 'COST_OF_REVENUE\|ACCOUNTS_PAYABLE' infra/terraform/snowflake/modules/native_pull/main.tf</automated>
  </verify>
  <done>Terraform SEC_FINANCIAL_DERIVED.columns lists COST_OF_REVENUE and ACCOUNTS_PAYABLE (grep returns at least 2); terraform plan shows only those two additions; SHOW COLUMNS confirms both physical columns after apply — OR the phase is documented as held-open because the pre-existing dev source-schema drift blocker prevents apply (never claimed as present without SHOW COLUMNS confirmation).</done>
  <acceptance_criteria>
    - `grep -c 'COST_OF_REVENUE\|ACCOUNTS_PAYABLE' infra/terraform/snowflake/modules/native_pull/main.tf` returns at least 2, both inside the SEC_FINANCIAL_DERIVED columns block.
    - `terraform plan` output shows exactly two column additions to SEC_FINANCIAL_DERIVED and no unrelated resource churn.
    - After `terraform apply`, `SHOW COLUMNS IN TABLE EDGARTOOLS_SOURCE.SEC_FINANCIAL_DERIVED` lists both COST_OF_REVENUE and ACCOUNTS_PAYABLE — OR the block is documented as held-open with the specific pre-existing-blocker reason (the columns are NOT asserted present without SHOW COLUMNS confirmation).
  </acceptance_criteria>
  <what-built>
    Task 1 added cost_of_revenue/accounts_payable to the Python silver layer and PyArrow export. This task adds the two physical columns to the Terraform-managed Snowflake source table AND runs the deploy against the dev account, because — per 03-RESEARCH.md Pitfall 1 and the live, currently-unresolved Phase 1/2 `current_assets` drift blocker on this EXACT table — the Terraform file edit alone does NOT create the physical column, and dbt compile/parse cannot detect a missing source column. This is flagged in the phase goal as the single most likely thing to go wrong in this phase.

    Automated portion (done before this checkpoint): the executor edits `infra/terraform/snowflake/modules/native_pull/main.tf` to add `{ name = "COST_OF_REVENUE", type = "FLOAT" },` and `{ name = "ACCOUNTS_PAYABLE", type = "FLOAT" },` to the `SEC_FINANCIAL_DERIVED.columns` list, after `{ name = "INVENTORY", type = "FLOAT" },` (line 229), using all-caps snake_case and `type = "FLOAT"` per the existing convention. The executor then attempts `terraform plan` (and, if creds are present, `terraform apply`) against the dev Snowflake account and `SHOW COLUMNS IN TABLE EDGARTOOLS_SOURCE.SEC_FINANCIAL_DERIVED`.
  </what-built>
  <how-to-verify>
    1. Confirm the Terraform edit is present: `grep -c 'COST_OF_REVENUE\|ACCOUNTS_PAYABLE' infra/terraform/snowflake/modules/native_pull/main.tf` returns at least 2, both inside the SEC_FINANCIAL_DERIVED columns block.
    2. From `infra/terraform/snowflake/accounts/prod` (or the dev root the project uses), run `terraform plan` and confirm the ONLY change is adding COST_OF_REVENUE and ACCOUNTS_PAYABLE to SEC_FINANCIAL_DERIVED — no unrelated resource churn.
    3. Run `terraform apply` against the dev Snowflake account.
    4. Run: `snow sql --connection snowconn -q "SHOW COLUMNS IN TABLE EDGARTOOLS_SOURCE.SEC_FINANCIAL_DERIVED"` and confirm BOTH `COST_OF_REVENUE` and `ACCOUNTS_PAYABLE` appear in the output.
    5. If the pre-existing `current_assets` drift blocker on this table is still unresolved and prevents apply, capture that explicitly and mark this phase held-open (same precedent as Phase 1/2) rather than silently proceeding — do NOT claim the columns exist when SHOW COLUMNS did not confirm them.
    Expected outcome: SHOW COLUMNS lists COST_OF_REVENUE and ACCOUNTS_PAYABLE, OR the apply is blocked by the known pre-existing source-schema gap and that block is documented as held-open.
  </how-to-verify>
  <resume-signal>Type "approved" once SHOW COLUMNS confirms both columns (or "held-open: &lt;reason&gt;" if the pre-existing drift blocker prevents apply), then describe which occurred.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface both silver columns through the dbt models, add DIO/DPO factors, document CCC-02 evidence, and add dbt unit tests</name>
  <files>infra/snowflake/dbt/edgartools_gold/models/gold/financial_derived.sql, infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql, infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml, infra/snowflake/dbt/edgartools_gold/models/gold/_financial_derived_unit_tests.yml, infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/models/gold/financial_derived.sql — the final `select` passthrough (lines 76-127, `w.inventory,` at 99). This model is column-by-column (never `select *`).
    - infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql — the "Base accounting inputs" passthrough block (`l.inventory,` at 96) and the new "V3 cash conversion cycle factors (Phase 3)" block created by plan 03-01 (where days_sales_outstanding now lives). Confirm days_outstanding() macro is available (created in 03-01).
    - infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml — the days_sales_outstanding doc block added by plan 03-01 (place DIO/DPO blocks after it) and the CAGR blocks' description density.
    - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_derived_unit_tests.yml — the `&row_defaults` anchor (lines 20+); EVERY source-input given row must gain cost_of_revenue/accounts_payable keys because financial_derived.sql now selects those source columns (dbt unit tests error on a source column that the fixture given rows do not provide).
    - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml — the `&factor_row_defaults` anchor (lines 10-36) and the DSO tests added by plan 03-01.
    - 03-PATTERNS.md `financial_derived.sql`, `financial_factors.sql`, `gold.yml`, and unit-test sections; 03-VALIDATION.md rows 03-02-03/04/05/06.
  </read_first>
  <behavior>
    - DIO happy-path: a row with inventory=30, cost_of_revenue=20 produces days_inventory_outstanding = 30.0 / 20 * 365 = 547.5.
    - DPO happy-path: a row with accounts_payable=15, cost_of_revenue=20 produces days_payable_outstanding = 15.0 / 20 * 365 = 273.75.
    - DIO/DPO explicit-null path: a row with cost_of_revenue explicitly null (inventory and accounts_payable non-null) produces days_inventory_outstanding = null AND days_payable_outstanding = null (the structural-inapplicability case per D-01 — this distinguishes "intentional null test" from "field doesn't exist yet").
  </behavior>
  <action>
    In `financial_derived.sql`, add `w.cost_of_revenue,` and `w.accounts_payable,` to the final select passthrough after `w.inventory,` (line 99) — this model lists every silver column explicitly or it never reaches financial_factors.sql.

    In `financial_factors.sql`, add `l.cost_of_revenue,` and `l.accounts_payable,` to the "Base accounting inputs" passthrough block after `l.inventory,` (line 96). Then, in the "V3 cash conversion cycle factors (Phase 3)" block (added by plan 03-01, where days_sales_outstanding lives), add two more columns: `{{ days_outstanding('l.inventory', 'l.cost_of_revenue') }} as days_inventory_outstanding,` and `{{ days_outstanding('l.accounts_payable', 'l.cost_of_revenue') }} as days_payable_outstanding,`. Both use cost_of_revenue as the flow denominator. Do NOT wrap in a fiscal_period='FY' gate — same-period ratios, per the phase_goal pattern-mapper correction.

    In `gold.yml`, after the days_sales_outstanding doc block (added by 03-01), add `- name: days_inventory_outstanding` and `- name: days_payable_outstanding` entries. Each `description: >` block must: state the formula (DIO = inventory / cost_of_revenue * 365; DPO = accounts_payable / cost_of_revenue * 365); state the null semantics (null for filers with no COGS/inventory line — banks, insurers, REITs, most SaaS — same structural-null class as gross_margin, NOT a coverage defect); and cite the CCC-02 coverage evidence: "~51.5-63.3% coverage of economically-applicable filers (Phase 3 D-01), measured live against the SEC XBRL Frames API — an accepted expected null rate, not a data-quality failure." This gold.yml documentation IS the shipped CCC-02 evidence cross-reference (03-VALIDATION.md row 03-02-06).

    In `_financial_derived_unit_tests.yml`, add `cost_of_revenue: 0` and `accounts_payable: 0` (or representative values) to the `&row_defaults` anchor so all source-input given rows provide the two new source columns financial_derived.sql now selects — otherwise dbt unit tests error with a missing-source-column failure.

    In `_financial_factors_unit_tests.yml`: add `cost_of_revenue` and `accounts_payable` to the `&factor_row_defaults` anchor (so every existing test row has non-null values and existing DSO/CAGR assertions stay valid). Add DIO/DPO happy-path assertions: in the `financial_factors_complete_fy_ratios` second expect row (accession "0002"), ensure the given row has inventory=30, cost_of_revenue=20, accounts_payable=15 and assert `days_inventory_outstanding: 547.5` and `days_payable_outstanding: 273.75` (exact terminating values). Add a NEW named test `financial_factors_dio_dpo_null_when_cost_of_revenue_null` with a given row deriving from the anchor but overriding `cost_of_revenue: null` explicitly (keep inventory and accounts_payable non-null), asserting `days_inventory_outstanding: null` and `days_payable_outstanding: null` — this explicit-null row is required per RESEARCH.md/03-VALIDATION.md row 03-02-04 because omission-equals-null would not distinguish "intentional null-path test" from "column absent."
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt compile --select financial_derived financial_factors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cost_of_revenue\|accounts_payable' infra/snowflake/dbt/edgartools_gold/models/gold/financial_derived.sql` returns at least 2 (both passthrough selects present).
    - `grep -c 'days_inventory_outstanding\|days_payable_outstanding' infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql` returns at least 2, and both invoke `days_outstanding(` with `l.cost_of_revenue` as the flow argument.
    - `grep -A6 'name: days_inventory_outstanding' infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml` shows a description citing the coverage rate (51.5 or 63.3 percent) and D-01 — CCC-02 evidence is in shipped docs, not only planning files.
    - `grep -c 'financial_factors_dio_dpo_null_when_cost_of_revenue_null' infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml` returns at least 1, and that test block contains `cost_of_revenue: null`.
    - `grep -c 'days_inventory_outstanding: 547.5' infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml` returns at least 1, and `grep -c 'days_payable_outstanding: 273.75' ...` returns at least 1 (exact computed happy-path values).
    - `dbt compile --select financial_derived financial_factors` succeeds. NOTE: live `dbt test` requires the Task 2 Snowflake columns to physically exist and inherits the pre-existing dev source-schema block (see verification) — compile success is the achievable bar; live `dbt test` is held-open per the Phase 1/2 precedent.
  </acceptance_criteria>
  <done>cost_of_revenue/accounts_payable pass through financial_derived.sql and financial_factors.sql; DIO/DPO columns compile via days_outstanding() with cost_of_revenue as denominator; gold.yml documents both factors with CCC-02 coverage evidence (D-01 framing); dbt unit tests cover DIO/DPO happy-path (547.5 / 273.75) and an explicit cost_of_revenue-null path; both dbt test ymls updated; dbt compile succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SEC companyfacts JSON -> silver parser | Already-fetched, already-typed XBRL facts; _pick() returns float|None — no new string/injection surface (V5 already satisfied per 03-RESEARCH.md) |
| Terraform -> Snowflake DDL | Declarative column addition to a warehouse-owned table; same EDGARTOOLS_DEV_DEPLOYER role model, no new grants (V4 unchanged) |
| dbt model -> Snowflake dynamic table | Compiled macro SQL over vetted silver columns; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | Terraform column drift — physical Snowflake column missing after apply (repeat of live Phase 1/2 current_assets blocker) | high | mitigate | Task 2 blocking-human checkpoint runs terraform apply + SHOW COLUMNS verification; phase held-open if the pre-existing drift block prevents apply (never claims columns exist unconfirmed) — per Pitfall 1 |
| T-03-04 | Tampering | Silent column drop — a merge_financial_derived location missed, so cost_of_revenue reaches DuckDB but not Snowflake export | medium | mitigate | Task 1 acceptance criteria grep-count all five merge locations + DDL + dict; Python unit test asserts extraction; matching-order check on PyArrow schema/SELECT |
| T-03-05 | Information Disclosure | Malformed/extreme XBRL COGS producing an absurd DIO/DPO | low | accept | Same class as every _pick()-derived numeric factor; no new mitigation — data-quality follow-up if observed (03-RESEARCH.md Security Domain) |

No package installs in this plan (Python stdlib + existing duckdb/pyarrow/dbt/Terraform deps only) — T-03-SC not applicable (03-RESEARCH.md Package Legitimacy Audit: "Not applicable — this phase installs no new packages").
</threat_model>

<verification>
- Per task commit: Task 1 -> `uv run pytest tests/unit/test_fundamentals_modules.py -x`; Task 3 -> `cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt compile --select financial_derived financial_factors`.
- Per wave: `uv run pytest tests/unit/ -x` and `cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt compile --select financial_derived financial_factors`.
- Terraform (Task 2): `terraform plan` shows only the two new columns; `terraform apply` against dev; `snow sql --connection snowconn -q "SHOW COLUMNS IN TABLE EDGARTOOLS_SOURCE.SEC_FINANCIAL_DERIVED"` confirms COST_OF_REVENUE and ACCOUNTS_PAYABLE.
- `git diff --stat` must touch ONLY the 10 files in `files_modified` — NO bronze/loader/runtime file (both fields are silver PARSER additions; the milestone "no new loader" constraint is enforced by inspecting that runtime.py and any bronze-capture file are untouched).
- **Environment caveat (per 03-VALIDATION.md, carried from Phase 1/2):** live `dbt test --select financial_derived financial_factors` requires Task 2's physical Snowflake columns AND inherits the pre-existing dev `SEC_FINANCIAL_DERIVED` source-schema gap (missing `current_assets` etc.). `dbt compile` + passing Python unit tests are the achievable bar; live `dbt test` is held-open per the Phase 1/2 precedent — do NOT mark the phase complete on compile-only without the same explicit held-open caveat.
</verification>

<success_criteria>
- cost_of_revenue and accounts_payable extracted by financials_derived.py (with verified fallback-preference), threaded through all silver_store merge locations + DDL + migration dict, present in the PyArrow schema/export SELECT in matching order; Python unit tests pass.
- Both physical columns confirmed in Snowflake EDGARTOOLS_SOURCE.SEC_FINANCIAL_DERIVED via SHOW COLUMNS after terraform apply (or held-open if blocked by the pre-existing drift blocker, documented not assumed).
- days_inventory_outstanding and days_payable_outstanding compile into financial_factors.sql via days_outstanding() with cost_of_revenue as denominator, ungated across fiscal periods.
- gold.yml documents both factors with the CCC-02 coverage evidence (51.5-63.3% economically-applicable-filer rate, D-01 framing).
- dbt unit tests cover DIO/DPO happy-path (547.5 / 273.75) and an explicit cost_of_revenue-null path; dbt compile succeeds.
- No loader/runtime/bronze change (milestone "no new loader" constraint holds; confirmed via git diff --stat).
</success_criteria>

<output>
Create `.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-02-SUMMARY.md` when done.
</output>
