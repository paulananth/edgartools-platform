---
phase: 07
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - edgar_warehouse/application/warehouse_orchestrator.py
  - edgar_warehouse/infrastructure/object_storage.py
  - edgar_warehouse/infrastructure/filing_artifact_service.py
  - edgar_warehouse/bronze_filing_artifacts.py
  - tests/application/test_warehouse_orchestrator_mdm.py
  - tests/unit/test_loader_idempotency.py
autonomous: true
requirements: [ARTF-01, ARTF-02]
must_haves:
  truths:
    - Partial silver candidates merge semantically without canonical key loss.
    - Canonical concurrent change and ambiguous conflicts fail safely.
    - Intact bronze artifacts cause zero SEC network calls.
  artifacts:
    - Fail-closed protected-table policy registry and semantic merge.
    - Conditional S3 staged promotion contract.
    - Global artifact idempotency and repair audit tests.
---

<objective>
Protect every MDM relationship input from silver regression, concurrent overwrite, and unnecessary SEC refetch.
</objective>

<tasks>
<task type="execute">
  <name>Task 1: Implement semantic silver merge and protected-table policies</name>
  <read_first>
    <file>edgar_warehouse/application/warehouse_orchestrator.py</file>
    <file>edgar_warehouse/silver.py</file>
    <file>tests/application/test_warehouse_orchestrator_mdm.py</file>
    <file>.planning/workstreams/fix-pipelines/phases/07-source-coverage-exclusions-and-artifact-hygiene/07-CONTEXT.md</file>
  </read_first>
  <action>Add a reviewed fail-closed registry classifying every canonical domain table and declaring business keys, provenance conflict rule, and schema policy. Exclude operational/staging tables explicitly. Localize canonical, apply compatible additive schema migrations, merge candidate rows transactionally, reject loss of canonical protected keys, and resolve same-key differences only through declared provenance rules. Emit a row-level conflict report on ambiguity. A new unclassified domain table and destructive schema/key changes fail closed. Silver `--force` never bypasses this logic.</action>
  <acceptance_criteria>
    <criterion>A smaller partial candidate adds its rows while preserving all canonical protected keys.</criterion>
    <criterion>An ambiguous same-key payload aborts and identifies table/business key/field differences.</criterion>
    <criterion>A new unclassified domain table and a dropped column both fail publication.</criterion>
    <criterion>`--force` does not disable protected-key, conflict, or schema checks.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/application/test_warehouse_orchestrator_mdm.py -k 'publish or merge or conflict or schema'</verify>
</task>

<task type="execute">
  <name>Task 2: Add staged optimistic S3 promotion and explicit repair boundary</name>
  <read_first>
    <file>edgar_warehouse/infrastructure/object_storage.py</file>
    <file>edgar_warehouse/application/warehouse_orchestrator.py</file>
    <file>tests/application/test_warehouse_orchestrator_mdm.py</file>
  </read_first>
  <action>Extend object storage metadata with S3 version/ETag reads and staged object writes. Upload and validate a merged candidate under an immutable staging key, then promote only when the canonical object version/ETag still equals the localization source. On conflict, preserve staged diagnostics and return an explicit retryable error. Define a separate destructive repair/migration command contract requiring operator reason, dry-run diff, and audit record; do not implement a `--force` bypass.</action>
  <acceptance_criteria>
    <criterion>A simulated canonical ETag/version change prevents promotion.</criterion>
    <criterion>Successful promotion records source version, staged checksum, and resulting canonical version.</criterion>
    <criterion>Destructive repair defaults to dry-run and requires a nonempty operator reason.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/application/test_warehouse_orchestrator_mdm.py -k 'etag or version or promote or repair'</verify>
</task>

<task type="execute">
  <name>Task 3: Enforce global bronze cache idempotency and audited repair</name>
  <read_first>
    <file>edgar_warehouse/infrastructure/filing_artifact_service.py</file>
    <file>edgar_warehouse/bronze_filing_artifacts.py</file>
    <file>tests/unit/test_loader_idempotency.py</file>
  </read_first>
  <action>Make the shared filing-artifact service contract assert zero download callback invocations for intact immutable cache hits across filing types. Add DEF 14A and 13F command integration cases. Retain bronze `--force` only as artifact repair and emit accession, prior object hash/version, replacement hash/version, operator context, and reason. Preserve the no-throttle-sleep cache-hit behavior.</action>
  <acceptance_criteria>
    <criterion>Intact cache hits invoke neither network callback nor throttle sleep.</criterion>
    <criterion>DEF 14A and 13F cached command paths make zero SEC calls.</criterion>
    <criterion>Forced replacement emits a complete repair audit record.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/unit/test_loader_idempotency.py</verify>
</task>
</tasks>

<verification>
Run application publication and loader idempotency suites with local and mocked S3 adapters, including conflict injection.
</verification>

<success_criteria>
- Canonical silver cannot regress through ordinary publishing.
- Concurrent writers fail safely instead of last-writer-wins.
- Bronze cache hits are globally network-free and repairs are audited.
</success_criteria>

<threat_model>
- Threat: TOCTOU overwrite loses canonical data. Mitigation: staged upload plus version/ETag compare-before-promote.
- Threat: malicious candidate deletes or mutates protected rows. Mitigation: fail-closed policy registry, monotonic key assertion, and deterministic provenance conflicts.
</threat_model>
