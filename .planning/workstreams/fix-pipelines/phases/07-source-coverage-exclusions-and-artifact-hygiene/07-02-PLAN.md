---
phase: 07
plan: 02
type: execute
wave: 3
depends_on: [07-01]
files_modified:
  - edgar_warehouse/mdm/coverage.py
  - edgar_warehouse/mdm/database.py
  - edgar_warehouse/mdm/migrations.py
  - edgar_warehouse/mdm/snowflake_graph.py
  - tests/mdm/test_relationship_coverage.py
autonomous: true
requirements: [EDGE-07, EDGE-08, RCOV-01, RCOV-02]
must_haves:
  truths:
    - Every registered relationship type has one current generation coverage status.
    - Stale exclusions and undocumented zero states fail closed.
    - Exclusions never invent graph facts.
  artifacts:
    - Machine-readable coverage/exclusion policy and evidence fingerprinting.
    - EDGE-07 and EDGE-08 reviewed policies.
    - Exhaustive coverage verification tests.
---

<objective>
Replace prose and temporary populated-type allowlists with an exhaustive, machine-readable relationship coverage ledger.
</objective>

<tasks>
<task type="execute">
  <name>Task 1: Add relationship coverage and evidence policy</name>
  <read_first>
    <file>edgar_warehouse/mdm/database.py</file>
    <file>edgar_warehouse/mdm/migrations.py</file>
    <file>edgar_warehouse/mdm/snowflake_graph.py</file>
    <file>.planning/workstreams/claude-mdm-source-recovery/FINDINGS.md</file>
    <file>.planning/workstreams/fix-pipelines/REQUIREMENTS.md</file>
  </read_first>
  <action>Add coverage records with generation ID, relationship type, status restricted to `populated|valid_zero|excluded`, expected count, evidence category, evidence/query version, evaluated date, population fingerprint, review trigger, and status. Implement exact deterministic fingerprints. Seed EDGE-07 `MANAGES_FUND` as `source_unavailable` using adviser CIK/accession evidence and EDGE-08 `HAS_PARENT_COMPANY` as `capability_not_implemented` using company-universe plus parser-capability version. Do not create placeholder nodes or edges.</action>
  <acceptance_criteria>
    <criterion>EDGE-07 and EDGE-08 have distinct categories and expected edge count zero.</criterion>
    <criterion>Fingerprint output is stable for the same sorted input and changes for relevant population/capability changes.</criterion>
    <criterion>No exclusion code writes `MdmRelationshipInstance` or graph edge rows.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_relationship_coverage.py -k 'fingerprint or exclusion'</verify>
</task>

<task type="execute">
  <name>Task 2: Enforce exhaustive coverage and per-generation valid-zero proof</name>
  <read_first>
    <file>edgar_warehouse/mdm/coverage.py</file>
    <file>edgar_warehouse/mdm/snowflake_graph.py</file>
    <file>tests/mdm/test_snowflake_graph_migration.py</file>
  </read_first>
  <action>Generate coverage for every active `MdmRelationshipType`. Recompute `valid_zero` every generation and store query version, source fingerprint, evaluated date, and zero result. Replace `POPULATED_RELATIONSHIP_TYPES` named-check scoping with exhaustive coverage evaluation. Fail verification on missing types, stale exclusion fingerprints, nonzero excluded types, zero populated types, contradictory statuses, or synthetic excluded edges.</action>
  <acceptance_criteria>
    <criterion>All 11 active relationship types appear exactly once in a complete coverage manifest.</criterion>
    <criterion>A deliberately stale exclusion and an omitted relationship type both fail verification.</criterion>
    <criterion>A valid-zero record cannot be reused for a later generation without recomputation.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_relationship_coverage.py tests/mdm/test_snowflake_graph_migration.py</verify>
</task>
</tasks>

<verification>
Run the coverage suite and inspect one manifest fixture containing populated, valid-zero, source-unavailable, and capability-not-implemented states.
</verification>

<success_criteria>
- Every registered relationship type is exhaustively classified per generation.
- EDGE-07/08 are evidenced and distinguish source unavailability from missing capability.
- Stale or incomplete coverage prevents verification and activation.
</success_criteria>

<threat_model>
- Threat: exclusions become permanent silent waivers. Mitigation: exact fingerprints, review triggers, and fail-closed staleness.
- Threat: fake placeholder edges pollute traversal. Mitigation: manifest-only exclusions and explicit nonzero-edge rejection.
</threat_model>
