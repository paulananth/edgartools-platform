---
phase: 07
plan: 04
type: execute
wave: 5
depends_on: [07-02, 07-03]
files_modified:
  - edgar_warehouse/mdm/generation.py
  - edgar_warehouse/mdm/cli.py
  - edgar_warehouse/mdm/snowflake_graph.py
  - infra/scripts/deploy-aws-application.sh
  - tests/mdm/test_graph_generation_builder.py
  - tests/architecture/test_aws_application_deploy.py
autonomous: true
requirements: [RSYNC-04]
must_haves:
  truths:
    - Generation work fans out safely and converges on one complete immutable manifest.
    - Unchanged partitions are reusable and failed partitions retry independently.
  artifacts:
    - Type-first partition planner/worker/fan-in implementation.
    - AWS Step Functions distributed Map deployment outside Terraform.
---

<objective>
Build complete graph generations efficiently through parallel immutable partitions and deterministic fan-in.
</objective>

<tasks>
<task type="execute">
  <name>Task 1: Implement partition planning, manifests, reuse, and fan-in</name>
  <read_first>
    <file>edgar_warehouse/mdm/snowflake_graph.py</file>
    <file>edgar_warehouse/mdm/publication.py</file>
    <file>edgar_warehouse/mdm/cli.py</file>
    <file>.planning/workstreams/fix-pipelines/phases/07-source-coverage-exclusions-and-artifact-hygiene/07-RESEARCH.md</file>
  </read_first>
  <action>Create one node partition per entity type and one edge partition per relationship type by default. Support configured stable-source hash sharding for high-volume types. Each immutable partition records generation, kind, type, shard, MDM watermark, rule/schema version, input fingerprint, row count, stable-key hash, property hash, and status. Reuse only exact content-address matches. Fan-in rejects missing/duplicate shards, mixed watermarks or versions, endpoint gaps, incomplete coverage, and failed partitions.</action>
  <acceptance_criteria>
    <criterion>Default planning emits all 6 node types and all 11 relationship types exactly once.</criterion>
    <criterion>Configured high-volume type sharding assigns every stable source key to exactly one shard.</criterion>
    <criterion>An unchanged fingerprint reuses output; any watermark/rule/schema/input change rebuilds it.</criterion>
    <criterion>A failed shard retries without rebuilding unchanged successful partitions.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_graph_generation_builder.py</verify>
</task>

<task type="execute">
  <name>Task 2: Deploy AWS fan-out/fan-in orchestration</name>
  <read_first>
    <file>infra/scripts/deploy-aws-application.sh</file>
    <file>tests/architecture/test_aws_application_deploy.py</file>
    <file>edgar_warehouse/mdm/cli.py</file>
  </read_first>
  <action>Add ECS command payloads for generation planning, partition building, fan-in verification, and activation. Generate a Step Functions Distributed Map with bounded concurrency and retry/catch behavior. New MDM commits queue for the next generation while one builds. Do not add task definitions, state machines, commands, schedules, or image rollout to passive Terraform.</action>
  <acceptance_criteria>
    <criterion>Generated state machine uses Distributed Map for partition workers and invokes activation only after fan-in verification.</criterion>
    <criterion>Worker failure routes to retry/failure state and never activation.</criterion>
    <criterion>Architecture tests confirm passive Terraform has no new runnable workload.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/architecture/test_aws_application_deploy.py tests/mdm/test_graph_generation_builder.py</verify>
</task>
</tasks>

<verification>
Render the Step Functions definition locally and assert partition fan-out, bounded concurrency, independent retry, fan-in, and guarded activation ordering.
</verification>

<success_criteria>
- Complete generations build in parallel without mixed watermarks.
- Partition reuse is content-addressed and fail closed.
- AWS orchestration remains operator-deployed and Terraform-passive.
</success_criteria>

<threat_model>
- Threat: stale partition substitution. Mitigation: content identity includes watermark, rules, schema, and input hashes.
- Threat: unbounded fan-out exhausts AWS/Snowflake. Mitigation: configured Distributed Map concurrency and per-partition resource limits.
</threat_model>
