---
phase: 07
plan: 03
type: execute
wave: 4
depends_on: [07-02]
files_modified:
  - edgar_warehouse/mdm/database.py
  - edgar_warehouse/mdm/migrations.py
  - edgar_warehouse/mdm/publication.py
  - edgar_warehouse/mdm/cli.py
  - edgar_warehouse/mdm/dashboard_readonly.py
  - tests/mdm/test_graph_publication_queue.py
autonomous: true
requirements: [RSYNC-01, RSYNC-03]
must_haves:
  truths:
    - An MDM commit and its publication request are atomic.
    - Queue lifecycle and freshness are observable without coupling writers to graph code.
  artifacts:
    - Transactional publication outbox and claim/retry state machine.
    - Five-minute warning and fifteen-minute hard-alert health contract.
---

<objective>
Decouple MDM writers from graph publication through a transactional, observable generation request queue.
</objective>

<tasks>
<task type="execute">
  <name>Task 1: Add transactional publication requests and watermarks</name>
  <read_first>
    <file>edgar_warehouse/mdm/database.py</file>
    <file>edgar_warehouse/mdm/migrations.py</file>
    <file>edgar_warehouse/mdm/cli.py</file>
    <file>edgar_warehouse/mdm/observability.py</file>
  </read_first>
  <action>Add publication request/outbox records with committed MDM watermark, lifecycle `mdm_committed|graph_pending|graph_building|graph_verified|graph_active|failed`, retry metadata, backfill-window metadata, and audit timestamps. Provide one transaction helper used by relationship-changing workflows so rollback removes both MDM changes and the request. Add safe claim/lease/retry operations and a CLI publisher/coordinator entry point.</action>
  <acceptance_criteria>
    <criterion>A forced transaction rollback leaves neither relationship changes nor a publication request.</criterion>
    <criterion>Concurrent claim attempts yield one owner per request lease.</criterion>
    <criterion>Expired claims are retryable without duplicating active generations.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_graph_publication_queue.py -k 'transaction or claim or retry'</verify>
</task>

<task type="execute">
  <name>Task 2: Expose freshness health and alert states</name>
  <read_first>
    <file>edgar_warehouse/mdm/publication.py</file>
    <file>edgar_warehouse/mdm/dashboard_readonly.py</file>
    <file>edgar_warehouse/mdm/observability.py</file>
    <file>tests/mdm/test_dashboard_readonly.py</file>
  </read_first>
  <action>Replace per-row pending-sync health as the primary publication signal with queue/generation lifecycle metrics. Report normal under five minutes, warning at five minutes, and hard alert at fifteen minutes. Exempt only explicitly declared bounded backfill windows, and alert when their deadline passes. Preserve compatible pending counts as secondary diagnostics.</action>
  <acceptance_criteria>
    <criterion>Boundary tests distinguish 4:59 normal, 5:00 warning, and 15:00 hard alert.</criterion>
    <criterion>An expired backfill window produces a hard alert.</criterion>
    <criterion>Dashboard/status output distinguishes MDM committed, graph pending, verified, and active.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_graph_publication_queue.py tests/mdm/test_dashboard_readonly.py</verify>
</task>
</tasks>

<verification>
Run queue tests against a real SQLAlchemy session and confirm transaction rollback, lease expiry, and SLO boundaries.
</verification>

<success_criteria>
- Every committed relationship change produces one transactional publication request.
- Writers contain no Snowflake/Neo4j orchestration logic.
- Publication freshness and failure are operationally explicit.
</success_criteria>

<threat_model>
- Threat: duplicate workers activate competing generations. Mitigation: lease ownership, immutable generation IDs, and guarded activation.
- Threat: queue tampering hides stale data. Mitigation: append/audit lifecycle transitions and hard age alerts.
</threat_model>
