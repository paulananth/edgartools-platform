---
phase: 07
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ops/verify-neo4j-phase7-capabilities.sh
  - tests/integration/test_neo4j_phase7_capabilities.py
  - .planning/workstreams/fix-pipelines/phases/07-source-coverage-exclusions-and-artifact-hygiene/07-NATIVE-APP-PREFLIGHT.md
autonomous: false
requirements: [RPRE-01]
must_haves:
  truths:
    - The live Snowflake-hosted Neo4j Native App supports every capability required by the Phase 7 architecture.
    - A missing required capability stops schema implementation with reproducible evidence.
  artifacts:
    - Repeatable dev-only capability preflight.
    - Dated GO or NO-GO evidence ledger.
---

<objective>
Resolve the gap left by Phase 5's contract-level verification by proving the required Neo4j Native App behaviors live before Phase 7 builds temporal generation schemas around them.
</objective>

<tasks>
<task type="execute">
  <name>Task 1: Build a bounded Native App capability preflight</name>
  <read_first>
    <file>edgar_warehouse/mdm/snowflake_graph.py</file>
    <file>edgar_warehouse/mdm/graph_readonly.py</file>
    <file>infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql</file>
    <file>.planning/workstreams/fix-pipelines/phases/05-node-and-populated-relationship-graph-parity/05-VERIFICATION.md</file>
    <file>.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-PLAN-REVIEW-QUESTIONS.md</file>
  </read_first>
  <action>Create a read-mostly dev preflight guarded by `SNOW_CONNECTION=snowconn`. Using temporary uniquely named test tables/views and cleanup traps, prove: Native App readiness; graph creation/loading from the existing node/edge contract views; typed DATE edge properties; supported `GRAPH_INFO`; BFS and a deterministic multi-hop traversal; semantic MDM↔graph parity; platform-registry discovery; and a stable view switching from test generation A to B without rebuilding the consumer-facing view name. Probe experimental `LIST_GRAPHS` for compatibility evidence only. Capture exact SQL, Native App version, database/schema, result rows, and errors. Do not alter production or persistent canonical graph tables.</action>
  <acceptance_criteria>
    <criterion>Script exits before SQL execution unless `SNOW_CONNECTION=snowconn`.</criterion>
    <criterion>Every required capability emits a machine-readable PASS or FAIL with the exact command and error.</criterion>
    <criterion>Temporary objects use unique names and are cleaned on success and failure.</criterion>
    <criterion>Integration tests verify guards, capability matrix completeness, cleanup, and fail-closed exit status.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/integration/test_neo4j_phase7_capabilities.py</verify>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Run live dev preflight and issue GO or NO-GO</name>
  <read_first>
    <file>scripts/ops/verify-neo4j-phase7-capabilities.sh</file>
    <file>.planning/workstreams/fix-pipelines/phases/07-source-coverage-exclusions-and-artifact-hygiene/07-CONTEXT.md</file>
  </read_first>
  <action>Run the preflight against dev with `SNOW_CONNECTION=snowconn`. Write `07-NATIVE-APP-PREFLIGHT.md` containing dated evidence for readiness, contract loading, typed dates, GRAPH_INFO, LIST_GRAPHS, BFS/multi-hop, and A→B stable-view switching. Issue GO only when all required capabilities pass. On any failure, issue NO-GO, include exact reproduction and ownership classification, and stop before 07-01. Do not absorb Phase 8's broader operator-readiness redesign into this plan.</action>
  <acceptance_criteria>
    <criterion>Evidence contains the live connection name, Native App version, commands, outputs, and cleanup confirmation.</criterion>
    <criterion>GO requires every required capability to pass; there is no warning-only override.</criterion>
    <criterion>NO-GO explicitly blocks 07-01 and routes the capability gap for resolution.</criterion>
  </acceptance_criteria>
  <verify>Human reviews `07-NATIVE-APP-PREFLIGHT.md` and types `approved` only for a complete GO verdict.</verify>
</task>
</tasks>

<verification>
Run the automated preflight tests, then execute the live dev check with `SNOW_CONNECTION=snowconn`; verify temporary object cleanup independently.
</verification>

<success_criteria>
- The expanded Phase 7 architecture has live Native App feasibility evidence, not only mocked contract tests.
- Missing supported graph metadata, semantic parity, platform-registry discovery, typed-property, generation-switch, or multi-hop capabilities block schema work. Experimental inventory API failures do not.
- Phase 8 retains responsibility for generalized readiness/parity error classification.
</success_criteria>

<threat_model>
- Threat: capability test mutates canonical/prod graph state. Mitigation: hard dev connection guard, unique temporary names, cleanup traps, and no canonical table writes.
- Threat: false GO from partial capability success. Mitigation: exhaustive required matrix and fail-closed aggregate verdict.
</threat_model>
