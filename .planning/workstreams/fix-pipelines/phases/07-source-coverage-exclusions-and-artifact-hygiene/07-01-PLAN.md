---
phase: 07
plan: 01
type: execute
wave: 2
depends_on: [07-00]
files_modified:
  - edgar_warehouse/mdm/database.py
  - edgar_warehouse/mdm/migrations.py
  - edgar_warehouse/mdm/graph.py
  - edgar_warehouse/mdm/relationships.py
  - tests/mdm/test_relationship_temporal_contract.py
autonomous: true
requirements: [RTEMP-01, RTEMP-03, RTEMP-04, RLINE-01]
must_haves:
  truths:
    - Relationship identity is stable across temporal versions and graph generations.
    - Business validity is date-only and half-open; provenance is explicit.
    - Conflicting overlaps cannot silently activate or destroy history.
  artifacts:
    - Additive PostgreSQL migration and ORM contract for logical relationships and versions.
    - Deterministic relationship conflict/source-priority policy.
    - Temporal contract regression suite.
---

<objective>
Establish the authoritative MDM relationship identity, temporal-version, provenance, and conflict contract used by every later generation plan.
</objective>

<tasks>
<task type="execute">
  <name>Task 1: Add the logical relationship and temporal-version schema</name>
  <read_first>
    <file>edgar_warehouse/mdm/database.py</file>
    <file>edgar_warehouse/mdm/migrations.py</file>
    <file>edgar_warehouse/mdm/graph.py</file>
    <file>.planning/workstreams/fix-pipelines/phases/07-source-coverage-exclusions-and-artifact-hygiene/07-CONTEXT.md</file>
  </read_first>
  <action>Add an additive, idempotent migration that preserves existing `instance_id` compatibility while introducing immutable logical relationship IDs and relationship-version IDs, `valid_from_date`, `valid_to_date`, `date_provenance`, `relationship_kind`, supersession/quarantine metadata, and source lineage. Enforce `valid_to_date > valid_from_date` when both are known. Backfill existing rows deterministically without physically rewriting their source provenance. Keep operational timestamps separate from business dates.</action>
  <acceptance_criteria>
    <criterion>Migration is rerunnable and preserves every pre-migration relationship row.</criterion>
    <criterion>Database constraints reject an empty/reversed half-open interval.</criterion>
    <criterion>Existing instance IDs map deterministically to stable logical/version IDs.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_relationship_temporal_contract.py -k 'migration or identity or interval'</verify>
</task>

<task type="execute">
  <name>Task 2: Implement versioning, direct/derived, and temporal conflict rules</name>
  <read_first>
    <file>edgar_warehouse/mdm/graph.py</file>
    <file>edgar_warehouse/mdm/relationships.py</file>
    <file>edgar_warehouse/mdm/database.py</file>
    <file>tests/mdm/test_pipeline_relationships.py</file>
  </read_first>
  <action>Replace duplicate-by-entire-row behavior with stable logical relationship lookup plus immutable temporal versions. Merge identical overlaps by adding provenance evidence. Resolve conflicting overlaps only through an explicit per-relationship source-priority/tie-break registry; otherwise quarantine the versions. Label direct versus governed derived relationships. Implement close/supersede/quarantine operations and expose no ordinary physical-delete operation.</action>
  <acceptance_criteria>
    <criterion>Repeated identical source evidence does not create a duplicate edge version.</criterion>
    <criterion>Conflicting overlaps without a unique configured winner are quarantined.</criterion>
    <criterion>No normal relationship command issues a physical DELETE against relationship history.</criterion>
    <criterion>Entity remap metadata preserves original source and canonical endpoint IDs.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_relationship_temporal_contract.py tests/mdm/test_pipeline_relationships.py</verify>
</task>
</tasks>

<verification>
Run `uv run pytest tests/mdm/test_relationship_temporal_contract.py tests/mdm/test_pipeline_relationships.py` and inspect the generated migration against both fresh and populated test databases.
</verification>

<success_criteria>
- Stable logical/version IDs and date provenance are queryable for every relationship.
- Half-open interval and overlap policies are deterministic and fail closed.
- Existing relationship data migrates without loss or destructive rewriting.
</success_criteria>

<threat_model>
- Threat: forged/ambiguous provenance wins a conflict. Mitigation: explicit allowlisted source priority and deterministic tie-breaks; unresolved states quarantine.
- Threat: destructive history loss. Mitigation: no ordinary delete path and migration row-count/identity assertions.
</threat_model>
