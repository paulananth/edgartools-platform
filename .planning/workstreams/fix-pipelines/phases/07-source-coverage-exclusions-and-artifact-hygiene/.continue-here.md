---
context: phase
phase: 07-source-coverage-exclusions-and-artifact-hygiene
task: 0
total_tasks: 2
status: paused
last_updated: 2026-07-13T00:17:37.349Z
---

# Phase 7 Pause Handoff

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Experimental API as health authority | The original preflight treated patch-32 `LIST_GRAPHS` failure as a hard NO-GO even though it is experimental and fails inside the Marketplace handler. | advisory | Required health checks must use semantic parity and documented supported operations; retain `LIST_GRAPHS` only as an external diagnostic. |
| Missing zero states | Node parity originally started from entity rows, causing registered adviser and fund types with zero rows to disappear and false-fail named checks. | advisory | Start parity from the active entity-type registry and left-join entities; keep the explicit zero-state regression test. |
| Internal algorithm-name coupling | The runner falsely failed an exit-0 verifier because it searched for a legacy WCC JSON fragment. | advisory | Evaluate the verifier's public `failure_domains` and `failure_summary.parity` contract. |

<current_state>

Phase 7 plan 07-00 is complete and human-approved. RPRE-01 is marked complete and the roadmap
shows 1/8 plans executed. The final dev preflight returned GO with 15,285 nodes, 1,117 edges,
semantic parity, supported GRAPH_INFO/BFS, typed dates, registry discovery, generation switching,
and cleanup all passing. Phase 8 is complete. Plan 07-01 has not started.

</current_state>

<completed_work>

Completed Tasks:

- 07-00 Task 1: repeatable bounded Native App preflight — Done.
- 07-00 Task 2: live dev GO/NO-GO evidence and human approval — Done.
- Fixed explicit zero-count entity parity in `snowflake_graph.py`.
- Updated Phase 7 runner to the current Neo4j sectioned project/compute/write API.
- Added and passed 16 targeted integration and graph-migration tests.
- Created `07-00-SUMMARY.md`; marked RPRE-01 complete and Phase 7 progress 1/8.

</completed_work>

<remaining_work>

- Wait for Claude's relevant Phase 6 work and final relationship population/zero-state ledger.
- Confirm edit ownership and cleanly isolate Codex work before starting 07-01.
- Execute plans 07-01 through 07-07 in dependency order.
- Preserve semantic health, temporal date-only relationships, monotonic publishing, audited repair,
  and the shared MDM/Neo4j active-generation pointer established in Phase 7 context.

</remaining_work>

<decisions_made>

- Semantic MDM↔Neo4j parity plus stable supported Native App operations define health.
- A platform-owned generation registry defines graph discovery and lifecycle state.
- Experimental `LIST_GRAPHS` remains visible but cannot independently block publication.
- Registered zero-count types must appear explicitly as `0 ↔ 0`; absence is not accepted as proof.
- No Codex commit may include Claude-owned `STATE.md` or Phase 6 staged artifacts.

</decisions_made>

<blockers>

- Phase 7 depends on Phase 6's final relationship population/zero-state ledger.
- Claude is actively working on Phase 6 in the shared checkout; `STATE.md` and staged
  `06-05-EDGE10-DISPOSITION.md` are outside Codex ownership.

</blockers>

## Required Reading (in order)

1. `07-00-SUMMARY.md` — completed plan, deviations, and verification result.
2. `07-NATIVE-APP-PREFLIGHT.md` — exact live GO evidence and job identifiers.
3. `07-CONTEXT.md` — locked semantic, temporal, generation, and repair decisions.
4. `07-01-PLAN.md` — next implementation plan.
5. `.planning/workstreams/fix-pipelines/REQUIREMENTS.md` — requirement status and traceability.
6. `.planning/workstreams/fix-pipelines/ROADMAP.md` — dependencies and current plan progress.

## Critical Anti-Patterns (do NOT repeat these)

- Do not use experimental `LIST_GRAPHS` as health or discovery authority; use semantic parity,
  supported operations, and the platform registry.
- Do not infer a valid zero from an absent row; emit and verify explicit registered zero states.
- Do not inspect verifier internals such as a specific algorithm name to determine health; use
  declared failure domains.
- Do not modify, stage, revert, or commit Claude-owned Phase 6 or `STATE.md` changes.

## Infrastructure State

- Snowflake dev connection: `SNOW_CONNECTION=snowconn`.
- Native App: `NEO4J_GRAPH_ANALYTICS` version `V1_0`, patch 32.
- Final live ledger: `/tmp/neo4j-phase7-preflight-20260712233410-87169.tsv`.
- No Phase 7 verifier, `snow sql`, or graph job process remains running.
- Final BFS output table was dropped successfully; temporary session objects expired.
- Experimental `LIST_GRAPHS` still fails app-internally on invalid `LIST_FILES` child-job usage.

<context>

07-00 and Phase 8 are closed, but the repository remains deliberately uncommitted because the
checkout contains concurrent Claude work. The next safe move is dependency and ownership
reconciliation—not immediate 07-01 edits.

</context>

<next_action>

Start with: check `git status --short`, inspect `.planning/active-workstream`, and confirm Claude
has completed the relevant Phase 6 dependency before executing `07-01-PLAN.md`.

</next_action>
