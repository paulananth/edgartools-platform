---
phase: 07
plan: 05
type: execute
wave: 6
depends_on: [07-01, 07-02, 07-04]
files_modified:
  - edgar_warehouse/mdm/snowflake_graph.py
  - edgar_warehouse/mdm/graph_readonly.py
  - edgar_warehouse/mdm/cli.py
  - tests/mdm/test_snowflake_graph_migration.py
  - tests/mdm/test_temporal_graph_queries.py
autonomous: true
requirements: [RSYNC-01, RSYNC-02, RSYNC-05, RTEMP-02, RLINE-01]
must_haves:
  truths:
    - One pointer exposes matching MDM serving and Neo4j node/edge generations.
    - Activation requires exact identity/property/temporal/coverage parity.
    - Historical traversal, entity remap, rollback, and retention are deterministic.
  artifacts:
    - Versioned Snowflake graph contracts and active-generation registry.
    - Guarded activation/rollback and temporal query surfaces.
---

<objective>
Make a complete verified MDM/Neo4j generation the only consumer-visible relationship state.
</objective>

<tasks>
<task type="execute">
  <name>Task 1: Version Snowflake graph contracts and shared serving views</name>
  <read_first>
    <file>edgar_warehouse/mdm/snowflake_graph.py</file>
    <file>edgar_warehouse/mdm/export.py</file>
    <file>tests/mdm/test_snowflake_graph_migration.py</file>
  </read_first>
  <action>Replace in-place `CREATE OR REPLACE TABLE` publication with additive generation, partition-manifest, coverage, and active-pointer tables. Make this platform-owned registry the authoritative graph discovery and lifecycle surface, including generation identity, projection contract/version, creation and activation dates, node/relationship counts, verification state, and active/retired status. Add `GENERATION_ID`, stable relationship/version IDs, typed `VALID_FROM_DATE`, `VALID_TO_DATE`, `DATE_PROVENANCE`, relationship kind, canonical/original endpoints, and provenance to staged graph rows. Preserve stable `GRAPH_APP_*`, `GRAPH_NODE_*`, and `GRAPH_EDGE_*` names by filtering through the active pointer. Add MDM serving views using that same pointer.</action>
  <acceptance_criteria>
    <criterion>All stable serving views resolve exactly one active generation.</criterion>
    <criterion>Node and edge rows cannot be mixed across generations.</criterion>
    <criterion>Existing view names remain available to Native App consumers.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_snowflake_graph_migration.py -k 'generation or view or temporal'</verify>
</task>

<task type="execute">
  <name>Task 2: Enforce exact verification and guarded activation/rollback</name>
  <read_first>
    <file>edgar_warehouse/mdm/snowflake_graph.py</file>
    <file>edgar_warehouse/mdm/coverage.py</file>
    <file>edgar_warehouse/mdm/generation.py</file>
    <file>edgar_warehouse/mdm/cli.py</file>
  </read_first>
  <action>Verify stable node IDs, relationship/version IDs, endpoints, query-relevant property hashes and row diffs, typed temporal fields, canonical entity remaps, manifest completeness, and coverage freshness. Activation requires verified state and atomically updates the single pointer. Rollback accepts only a retained verified generation. Cleanup retains at least the newest three verified generations and every generation within 30 days and never deletes active/immediate predecessor generations.</action>
  <acceptance_criteria>
    <criterion>A matching count with a different edge identity or property fails verification.</criterion>
    <criterion>A missing endpoint, stale exclusion, temporal conflict, or incomplete partition blocks activation.</criterion>
    <criterion>A failed activation leaves the previous pointer unchanged.</criterion>
    <criterion>Rollback switches MDM and graph views together to a verified retained generation.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_snowflake_graph_migration.py -k 'parity or activation or rollback or retention'</verify>
</task>

<task type="execute">
  <name>Task 3: Add strict current and historical graph query contracts</name>
  <read_first>
    <file>edgar_warehouse/mdm/graph_readonly.py</file>
    <file>edgar_warehouse/mdm/api/routers/graph.py</file>
    <file>edgar_warehouse/mdm/api/schemas/graph.py</file>
  </read_first>
  <action>Expose current-by-default and explicit `as_of_date` traversal filters using inclusive start/exclusive end. Strict queries exclude unknown bounds that cannot prove validity. Add explicit `include_unknown_dates` and label uncertain path results. Compute ordinary multi-hop paths at query time. Remap traversal to canonical merged entities while returning original source identity/merge lineage metadata.</action>
  <acceptance_criteria>
    <criterion>An edge ending on the query date is excluded and one starting on that date is included.</criterion>
    <criterion>Unknown-date edges are excluded by default and labeled when explicitly included.</criterion>
    <criterion>A merged-entity multi-hop path remains connected and returns original provenance.</criterion>
  </acceptance_criteria>
  <verify>uv run pytest tests/mdm/test_temporal_graph_queries.py</verify>
</task>
</tasks>

<verification>
Run Snowflake SQL rendering/executor tests and temporal traversal tests; confirm one-pointer activation and rollback alter all stable views together.
</verification>

<success_criteria>
- Consumers can never observe mismatched MDM and Neo4j relationship generations.
- Verification is identity/property/temporal exact, not count-only.
- Historical multi-hop queries and rollback work within the locked contracts.
</success_criteria>

<threat_model>
- Threat: unauthorized pointer switch exposes unverified data. Mitigation: verified-state guard, scoped Snowflake role, and immutable activation audit.
- Threat: temporal query broadening leaks uncertain facts as certain. Mitigation: strict default and explicit uncertainty labeling.
</threat_model>
