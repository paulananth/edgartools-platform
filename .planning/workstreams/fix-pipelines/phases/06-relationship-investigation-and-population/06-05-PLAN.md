---
phase: 06-relationship-investigation-and-population
plan: 05
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-EDGE10-DISPOSITION.md
autonomous: false
requirements: [EDGE-10]
must_haves:
  truths:
    - "AUDITED_BY (EDGE-10) is disposed against SEC companyfacts (entity-facts) → sec_accounting_flag.auditor_pcaob_id coverage: nonzero graph edges once the entity-facts prerequisite lands, or a written exclusion naming the companyfacts dependency"
    - "The companyfacts artifact is confirmed fetchable (not a paper-filing dead end), so the expected outcome is population once entity-facts data is present in the unified silver.duckdb"
    - "Any entity-facts run coordinates with the fundamental-factors-v2 (Codex) workstream before executing in dev"
  artifacts:
    - "06-05-EDGE10-DISPOSITION.md (entity-facts availability finding, coordination record, derive→sync→verify result or exclusion)"
  key_links:
    - "EDGE-10 was a STATE.md blocker: derivation is blocked until a fundamentals entity-facts run lands sec_accounting_flag in the unified silver/sec/silver.duckdb"
    - "_derive_audited_by already exists and is correct — this plan is data availability + coordination + graph verification, not new deriver code"
---

<objective>
Dispose AUDITED_BY (EDGE-10) against SEC companyfacts (XBRL entity-facts) →
`sec_accounting_flag.auditor_pcaob_id` coverage (D — EDGE-10 coordination decision). The
artifact is confirmed fetchable (not a paper-filing dead end like MANAGES_FUND), so the
expected outcome is nonzero graph edges once the entity-facts prerequisite lands in the unified
`silver/sec/silver.duckdb` — coordinated with the fundamental-factors-v2 (Codex) workstream to
avoid overlap (ROADMAP Success Criteria 5, 6).

Purpose: EDGE-10 was recorded in STATE.md as blocked on a fundamentals entity-facts run. This
plan clears that blocker (via 06-03's load and/or a coordinated entity-facts run), then derives
and graph-verifies AUDITED_BY.
Output: AUDITED_BY graph-verified populated or excluded-with-evidence, plus the coordination
record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-CONTEXT.md
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md
@.planning/workstreams/fundamental-factors-v2/STATE.md
@.planning/workstreams/fix-pipelines/REQUIREMENTS.md
@edgar_warehouse/mdm/pipeline.py
</context>

<artifacts_this_phase_produces>
- `06-05-EDGE10-DISPOSITION.md` — entity-facts availability finding (from 06-03 or a coordinated run), fundamental-factors-v2 coordination record, derive→sync→verify result (graph edge count) or written exclusion naming the companyfacts dependency
- (Conditional) minimal fix in `edgar_warehouse/parsers/accounting_flags.py` only if entity-facts artifacts are present but `sec_accounting_flag` is empty (parser gap)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Coordination gate for any additional entity-facts fundamentals run</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-EDGE10-DISPOSITION.md</files>
  <read_first>
    - .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md (EDGE-10 classification)
    - .planning/workstreams/fundamental-factors-v2/STATE.md (Codex's current scope)
    - CLAUDE.md ("coordinate before running fundamentals in dev"; DEC-009)
  </read_first>
  <action>
    Coordination gate for any ADDITIONAL entity-facts fundamentals run. If 06-03's load_history
    already landed `sec_accounting_flag.auditor_pcaob_id` rows, no new run is needed and this gate
    is a quick confirmation. If it did NOT (entity-facts absent from the unified silver.duckdb),
    EDGE-10 requires a fresh `bootstrap-fundamentals entity-facts` run in dev — which touches the
    fundamentals fetch path and must be coordinated with the active fundamental-factors-v2 (Codex)
    workstream per CLAUDE.md/PROJECT.md's standing caution, even though 06-CONTEXT.md assesses the
    overlap risk as low (Codex Phase 3 is gold-dbt-layer-only). Steps: (1) from
    06-03-LOAD-COVERAGE-EVIDENCE.md, confirm whether sec_accounting_flag.auditor_pcaob_id has rows
    post-load (if yes → approve, no new run needed); (2) if no → re-read
    fundamental-factors-v2/STATE.md and confirm Codex is not mid-flight on a fundamentals
    loader/fetch task in dev and no RUNNING dev fundamentals execution exists; (3) confirm any
    entity-facts run will honor DEC-009 (no --force). Record the coordination outcome and whether a
    run is needed in the disposition doc.
  </action>
  <verify>
    <automated>grep -q "sec_accounting_flag" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The disposition doc records whether a fresh entity-facts run is needed (based on 06-03's post-load sec_accounting_flag state).
    - If a run is needed, the coordination outcome (no Codex overlap, no in-flight dev execution) is recorded before any run.
  </acceptance_criteria>
  <done>Entity-facts run need determined and, if needed, Codex coordination cleared; recorded in the disposition doc.</done>
  <resume-signal>Type "approved" (state whether an entity-facts run is needed) or describe the coordination blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Ensure entity-facts prerequisite is present in unified silver</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-EDGE10-DISPOSITION.md</files>
  <read_first>
    - 06-03-LOAD-COVERAGE-EVIDENCE.md (EDGE-10 classification: sec_accounting_flag present/empty)
    - edgar_warehouse/application/commands/bootstrap_fundamentals.py (entity-facts mode → sec_accounting_flag, EDGE-10)
    - edgar_warehouse/parsers/accounting_flags.py (10-K XBRL DEI auditor / PCAOB ID parser — only if root-causing a parser gap)
    - CLAUDE.md (DEC-009 idempotency; "coordinate before running fundamentals in dev")
  </read_first>
  <action>
    If 06-03 already landed `sec_accounting_flag.auditor_pcaob_id` rows, record that and skip to
    Task 2. Otherwise (and only after the coordination checkpoint approved a run), trigger a
    bounded `bootstrap-fundamentals entity-facts` run in dev for the loaded universe — honoring
    DEC-009 (no --force; already-captured companyfacts skipped) — so the SEC companyfacts API
    responses land `sec_accounting_flag.auditor_pcaob_id` in the unified
    `s3://edgartools-dev-warehouse/warehouse/silver/sec/silver.duckdb`. If companyfacts artifacts
    are present but `sec_accounting_flag` remains empty, 5-whys the accounting_flags parser gap
    and fix or document. Record the entity-facts availability outcome (row count) and the
    coordination record (checkpoint result, Codex overlap assessment) in the disposition doc.
  </action>
  <verify>
    <automated>grep -q "sec_accounting_flag" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-EDGE10-DISPOSITION.md && grep -Eiq "entity-facts|companyfacts" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-EDGE10-DISPOSITION.md</automated>
  </verify>
  <acceptance_criteria>
    - The disposition doc records whether sec_accounting_flag.auditor_pcaob_id has rows and how they got there (06-03 load vs a coordinated entity-facts run vs still absent).
    - The fundamental-factors-v2 coordination record is present (checkpoint outcome + overlap assessment).
    - Any entity-facts run honored DEC-009 (no --force).
  </acceptance_criteria>
  <done>Entity-facts prerequisite state established and recorded; the STATE.md EDGE-10 blocker is either cleared or shown to persist with reason.</done>
</task>

<task type="auto">
  <name>Task 3: Derive AUDITED_BY, sync graph, and verify — or write the exclusion</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-EDGE10-DISPOSITION.md</files>
  <read_first>
    - edgar_warehouse/mdm/pipeline.py (lines 1214-1330 _derive_audited_by — PCAOB-id-first / firm-name-fallback resolution against the seeded audit_firm entities; deriver already exists)
    - 06-05-EDGE10-DISPOSITION.md (Task 1 entity-facts outcome)
    - infra/aws-dev-application.json (mdm_run / mdm_sync_graph ARNs); REQUIREMENTS.md (EDGE-10 text)
  </read_first>
  <action>
    If `sec_accounting_flag` has auditor rows: run the AUDITED_BY derivation against dev (mdm
    relationship derivation via `edgartools-dev-mdm-run` /
    `edgar-warehouse mdm derive-relationships --relationship-type AUDITED_BY`), then `mdm
    sync-graph` (arn:aws:states:us-east-1:690839588395:stateMachine:edgartools-dev-mdm-sync-graph),
    then confirm nonzero AUDITED_BY edges graph-side. Record insert/skip counters (note that
    long-tail firms outside the Big4/Next6 seed are acceptable skips per the deriver docstring).
    If entity-facts data could not be landed at all (Task 1 showed the artifact genuinely
    unavailable for the loaded universe), write a source-coverage exclusion naming the SEC
    companyfacts entity-facts dependency and the reason it was unobtainable this run. Record the
    resulting graph edge count or exclusion text.
  </action>
  <verify>
    <automated>grep -Eq "AUDITED_BY" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-EDGE10-DISPOSITION.md && grep -Eiq "graph edge count|source-coverage exclusion" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-EDGE10-DISPOSITION.md</automated>
  </verify>
  <acceptance_criteria>
    - Outcome is exactly one of: nonzero AUDITED_BY graph edges (with derive→sync→count evidence) OR a written exclusion naming the companyfacts entity-facts dependency.
    - If populated, the doc shows the derivation ran BEFORE sync-graph (D-05 sequencing) and graph rows exist post-sync.
    - No new deriver code was written (the existing _derive_audited_by is used).
  </acceptance_criteria>
  <done>AUDITED_BY disposed: graph-verified populated or evidenced exclusion, recorded for the closure ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SEC EDGAR companyfacts API → warehouse fetch | entity-facts XBRL responses (untrusted external) feed sec_accounting_flag |
| fundamental-factors-v2 (Codex) workstream | any fresh entity-facts run in dev could overlap another runtime |
| MDM Postgres → Snowflake graph sync | AUDITED_BY edges materialized into graph tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering (cross-workstream overlap) | dev entity-facts run vs Codex fundamental-factors-v2 | high | mitigate | Blocking coordination checkpoint confirms no in-flight Codex fundamentals work + no RUNNING dev execution before any entity-facts run |
| T-06-04 | Tampering (non-idempotent re-fetch) | companyfacts entity-facts fetch | medium | mitigate | Run without `--force`; DEC-009 requires already-captured companyfacts skip; recorded in disposition doc |
| T-06-07 | Repudiation (undocumented zero state) | AUDITED_BY left at zero without evidence | medium | mitigate | Type must exit with graph-verified rows or a written exclusion; disposition doc + 06-06 ledger enforce this |
| T-06-SC | Supply chain | package installs | low | accept | No package installs — uses existing bootstrap-fundamentals + derivers |

Block on: high. T-06-05 is held by the blocking coordination checkpoint before any entity-facts run.
</threat_model>

<verification>
- AUDITED_BY ends in graph-verified nonzero edges (post sync-graph) OR a written exclusion naming the companyfacts dependency.
- Coordination checkpoint cleared before any entity-facts run; DEC-009 honored.
</verification>

<success_criteria>
EDGE-10 is disposed against real companyfacts entity-facts coverage — populated-and-graph-verified
or excluded-with-evidence — with the fundamental-factors-v2 coordination recorded and no
undocumented zero state.
</success_criteria>

<output>
Create `.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-SUMMARY.md` when done.
</output>
