---
context: phase
phase: 06-relationship-investigation-and-population
workstream: fix-pipelines
task: "06-03 Task 2 of 3"
total_tasks: "6 plans (2 complete, 06-03 in progress, 3 not started)"
status: in_progress
last_updated: 2026-07-11T16:31:18.762Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: never-run-states-harbor-latent-bugs — `load_history` had zero dev executions before this plan; three structurally-invalid-since-written bugs were found one stage at a time (no CIK bound → `603fae3`; INLINE Maps with ItemReader → `e3c5fcb`; unprovisioned `MDM_*` MERGE targets → `caa9964`). The stages after `MdmExport` (`MdmSync`, `MdmVerify`, gold refresh) have ALSO never executed. — Mitigation: on ANY failure, pull the exact traceback from CloudWatch and run a 5-whys before touching anything; NEVER blind-retry the execution. Pre-flight each never-run stage's assumptions read-only where possible.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| blind-retry of failed executions | Two executions failed for entirely different root causes; a retry without diagnosis would have wasted 10+ hours each time | blocking | 5-whys with CloudWatch traceback before any re-trigger (per CLAUDE.md debugging discipline) |
| trusting documented runtime estimates for load_history | CLAUDE.md's "~15 min for 100 companies" describes the parallel bootstrap-batch pipeline; load_history is sequential (MaxConcurrency:1) — exec #2 took 10.6h, exec #3 is 17h+ | advisory | Treat long runtimes as expected; distinguish slow-vs-stalled via ECS task liveness + log recency, not wall clock alone |
| staging unrelated working-tree files | Working tree contains other-runtime prod artifacts that must never land on this branch | advisory | `git add` by explicit path only; never `git add -A`/`git add .`; leave `infra/aws-prodb-application.json`, `infra/snowflake/sql/prod_native_pull_handshake.json`, `infra/aws-prod-application.json` untouched |

<current_state>
Orchestrating `/gsd-execute-phase 6 --ws fix-pipelines` (6 plans, 4 waves; worktrees auto-degraded to
sequential on main tree — normal unpushed-branch divergence, #683). Wave 1 (06-01, 06-02) complete with
SUMMARYs. Wave 2 (06-03) mid-Task-2: the bounded dev `load_history` run.

Execution #3 `load-history-06-1783726338` (input `{"total_cik_limit": 150}`) has been RUNNING since
2026-07-10 23:32Z. **Suspect stall**: ~17h inside `WindowedBootstrap`'s single 150-CIK window
(map itemCounts 1 running / 0 failed / 0 succeeded of 1; map run started 23:40Z Jul 10, no stop),
while exec #2 completed ALL of Stage 1 + Stage 1B in ~10.6h doing full fetches. At pause time the
newest `warehouse-medium` log stream activity was ~47 min old, with 2 RUNNING ECS tasks on
`edgartools-dev-warehouse` (at least one may be an unrelated scheduled `daily-incremental`).
Async-job manifest: `.planning/async-jobs/load-history-06-1783726338.json`.

PR #125 (Waves 1-2 work so far, 19 commits) merged to main at user request (`2eaa650`); the branch
`claude/fix-pipelines-v2` continues for remaining phase-6 work toward a follow-up PR.
</current_state>

<completed_work>

- 06-01: INSTITUTIONAL_HOLDS CIK-range batching + 5 regression tests — done, SUMMARY committed (`b303fad`)
- 06-02: 2026-07-06 bootstrap SFN failure 5-whys (stale MDM DSN, self-resolved) + GO verdict — done, SUMMARY committed (`5524ea1`)
- 06-03 Task 1: coordination/readiness gate cleared, user approved (`e62ed3e`)
- 06-03 Task 2 (in progress): Rule 4 found (no CIK bound, `d0f20c0`) → user chose Option B →
  `--total-cik-limit` implemented (`603fae3`) → exec #1 failed (INLINE Maps, fixed `e3c5fcb` + redeploy) →
  exec #2 failed after 10.6h (MdmExport MERGE targets missing; DDL fix `caa9964`, applied to dev via snowconn) →
  evidence doc updated (`486e8b0`) → exec #3 triggered, currently RUNNING/suspect-stalled
- User-directed: committed workstream config (`f4d7417`) + codex-fix-instructions.md (`a8ee621`); PR #125 merged
</completed_work>

<remaining_work>

- 06-03 Task 2: resolve exec #3 — stall investigation first; then SUCCEEDED → Task 3, or FAILED → 5-whys
- 06-03 Task 3: per-type EDGE-09/10/11 coverage evidence (READ-ONLY — memory: never run silver-publishing
  commands locally; use mdm-counts SM or pulled silver.duckdb copy), then SUMMARY.md → STATE/ROADMAP updates
- Wave 3: 06-04 (autonomous), 06-05 (BLOCKING human checkpoint — Codex/fundamental-factors-v2 coordination)
- Wave 4: 06-06 (autonomous; closes EDGE-05/06 + phase ledger + REQUIREMENTS.md)
- Phase: gsd-verifier spawn, verification routing, phase completion, follow-up PR
</remaining_work>

<decisions_made>

- Option B (user): real `--total-cik-limit` SM input over MDM state mutation or unbounded run
- Retry-after-root-caused-fix needs no fresh approval; blind retry is forbidden
- prodb/prod infra files stay uncommitted (user: "commit config.json, and codex leave the rest")
- PR #125 merged mid-phase (user: "merge"); merge-commit convention per repo history
- REQUIREMENTS.md note: 06-02's executor marked EDGE-09/10/11 Complete early (plan frontmatter lists all
  three); actual dispositions land in 06-04/06-05/06-06 — verifier should validate against dispositions,
  not the early checkmarks
</decisions_made>

<blockers>
- exec #3 possible stall (~17h in WindowedBootstrap): apply stall policy — identify the WindowedBootstrap
  child's ECS task (vs the unrelated daily-incremental tasks), tail its warehouse-medium log stream (look
  for SEC 429/backoff loops), then choose: continue waiting / stop execution + re-trigger
  `{"total_cik_limit": 150}` (no --force) / deeper root-cause. Child execs of the DISTRIBUTED map are
  separate execution ARNs — find via `aws stepfunctions describe-map-run` / `list-executions` on the map run.
</blockers>

## Required Reading (in order)
1. `.planning/async-jobs/load-history-06-1783726338.json` — the live external job; primary resume context
2. `06-03-LOAD-COVERAGE-EVIDENCE.md` — full 3-execution evidence trail + 5-whys chains
3. `06-03-PLAN.md` — Tasks 2/3 acceptance criteria (Task 3 is read-only evidence capture)
4. `CLAUDE.md` — load_history recipe, DEC-009 idempotency, 5-whys discipline, snowconn naming

## Infrastructure State
- exec #3 `load-history-06-1783726338`: RUNNING (verify first — may have finished or failed since pause)
- Dev Snowflake: `MDM_COMPANY/ADVISER/PERSON/SECURITY/FUND` now exist in `EDGARTOOLS_DEV.EDGARTOOLS_GOLD`
  (bootstrap/07_mdm_export_targets.sql, applied via snowconn)
- Dev SM/task defs: deployed at revisions in committed `infra/aws-dev-application.json` (small:42, medium:39,
  large:39, mdm-small:19, mdm-medium:19); exec #3 pinned to these — post-merge CI image rebuilds don't affect it
- No watcher running (session-bound background jobs died with prior sessions)
- Prior executor agent IDs are dead; spawn fresh continuation agents seeded from disk/git/AWS state

<context>
The whole point of 06-03 is producing coverage evidence so Wave 3 can decide populate-vs-exclude for
EDGE-09/10/11. The load run is instrumental, not the deliverable — if exec #3 is unrecoverable, consider
whether exec #2's already-captured Stage 1/1B artifacts (10.6h of real fetching, all silver-loaded up
through MdmBackfill) already satisfy Task 3's evidence needs; a full green end-to-end run matters for the
pipeline-hardening story but the phase's EDGE dispositions primarily need the silver row counts.
</context>

<next_action>
Start with: run the async-job manifest's verification_command. If RUNNING → stall investigation per
<blockers>. If SUCCEEDED → fresh continuation executor for 06-03 Task 3 + SUMMARY (then Waves 3-4 via
`/gsd-execute-phase 6 --ws fix-pipelines`). If FAILED → CloudWatch traceback + 5-whys before anything.
</next_action>
