---
phase: 06-relationship-investigation-and-population
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified:
  - .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-04-EDGE09-EDGE11-DISPOSITION.md
autonomous: true
requirements: [EDGE-09, EDGE-11]
must_haves:
  truths:
    - "EMPLOYED_BY (EDGE-09) is root-caused against DEF 14A → sec_executive_record coverage and ends in either nonzero graph edges or a written source-coverage exclusion naming its artifact dependency"
    - "INSTITUTIONAL_HOLDS (EDGE-11) is root-caused against 13F-HR → sec_thirteenf_holding coverage (using the 06-01 batched deriver) and ends in either nonzero graph edges or a written exclusion"
    - "Any newly-fetched DEF 14A / 13F artifact honors DEC-009 (no --force; already-captured filings skipped)"
  artifacts:
    - "06-04-EDGE09-EDGE11-DISPOSITION.md (per-type: coverage finding, action taken, final graph edge count or exclusion text)"
  key_links:
    - "Per D-05 sequencing: for any populated type, MDM relationship derivation runs, THEN mdm sync-graph, THEN graph rows exist — verify the sync ran before asserting graph-side counts"
    - "EDGE-11 derivation at scale must use the 06-01 CIK-range batched _derive_institutional_holds to avoid OOM"
---

<objective>
Root-cause and dispose EMPLOYED_BY (EDGE-09) and INSTITUTIONAL_HOLDS (EDGE-11) against the
06-03 coverage evidence: for each, either populate (derive → sync-graph → confirm graph rows)
or write an evidenced source-coverage exclusion naming its artifact dependency. No type exits in
an undocumented zero state (ROADMAP Success Criteria 3, 4, 6).

Purpose: These two are the DEF-14A- and 13F-backed relationship types whose zero-row status is
"never run at scale," not "missing integration." With 06-03 having loaded real data, the
investigation is now against actual artifact/silver coverage.
Output: Both types populated-and-graph-verified or excluded-with-evidence, recorded in a
disposition doc for the 06-06 closure ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-CONTEXT.md
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md
@.planning/workstreams/fix-pipelines/REQUIREMENTS.md
@edgar_warehouse/mdm/pipeline.py
@infra/aws-dev-application.json
</context>

<artifacts_this_phase_produces>
- `06-04-EDGE09-EDGE11-DISPOSITION.md` — per type (EMPLOYED_BY, INSTITUTIONAL_HOLDS): coverage classification carried from 06-03, action taken (derive/sync/verify OR root-cause parser gap OR fetchability triage OR exclusion), and the resulting graph edge count or exclusion statement
- (Conditional) minimal parser/pipeline fix in `edgar_warehouse/parsers/proxy_fundamentals.py` (EDGE-09) and/or `edgar_warehouse/parsers/thirteenf.py` (EDGE-11) only if 06-03 showed "artifact present, silver empty"
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EDGE-09 EMPLOYED_BY — root-cause DEF 14A coverage, then populate or exclude</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-04-EDGE09-EDGE11-DISPOSITION.md</files>
  <read_first>
    - 06-03-LOAD-COVERAGE-EVIDENCE.md (EDGE-09 classification: sec_executive_record present/empty, DEF 14A artifact present/missing)
    - edgar_warehouse/mdm/pipeline.py (lines 1114-1212 _derive_employed_by — the deriver already exists and is correct)
    - edgar_warehouse/parsers/proxy_fundamentals.py (DEF 14A parser — only if root-causing an artifact-present/silver-empty gap)
    - REQUIREMENTS.md (EDGE-09 text); CLAUDE.md (DEC-009 idempotency; 5-whys discipline)
  </read_first>
  <action>
    Branch on the 06-03 EDGE-09 classification. If ARTIFACT+SILVER PRESENT: run the EMPLOYED_BY
    derivation against dev (mdm relationship derivation via the `edgartools-dev-mdm-run` state
    machine / `edgar-warehouse mdm derive-relationships --relationship-type EMPLOYED_BY`), then
    run `mdm sync-graph` (state machine
    arn:aws:states:us-east-1:690839588395:stateMachine:edgartools-dev-mdm-sync-graph), then
    confirm nonzero EMPLOYED_BY edges graph-side. If ARTIFACT PRESENT, SILVER EMPTY: 5-whys the
    proxy_fundamentals parser/pipeline gap (why DEF 14A bronze did not yield sec_executive_record
    rows) and apply the minimal fix or document the blocker. If ARTIFACT MISSING: triage
    fetchability — determine whether `bootstrap-fundamentals` per-filing mode can fetch the DEF
    14A documents for the loaded universe (honoring DEC-009: no --force), and either fetch+derive
    or write a source-coverage exclusion naming the DEF 14A dependency. Record the branch taken,
    commands, and the resulting graph edge count or exclusion text in the disposition doc.
  </action>
  <verify>
    <automated>grep -Eq "EMPLOYED_BY" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-04-EDGE09-EDGE11-DISPOSITION.md && grep -Eiq "graph edge count|source-coverage exclusion" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-04-EDGE09-EDGE11-DISPOSITION.md</automated>
  </verify>
  <acceptance_criteria>
    - The disposition doc states EDGE-09's 06-03 classification and the branch taken.
    - Outcome is exactly one of: nonzero EMPLOYED_BY graph edges (with the sync-then-count evidence) OR a written exclusion naming the DEF 14A artifact dependency.
    - If a parser fix was applied, it is committed with a one-line root-cause rationale; any fetch honored DEC-009 (no --force).
  </acceptance_criteria>
  <done>EMPLOYED_BY disposed: graph-verified populated or evidenced exclusion, recorded for the closure ledger.</done>
</task>

<task type="auto">
  <name>Task 2: EDGE-11 INSTITUTIONAL_HOLDS — populate via batched deriver or exclude</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-04-EDGE09-EDGE11-DISPOSITION.md</files>
  <read_first>
    - 06-03-LOAD-COVERAGE-EVIDENCE.md (EDGE-11 classification: sec_thirteenf_holding present/empty, 13F artifact present/missing)
    - edgar_warehouse/mdm/pipeline.py (lines 1330-1436 _derive_institutional_holds — now CIK-range batched by 06-01)
    - edgar_warehouse/parsers/thirteenf.py + edgar_warehouse/scripts/build_13f_filer_list.py (only if root-causing an artifact-present/silver-empty gap)
    - REQUIREMENTS.md (EDGE-11 text); 06-CONTEXT.md (D-03 batching; adviser resolution is per-CIK)
  </read_first>
  <action>
    Branch on the 06-03 EDGE-11 classification. If ARTIFACT+SILVER PRESENT: run the
    INSTITUTIONAL_HOLDS derivation against dev — this now uses the 06-01 CIK-range batched
    `_derive_institutional_holds`, so it is safe at scale (no OOM) — then `mdm sync-graph`, then
    confirm nonzero INSTITUTIONAL_HOLDS edges graph-side. Note the adviser side depends on the
    13F filer list bootstrap having produced mdm_adviser rows for the filing CIKs; if source
    advisers are unresolved, record the skip counts. If ARTIFACT PRESENT, SILVER EMPTY: 5-whys the
    thirteenf parser / 13F filer-list gap and fix or document. If ARTIFACT MISSING: triage
    fetchability (13F-HR INFORMATION TABLE fetch via bootstrap-fundamentals thirteenf mode,
    honoring DEC-009) and either fetch+derive or write a source-coverage exclusion naming the 13F
    dependency. Record the branch, commands, batched-run confirmation, and the resulting graph
    edge count or exclusion text.
  </action>
  <verify>
    <automated>grep -Eq "INSTITUTIONAL_HOLDS" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-04-EDGE09-EDGE11-DISPOSITION.md && grep -Eiq "graph edge count|source-coverage exclusion" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-04-EDGE09-EDGE11-DISPOSITION.md</automated>
  </verify>
  <acceptance_criteria>
    - The disposition doc states EDGE-11's 06-03 classification and the branch taken, and confirms the batched deriver was used for any at-scale run.
    - Outcome is exactly one of: nonzero INSTITUTIONAL_HOLDS graph edges (with sync-then-count evidence) OR a written exclusion naming the 13F artifact dependency.
    - Any 13F fetch honored DEC-009 (no --force).
  </acceptance_criteria>
  <done>INSTITUTIONAL_HOLDS disposed: graph-verified populated (via batched deriver) or evidenced exclusion, recorded for the closure ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| silver DuckDB → MDM derivation | DEF 14A / 13F silver rows drive relationship derivation |
| MDM Postgres → Snowflake graph sync | derived edges materialized into graph tables |
| SEC EDGAR → warehouse fetch | any fetchability-triage fetch pulls untrusted external documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering (non-idempotent re-fetch) | DEF 14A / 13F fetchability triage | medium | mitigate | Any triage fetch runs without `--force` per DEC-009; already-captured filings skip; recorded in disposition doc |
| T-06-02 | Denial of Service (OOM at 13F scale) | INSTITUTIONAL_HOLDS derivation over sec_thirteenf_holding | high | mitigate | Use the 06-01 CIK-range batched deriver for any at-scale run (dependency enforced via depends_on: 06-01) |
| T-06-07 | Repudiation (undocumented zero state) | a type left at zero with no evidence | medium | mitigate | Each type must exit with graph-verified rows or a written exclusion; disposition doc + 06-06 ledger enforce this |
| T-06-SC | Supply chain | package installs | low | accept | No package installs — runs existing derivers/state machines |

Block on: high. T-06-02 is mitigated by mandating the 06-01 batched deriver for at-scale runs.
</threat_model>

<verification>
- Each of EMPLOYED_BY and INSTITUTIONAL_HOLDS ends in graph-verified nonzero edges (post sync-graph) OR a written source-coverage exclusion naming its artifact dependency.
- Any at-scale INSTITUTIONAL_HOLDS run used the batched deriver; any fetch honored DEC-009.
</verification>

<success_criteria>
EDGE-09 and EDGE-11 are each root-caused against their real (post-06-03) artifact coverage and
end in a documented, evidenced disposition — populated-and-graph-verified or excluded — with no
undocumented zero state.
</success_criteria>

<output>
Create `.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-04-SUMMARY.md` when done.
</output>
