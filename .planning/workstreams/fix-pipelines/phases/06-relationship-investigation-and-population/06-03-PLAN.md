---
phase: 06-relationship-investigation-and-population
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md
autonomous: false
requirements: [EDGE-09, EDGE-10, EDGE-11]
user_setup:
  - service: aws
    why: "Triggering the dev load_history Step Function and querying dev silver requires AWS creds for account 690839588395 (us-east-1)"
    env_vars:
      - name: AWS_DEFAULT_REGION
        source: "us-east-1 (infra region, not the default us-east-2)"
must_haves:
  truths:
    - "A bounded load_history execution (~100-200 companies, D-02) ran to SUCCEEDED in dev 690839588395"
    - "Post-load, the presence/absence of each EDGE-09/10/11 source is recorded as evidence: DEF 14A → sec_executive_record, companyfacts → sec_accounting_flag, 13F-HR → sec_thirteenf_holding"
    - "The run honored SEC idempotency (DEC-009): no --force, already-captured filings skipped by default"
  artifacts:
    - "06-03-LOAD-COVERAGE-EVIDENCE.md (execution ARN + status, per-type bronze-artifact presence and silver row counts, cost/time notes)"
  key_links:
    - "The load includes Stage1B fundamentals (entity-facts / per-filing / thirteenf) — a coordination checkpoint with the fundamental-factors-v2 (Codex) workstream must clear before triggering"
    - "This evidence is the input Wave 3 (06-04/06-05) uses to decide populate-vs-exclude per relationship type"
---

<objective>
Trigger a bounded `load_history` execution in dev at ~100-200 companies (D-02) and capture the
artifact-coverage evidence EDGE-09/EDGE-10/EDGE-11 population decisions depend on. D-02 is a
deliberate user override of the advisor-research "investigate against existing data first"
recommendation: load more real data now. ~100-200 companies matches CLAUDE.md's documented
scale (~15 min via parallel ECS batches) — enough breadth to likely surface DEF 14A / 13F /
XBRL coverage without full-active-universe cost/time.

Purpose: `load_history` has zero prior dev executions; this is its first real exercise, and the
run is what makes DEF 14A / companyfacts / 13F silver tables available for the three
artifact-backed relationship types.
Output: A completed bounded load plus a per-type coverage evidence doc for Wave 3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-CONTEXT.md
@.planning/workstreams/fundamental-factors-v2/STATE.md
@infra/aws-dev-application.json
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-02-BOOTSTRAP-FAILURE-FINDINGS.md
</context>

<artifacts_this_phase_produces>
- `06-03-LOAD-COVERAGE-EVIDENCE.md` — load_history execution ARN + final status; per relationship type (EDGE-09/10/11): bronze artifact present? silver table row count; cost/time notes; idempotency (DEC-009) confirmation
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Coordination + readiness gate before triggering the dev load</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md</files>
  <read_first>
    - .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-02-BOOTSTRAP-FAILURE-FINDINGS.md (the load_history readiness verdict)
    - .planning/workstreams/fundamental-factors-v2/STATE.md (Codex's current scope)
    - 06-CONTEXT.md (D-02; the low-but-nonzero fundamentals overlap assessment)
  </read_first>
  <action>
    Precondition gate before triggering the dev load. Two things must hold: (1) 06-02 issued a
    load_history readiness verdict of GO, and (2) running Stage1B fundamentals (entity-facts /
    per-filing / thirteenf) in dev does not overlap the active fundamental-factors-v2 (Codex)
    workstream. Per 06-CONTEXT.md, Codex's current Phase 3 is gold-dbt-layer-only ("no new
    loader, no new SEC fetch path") and does not touch bootstrap_fundamentals.py,
    accounting_flags.py, proxy_fundamentals.py, or thirteenf.py — so overlap risk is low — but
    CLAUDE.md/PROJECT.md's standing "coordinate before running fundamentals in dev" caution still
    applies procedurally. Steps: (1) confirm 06-02-BOOTSTRAP-FAILURE-FINDINGS.md's readiness
    verdict reads GO (not NO-GO); (2) re-read fundamental-factors-v2/STATE.md and confirm Codex is
    not mid-flight on a fundamentals loader/fetch task in dev; (3) confirm no in-flight dev
    execution via `aws stepfunctions list-executions --region us-east-1 --state-machine-arn
    arn:aws:states:us-east-1:690839588395:stateMachine:edgartools-dev-load-history
    --status-filter RUNNING` returns none. Record the coordination outcome in the evidence doc and
    do not proceed to Task 2 until the operator confirms GO and no overlap.
  </action>
  <verify>
    <automated>grep -Eiq "GO" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-02-BOOTSTRAP-FAILURE-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 06-02 readiness verdict is GO (not NO-GO); the coordination outcome is recorded in the evidence doc.
    - No RUNNING dev load_history / fundamentals execution is in flight, and Codex is not mid-flight on a dev fundamentals task.
  </acceptance_criteria>
  <done>Operator confirmed GO + no Codex fundamentals overlap; coordination outcome recorded in the evidence doc.</done>
  <resume-signal>Type "approved" to proceed with the bounded load, or describe the overlap/NO-GO blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Trigger + monitor the bounded load_history execution (D-02)</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md</files>
  <read_first>
    - CLAUDE.md ("Phased Pipeline" section — load_history usage, the documented start-execution recipe, BOOTSTRAP_BATCH_CONCURRENCY 2-5, and the 9 req/sec per-task SEC limiter; SEC idempotency / DEC-009)
    - infra/aws-dev-application.json (line 39 load_history ARN: arn:aws:states:us-east-1:690839588395:stateMachine:edgartools-dev-load-history)
    - 06-CONTEXT.md (D-02, and Claude's Discretion on --tracking-status-filter active vs an explicit --cik-list/limit)
  </read_first>
  <action>
    Start a bounded `load_history` execution against
    `arn:aws:states:us-east-1:690839588395:stateMachine:edgartools-dev-load-history` sized to
    ~100-200 companies. Choose the bounding mechanism per Claude's Discretion (D-02): either
    `--tracking-status-filter active` capped to that range, or an explicit `--cik-list`/limit —
    pick whichever is expected to surface the broadest DEF 14A / 13F / XBRL coverage for
    EDGE-09/10/11. Do NOT pass `--force`; already-captured filings must skip by default
    (DEC-009). Name the execution `load-history-06-<timestamp>`. Monitor with
    `aws stepfunctions describe-execution --execution-arn <arn> --query status` until it reaches
    SUCCEEDED (or FAILED — if FAILED, stop and record the failure; do not retry blindly). Record
    the execution ARN, input, final status, wall-clock duration, and the effective company count
    in the evidence doc.
  </action>
  <verify>
    <automated>test "$(aws stepfunctions describe-execution --region us-east-1 --execution-arn "$(grep -oE 'arn:aws:states:us-east-1:690839588395:execution:[^ )]+' .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md | head -1)" --query status --output text)" = "SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - The evidence doc records a real dev `load_history` execution ARN whose `describe-execution` status is SUCCEEDED.
    - The run was bounded to ~100-200 companies (input recorded), not the full active universe.
    - No `--force` was used; idempotency (DEC-009) is noted.
  </acceptance_criteria>
  <done>Bounded load_history execution SUCCEEDED in dev; ARN, input, status, and duration recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Capture per-type artifact-coverage evidence for EDGE-09/10/11</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md</files>
  <read_first>
    - 06-CONTEXT.md (<code_context> — MDM derivation reads from silver DuckDB `s3://edgartools-dev-warehouse/warehouse/silver/sec/silver.duckdb`, not EDGARTOOLS_SOURCE)
    - .claude/CLAUDE.md / MEMORY ("No local silver publish" — read counts only; never run silver-PUBLISHING commands locally against dev S3)
    - REQUIREMENTS.md (EDGE-09 sec_executive_record / EDGE-10 sec_accounting_flag / EDGE-11 sec_thirteenf_holding source mappings)
  </read_first>
  <action>
    After the load, record per relationship type whether its source landed, using READ-ONLY
    access to the canonical dev silver (do not run any silver-publishing command locally — memory
    "No local silver publish"). For each of EDGE-09 (`sec_executive_record`, from DEF 14A), EDGE-10
    (`sec_accounting_flag.auditor_pcaob_id`, from companyfacts entity-facts), EDGE-11
    (`sec_thirteenf_holding`, from 13F-HR): capture (a) whether the bronze artifact class is present
    for the loaded universe, and (b) the silver table row count. Obtain counts via a read-only path
    — e.g. the `edgartools-dev-mdm-counts` state machine
    (arn:aws:states:us-east-1:690839588395:stateMachine:edgartools-dev-mdm-counts) or a read-only
    query against a pulled copy of silver.duckdb — never a write/publish. Write these into
    `06-03-LOAD-COVERAGE-EVIDENCE.md` as a per-type table classifying each as one of:
    ARTIFACT+SILVER PRESENT (ready to populate) / ARTIFACT PRESENT, SILVER EMPTY (parser/pipeline
    gap to root-cause in Wave 3) / ARTIFACT MISSING (fetchability triage in Wave 3). Flag EDGE-10
    explicitly with the fundamental-factors-v2 coordination note carried from 06-CONTEXT.md.
  </action>
  <verify>
    <automated>grep -q "sec_executive_record" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md && grep -q "sec_accounting_flag" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md && grep -q "sec_thirteenf_holding" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-LOAD-COVERAGE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The evidence doc has a per-type row for EDGE-09, EDGE-10, EDGE-11 naming the source table and its post-load row count.
    - Each type is classified ARTIFACT+SILVER PRESENT / ARTIFACT PRESENT+SILVER EMPTY / ARTIFACT MISSING.
    - No silver-publishing command was run locally (counts obtained read-only); EDGE-10 carries the Codex coordination note.
  </acceptance_criteria>
  <done>Per-type coverage evidence recorded, giving Wave 3 the populate-vs-root-cause-vs-triage input for each artifact-backed type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/AWS → dev Step Functions | Triggering a real, cost-bearing load_history run in dev 690839588395 |
| SEC EDGAR → warehouse fetch | Untrusted external SEC responses fetched at batch scale; per-IP rate limits apply |
| fundamental-factors-v2 (Codex) workstream | Stage1B fundamentals fetch could overlap another runtime's dev work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Denial of Service (cost / SEC rate-limit ban) | bounded load_history run + SEC fetch fan-out | high | mitigate | Bound to ~100-200 companies (D-02); rely on BOOTSTRAP_BATCH_CONCURRENCY 2-5 and the 9 req/sec per-task limiter; gate behind 06-02 GO verdict |
| T-06-04 | Tampering (non-idempotent re-fetch) | Stage1B artifact fetch | medium | mitigate | Run without `--force`; DEC-009 requires already-captured filings skip by default; recorded in evidence doc |
| T-06-05 | Tampering (cross-workstream overlap) | dev fundamentals run vs Codex fundamental-factors-v2 | high | mitigate | Blocking coordination checkpoint confirms no in-flight Codex fundamentals work + no RUNNING dev execution before trigger |
| T-06-SC | Supply chain | package installs | low | accept | No package installs — uses existing published ECR images for the ECS tasks |

Block on: high. T-06-02 and T-06-05 are each held by a blocking gate (06-02 GO verdict; coordination checkpoint) before the run.
</threat_model>

<verification>
- load_history execution reaches SUCCEEDED (describe-execution status).
- Evidence doc classifies EDGE-09/10/11 source coverage post-load.
- Coordination checkpoint cleared; DEC-009 idempotency honored (no --force).
</verification>

<success_criteria>
A bounded ~100-200 company load_history run completed in dev and produced a per-type
artifact-coverage evidence doc that tells Wave 3 which of EDGE-09/10/11 to populate, root-cause,
or triage for fetchability.
</success_criteria>

<output>
Create `.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-03-SUMMARY.md` when done.
</output>
