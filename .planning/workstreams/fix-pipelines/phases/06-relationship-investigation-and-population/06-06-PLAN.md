---
phase: 06-relationship-investigation-and-population
plan: 06
type: execute
wave: 4
depends_on: [06-04, 06-05]
files_modified:
  - edgar_warehouse/mdm/snowflake_graph.py
  - tests/mdm/test_cli_snowflake_graph.py
  - .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-06-EDGE05-EDGE06-DISPOSITION.md
  - .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-PHASE-CLOSURE-LEDGER.md
  - .planning/workstreams/fix-pipelines/REQUIREMENTS.md
autonomous: true
requirements: [EDGE-05, EDGE-06]
must_haves:
  truths:
    - "IS_ENTITY_OF (EDGE-05) and IS_PERSON_OF (EDGE-06) are closed via the D-04 SQL-confirmed zero-overlap check against the post-load MDM universe: populated if CIK overlap exists, else a written exclusion scoped to the current tracking-list universe"
    - "Every relationship type this phase confirmed graph-populated is added to POPULATED_RELATIONSHIP_TYPES so verify-graph emits a named parity check for it (D-05), sequenced derive→sync→verify"
    - "A phase closure ledger shows each of EDGE-05/06/09/10/11 ended in exactly one of {nonzero graph-verified rows | written evidenced source-coverage exclusion} — no undocumented zero state (Success Criterion 6)"
  artifacts:
    - "edgar_warehouse/mdm/snowflake_graph.py (POPULATED_RELATIONSHIP_TYPES extended; docstring comment updated)"
    - "tests/mdm/test_cli_snowflake_graph.py (named parity-check tests for each newly-populated type)"
    - "06-06-EDGE05-EDGE06-DISPOSITION.md; 06-PHASE-CLOSURE-LEDGER.md; REQUIREMENTS.md traceability updates"
  key_links:
    - "Do NOT add a type to POPULATED_RELATIONSHIP_TYPES before mdm sync-graph has run for it — 05-03's design makes that fail closed on purpose"
    - "verify-graph must remain the single atomic gate consumed by Step Functions mdm_verify_graph and go-live.sh — do not add a second/parallel verification path (Phase 5 D-01)"
---

<objective>
Close EDGE-05 (IS_ENTITY_OF) and EDGE-06 (IS_PERSON_OF) via the D-04 SQL-confirmed zero-overlap
check against the post-06-03 MDM universe, extend `POPULATED_RELATIONSHIP_TYPES` with every type
this phase confirmed graph-populated (D-05), and produce the phase closure ledger proving every
investigated type ended in a documented disposition (Success Criterion 6).

Purpose: EDGE-05/06 have no source artifact — their pairing comes from MDM's own
`mdm_adviser.linked_company_entity_id` and the `MdmAdviser.cik == MdmPerson.owner_cik` crosswalk.
D-04 closes them with a SQL check that independently verifies exactly what
`AdviserResolver._link_to_company` / `_adviser_person_pairs` already compute. D-05 makes any
newly-populated type graph-verified through the same single verify-graph gate 05-03 built.
Output: EDGE-05/06 disposed; verify-graph named-checking every phase-populated type; a closure
ledger; REQUIREMENTS.md updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-CONTEXT.md
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-PATTERNS.md
@.planning/workstreams/fix-pipelines/REQUIREMENTS.md
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-04-EDGE09-EDGE11-DISPOSITION.md
@.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-05-EDGE10-DISPOSITION.md
@edgar_warehouse/mdm/snowflake_graph.py
@edgar_warehouse/mdm/pipeline.py
</context>

<artifacts_this_phase_produces>
- Extended `POPULATED_RELATIONSHIP_TYPES` tuple in `edgar_warehouse/mdm/snowflake_graph.py` (adds each phase-confirmed-populated type; comment moved out of the "remaining" enumeration)
- New named parity-check tests in `tests/mdm/test_cli_snowflake_graph.py` (all-present-and-ok + missing-type-fails-closed) for each newly-populated type, mirroring 05-03
- `06-06-EDGE05-EDGE06-DISPOSITION.md` — CIK-overlap counts + populate/exclusion outcome for IS_ENTITY_OF and IS_PERSON_OF
- `06-PHASE-CLOSURE-LEDGER.md` — per-type final disposition for EDGE-05/06/09/10/11
- REQUIREMENTS.md traceability status updates for the 5 phase requirement IDs
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EDGE-05/06 SQL-confirmed zero-overlap closure (D-04)</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-06-EDGE05-EDGE06-DISPOSITION.md</files>
  <read_first>
    - edgar_warehouse/mdm/pipeline.py (lines 1438-1454 _adviser_company_pairs / _adviser_person_pairs — the exact join logic to independently verify; lines 662-726 _derive_is_entity_of / _derive_is_person_of)
    - edgar_warehouse/mdm/resolvers/adviser.py (~line 167 _link_to_company — the confirmed-active resolver that populates linked_company_entity_id)
    - 06-CONTEXT.md (D-04 closure bar and the "scoped to current tracking-list universe" requirement); REQUIREMENTS.md (EDGE-05, EDGE-06 text)
  </read_first>
  <action>
    Run the D-04 SQL-confirmed zero-overlap check against the post-06-03 dev MDM universe. For
    EDGE-05 (IS_ENTITY_OF): compute the CIK intersection of `MdmCompany.cik` and `MdmAdviser.cik`
    for advisers with `linked_company_entity_id IS NOT NULL` — mirroring `_adviser_company_pairs`.
    For EDGE-06 (IS_PERSON_OF): compute the CIK intersection of `MdmPerson.owner_cik` and
    `MdmAdviser.cik` — mirroring `_adviser_person_pairs`. Record the exact overlap counts. If a
    type has nonzero overlap: run its derivation (mdm derive-relationships for that type) → `mdm
    sync-graph` → confirm graph rows, and mark it for inclusion in Task 2's tuple. If a type has
    zero overlap: write a source-coverage exclusion that explicitly scopes the finding to "as of
    the current tracking-list universe (post-06-03 load); re-check required if the tracking list
    expands" — per D-04, this is not a weaker proxy, it independently verifies what the
    always-on `_link_to_company` resolver already computes. Record both types' overlap counts and
    dispositions in the disposition doc.
  </action>
  <verify>
    <automated>grep -q "IS_ENTITY_OF" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-06-EDGE05-EDGE06-DISPOSITION.md && grep -q "IS_PERSON_OF" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-06-EDGE05-EDGE06-DISPOSITION.md && grep -Eiq "tracking-list universe|graph edge count" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-06-EDGE05-EDGE06-DISPOSITION.md</automated>
  </verify>
  <acceptance_criteria>
    - The disposition doc records exact CIK-overlap counts for both EDGE-05 (company∩adviser) and EDGE-06 (person.owner_cik∩adviser).
    - Each type ends in exactly one of: nonzero graph-verified edges (derive→sync→count evidence) OR a written exclusion scoped explicitly to "the current tracking-list universe."
    - The check mirrors the production `_adviser_company_pairs` / `_adviser_person_pairs` join logic (independent verification, not a different query).
  </acceptance_criteria>
  <done>IS_ENTITY_OF and IS_PERSON_OF disposed via the D-04 SQL check; any populated type flagged for Task 2's tuple.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend POPULATED_RELATIONSHIP_TYPES + named parity checks for populated types (D-05)</name>
  <files>edgar_warehouse/mdm/snowflake_graph.py, tests/mdm/test_cli_snowflake_graph.py</files>
  <read_first>
    - edgar_warehouse/mdm/snowflake_graph.py (lines 28-39 the ALLOWED/POPULATED tuples + docstring comment; lines 1193-1260 _named_node_parity_checks / _named_relationship_parity_checks; verify() wiring ~313-314)
    - tests/mdm/test_cli_snowflake_graph.py (the 05-03 named-relationship-check tests + _all_4_populated_relationship_rows_at_parity helper to mirror)
    - 06-04-EDGE09-EDGE11-DISPOSITION.md + 06-05-EDGE10-DISPOSITION.md + 06-06-EDGE05-EDGE06-DISPOSITION.md (which types were confirmed graph-populated this phase)
    - 06-PATTERNS.md (D-05 section — tuple edit is the entire code change; the check function is already generic)
  </read_first>
  <behavior>
    - For each relationship type this phase confirmed graph-populated, verify-graph emits a `relationship_parity_<type>` named check present in payload["named_checks"]["relationship_parity"] with an ok status at parity.
    - A newly-added populated type that is entirely absent from the parity rows fails closed (present=False → status failed), flipping the exit code, exactly like the 05-03 types.
    - Types this phase left as documented exclusions (and the still-deferred HAS_PARENT_COMPANY / MANAGES_FUND) do NOT appear as named checks — they must not false-fail verify-graph.
  </behavior>
  <action>
    Add each relationship type this phase confirmed graph-populated (from the three disposition
    docs — candidates: EMPLOYED_BY, INSTITUTIONAL_HOLDS, AUDITED_BY, IS_ENTITY_OF, IS_PERSON_OF,
    whichever ended nonzero-graph-verified) to the `POPULATED_RELATIONSHIP_TYPES` tuple in
    `edgar_warehouse/mdm/snowflake_graph.py`, and update the docstring comment above the tuple to
    move each added type out of the "remaining" enumeration while keeping the T-05-05 false-fail
    rationale for types still deferred/excluded. The `_named_relationship_parity_checks` function
    needs NO change — it already iterates the tuple generically; editing the tuple + comment is
    the entire code change (per D-05 / 06-PATTERNS.md). Sequencing guard: only add a type whose
    `mdm sync-graph` has already run (confirmed in its disposition doc) — adding a type before its
    sync fails closed by 05-03's design, which is correct behavior, not a bug to route around. Add
    named parity-check tests in `tests/mdm/test_cli_snowflake_graph.py` mirroring 05-03's pattern:
    an all-newly-populated-types-present-and-ok test and a missing-type-fails-closed test, using
    the existing FakeSnowflakeConnection/build_parser harness (no live Snowflake/AWS).
  </action>
  <verify>
    <automated>uv run --extra s3 --extra snowflake --extra mdm pytest tests/mdm/test_cli_snowflake_graph.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "POPULATED_RELATIONSHIP_TYPES = " edgar_warehouse/mdm/snowflake_graph.py` shows the tuple extended with each phase-confirmed-populated type.
    - New tests assert each newly-populated type appears as a `relationship_parity_<type>` named check (present + ok) and that an absent one fails closed, mirroring 05-03.
    - `tests/mdm/test_cli_snowflake_graph.py` passes green; no second/parallel verification command was introduced (verify() remains the single gate).
  </acceptance_criteria>
  <done>POPULATED_RELATIONSHIP_TYPES covers every phase-populated type; verify-graph named-checks each; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase closure ledger + REQUIREMENTS.md traceability (Success Criterion 6)</name>
  <files>.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-PHASE-CLOSURE-LEDGER.md, .planning/workstreams/fix-pipelines/REQUIREMENTS.md</files>
  <read_first>
    - 06-04-EDGE09-EDGE11-DISPOSITION.md, 06-05-EDGE10-DISPOSITION.md, 06-06-EDGE05-EDGE06-DISPOSITION.md (all per-type outcomes)
    - .planning/workstreams/fix-pipelines/REQUIREMENTS.md (EDGE-05/06/09/10/11 rows + Traceability table)
    - ROADMAP.md (Phase 6 Success Criterion 6 — no type exits in an undocumented zero state)
  </read_first>
  <action>
    Write `06-PHASE-CLOSURE-LEDGER.md` with one row per relationship type EDGE-05, EDGE-06,
    EDGE-09, EDGE-10, EDGE-11, each recording exactly one final disposition: POPULATED (with the
    graph-verified edge count and its verify-graph named check) OR EXCLUDED (with the written
    source-coverage exclusion text naming its artifact dependency or explicit absence of one, and
    a cross-reference to the disposition doc). Assert explicitly that no type is in an
    undocumented zero state (Success Criterion 6). Then update REQUIREMENTS.md: mark each of the 5
    EDGE IDs' checkbox/status and the Traceability table according to its ledger disposition
    (Complete for populated; a documented exclusion note for excluded — do not silently mark an
    exclusion "Complete" without the exclusion reference).
  </action>
  <verify>
    <automated>for id in EDGE-05 EDGE-06 EDGE-09 EDGE-10 EDGE-11; do grep -q "$id" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-PHASE-CLOSURE-LEDGER.md || exit 1; done; grep -Eiq "populated|excluded" .planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-PHASE-CLOSURE-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - The ledger names all 5 EDGE IDs, each with exactly one disposition (POPULATED with graph-verified count, or EXCLUDED with exclusion text + disposition-doc reference).
    - The ledger explicitly states no type exits in an undocumented zero state.
    - REQUIREMENTS.md status + Traceability rows for EDGE-05/06/09/10/11 are updated to match the ledger (exclusions carry their exclusion reference, not a bare Complete).
  </acceptance_criteria>
  <done>Phase closure ledger complete; REQUIREMENTS.md traceability reconciled for all 5 phase requirement IDs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDM Postgres → SQL overlap check | Read-only diagnostic join over MDM entity tables (post-load universe) |
| MDM Postgres → Snowflake graph sync/verify | Any newly-populated type materialized then gated by verify-graph |
| verify-graph gate | Single atomic gate consumed by Step Functions mdm_verify_graph + go-live.sh preflight |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Tampering (verification bypass) | premature POPULATED_RELATIONSHIP_TYPES addition | medium | mitigate | Only add a type after its mdm sync-graph has run (per disposition doc); 05-03's fail-closed design catches a premature add as a hard failure |
| T-06-09 | Elevation/Spoofing (gate fragmentation) | a second/parallel verify path bypassing the single gate | high | mitigate | Extend the existing verify() named-check path only; introduce no new verification command (Phase 5 D-01) |
| T-06-07 | Repudiation (undocumented zero state) | a type marked done without evidence | medium | mitigate | Closure ledger requires exactly one evidenced disposition per type; REQUIREMENTS.md exclusions carry their exclusion reference |
| T-06-SC | Supply chain | package installs | low | accept | No package installs — tuple/comment edit + tests + docs only |

Block on: high. T-06-09 is mitigated by extending the single existing gate rather than adding a parallel verification path.
</threat_model>

<verification>
- `uv run --extra s3 --extra snowflake --extra mdm pytest tests/mdm/test_cli_snowflake_graph.py -q` passes with the new named-check tests.
- Closure ledger names all 5 EDGE IDs each with exactly one evidenced disposition; REQUIREMENTS.md reconciled.
- verify-graph remains the single gate (no parallel command added).
</verification>

<success_criteria>
EDGE-05/06 are closed via the D-04 SQL check; POPULATED_RELATIONSHIP_TYPES (and thus verify-graph)
covers every phase-populated relationship type; and the closure ledger proves each of the 5 phase
relationship types ended in a documented, evidenced disposition with no undocumented zero state.
</success_criteria>

<output>
Create `.planning/workstreams/fix-pipelines/phases/06-relationship-investigation-and-population/06-06-SUMMARY.md` when done.
</output>
