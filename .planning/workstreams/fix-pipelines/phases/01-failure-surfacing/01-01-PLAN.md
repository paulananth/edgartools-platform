---
phase: 01-failure-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/scripts/deploy-aws-application.sh
autonomous: true
requirements:
  - OBS-01
  - OBS-02

must_haves:
  truths:
    - "deploy-aws-application.sh lines 1288-1530 contain no occurrences of '\"MaxAttempts\": 2'"
    - "deploy-aws-application.sh lines 1288-1530 contain no occurrences of '\"ToleratedFailurePercentage\": 10'"
    - "The deployed edgartools-dev-bootstrap-phased state machine definition contains '\"ToleratedFailurePercentage\": 0' on BatchBootstrap"
    - "The deployed edgartools-dev-silver-mdm-gold state machine definition contains '\"ToleratedFailurePercentage\": 0' on BatchSilver"
    - "gold-refresh, mdm-gold, and ownership-mdm-gold use sequential ECS task states with no Distributed Map and no ToleratedFailurePercentage field — they already hard-fail with no code changes required (verified in RESEARCH.md OBS-02 Coverage section)"
  artifacts:
    - path: "infra/scripts/deploy-aws-application.sh"
      provides: "State machine definition generator with updated retry and tolerance values"
      contains: '"MaxAttempts": 3'
  key_links:
    - from: "infra/scripts/deploy-aws-application.sh write_bootstrap_phased_definition()"
      to: "arn:aws:states:us-east-1:077127448006:stateMachine:edgartools-dev-bootstrap-phased"
      via: "upsert_state_machine() → aws stepfunctions update-state-machine"
      pattern: "update-state-machine"
    - from: "infra/scripts/deploy-aws-application.sh write_silver_mdm_gold_definition()"
      to: "arn:aws:states:us-east-1:077127448006:stateMachine:edgartools-dev-silver-mdm-gold"
      via: "upsert_state_machine() → aws stepfunctions update-state-machine"
      pattern: "update-state-machine"
---

<objective>
Edit the two Python-in-bash heredocs in deploy-aws-application.sh to change ToleratedFailurePercentage from 10 to 0 and MaxAttempts from 2 to 3 in both write_bootstrap_phased_definition() and write_silver_mdm_gold_definition(). Then redeploy to push the new definitions live.

Purpose: Satisfies OBS-01 and OBS-02 — any batch failure in bootstrap_phased or silver_mdm_gold now drives the parent execution to FAILED rather than being absorbed by a 10% tolerance window. The remaining 3 state machines (gold-refresh, mdm-gold, ownership-mdm-gold) already hard-fail because they use sequential ECS task states with no Distributed Map — no changes needed for them.

Output: Updated deploy-aws-application.sh committed; deployed edgartools-dev-bootstrap-phased and edgartools-dev-silver-mdm-gold state machines with the new definitions in effect for all new executions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fix-pipelines/ROADMAP.md
@.planning/workstreams/fix-pipelines/REQUIREMENTS.md
@.planning/PROJECT.md

<interfaces>
From infra/scripts/deploy-aws-application.sh lines 1310-1327 (write_bootstrap_phased_definition heredoc):
```python
def ecs_state(task_def_arn, cmd_expr, next_state=None, is_end=False, retry_secs=120):
    s = {
        ...
        "Retry": [{"ErrorEquals": ["States.TaskFailed"], "IntervalSeconds": retry_secs,
                   "BackoffRate": 2.0, "MaxAttempts": 2}],
    }
```

From infra/scripts/deploy-aws-application.sh line 1350 (write_bootstrap_phased_definition heredoc):
```python
batch_map = {
    "Type": "Map",
    ...
    "ToleratedFailurePercentage": 10,
```

From infra/scripts/deploy-aws-application.sh lines 1431-1448 (write_silver_mdm_gold_definition heredoc — separate Python subprocess):
```python
def ecs_state(task_def_arn, cmd_expr, next_state=None, is_end=False, retry_secs=120):
    s = {
        ...
        "Retry": [{"ErrorEquals": ["States.TaskFailed"], "IntervalSeconds": retry_secs,
                   "BackoffRate": 2.0, "MaxAttempts": 2}],
    }
```

From infra/scripts/deploy-aws-application.sh line 1480 (write_silver_mdm_gold_definition heredoc):
```python
batch_map = {
    "Type": "Map",
    ...
    "ToleratedFailurePercentage": 10,
```

These are two independent Python heredoc subprocesses — ecs_state() is NOT shared between them. Both must be edited independently.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change MaxAttempts and ToleratedFailurePercentage in both heredocs</name>
  <files>infra/scripts/deploy-aws-application.sh</files>
  <read_first>
    Read infra/scripts/deploy-aws-application.sh lines 1288-1530 in full before making any edit. This range covers both write_bootstrap_phased_definition() (lines 1288-1403) and write_silver_mdm_gold_definition() (lines 1410-1530). Understand the exact positions of all four values before changing any of them.
  </read_first>
  <action>
    Make exactly four literal value changes in infra/scripts/deploy-aws-application.sh. Do not change any other lines, logic, or surrounding context.

    Change 1 — write_bootstrap_phased_definition() ecs_state() at approximately line 1327:
      "MaxAttempts": 2  →  "MaxAttempts": 3
      (inside the Retry block of ecs_state() defined at line ~1310)

    Change 2 — write_bootstrap_phased_definition() batch_map at approximately line 1350:
      "ToleratedFailurePercentage": 10  →  "ToleratedFailurePercentage": 0
      (the BatchBootstrap Map state dict)

    Change 3 — write_silver_mdm_gold_definition() ecs_state() at approximately line 1447:
      "MaxAttempts": 2  →  "MaxAttempts": 3
      (inside the Retry block of the SEPARATE ecs_state() defined at line ~1431; this is a different Python subprocess from Change 1)

    Change 4 — write_silver_mdm_gold_definition() batch_map at approximately line 1480:
      "ToleratedFailurePercentage": 10  →  "ToleratedFailurePercentage": 0
      (the BatchSilver Map state dict)

    DEC-004 invariant: Do not disturb the '--artifact-policy', 'skip' argument on the batch ecs_state call in write_silver_mdm_gold_definition (line ~1473). That argument is on a different line from MaxAttempts and must remain unchanged.

    Do NOT change the loop at line 1572 or any write_single_workflow_definition() calls. Those are out of scope per D-01/D-02 which target only the two heredoc definitions.

    OBS-02 note: gold-refresh, mdm-gold, and ownership-mdm-gold state machines are generated by the loop at line 1572 using sequential ECS task states with no Distributed Map. They have no ToleratedFailurePercentage field and already hard-fail on any stage error. No changes are needed for those three machines.
  </action>
  <verify>
    <automated>sed -n '1288,1530p' infra/scripts/deploy-aws-application.sh | grep -v '^#' | grep -c '"MaxAttempts": 2'</automated>
    Expected output: 0

    <automated>sed -n '1288,1530p' infra/scripts/deploy-aws-application.sh | grep -v '^#' | grep -c '"ToleratedFailurePercentage": 10'</automated>
    Expected output: 0

    <automated>sed -n '1288,1530p' infra/scripts/deploy-aws-application.sh | grep '"MaxAttempts": 3' | wc -l</automated>
    Expected output: 2 (one per heredoc)

    <automated>sed -n '1288,1530p' infra/scripts/deploy-aws-application.sh | grep '"ToleratedFailurePercentage": 0' | wc -l</automated>
    Expected output: 2 (one per heredoc)

    <automated>grep '"artifact-policy", .skip.' infra/scripts/deploy-aws-application.sh | wc -l</automated>
    Expected output: at least 1 (DEC-004 invariant intact)
  </verify>
  <acceptance_criteria>
    - sed -n '1288,1530p' infra/scripts/deploy-aws-application.sh | grep -v '^#' | grep '"MaxAttempts": 2' returns no output
    - sed -n '1288,1530p' infra/scripts/deploy-aws-application.sh | grep -v '^#' | grep '"ToleratedFailurePercentage": 10' returns no output
    - Both occurrences of MaxAttempts in lines 1288-1530 read "MaxAttempts": 3
    - Both occurrences of ToleratedFailurePercentage in lines 1288-1530 read "ToleratedFailurePercentage": 0
    - The '--artifact-policy', 'skip' argument is still present in write_silver_mdm_gold_definition
  </acceptance_criteria>
  <done>Four literal value changes made and verified via grep. No other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Redeploy state machines with updated definitions</name>
  <files>infra/scripts/deploy-aws-application.sh</files>
  <read_first>
    Read infra/scripts/deploy-aws-application.sh lines 1-80 (usage/flags section) to understand required flags. The deploy script self-documents its interface.
    Read CLAUDE.md section "Manual AWS build and deploy" to confirm the --skip-build recipe and the ECR digest-lookup command pattern.
  </read_first>
  <action>
    Deploy the updated state machine definitions to the dev environment using deploy-aws-application.sh with --skip-build (no Docker build needed — only the Step Functions definitions change).

    Step 1 — Derive the current image refs from ECR (required even with --skip-build):

      WAREHOUSE_REF=$(aws ecr describe-images \
        --region us-east-1 \
        --repository-name edgartools-dev-warehouse \
        --query "sort_by(imageDetails,&imagePushedAt)[-1].imageDigest" \
        --output text | xargs -I{} echo "077127448006.dkr.ecr.us-east-1.amazonaws.com/edgartools-dev-warehouse@{}")

      MDM_REF=$(aws ecr describe-images \
        --region us-east-1 \
        --repository-name edgartools-dev-mdm \
        --query "sort_by(imageDetails,&imagePushedAt)[-1].imageDigest" \
        --output text | xargs -I{} echo "077127448006.dkr.ecr.us-east-1.amazonaws.com/edgartools-dev-mdm@{}")

    Step 2 — Run the deploy script:

      bash infra/scripts/deploy-aws-application.sh \
        --env dev \
        --skip-build \
        --image-ref "$WAREHOUSE_REF" \
        --mdm-image-ref "$MDM_REF" \
        --enable-mdm

    Step 3 — Verify the deployed state machine definitions contain the new values.
    Run these two commands after deploy completes:

      ACCOUNT=$(aws sts get-caller-identity --query Account --output text)

      aws stepfunctions describe-state-machine \
        --state-machine-arn "arn:aws:states:us-east-1:${ACCOUNT}:stateMachine:edgartools-dev-bootstrap-phased" \
        --query 'definition' --output text \
        | python3 -c "import json,sys; d=json.loads(sys.stdin.read()); bp=d['States']['BatchBootstrap']; print('BatchBootstrap ToleratedFailurePercentage:', bp.get('ToleratedFailurePercentage')); print('RunBatch MaxAttempts:', bp['ItemProcessor']['States']['RunBatch']['Retry'][0]['MaxAttempts'])"

      aws stepfunctions describe-state-machine \
        --state-machine-arn "arn:aws:states:us-east-1:${ACCOUNT}:stateMachine:edgartools-dev-silver-mdm-gold" \
        --query 'definition' --output text \
        | python3 -c "import json,sys; d=json.loads(sys.stdin.read()); bs=d['States']['BatchSilver']; print('BatchSilver ToleratedFailurePercentage:', bs.get('ToleratedFailurePercentage')); print('RunBatch MaxAttempts:', bs['ItemProcessor']['States']['RunBatch']['Retry'][0]['MaxAttempts'])"

    Expected output from each command:
      BatchBootstrap ToleratedFailurePercentage: 0
      RunBatch MaxAttempts: 3
      (and same pattern for BatchSilver)
  </action>
  <verify>
    <automated>
      ACCOUNT=$(aws sts get-caller-identity --query Account --output text) && \
      aws stepfunctions describe-state-machine \
        --state-machine-arn "arn:aws:states:us-east-1:${ACCOUNT}:stateMachine:edgartools-dev-bootstrap-phased" \
        --query 'definition' --output text \
        | python3 -c "import json,sys; d=json.loads(sys.stdin.read()); v=d['States']['BatchBootstrap'].get('ToleratedFailurePercentage'); assert v==0, f'Expected 0 got {v}'; print('PASS: bootstrap-phased BatchBootstrap ToleratedFailurePercentage=0')"
    </automated>
    <automated>
      ACCOUNT=$(aws sts get-caller-identity --query Account --output text) && \
      aws stepfunctions describe-state-machine \
        --state-machine-arn "arn:aws:states:us-east-1:${ACCOUNT}:stateMachine:edgartools-dev-silver-mdm-gold" \
        --query 'definition' --output text \
        | python3 -c "import json,sys; d=json.loads(sys.stdin.read()); v=d['States']['BatchSilver'].get('ToleratedFailurePercentage'); assert v==0, f'Expected 0 got {v}'; print('PASS: silver-mdm-gold BatchSilver ToleratedFailurePercentage=0')"
    </automated>
  </verify>
  <acceptance_criteria>
    - deploy-aws-application.sh exits 0
    - aws stepfunctions describe-state-machine for edgartools-dev-bootstrap-phased returns a definition where States.BatchBootstrap.ToleratedFailurePercentage == 0 and States.BatchBootstrap.ItemProcessor.States.RunBatch.Retry[0].MaxAttempts == 3
    - aws stepfunctions describe-state-machine for edgartools-dev-silver-mdm-gold returns a definition where States.BatchSilver.ToleratedFailurePercentage == 0 and States.BatchSilver.ItemProcessor.States.RunBatch.Retry[0].MaxAttempts == 3
  </acceptance_criteria>
  <done>Both deployed state machine definitions verified to contain ToleratedFailurePercentage: 0 and MaxAttempts: 3 via live AWS API query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → AWS API | deploy-aws-application.sh calls aws stepfunctions update-state-machine using ambient IAM credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | deploy-aws-application.sh heredoc edit | accept | Edits are in version-controlled source; git diff reviewed before commit |
| T-01-01-02 | Repudiation | update-state-machine call | accept | AWS CloudTrail logs the API call with caller identity; no additional logging needed |
| T-01-01-03 | Denial of Service | upsert_state_machine updating live SM during active execution | accept | In-flight executions run under old definition; new definition only affects new executions per AWS guarantee |
</threat_model>

<verification>
After both tasks complete:
1. `sed -n '1288,1530p' infra/scripts/deploy-aws-application.sh | grep '"MaxAttempts": 2'` — no output
2. `sed -n '1288,1530p' infra/scripts/deploy-aws-application.sh | grep '"ToleratedFailurePercentage": 10'` — no output
3. Live AWS API confirms both deployed state machines have ToleratedFailurePercentage: 0 and MaxAttempts: 3 on their Map states
</verification>

<success_criteria>
- Four literal value changes in infra/scripts/deploy-aws-application.sh verified by grep
- Both edgartools-dev-bootstrap-phased and edgartools-dev-silver-mdm-gold deployed with new definitions
- Live AWS state machine definitions confirm ToleratedFailurePercentage: 0 on BatchBootstrap and BatchSilver
- Live AWS state machine definitions confirm MaxAttempts: 3 on ECS Task states in both Map processors
- gold-refresh, mdm-gold, and ownership-mdm-gold already hard-fail — no changes needed (OBS-02 satisfied without code change for those 3)
</success_criteria>

<output>
After completion, create .planning/workstreams/fix-pipelines/phases/01-failure-surfacing/01-01-SUMMARY.md
</output>
