---
phase: 01-failure-surfacing
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified: []
autonomous: true
requirements:
  - OBS-01

must_haves:
  truths:
    - "Running test-failure-surfacing.sh with BRONZE_BUCKET set exits 0"
    - "The execution ARN printed by the script shows status FAILED in Step Functions console"
    - "The execution never reaches SUCCEEDED when CIK 9999999 is the only batch item"
  artifacts:
    - path: "scripts/ops/test-failure-surfacing.sh"
      provides: "Confirmed regression test — green run recorded in SUMMARY"
      contains: "PASS: execution reached FAILED state"
  key_links:
    - from: "scripts/ops/test-failure-surfacing.sh exit code 0"
      to: "aws stepfunctions describe-execution status == FAILED"
      via: "live execution of deployed edgartools-dev-bootstrap-phased"
      pattern: "PASS: execution reached FAILED state"
---

<objective>
Run the failure-injection test script against the deployed dev environment to confirm that bootstrap_phased reaches FAILED state (not SUCCEEDED) when all batch items are invalid. This is the live behavioral proof of OBS-01.

Purpose: Confirms that the deployed state machine definition (from Plan 01) behaves correctly at runtime. The test script (from Plan 02) must exit 0. A green run is recorded in the SUMMARY for this phase.

Output: One completed bootstrap_phased execution in AWS with status FAILED; test script exit code 0; execution ARN recorded in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fix-pipelines/ROADMAP.md
@.planning/workstreams/fix-pipelines/phases/01-failure-surfacing/01-01-SUMMARY.md
@.planning/workstreams/fix-pipelines/phases/01-failure-surfacing/01-02-SUMMARY.md

<interfaces>
From scripts/ops/test-failure-surfacing.sh (created in Plan 02):
  Usage: BRONZE_BUCKET=<bucket-name> ./scripts/ops/test-failure-surfacing.sh [--env dev]
  Exit 0: stdout contains "PASS: execution reached FAILED state as expected" + execution ARN
  Exit 1: assertion failed (SUCCEEDED or timed out) or SeedUniverse timeout
  Runtime: 25-35 minutes typical; 45-minute hard timeout (MAX_WAIT_SECONDS=2700)
  The script is a long-running blocking process — it must be run in the background.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the failure-injection test in the background and record the result</name>
  <files></files>
  <read_first>
    Read .planning/workstreams/fix-pipelines/phases/01-failure-surfacing/01-01-SUMMARY.md to confirm the deployed state machines have ToleratedFailurePercentage: 0 (prerequisite for this test to pass).
    Read .planning/workstreams/fix-pipelines/phases/01-failure-surfacing/01-02-SUMMARY.md to confirm scripts/ops/test-failure-surfacing.sh was created successfully (prerequisite).
    Read scripts/ops/test-failure-surfacing.sh lines 1-30 to confirm BRONZE_BUCKET requirement and any other pre-conditions.
  </read_first>
  <action>
    IMPORTANT: The test script runs for 25-35 minutes. It must be executed as a background task using run_in_background=true. Do NOT run it in the foreground — the Bash tool has a 10-minute ceiling and the script will be killed mid-run.

    Step 1 — Resolve the bronze bucket name from the deployment manifest (authoritative source):
      BRONZE_BUCKET=$(python3 -c "import json; d=json.load(open('infra/aws-dev-application.json')); print(d.get('bronze_bucket_name',''))" 2>/dev/null)

      If infra/aws-dev-application.json does not exist or does not contain bronze_bucket_name, fall back to bucket prefix matching:
        BRONZE_BUCKET=$(aws s3api list-buckets \
          --query "Buckets[?starts_with(Name,'edgartools-dev-bronze')].Name | [0]" \
          --output text)

      Confirm BRONZE_BUCKET is non-empty before proceeding:
        echo "Bronze bucket: ${BRONZE_BUCKET}"
        [[ -z "${BRONZE_BUCKET}" || "${BRONZE_BUCKET}" == "None" ]] && { echo "ERROR: could not resolve bronze bucket"; exit 1; }

    Step 2 — Run the test script as a background task:
      Use run_in_background=true with the command:
        BRONZE_BUCKET="${BRONZE_BUCKET}" bash scripts/ops/test-failure-surfacing.sh --env dev

      The background task will emit each stdout line as a notification event. Monitor them as they arrive. Expected sequence:
        [1/5] Starting bootstrap_phased execution: test-failure-surfacing-<timestamp>
            Execution ARN: arn:aws:states:us-east-1:077127448006:...
        [2/5] Waiting for SeedUniverse to complete...
            SeedUniverse completed.
        [3/5] Overwriting cik_batches.jsonl with invalid CIK 9999999...
            Wrote s3://<bucket>/warehouse/bronze/reference/cik_universe/runs/.../cik_batches.jsonl
        [4/5] Polling for terminal state (timeout: 45 min)...
            30s elapsed — status: RUNNING
            60s elapsed — status: RUNNING
            ... (many polling lines; expect ~25-35 min total)
        [5/5] Asserting execution reached FAILED state...
        PASS: execution reached FAILED state as expected
              Execution ARN: arn:aws:states:us-east-1:...
              Inspect in console: https://console.aws.amazon.com/states/home#/executions/details/...

    Step 3 — When the background task completes, verify exit code and record results:
      Capture the execution ARN from the PASS line in the output.
      Run this verification (fast, synchronous — not in background):
        aws stepfunctions describe-execution \
          --execution-arn "<ARN-from-PASS-line>" \
          --query 'status' --output text

      Expected: FAILED

    If the script exits 1 with "ASSERTION FAILED: expected FAILED, got SUCCEEDED":
      This means the deployed state machine still has ToleratedFailurePercentage > 0.
      Re-verify Plan 01 Task 2 acceptance criteria before escalating. Do NOT mark this plan complete.

    If the script exits 1 with "timed out":
      Check execution status directly:
        aws stepfunctions describe-execution --execution-arn "<ARN>" --query status --output text
      If it shows FAILED, the test completed correctly but after the script's timeout. Record as a pass with a note.

    No cleanup is needed. CIK 9999999 causes failure before any S3 bronze writes occur.
  </action>
  <verify>
    Verification is performed by the background task itself — the script's internal assert at step 5 is the primary gate. After background task completion, run this short synchronous check using the ARN from the PASS line:

    <automated>
      aws stepfunctions describe-execution \
        --execution-arn "<execution-ARN-from-PASS-output>" \
        --query 'status' --output text
    </automated>
    Expected output: FAILED
  </verify>
  <acceptance_criteria>
    - Background task (scripts/ops/test-failure-surfacing.sh) exits 0
    - Background task stdout contains the line "PASS: execution reached FAILED state as expected"
    - Background task stdout contains an execution ARN (format: arn:aws:states:us-east-1:...:execution:...)
    - Querying that execution ARN via describe-execution returns status FAILED
    - Background task stdout does NOT contain "ASSERTION FAILED: expected FAILED, got SUCCEEDED"
    - Background task stdout does NOT contain "ERROR: Execution still RUNNING after"
  </acceptance_criteria>
  <done>
    Live bootstrap_phased execution with CIK 9999999 reached FAILED state. Background test script exited 0. Execution ARN recorded in SUMMARY. OBS-01 runtime behavior confirmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → Step Functions | start-execution and describe-execution against dev environment |
| Developer workstation → S3 | single s3 cp to overwrite a run-scoped cik_batches.jsonl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | Live S3 overwrite of cik_batches.jsonl | accept | Key is scoped to test execution name; cannot collide with production runs; CIK 9999999 produces no bronze writes |
| T-01-03-02 | Denial of Service | Starts a live ECS execution against dev environment | accept | Bounded: single batch, 4 ECS task attempts max, terminates within 45 min; documented in test script header |
</threat_model>

<verification>
After task completes:
1. Background task exit code is 0
2. Stdout line "PASS: execution reached FAILED state as expected" is present
3. Execution ARN from stdout: aws stepfunctions describe-execution --execution-arn <ARN> --query status --output text returns FAILED
</verification>

<success_criteria>
- scripts/ops/test-failure-surfacing.sh exits 0 against the deployed dev bootstrap_phased state machine (run as background task)
- Execution ARN recorded in SUMMARY with status FAILED confirmed via live API query
- OBS-01 behavioral requirement satisfied: ToleratedFailurePercentage: 0 confirmed at runtime, not just in the definition text
</success_criteria>

<output>
After completion, create .planning/workstreams/fix-pipelines/phases/01-failure-surfacing/01-03-SUMMARY.md

Include in the SUMMARY:
- Test execution ARN
- Execution status (FAILED confirmed via aws stepfunctions describe-execution)
- Wall-clock time from execution start to FAILED terminal state (from the polling elapsed output)
- Confirmation that PASS message appeared in test script output
</output>
