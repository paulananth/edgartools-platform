# Implement dashboard subject resolver and UI contract tests

Type: task (AFK)
Status: blocked
Blocked by: 03-select-rendered-ui-test-seam

## Question

After the contract and rendered-UI test seam are fixed, how will the dashboard
expose the bounded subject resolver and verify each tab's real input/output
behavior without asserting pipeline completeness or release readiness?
