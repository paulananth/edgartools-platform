---
phase: 08-dashboard-foundations-and-read-only-data-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - edgar_warehouse/mdm/dashboard_readonly.py
  - tests/mdm/test_dashboard_readonly.py
autonomous: true
requirements:
  - DASH-02
  - ISO-02
must_haves:
  truths:
    - "D-04: Add dedicated read-only MDM query helpers for the dashboard; do not call mutating CLI handlers directly from dashboard code."
    - "D-05: MDM helper functions return structured data, not printed JSON, and use SQLAlchemy sessions only for bounded SELECT-style queries."
    - "D-06: Tests prove dashboard helper code does not reach mutation surfaces such as MDMPipeline, resolver writes, migrations, or graph sync orchestration."
    - "Dashboard MDM helper code can verify required MDM connectivity through bounded read-only queries."
    - "MDM dashboard helper functions return structured data and do not print JSON or call mutating CLI/pipeline paths."
    - "MDM failures are converted to secret-safe status objects that mention env var names, not values."
  artifacts:
    - path: "edgar_warehouse/mdm/dashboard_readonly.py"
      provides: "Dedicated MDM read-only dashboard helper module"
      exports: ["MdmDashboardStatus", "MdmSmokeResult", "check_mdm_status", "run_mdm_smoke_query"]
    - path: "tests/mdm/test_dashboard_readonly.py"
      provides: "Credential-free tests for MDM read-only helper behavior"
  key_links:
    - from: "edgar_warehouse/mdm/dashboard_readonly.py"
      to: "edgar_warehouse/mdm/database.py"
      via: "get_engine/get_session and SQLAlchemy select-only access"
      pattern: "get_(engine|session)|select\\("
    - from: "tests/mdm/test_dashboard_readonly.py"
      to: "edgar_warehouse/mdm/dashboard_readonly.py"
      via: "unit tests import helper contracts"
      pattern: "dashboard_readonly"
---

<objective>
Create the reusable read-only MDM query boundary for the Phase 8 dashboard.

Purpose: Satisfy DASH-02 by giving the Streamlit shell a structured, testable MDM status/smoke-query API that cannot mutate MDM state and does not expose secrets.
Output: `edgar_warehouse/mdm/dashboard_readonly.py` and `tests/mdm/test_dashboard_readonly.py`.
</objective>

<execution_context>
@/Users/aneenaananth/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aneenaananth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/active-workstream
@.planning/workstreams/mdm-neo4j-dashboard/PROJECT.md
@.planning/workstreams/mdm-neo4j-dashboard/STATE.md
@.planning/workstreams/mdm-neo4j-dashboard/ROADMAP.md
@.planning/workstreams/mdm-neo4j-dashboard/REQUIREMENTS.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/08-dashboard-foundations-and-read-only-data-access/08-CONTEXT.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/08-dashboard-foundations-and-read-only-data-access/08-UI-SPEC.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/08-dashboard-foundations-and-read-only-data-access/08-RESEARCH.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/08-dashboard-foundations-and-read-only-data-access/08-VALIDATION.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/08-dashboard-foundations-and-read-only-data-access/08-PATTERNS.md
@edgar_warehouse/mdm/database.py
@edgar_warehouse/mdm/cli.py
@tests/mdm/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define credential-free MDM read-only helper tests</name>
  <files>tests/mdm/test_dashboard_readonly.py</files>
  <read_first>
    - `.planning/workstreams/mdm-neo4j-dashboard/phases/08-dashboard-foundations-and-read-only-data-access/08-VALIDATION.md` for Task 08-01-01 coverage.
    - `.planning/workstreams/mdm-neo4j-dashboard/phases/08-dashboard-foundations-and-read-only-data-access/08-PATTERNS.md` sections for `tests/mdm/test_dashboard_readonly.py`.
    - `tests/mdm/conftest.py` for the in-memory SQLite `db_session` fixture and MDM seed helpers.
    - `edgar_warehouse/mdm/database.py` for model names and session conventions.
  </read_first>
  <behavior>
    - `check_mdm_status(session=...)` returns a structured connected status for a valid SQLite MDM session.
    - `run_mdm_smoke_query(session=..., limit=5)` returns bounded structured rows and never calls `Session.commit`.
    - Missing/unreachable MDM configuration maps to the exact safe UI copy: `MDM database unavailable. Check `MDM_DATABASE_URL`, confirm the database is reachable, and restart the dashboard.`
    - Returned error/status payloads contain `MDM_DATABASE_URL` as an env var name but do not contain raw DSN values, usernames, passwords, hostnames, or raw exception strings.
  </behavior>
  <action>Create `tests/mdm/test_dashboard_readonly.py` before production implementation. Use existing pytest and SQLite fixture patterns. Tests must import expected public identifiers from `edgar_warehouse.mdm.dashboard_readonly`, monkeypatch or fake `Session.commit` to fail if called, and assert helpers return structured Python objects or dictionaries rather than printed JSON. Require the exact MDM failure copy `MDM database unavailable. Check `MDM_DATABASE_URL`, confirm the database is reachable, and restart the dashboard.` Include static assertions that this helper module text does not import `MDMPipeline`, MDM migrations runtime, resolver write modules, stewardship modules, graph sync modules, or CLI mutation handlers, per D-04, D-05, D-06, DASH-02, and ISO-02.</action>
  <verify>
    <automated>uv run pytest tests/mdm/test_dashboard_readonly.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and initially describes the MDM helper contracts from D-04 through D-06.
    - Tests require bounded read behavior, no `commit()`, structured return values, and secret-safe error text.
    - Tests do not require live `MDM_DATABASE_URL` credentials.
  </acceptance_criteria>
  <done>`tests/mdm/test_dashboard_readonly.py` fails only because `edgar_warehouse.mdm.dashboard_readonly` does not yet exist or lacks the planned identifiers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MDM dashboard read-only helpers</name>
  <files>edgar_warehouse/mdm/dashboard_readonly.py, tests/mdm/test_dashboard_readonly.py</files>
  <read_first>
    - `edgar_warehouse/mdm/database.py` for `get_engine`, `get_session`, `MdmEntityTypeDefinition`, `MdmRelationshipType`, and `MdmRelationshipInstance`.
    - `edgar_warehouse/mdm/cli.py` for `_relationship_counts_by_type` as a structured-return reference only; do not call CLI handlers.
    - `.planning/workstreams/mdm-neo4j-dashboard/phases/08-dashboard-foundations-and-read-only-data-access/08-UI-SPEC.md` for exact MDM error copy.
    - `tests/mdm/test_dashboard_readonly.py` from Task 1.
  </read_first>
  <behavior>
    - `check_mdm_status(engine=None, session=None)` returns connected/unavailable status without printing.
    - `run_mdm_smoke_query(engine=None, session=None, limit=5)` performs only bounded SELECT-style SQLAlchemy reads and returns no more than 5 rows unless a smaller explicit safe bound is passed.
    - Helper code closes/rolls back owned sessions after read attempts and never calls `commit()`.
    - Helper code does not import or call MDM pipeline, migration, resolver write, stewardship, graph sync, or runtime rollout surfaces.
  </behavior>
  <action>Create `edgar_warehouse/mdm/dashboard_readonly.py` with dataclass or typed-dict style structured results named `MdmDashboardStatus` and `MdmSmokeResult`, plus public functions `check_mdm_status` and `run_mdm_smoke_query`. Use `edgar_warehouse.mdm.database.get_engine()` and `get_session()` for owned connections while allowing injected `Session`/`Engine` for tests. Use SQLAlchemy `select()` or `text("SELECT 1 AS ok")` only for bounded read checks; do not accept raw SQL from callers. Map missing or failed MDM connectivity to exact copy `MDM database unavailable. Check `MDM_DATABASE_URL`, confirm the database is reachable, and restart the dashboard.`, and expose only env var names such as `MDM_DATABASE_URL`, never environment values or raw exception strings. This implements D-04, D-05, and D-06 by adding dedicated structured read-only helpers and excluding mutating CLI/pipeline paths. Implement after the Task 1 tests are present and keep Phase 8 smoke output minimal; do not add Phase 9 entity or relationship count surfaces.</action>
  <verify>
    <automated>uv run pytest tests/mdm/test_dashboard_readonly.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/mdm/test_dashboard_readonly.py -q` passes.
    - `edgar_warehouse/mdm/dashboard_readonly.py` contains no mutation imports or `commit(` calls.
    - Public helpers return structured data suitable for Streamlit rendering without stdout parsing.
    - Error/status payloads are secret-safe and include the exact required MDM unavailable copy.
  </acceptance_criteria>
  <done>MDM read-only helper API is implemented, tested without live credentials, and ready for the Streamlit shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Streamlit shell -> MDM helper | Dashboard UI calls helper code that may open database sessions using environment configuration. |
| Environment -> error/status rendering | Secret-bearing env vars and driver errors must be transformed into safe operator messages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `edgar_warehouse/mdm/dashboard_readonly.py` | mitigate | Implement only bounded SELECT-style helpers, expose no mutation entrypoints, and test that `Session.commit` is never called. |
| T-08-02 | Information Disclosure | MDM connection error handling | mitigate | Return fixed UI-SPEC message mentioning `MDM_DATABASE_URL` only by name; do not render raw DSNs, hostnames, credentials, or raw exception strings. |
| T-08-03 | Elevation of Privilege | Dashboard helper imports | mitigate | Static tests block imports/calls to `MDMPipeline`, migrations, resolver writes, stewardship, graph sync, and CLI mutation surfaces. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task is planned; existing `uv` extras are used. |
</threat_model>

<verification>
Run `uv run pytest tests/mdm/test_dashboard_readonly.py -q` after each task. At wave end, run `uv run pytest tests/mdm tests/architecture` when all wave work is merged.
</verification>

<success_criteria>
- `edgar_warehouse/mdm/dashboard_readonly.py` exists with `MdmDashboardStatus`, `MdmSmokeResult`, `check_mdm_status`, and `run_mdm_smoke_query`.
- `tests/mdm/test_dashboard_readonly.py` passes without live credentials.
- MDM helper code is read-only, bounded, structured, and secret-safe.
- No Phase 9 metrics, mutation controls, deployment artifacts, Terraform, dbt/gold, Step Functions, or rollout changes are introduced.
</success_criteria>

<output>
Create `.planning/workstreams/mdm-neo4j-dashboard/phases/08-dashboard-foundations-and-read-only-data-access/08-01-SUMMARY.md` when done.
</output>
