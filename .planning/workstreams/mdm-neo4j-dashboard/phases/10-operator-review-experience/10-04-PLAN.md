---
phase: 10-operator-review-experience
plan: 04
type: execute
wave: 4
depends_on:
  - 10-03
files_modified:
  - examples/mdm_graph_dashboard/README.md
  - tests/architecture/test_dashboard_foundation_boundaries.py
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
must_haves:
  truths:
    - "D-13: Runbook explains local launch, required and optional environment variables, what to inspect first, filters, and common failure states."
    - "D-14: Runbook references existing check/remediation commands only and adds no dashboard buttons or mutation controls."
    - "D-15: Runbook prominently guarantees the dashboard does not run sync, repair, migrate, load, or write actions."
    - "D-16: Focused credential-free validation command is documented and passes; no manual browser checklist is required."
  artifacts:
    - path: "examples/mdm_graph_dashboard/README.md"
      provides: "Guided operator workflow and read-only runbook"
      contains: "This dashboard does not run sync, repair, migrate, load, or write actions."
    - path: "tests/architecture/test_dashboard_foundation_boundaries.py"
      provides: "README contract assertions"
      contains: "edgar-warehouse mdm verify-graph"
  key_links:
    - from: "examples/mdm_graph_dashboard/README.md"
      to: "examples/mdm_graph_dashboard/streamlit_app.py"
      via: "same navigation labels and filter behavior"
      pattern: "Mismatch Diagnostics"
---

<objective>
Update the operator runbook to match the final Phase 10 dashboard experience and validate the full credential-free suite.

Purpose: Operators need a guided workflow with safe setup instructions, read-only guarantees, failure-state expectations, and the exact focused test command.
Output: README runbook and architecture documentation guards.
</objective>

<execution_context>
@/Users/aneenaananth/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aneenaananth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/mdm-neo4j-dashboard/STATE.md
@.planning/workstreams/mdm-neo4j-dashboard/ROADMAP.md
@.planning/workstreams/mdm-neo4j-dashboard/REQUIREMENTS.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-CONTEXT.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-VALIDATION.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-03-SUMMARY.md
@examples/mdm_graph_dashboard/README.md
@tests/architecture/test_dashboard_foundation_boundaries.py

<interfaces>
README must document the final view labels provided by Plans 10-01 through 10-03: `Overview`, `MDM Overview`, `Neo4j Overview`, and `Mismatch Diagnostics`. It must not add dashboard controls for existing operational commands.
</interfaces>
</context>

<source_audit>
GOAL covered: runbook documentation and final validation.
REQ covered: UX-01, UX-02, UX-03.
RESEARCH covered: Plan slice 10-04 and doc drift risk.
CONTEXT covered: D-13: guided review workflow docs; D-14: docs reference existing check/remediation commands only; D-15: prominent read-only guarantee; D-16: focused credential-free automated test command. This plan also performs final verification for D-01: through D-12:. Deferred manual browser checklist, managed deployment, historical trends, and graph visualization are not included.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add README contract assertions</name>
  <files>tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    @tests/architecture/test_dashboard_foundation_boundaries.py
    @examples/mdm_graph_dashboard/README.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-CONTEXT.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-VALIDATION.md
  </read_first>
  <behavior>
    - README guard expects sections in this order: Purpose, Read-only guarantee, Prerequisites, Launch, Review workflow, Filters, Failure states, Existing checks, Validation.
    - README guard expects env vars `MDM_DATABASE_URL`, `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD`, `NEO4J_DATABASE`, `NEO4J_SECRET_JSON`.
    - README guard expects existing external commands only: `edgar-warehouse mdm check-connectivity --neo4j`, `edgar-warehouse mdm counts`, `edgar-warehouse mdm verify-graph`.
    - README guard expects focused command `uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py tests/architecture/test_dashboard_foundation_boundaries.py -q`.
    - README guard rejects manual browser checklist headings and dashboard remediation-button language.
  </behavior>
  <action>Add documentation-focused architecture assertions per D-13 through D-16. Keep command-offender detection precise so the allowed existing check commands are permitted in README prose but not treated as dashboard UI controls. Do not weaken existing mutation, out-of-scope, filter, navigation, or secret-safety guards.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail until the README is updated.
    - README assertions include the exact focused credential-free test command.
    - Existing dashboard source guards remain active.
  </acceptance_criteria>
  <done>README requirements are encoded as automated architecture checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite runbook for guided operator review</name>
  <files>examples/mdm_graph_dashboard/README.md</files>
  <read_first>
    @examples/mdm_graph_dashboard/README.md
    @tests/architecture/test_dashboard_foundation_boundaries.py
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-CONTEXT.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
  </read_first>
  <behavior>
    - README reads as a guided operator workflow, not a feature list.
    - Read-only guarantee appears before launch steps and states exactly that dashboard does not run sync, repair, migrate, load, or write actions.
    - Prerequisites list required `MDM_DATABASE_URL` and optional Neo4j env vars.
    - Review workflow tells operators to inspect `Overview`, then `MDM Overview`, `Neo4j Overview`, and `Mismatch Diagnostics`.
    - Filters section explains `Row limit` choices `25`, `50`, `100`, `250`, default `50`, and page-specific `All` default filters.
    - Failure states explain MDM blocking and Neo4j non-blocking behavior with secret-safe errors.
    - Validation section includes the focused credential-free test command.
  </behavior>
  <action>Rewrite `examples/mdm_graph_dashboard/README.md` to the required section order from `10-UI-SPEC.md`. Reference existing commands only as external checks, not dashboard controls, per D-14. Do not add managed deployment, Terraform, dbt, Step Functions, Snowflake, generated application JSON, manual browser checklist, mutation controls, or new secret-management instructions.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - README contains all four final navigation labels and no old primary section list.
    - README contains the exact read-only guarantee sentence.
    - README contains the three allowed external check commands and no dashboard button/remediation command instructions.
    - The architecture command passes.
  </acceptance_criteria>
  <done>Operator runbook matches the final dashboard and locked documentation decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Run full focused Phase 10 validation</name>
  <files>examples/mdm_graph_dashboard/README.md, tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-VALIDATION.md
    @examples/mdm_graph_dashboard/README.md
    @tests/architecture/test_dashboard_foundation_boundaries.py
  </read_first>
  <action>Run the full focused credential-free command from `10-VALIDATION.md`: `uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py tests/architecture/test_dashboard_foundation_boundaries.py -q`. If it fails, fix only Phase 10 dashboard/docs/test guard issues needed to satisfy the locked contracts; do not edit helper modules unless a Phase 10 change demonstrably broke their existing contract.</action>
  <verify>
    <automated>uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Full focused command exits 0 without live MDM or Neo4j credentials.
    - No Phase 10 plan leaves `expectedFailure` on the GRAPH-01 regression.
    - `git diff --check` reports no whitespace errors.
  </acceptance_criteria>
  <done>Full Phase 10 credential-free validation passes and README/doc guards match the final operator experience.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> operator action | Runbook commands and setup steps can cause unsafe actions if mutation or deployment paths are introduced. |
| Validation -> phase sign-off | Focused tests must prove dashboard behavior without live credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Information Disclosure | README env var guidance | mitigate | Document env var names only and avoid sample secret values, hostnames, raw URLs, or secret JSON payloads. |
| T-10-11 | Elevation of Privilege | README operator commands | mitigate | Reference existing check commands only; do not add sync, repair, migrate, load, write, deployment, Terraform, dbt, or Step Functions steps. |
| T-10-12 | Repudiation | validation command | mitigate | README and architecture tests include the exact focused credential-free command. |
| T-10-SC | Tampering | package installs | accept | No package install task is planned. |
</threat_model>

<verification>
Run `uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q` after documentation changes, then run `uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py tests/architecture/test_dashboard_foundation_boundaries.py -q`.
</verification>

<success_criteria>
- README satisfies D-13 through D-16 and matches final UI labels/filter behavior.
- Full focused credential-free tests pass.
- No out-of-scope deployment, mutation, write, or secret-management guidance is added.
</success_criteria>

<output>
Create `.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-04-SUMMARY.md` when done.
</output>
