---
phase: 10-operator-review-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - examples/mdm_graph_dashboard/streamlit_app.py
  - tests/architecture/test_dashboard_foundation_boundaries.py
autonomous: true
requirements:
  - UX-02
must_haves:
  truths:
    - "D-05: Operator can globally bound large review tables and use page-specific entity and relationship filters."
    - "D-06: Row limit defaults to 50."
    - "D-07: Row limit choices are 25, 50, 100, and 250."
    - "D-08: Entity type and relationship type filters are single-select controls with All as the default."
    - "D-12: Filtered-empty tables render exact copy: No rows match the current filters."
    - "Overview aggregate metrics remain unfiltered."
  artifacts:
    - path: "examples/mdm_graph_dashboard/streamlit_app.py"
      provides: "Global row limit and page-specific selectbox filters"
      contains: "Row limit"
    - path: "tests/architecture/test_dashboard_foundation_boundaries.py"
      provides: "Static filter and bounded-control guards"
      contains: "No rows match the current filters."
  key_links:
    - from: "examples/mdm_graph_dashboard/streamlit_app.py"
      to: "MDM/Neo4j/mismatch render helpers"
      via: "row_limit argument and selectbox filter values"
      pattern: "row_limit"
---

<objective>
Add bounded review controls without introducing free-form inputs or mutation-like controls.

Purpose: Large MDM and Neo4j stores must remain inspectable through predictable Streamlit-native filters.
Output: Row-limit sidebar control, page-specific filters, filtered table rendering, and focused architecture guards.
</objective>

<execution_context>
@/Users/aneenaananth/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aneenaananth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/mdm-neo4j-dashboard/STATE.md
@.planning/workstreams/mdm-neo4j-dashboard/REQUIREMENTS.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-CONTEXT.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-VALIDATION.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-01-SUMMARY.md
@examples/mdm_graph_dashboard/streamlit_app.py
@tests/architecture/test_dashboard_foundation_boundaries.py

<interfaces>
Plan 10-01 provides final view handlers and registry-label entity-domain coverage. This plan should extend those handlers with `row_limit`, entity filter, and relationship filter parameters rather than restoring old route names.
</interfaces>
</context>

<source_audit>
GOAL covered: bounded filters for MDM overview, Neo4j overview, and mismatch diagnostics.
REQ covered: UX-02.
RESEARCH covered: Plan slice 10-02 and filter semantics.
CONTEXT covered: D-05: hybrid global and page-specific filters; D-06: default row limit 50; D-07: row limit choices 25, 50, 100, 250; D-08: single-select filters with All default; D-12: exact filtered-empty table copy. Deferred graph visualization and deployment ideas are not included.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add architecture guards for bounded filters</name>
  <files>tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    @tests/architecture/test_dashboard_foundation_boundaries.py
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-VALIDATION.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-01-SUMMARY.md
  </read_first>
  <behavior>
    - Static guard expects sidebar selectbox label `Row limit`, values `25`, `50`, `100`, `250`, and default index/value `50`.
    - Static guard expects page selectbox labels `Entity type` and `Relationship type` with `All` default.
    - Static guard rejects `st.text_input`, `st.text_area`, `st.number_input`, `st.multiselect`, `st.checkbox`, and `st.toggle` in the dashboard app.
    - Static guard expects exact filtered-empty copy `No rows match the current filters.`
  </behavior>
  <action>Add filter-specific architecture assertions per D-05 through D-08 and D-12. Keep the existing mutation-control guard, but ensure it does not treat allowed labels `Row limit`, `Entity type`, and `Relationship type` as mutation controls. Prefer static source assertions over live Streamlit execution unless the fake harness already supports the needed calls.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test names identify row-limit choices/default and single-select `All` filters.
    - Tests fail until Task 2 implements the controls.
    - Existing GRAPH-01 and navigation guards remain present.
  </acceptance_criteria>
  <done>Architecture tests define the bounded filter contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement global row limit and page filters</name>
  <files>examples/mdm_graph_dashboard/streamlit_app.py</files>
  <read_first>
    @examples/mdm_graph_dashboard/streamlit_app.py
    @tests/architecture/test_dashboard_foundation_boundaries.py
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-RESEARCH.md
  </read_first>
  <behavior>
    - Sidebar has `st.sidebar.selectbox("Row limit", [25, 50, 100, 250], index=1)` or equivalent exact-label/options/default behavior.
    - Page filters use `st.selectbox("Entity type", ["All", ...], index=0)` and `st.selectbox("Relationship type", ["All", ...], index=0)` where relevant.
    - MDM Overview filters MDM entity and relationship tables.
    - Neo4j Overview filters Neo4j node and relationship tables.
    - Mismatch Diagnostics filters entity-domain coverage, relationship coverage, pending sync samples, missing-edge samples, and extra graph samples.
    - Row limit applies after filters and before rendering large detail/sample tables; Overview aggregate metrics are not filtered.
  </behavior>
  <action>Implement helper functions for registry/entity options, relationship options, selected-filter application, row limiting, and filtered-empty rendering. Source entity options from `mdm_metrics["registry"]["entity_type_details"]` and relationship options from `mdm_metrics["relationship_counts"]`; keep `All` first and preserve existing registry/order where possible. Pass `row_limit` from `main()` into detail renderers. Use `st.selectbox` only for filters and keep `Refresh metrics` as the only action button.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `examples/mdm_graph_dashboard/streamlit_app.py` contains `Row limit`, `25`, `50`, `100`, and `250`.
    - The file contains exact copy `No rows match the current filters.`
    - The file contains no `st.text_input`, `st.text_area`, `st.number_input`, `st.multiselect`, `st.checkbox`, or `st.toggle`.
    - The architecture command passes.
  </acceptance_criteria>
  <done>Operators can bound and filter every large review surface with native single-select controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator filter input -> Streamlit render helpers | Selected values control which rows are rendered from already-read metric payloads. |
| Metric payload -> dataframe | Large structured result sets are bounded before display. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Denial of Service | review tables and samples | mitigate | Bound table rendering with global row-limit choices 25/50/100/250 and default 50. |
| T-10-05 | Tampering | filter controls | mitigate | Use selectbox options from structured registry/metrics payloads; no free-form filter input. |
| T-10-06 | Information Disclosure | filtered-empty states | mitigate | Use neutral copy `No rows match the current filters.` and avoid raw payload/error dumps. |
| T-10-SC | Tampering | package installs | accept | No package install task is planned. |
</threat_model>

<verification>
Run `uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q` after each task.
</verification>

<success_criteria>
- Row-limit control and page filters match D-05 through D-08.
- Large detail and sample tables are bounded after filtering.
- Overview snapshot remains unfiltered.
</success_criteria>

<output>
Create `.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-02-SUMMARY.md` when done.
</output>
