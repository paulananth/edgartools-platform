---
phase: 10-operator-review-experience
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - examples/mdm_graph_dashboard/streamlit_app.py
  - tests/architecture/test_dashboard_foundation_boundaries.py
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - "D-09: Missing MDM configuration or connection failure blocks metric rendering with setup copy."
    - "D-10: Neo4j unavailable state does not block MDM Overview."
    - "D-11: Error copy includes environment variable names and next actions only, without hostnames, usernames, passwords, raw URLs, secret JSON, or driver exception text."
    - "D-12: Filtered-empty tables render exact copy: No rows match the current filters."
    - "Filtered-empty, partial, disconnected, and permission-error states are clear and safe."
  artifacts:
    - path: "examples/mdm_graph_dashboard/streamlit_app.py"
      provides: "State rendering for MDM blocking, Neo4j non-blocking, filtered-empty, and permission-denied conditions"
      contains: "MDM configuration is required. Set `MDM_DATABASE_URL`, then restart the dashboard."
    - path: "tests/architecture/test_dashboard_foundation_boundaries.py"
      provides: "Secret-safe state copy guards"
      contains: "Neo4j graph metrics unavailable. MDM overview remains available."
  key_links:
    - from: "examples/mdm_graph_dashboard/streamlit_app.py"
      to: "dashboard_readonly.get_mdm_dashboard_metrics"
      via: "MDM metrics unavailable payload"
      pattern: "MDM_DATABASE_URL"
    - from: "examples/mdm_graph_dashboard/streamlit_app.py"
      to: "graph_readonly.get_neo4j_graph_metrics"
      via: "Neo4j optional unavailable payload"
      pattern: "Neo4j graph metrics unavailable"
---

<objective>
Harden the operator state model for empty, partial, disconnected, permission-denied, and secret-safe error states.

Purpose: Operators need clear safe guidance without dashboard writes, raw driver text, or misleading partial metrics.
Output: Updated Streamlit state copy and architecture tests for UX-03.
</objective>

<execution_context>
@/Users/aneenaananth/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aneenaananth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/mdm-neo4j-dashboard/STATE.md
@.planning/workstreams/mdm-neo4j-dashboard/REQUIREMENTS.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-CONTEXT.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-VALIDATION.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-02-SUMMARY.md
@examples/mdm_graph_dashboard/streamlit_app.py
@tests/architecture/test_dashboard_foundation_boundaries.py
@tests/mdm/test_dashboard_readonly.py
@tests/mdm/test_graph_readonly.py

<interfaces>
Existing helper payloads expose secret-safe fields such as `available`, `message`, `error_env_var`, `env_vars`, `warnings`, `state`, and `connected`. This plan should render those safe fields and exact UI-SPEC copy without importing lower-level database or driver objects.
</interfaces>
</context>

<source_audit>
GOAL covered: clear empty, partial, disconnected, and permission-error states.
REQ covered: UX-03.
RESEARCH covered: Plan slice 10-03, secret leakage risk, MDM blocking and Neo4j non-blocking behavior.
CONTEXT covered: D-09: missing MDM blocks rendering; D-10: Neo4j failure is non-blocking; D-11: secret-safe error copy with env vars and next action only; D-12: exact filtered-empty table copy. Deferred manual browser checklist is not included.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add secret-safe state copy guards</name>
  <files>tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    @tests/architecture/test_dashboard_foundation_boundaries.py
    @tests/mdm/test_dashboard_readonly.py
    @tests/mdm/test_graph_readonly.py
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-VALIDATION.md
  </read_first>
  <behavior>
    - Architecture guard expects exact MDM config copy: `MDM configuration is required. Set `MDM_DATABASE_URL`, then restart the dashboard.`
    - Guard expects MDM unavailable copy: `MDM database unavailable. Check `MDM_DATABASE_URL`, confirm the database is reachable, and restart the dashboard.`
    - Guard expects Neo4j unavailable copy: `Neo4j graph metrics unavailable. MDM overview remains available.`
    - Guard expects Neo4j permission copy: `Neo4j permission denied. Confirm the configured graph user can run read-only MATCH queries.`
    - Guard rejects raw secret/connection placeholder tokens in dashboard-rendered copy: `password=`, `postgresql://`, `neo4j://`, `bolt://`, `example.internal`, `traceback`, `RuntimeError(`.
  </behavior>
  <action>Add tests that pin the exact state copy from `10-UI-SPEC.md` and ensure the dashboard source does not render raw secret-bearing connection strings or exception text. If extending `_FakeStreamlit`, capture calls for `error`, `warning`, `info`, and `success` so behavior assertions can inspect rendered messages without live credentials.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests mention D-09, D-10, D-11, and D-12 target copy through exact strings.
    - Tests fail until Task 2 implements or centralizes the copy.
    - Existing helper tests stay referenced as source-of-truth for safe payloads.
  </acceptance_criteria>
  <done>State-copy and secret-safety expectations are encoded before implementation changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render blocking and non-blocking states safely</name>
  <files>examples/mdm_graph_dashboard/streamlit_app.py</files>
  <read_first>
    @examples/mdm_graph_dashboard/streamlit_app.py
    @tests/architecture/test_dashboard_foundation_boundaries.py
    @tests/mdm/test_dashboard_readonly.py
    @tests/mdm/test_graph_readonly.py
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-CONTEXT.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
  </read_first>
  <behavior>
    - Missing MDM config renders a blocking setup page and metric loading stops after `_read_mdm_metrics`.
    - MDM connection or permission failure renders blocking error state and does not render stale/partial MDM tables.
    - Neo4j unavailable renders warning/info state in Neo4j and mismatch views while MDM Overview remains usable.
    - Filtered-empty tables render `No rows match the current filters.` and body `Adjust the selected type or row limit, then review the table again.`
    - Secret-safe state messages contain env var names and next actions only.
  </behavior>
  <action>Centralize state copy constants from `10-UI-SPEC.md` in `streamlit_app.py`. Update `_render_mdm_unavailable` or equivalent to distinguish missing configuration, connection unavailable, and permission denied when safe structured fields allow it; otherwise fall back to the exact MDM unavailable copy. Ensure `main()` does not call diagnostic or Neo4j reads when MDM metrics are unavailable. Update Neo4j unavailable rendering in `render_neo4j_overview` and `render_mismatch_diagnostics` to use the exact non-blocking copy and preserve MDM-only review. Keep all copy secret-safe and do not render raw exception strings.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The exact filtered-empty copy appears once as a reusable constant or function.
    - Missing MDM and Neo4j unavailable exact copy from UI-SPEC appears in dashboard source.
    - The Streamlit app still imports only `dashboard_readonly` and `graph_readonly` from `edgar_warehouse.mdm`.
    - The architecture command passes.
  </acceptance_criteria>
  <done>Dashboard state rendering is safe, blocking only where required, and clear for partial graph availability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helper failure payload -> operator copy | Failure states can reveal secrets if raw exception text is rendered. |
| Optional Neo4j state -> MDM views | Neo4j failures must not incorrectly block MDM review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Information Disclosure | MDM/Neo4j error rendering | mitigate | Render exact safe copy naming env vars and next actions only; architecture guard rejects raw URI/exception tokens. |
| T-10-08 | Denial of Service | MDM unavailable path | mitigate | Block dependent metric loading after MDM unavailable payload and avoid stale/partial tables. |
| T-10-09 | Spoofing | Neo4j unavailable state | mitigate | Mark graph-backed views unavailable while keeping MDM Overview explicitly available. |
| T-10-SC | Tampering | package installs | accept | No package install task is planned. |
</threat_model>

<verification>
Run `uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q` after each task, then run `uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py tests/architecture/test_dashboard_foundation_boundaries.py -q` before summary creation.
</verification>

<success_criteria>
- UX-03 copy and behavior are pinned in tests and implementation.
- No raw credentials, hostnames, database URLs, secret JSON, or driver exceptions are rendered.
- MDM unavailable blocks; Neo4j unavailable does not block MDM review.
</success_criteria>

<output>
Create `.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-03-SUMMARY.md` when done.
</output>
