---
phase: 10-operator-review-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/mdm_graph_dashboard/streamlit_app.py
  - tests/architecture/test_dashboard_foundation_boundaries.py
autonomous: true
requirements:
  - GRAPH-01
  - UX-01
user_setup: []
must_haves:
  truths:
    - "D-01: Operator lands on Overview and can drill into MDM, Neo4j, and mismatch detail pages."
    - "D-02: Primary navigation is exactly Overview, MDM Overview, Neo4j Overview, Mismatch Diagnostics."
    - "D-03: Operator sees attention-needed content before snapshot counts."
    - "D-04: Entity-domain Neo4j counts use registry labels such as Company, Security, and Person."
  artifacts:
    - path: "examples/mdm_graph_dashboard/streamlit_app.py"
      provides: "Final primary navigation and registry-label entity-domain comparison"
      contains: "SECTIONS = [\"Overview\", \"MDM Overview\", \"Neo4j Overview\", \"Mismatch Diagnostics\"]"
    - path: "tests/architecture/test_dashboard_foundation_boundaries.py"
      provides: "Passing GRAPH-01 and navigation regressions"
      contains: "test_entity_comparison_uses_registry_labels_for_neo4j_node_counts"
  key_links:
    - from: "examples/mdm_graph_dashboard/streamlit_app.py"
      to: "mdm_metrics['registry']['entity_type_details']"
      via: "Neo4j label lookup for entity-domain coverage"
      pattern: "entity_type_details.*neo4j_label"
    - from: "tests/architecture/test_dashboard_foundation_boundaries.py"
      to: "examples/mdm_graph_dashboard/streamlit_app.py"
      via: "fake Streamlit module import and renderer invocation"
      pattern: "_render_entity_comparison"
---

<objective>
Close the carried GRAPH-01 correctness gap and establish the final review-first dashboard structure.

Purpose: Operators must not see misleading zero Neo4j counts, stale placeholder routes, or count-first landing content.
Output: Updated Streamlit navigation/rendering and passing architecture regressions for GRAPH-01 and UX-01.
</objective>

<execution_context>
@/Users/aneenaananth/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aneenaananth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/mdm-neo4j-dashboard/STATE.md
@.planning/workstreams/mdm-neo4j-dashboard/ROADMAP.md
@.planning/workstreams/mdm-neo4j-dashboard/REQUIREMENTS.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-CONTEXT.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-RESEARCH.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-VALIDATION.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/09-mdm-and-neo4j-review-metrics/09-VALIDATION.md
@.planning/workstreams/mdm-neo4j-dashboard/phases/09-mdm-and-neo4j-review-metrics/09-03-SUMMARY.md
@examples/mdm_graph_dashboard/streamlit_app.py
@tests/architecture/test_dashboard_foundation_boundaries.py

<interfaces>
Current Streamlit app identifiers:
- `SECTIONS`
- `_render_grouped_warnings(mdm_metrics, neo4j_metrics, coverage_rows)`
- `_render_snapshot(mdm_metrics, neo4j_metrics, coverage_rows)`
- `_render_entity_comparison(mdm_metrics, neo4j_metrics)`
- `render_overview(mdm_metrics, neo4j_metrics, coverage_rows)`
- `render_entities(mdm_metrics)`
- `render_relationships(mdm_metrics, neo4j_metrics)`
- `render_graph_coverage(mdm_metrics, neo4j_metrics, coverage_rows)`
- `main()`

Current test helper identifiers:
- `_FakeStreamlit.dataframes`
- `_load_streamlit_app_with_fake_streamlit()`
- `DashboardFoundationBoundaryTests.test_entity_comparison_uses_registry_labels_for_neo4j_node_counts`
</interfaces>
</context>

<source_audit>
GOAL covered: separate review views and correct mismatch diagnostics start here.
REQ covered: UX-01 and carried GRAPH-01.
RESEARCH covered: Plan slice 10-01, GRAPH-01 closure, final navigation labels, overview ordering.
CONTEXT covered: D-01: triage overview first; D-02: final navigation labels; D-03: attention-first overview ordering; D-04: carried GRAPH-01 registry-label fix. Deferred deployment, trend, manual checklist, and graph visualization ideas are not included.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert GRAPH-01 and navigation guards to passing assertions</name>
  <files>tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    @tests/architecture/test_dashboard_foundation_boundaries.py
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-CONTEXT.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/09-mdm-and-neo4j-review-metrics/09-VALIDATION.md
  </read_first>
  <behavior>
    - GRAPH-01 regression expects Companies=30, Securities=40, People=50 using registry labels Company, Security, Person.
    - Regression is a normal passing test; `@unittest.expectedFailure` is absent.
    - Static navigation guard expects exactly `Overview`, `MDM Overview`, `Neo4j Overview`, `Mismatch Diagnostics` and rejects primary labels `Entities`, `Relationships`, `Graph Coverage`, `Neighborhood`.
    - Overview ordering guard proves `_render_grouped_warnings` appears before `_render_snapshot` inside `render_overview`.
  </behavior>
  <action>Update `DashboardFoundationBoundaryTests` per D-01, D-02, D-03, and D-04. Remove the `@unittest.expectedFailure` decorator from `test_entity_comparison_uses_registry_labels_for_neo4j_node_counts`. Extend the fake Streamlit harness only as needed for this plan's assertions. Add focused static assertions for the final `SECTIONS` order and for the old labels not remaining in `SECTIONS`; do not forbid those old words in historical README prose yet because README is handled in Plan 10-04.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "expectedFailure" tests/architecture/test_dashboard_foundation_boundaries.py` returns no match for the GRAPH-01 test.
    - The architecture test file contains all four final navigation labels.
    - The GRAPH-01 test still calls `_render_entity_comparison` with registry `entity_type_details`.
    - The automated command fails before Task 2 and passes after Task 2.
  </acceptance_criteria>
  <done>Architecture tests encode the final navigation contract and the carried GRAPH-01 bug as passing requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registry-label counts and review-first navigation</name>
  <files>examples/mdm_graph_dashboard/streamlit_app.py</files>
  <read_first>
    @examples/mdm_graph_dashboard/streamlit_app.py
    @tests/architecture/test_dashboard_foundation_boundaries.py
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-UI-SPEC.md
    @.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-RESEARCH.md
  </read_first>
  <behavior>
    - `SECTIONS` is exactly `Overview`, `MDM Overview`, `Neo4j Overview`, `Mismatch Diagnostics`.
    - `render_overview` renders attention-needed warnings before snapshot metrics.
    - `_render_entity_comparison` looks up Neo4j counts by registry `neo4j_label`; it does not use `rstrip("s")`.
    - Entity-domain coverage includes columns `Domain`, `MDM Count`, `Neo4j Label`, `Neo4j Count`, `Status`.
    - When Neo4j is unavailable, entity-domain coverage shows MDM counts, `-` Neo4j counts, and `Unavailable`.
  </behavior>
  <action>Change `SECTIONS` and `main()` routing per D-02. Rename or replace the old render destinations so primary view handlers correspond to `render_mdm_overview`, `render_neo4j_overview`, and `render_mismatch_diagnostics` or equivalent exact labels. Move `_render_grouped_warnings(...)` before `_render_snapshot(...)` inside `render_overview` per D-03. Replace the plural-stripping `graph_key = label.rstrip("s")` path with a helper that reads `mdm_metrics["registry"]["entity_type_details"]`, maps each MDM `domain` or `entity_type` to its exact `neo4j_label`, and uses that label to read `neo4j_metrics["node_counts"]` per D-04. Preserve Streamlit native primitives and read-only helper imports only.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `examples/mdm_graph_dashboard/streamlit_app.py` contains no `rstrip("s")` entity-to-label mapping.
    - `SECTIONS` contains exactly four values in the required order.
    - The app has no primary `Neighborhood` placeholder route.
    - The architecture command passes.
  </acceptance_criteria>
  <done>Operators land on a review-first Overview, can navigate to the three final detail views, and see entity-domain Neo4j counts sourced from registry labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDM metrics -> Streamlit UI | Structured helper payloads become operator-visible tables and status text. |
| Neo4j metrics -> Streamlit UI | Optional graph payloads can be unavailable, partial, or sparse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `_render_entity_comparison` | mitigate | Use registry `neo4j_label` values, not display-label guessing, and convert GRAPH-01 regression to passing. |
| T-10-02 | Repudiation | `SECTIONS` and `main()` routing | mitigate | Static architecture guard asserts final navigation labels and absence of stale primary labels. |
| T-10-03 | Information Disclosure | status/warning rendering | mitigate | Preserve helper safe-copy payloads and do not introduce raw exception rendering. |
| T-10-SC | Tampering | package installs | accept | No npm, pip, cargo, or uv dependency install task is planned. |
</threat_model>

<verification>
Run `uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q` after each task and before summary creation.
</verification>

<success_criteria>
- Final navigation labels and Overview ordering match D-01 through D-03.
- GRAPH-01 registry-label regression is passing.
- Dashboard remains read-only and uses Streamlit native primitives only.
</success_criteria>

<output>
Create `.planning/workstreams/mdm-neo4j-dashboard/phases/10-operator-review-experience/10-01-SUMMARY.md` when done.
</output>
