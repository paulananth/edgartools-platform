---
phase: 04-operator-dashboard-and-data-issue-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/mdm_graph_dashboard/README.md
  - tests/architecture/test_dashboard_foundation_boundaries.py
autonomous: true
requirements: [DASH-03, DASH-01]

must_haves:
  truths:
    - "DASH-03: The dashboard README documents launching the read-only dashboard with `uv` and no longer instructs operators to configure external `NEO4J_*`, Bolt, Aura, or `check-connectivity --neo4j` setup paths."
    - "DASH-03: The README preserves the `Neo4j Overview` route label while describing the page as Snowflake-hosted Neo4j Graph Analytics (per upstream 04-03-PLAN.md D-03)."
    - "DASH-03: The README points to `edgar-warehouse mdm verify-graph`, `infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql`, and the AWS hosted graph E2E command as documentation text only, and states the dashboard has no destructive or write controls."
    - "DASH-03: The architecture test `test_dashboard_foundation_boundaries.py` enforces the post-rewrite README contract (no active external NEO4J_* setup, allowed mdm command set without `check-connectivity --neo4j`) and all tests pass."
    - "DASH-01: The README tells operators how to load `MDM_DATABASE_URL` from AWS Secrets Manager into the environment without printing the value, and lists the four dashboard views (Overview, MDM Overview, Neo4j Overview, Mismatch Diagnostics)."
  artifacts:
    - path: examples/mdm_graph_dashboard/README.md
      provides: "Read-only MDM + Snowflake-hosted graph dashboard operator runbook with no active external Neo4j setup path."
      contains: "verify-graph"
    - path: tests/architecture/test_dashboard_foundation_boundaries.py
      provides: "Updated README-contract architecture gate enforcing the post-rewrite contract."
      contains: "Neo4j Overview"
  key_links:
    - from: "examples/mdm_graph_dashboard/README.md"
      to: "tests/architecture/test_dashboard_foundation_boundaries.py (test_d13_d14_d15_d16_readme_contract_matches_operator_runbook)"
      via: "architecture test asserting README headings, env vars, allowed mdm command set, and absence of active external Neo4j setup"
      pattern: "test_d13_d14_d15_d16_readme_contract_matches_operator_runbook"
---

<objective>
Execute the README rewrite and architecture-test update from upstream
`neo4j-snowflake` Phase 4 `04-03-PLAN.md` Task 1/Task 2, scoped for go-live and
committed to `workspace/go-live` only (per D-01/D-02). Rewrite
`examples/mdm_graph_dashboard/README.md` so it describes the dashboard as a
read-only MDM + Snowflake-hosted graph inspection surface with no active
external `NEO4J_*` / Bolt / Aura / `check-connectivity --neo4j` setup path, and
update the coupled architecture test
`tests/architecture/test_dashboard_foundation_boundaries.py` so it enforces the
new README contract.

Purpose: the README is the operator runbook for the dashboard. As long as it
documents external Neo4j credential setup, the launch gate row 27 ("Dashboard
README NEO4J_* cleanup") stays BLOCKED. The architecture test currently
*requires* the old NEO4J_* content, so the README and the test MUST change in
the same atomic task — the gate is green today (24 passed) only because the old
README satisfies the old assertions.

Output: rewritten `README.md` (9 headings preserved, content updated) and an
updated `test_dashboard_foundation_boundaries.py` whose README-contract test
asserts the post-rewrite contract. `uv run pytest
tests/architecture/test_dashboard_foundation_boundaries.py -q` must pass (per
D-03). No commits land on the neo4j-snowflake workstream branch; the upstream
`04-DASHBOARD-VERIFICATION.md` file is NOT created here (it lives in the other
workstream — out of scope per D-01/D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/04-CONTEXT.md

# The upstream plan whose Task 1/Task 2 scope this plan executes (README + test):
@.planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-03-PLAN.md

# The file being rewritten and the read-only dashboard it documents:
@examples/mdm_graph_dashboard/README.md
@examples/mdm_graph_dashboard/streamlit_app.py

# The coupled architecture gate (MUST be edited in the same task as the README):
@tests/architecture/test_dashboard_foundation_boundaries.py

<interfaces>
<!-- BASELINE CONFIRMED during planning: the gate is GREEN today (24 passed) and -->
<!-- streamlit_app.py is already migrated (line 41 calls -->
<!-- graph_readonly.get_snowflake_graph_metrics; no get_neo4j_graph_metrics). -->
<!-- So this plan only changes README.md + the README-contract test. Do NOT touch -->
<!-- streamlit_app.py, graph_readonly.py, or dashboard_readonly.py. -->

<!-- The README-contract test to edit is -->
<!-- test_d13_d14_d15_d16_readme_contract_matches_operator_runbook (lines 528-597). -->
<!-- It currently REQUIRES (assertIn) NEO4J_URI/USER/PASSWORD/DATABASE/SECRET_JSON -->
<!-- in the env-var loop (lines 550-558) and includes -->
<!-- `edgar-warehouse mdm check-connectivity --neo4j` in allowed_commands (line 572). -->
<!-- These two assertions MUST flip when the README drops that content, or the test -->
<!-- fails. The streamlit-copy test at lines 425-447 is the existing pattern for -->
<!-- "allow the Neo4j Overview label, forbid active NEO4J_* setup" — mirror it. -->

<!-- The 9 README headings the test pins via assertEqual (lines 532-545) MUST be -->
<!-- preserved exactly, in order: -->
<!--   Purpose, Read-only guarantee, Prerequisites, Launch, Review workflow, -->
<!--   Filters, Failure states, Existing checks, Validation -->
<!-- Change content under headings; do not add/remove/reorder headings unless you -->
<!-- update the assertEqual list to match. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite dashboard README to remove active external Neo4j setup and flip the coupled architecture test</name>
  <files>examples/mdm_graph_dashboard/README.md, tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    - examples/mdm_graph_dashboard/README.md
    - examples/mdm_graph_dashboard/streamlit_app.py
    - tests/architecture/test_dashboard_foundation_boundaries.py
    - .planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-03-PLAN.md
  </read_first>
  <behavior>
    - After rewrite: `uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q` passes (all tests, not just the README test).
    - README "Prerequisites" no longer lists `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD`, `NEO4J_DATABASE`, `NEO4J_SECRET_JSON` as optional graph connection variables to set; the Snowflake-hosted graph requires a Snowflake connection (`SNOWFLAKE_CONNECTION` / `DBT_SNOWFLAKE_*` / `MDM_SNOWFLAKE_*`), not external Bolt/Aura credentials.
    - README "Existing checks" lists exactly `edgar-warehouse mdm counts` and `edgar-warehouse mdm verify-graph` (drops `edgar-warehouse mdm check-connectivity --neo4j`).
    - README still names the four route labels including the preserved `Neo4j Overview` label, described as Snowflake-hosted Neo4j Graph Analytics.
  </behavior>
  <action>
Rewrite `examples/mdm_graph_dashboard/README.md` in place, preserving the 9
existing `## ` headings exactly and in order (Purpose, Read-only guarantee,
Prerequisites, Launch, Review workflow, Filters, Failure states, Existing
checks, Validation). Change content as follows, following the upstream
04-03-PLAN.md Task 1 acceptance criteria:

1. Purpose / Read-only guarantee: keep the read-only framing. Keep the exact
   sentence "This dashboard does not run sync, repair, migrate, load, or write
   actions." (the test asserts it verbatim). State the dashboard has no
   destructive or write controls.

2. Prerequisites: keep `MDM_DATABASE_URL` as the required MDM input and keep the
   existing secret-safe load block (`aws secretsmanager get-secret-value ...
   --query SecretString --output text` into an env var, value never printed —
   per D-13). REMOVE the "Optional graph connection variables: NEO4J_URI,
   NEO4J_USER, NEO4J_PASSWORD" and "Optional graph settings: NEO4J_DATABASE,
   NEO4J_SECRET_JSON" lines as *active setup instructions*. Replace with: the
   Snowflake-hosted graph is read via the Snowflake connection context the
   helper accepts (`SNOWFLAKE_CONNECTION`, `DBT_SNOWFLAKE_*`, `MDM_SNOWFLAKE_*`);
   Native App grant / compute-pool prerequisites are documented in
   `infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql` (text reference
   only). Do NOT introduce `bolt://`, `neo4j://`, `Aura`, or `read-only MATCH`.

3. Launch: keep the exact command
   `uv run --extra dashboard --extra mdm-runtime streamlit run examples/mdm_graph_dashboard/streamlit_app.py`.

4. Review workflow: keep the four route labels — `Overview`, `MDM Overview`,
   `Neo4j Overview`, `Mismatch Diagnostics`. The `Neo4j Overview` label is
   preserved per upstream D-03; describe it as showing Snowflake-hosted Neo4j
   Graph Analytics node/edge counts (not an external graph connection).

5. Filters: keep `Row limit` with choices `25`, `50`, `100`, `250` and `All`
   default for type filters (the test asserts these literals are present).

6. Failure states: keep MDM-config-required guidance copy. Replace any
   "Neo4j unavailable / external graph" copy with Snowflake-hosted-graph-
   unavailable guidance that does not block `MDM Overview`.

7. Existing checks: list `edgar-warehouse mdm counts` and
   `edgar-warehouse mdm verify-graph` as the external acceptance-gate commands.
   Add documentation-text references to `edgar-warehouse mdm verify-graph`,
   `infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql`, and the AWS
   hosted graph E2E command (`infra/scripts/run-aws-mdm-e2e.sh`) as text only.
   REMOVE `edgar-warehouse mdm check-connectivity --neo4j`.

8. Validation: keep the exact command
   `uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py tests/architecture/test_dashboard_foundation_boundaries.py -q`.

Then update `tests/architecture/test_dashboard_foundation_boundaries.py` so the
README-contract test
(`test_d13_d14_d15_d16_readme_contract_matches_operator_runbook`) matches the
rewritten README:
- In the required-env-var loop, drop `NEO4J_URI`, `NEO4J_USER`,
  `NEO4J_PASSWORD`, `NEO4J_DATABASE`, `NEO4J_SECRET_JSON` from the `assertIn`
  set (keep `MDM_DATABASE_URL`). Add an assert-absent check for active external
  Neo4j setup mirroring the existing streamlit pattern at lines 425-447 — allow
  the `Neo4j Overview` label and `Snowflake-hosted Neo4j Graph Analytics` copy,
  but forbid `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD`, `NEO4J_DATABASE`,
  `NEO4J_SECRET_JSON`, `bolt://`, `neo4j://`, `Aura`, `read-only MATCH` in the
  README.
- Update `allowed_commands` to `{"edgar-warehouse mdm counts",
  "edgar-warehouse mdm verify-graph"}` (drop the `check-connectivity --neo4j`
  entry) so the `assertEqual(mdm_commands, allowed_commands)` matches the
  rewritten "Existing checks" section.
- Keep `assertIn("Neo4j Overview", text)` (route label preserved per D-03).
- Keep the heading `assertEqual` list unchanged (the rewrite preserves all 9
  headings); only change content-level assertions.

Do NOT modify `streamlit_app.py`, `graph_readonly.py`, or
`dashboard_readonly.py` (already migrated; baseline gate is green). Do NOT
create `.planning/workstreams/neo4j-snowflake/.../04-DASHBOARD-VERIFICATION.md`
(other workstream — out of scope per D-01/D-02).
  </action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
    <automated>f=examples/mdm_graph_dashboard/README.md; test "$(grep -cE 'NEO4J_URI|NEO4J_USER|NEO4J_PASSWORD|NEO4J_SECRET_JSON|bolt://|neo4j://|Aura|check-connectivity --neo4j' "$f")" -eq 0 && grep -q 'Neo4j Overview' "$f" && grep -q 'verify-graph' "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q` reports all tests passed (was 24 passed at baseline; count may change if the new assert-absent check adds a test, but exit code must be 0).
    - `grep -c` over `examples/mdm_graph_dashboard/README.md` for any of `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD`, `NEO4J_SECRET_JSON`, `bolt://`, `neo4j://`, `Aura`, `check-connectivity --neo4j` returns 0.
    - `examples/mdm_graph_dashboard/README.md` contains the literal string `Neo4j Overview` and the literal string `verify-graph`.
    - The README "Existing checks" section, scanned by the test regex `^edgar-warehouse mdm [^\n`]+$`, yields exactly `{edgar-warehouse mdm counts, edgar-warehouse mdm verify-graph}`.
    - The 9 README headings remain exactly Purpose, Read-only guarantee, Prerequisites, Launch, Review workflow, Filters, Failure states, Existing checks, Validation, in order.
    - `examples/mdm_graph_dashboard/streamlit_app.py`, `edgar_warehouse/mdm/graph_readonly.py`, and `edgar_warehouse/mdm/dashboard_readonly.py` are unmodified by this task (git diff shows only README.md and the test file changed).
  </acceptance_criteria>
  <done>
README rewritten with the 9 headings preserved, no active external NEO4J_* / Bolt
/ Aura / `check-connectivity --neo4j` setup path, `Neo4j Overview` route label
preserved and described as Snowflake-hosted Neo4j Graph Analytics, and
documentation-text references to `verify-graph`,
`neo4j_graph_analytics_app_grants.sql`, and the AWS hosted graph E2E command.
The README-contract architecture test is updated to assert the new contract and
the full `test_dashboard_foundation_boundaries.py` suite passes. Only README.md
and the test file changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → committed README | The secret-safe load block names `aws secretsmanager get-secret-value`; the resolved `MDM_DATABASE_URL` value must never be pasted into the README (env-only per D-13). |
| dashboard runbook → operator action | The README must not document any destructive, write, sync, repair, migrate, load, grant, or deploy control; the dashboard is inspection only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | README secret-safe load block | mitigate | The load block uses `--query SecretString --output text` into an env var only; the README states the value must not be printed or pasted (D-13). No DSNs, passwords, or tokens appear as literals — only the env-var name and the AWS CLI command shape. |
| T-04-01-02 | Tampering | Dashboard operator runbook | mitigate | The README documents only external read-only acceptance-gate commands (`mdm counts`, `mdm verify-graph`) and text references to grant SQL / AWS E2E; no dashboard sync/repair/migrate/load/grant/deploy controls. The architecture test `test_dashboard_text_contains_no_mutation_controls` enforces this and must stay green. |
| T-04-01-03 | Repudiation | README ↔ test contract | mitigate | The README and its architecture test are edited atomically; the passing `test_dashboard_foundation_boundaries.py` run is the recorded proof the post-rewrite contract holds (D-03). |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — documentation + test-edit only; `uv run pytest` uses existing locked deps. No supply-chain checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q` exits 0 (D-03 gate).
- README contains no active external `NEO4J_*` / Bolt / Aura / `check-connectivity --neo4j` setup path (grep gate returns 0).
- README preserves the `Neo4j Overview` route label and the 9 headings; `verify-graph` referenced as text.
- `git diff --name-only` shows only `examples/mdm_graph_dashboard/README.md` and `tests/architecture/test_dashboard_foundation_boundaries.py` changed.
</verification>

<success_criteria>
- `examples/mdm_graph_dashboard/README.md` describes the read-only MDM +
  Snowflake-hosted graph dashboard with no active external Neo4j setup path,
  preserves the `Neo4j Overview` route label, and references `verify-graph`,
  `neo4j_graph_analytics_app_grants.sql`, and the AWS hosted graph E2E command
  as documentation text only.
- `tests/architecture/test_dashboard_foundation_boundaries.py` enforces the
  post-rewrite README contract and the full suite passes.
- No secrets, DSNs, passwords, or tokens appear in either file.
- `<threat_model>` present covering Information Disclosure and Tampering.
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/04-01-SUMMARY.md` when done.
</output>
