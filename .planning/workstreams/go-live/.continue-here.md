---
context: workstream
workstream: go-live
phase: 02-aws-and-snowflake-production-deployment-dry-run
task: 10-12
total_tasks: 14
status: in_progress_needs_revision
last_updated: 2026-06-14T19:54:07.306Z
branch: workspace/go-live
---

# Go-Live Workstream Handoff To Claude

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These were discovered through actual failures in this workflow. Acknowledge them before continuing.

- [ ] CONSTRAINT: Correct worktree only - all Phase 2 go-live work is in `/Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform` on branch `workspace/go-live`. Do not use `/Users/aneenaananth/gsd-workspaces/dashboard-uat` or the main project checkout for this task.
- [ ] CONSTRAINT: Plan-checker findings must be fixed before Step 13 - do not mark Phase 2 planned or update STATE/ROADMAP until the four content blockers below are patched and locally validated.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Wrong worktree | Earlier exploration initially read stale handoff files from `/Users/aneenaananth/gsd-workspaces/dashboard-uat`; the active work is the go-live worktree. | blocking | Start every shell command with `cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform` or set that as the tool workdir. Check `git status --short --branch` and `.planning/active-workstream` before edits. |
| Treating reviewer availability as plan quality | Earlier handoffs said plan-checker was blocked by socket errors. A later retry completed and returned real `## ISSUES FOUND`, so the issue is no longer just tooling availability. | blocking | Patch the four findings below first. Then run direct `gsd-sdk` validation and either run a focused checker retry or record the direct validation fallback explicitly. |
| Assuming structural smoke is always safe | `deploy-snowflake-stack.sh --env prod` currently stops on missing `backend.hcl` before state-changing Terraform/Snowflake actions, but the plan must not rely on that remaining true forever. | advisory | Add a preflight guard to the plan: assert the three prod `backend.hcl` files are absent and statically verify backend checks occur before any `terraform apply`, Snowflake DDL, dbt, or dashboard upload. |

<current_state>
Worktree: `/Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform`
Branch: `workspace/go-live`
Active workstream: `go-live`

Phase 2 planning is mid-`/gsd:plan-phase 2 go-live`, at Step 10-12 (plan-checker revision loop). Steps 1 through 8 are done and committed. A successful `gsd-plan-checker` retry completed after the earlier socket-error handoff and returned `## ISSUES FOUND`.

No plan fixes have been applied yet in this side conversation. The worktree was clean before this handoff update. The next logical step is to patch the Phase 2 plan artifacts for the four plan-checker blockers, then validate the plan files before Step 13 coverage gates.
</current_state>

<completed_work>

Completed and committed before this handoff:

- `f3160cf` - `02-CONTEXT.md` with locked Phase 2 decisions D-01 through D-07.
- `0ddc694` - `02-RESEARCH.md`.
- `aa54b19` - `02-VALIDATION.md`.
- `6f73702` - `02-PATTERNS.md`.
- `ff39406` - `02-01-PLAN.md`, `02-02-PLAN.md`, and finalized `02-VALIDATION.md` mapping.
- `6a1c135` - prior handoff documenting repeated plan-checker socket errors.

Read-only work completed after the prior handoff:

- Correct worktree confirmed: `workspace/go-live`, active workstream `go-live`.
- Existing Phase 2 handoff and structured handoff were read.
- `gsd-plan-checker` was spawned as agent `019ec77b-b8bb-7842-a0bf-86bb624d1f7d` and completed with `## ISSUES FOUND`.
- Relevant plan sections were inspected:
  - `02-CONTEXT.md`
  - `02-01-PLAN.md`
  - `02-02-PLAN.md`
  - `02-RESEARCH.md`
  - `02-VALIDATION.md`
  - `infra/scripts/deploy-snowflake-stack.sh`

</completed_work>

<remaining_work>

Next logical task: patch Phase 2 planning docs for the plan-checker findings.

Required fixes:

1. D-03/SNOW-02 was silently narrowed to documentation-only.
   - `02-CONTEXT.md` says dev-target `dbt compile`, `dbt run`, and `dbt test` should run and be recorded as dev-precedent.
   - `02-02-PLAN.md` currently says no dbt commands run.
   - Patch `02-02-PLAN.md` and `02-VALIDATION.md` to add a secret-safe human credential checkpoint and a task to run:
     - `uv run --with dbt-snowflake dbt deps`
     - `uv run --with dbt-snowflake dbt compile --target dev`
     - `uv run --with dbt-snowflake dbt run --target dev`
     - `uv run --with dbt-snowflake dbt test --target dev`
   - Evidence must be recorded in `evidence/snowflake.md` with the exact label `dev precedent only - prod proof required separately` or the existing file's verbatim dash variant if preserving existing typography. Do not print secrets, account locators, raw connector traces, compiled SQL, or passwords.
   - If dev credentials are unavailable during execution, the plan must require an honest BLOCKED/failed evidence entry, not a silent downgrade.

2. SNOW-01 smoke test can become state-changing if repo state changes.
   - `02-02-PLAN.md` currently invokes `deploy-snowflake-stack.sh --env prod` and only checks the failure afterward.
   - Patch task 02-02-01 so it first asserts all three prod backend files are absent:
     - `infra/terraform/access/aws/accounts/prod/backend.hcl`
     - `infra/terraform/snowflake/accounts/prod/backend.hcl`
     - `infra/terraform/access/snowflake/accounts/prod/backend.hcl`
   - Add a static-order check against `infra/scripts/deploy-snowflake-stack.sh` proving those backend checks appear before any `terraform_apply`, `terraform_apply_root`, `terraform_apply_storage_integration_only`, `snow sql`, dbt run/test, or dashboard upload. If that guard fails, the plan must record SNOW-01 as BLOCKED and must not run the wrapper.
   - Correct wording that claims the backend check is the "very first action"; it is before state-changing actions, not literally before all setup and temp-file creation.

3. D-07 MDM secret-name blocker list is partial.
   - `02-01-PLAN.md` documents the MDM secret names in the runbook task, but does not require matrix/evidence updates listing them as blocked identifiers.
   - Patch `02-01-PLAN.md` to require `01-LAUNCH-GATE-MATRIX.md` and/or `evidence/aws.md` to list these names as required identifiers, names only:
     - `edgartools-prod/mdm/postgres_dsn`
     - `edgartools-prod/mdm/neo4j`
     - `edgartools-prod/mdm/api_keys`
     - `edgartools-prod/mdm/snowflake`
   - Add grep verification so the names cannot be omitted.

4. `02-RESEARCH.md` still has unresolved Open Questions.
   - Patch `## Open Questions` to `## Open Questions (RESOLVED)` or equivalent.
   - Record decisions:
     - D-06 is resolved as A1: tag `edgartools-dev-warehouse:prod` and `edgartools-dev-mdm:prod` in place; no separate prod ECR repos in Phase 2.
     - D-03 dev dbt credentials are handled by a human credential checkpoint plus dev-target dbt validation task; if unavailable, execution records BLOCKED/failed evidence rather than narrowing the requirement.
     - Pattern 0 committed `versions.tf` fix is not authorized for Phase 2; Pattern 1 temp edit/revert remains the plan of record, with permanent fix deferred unless separately authorized.

After patching:

- Run direct validation:
  - `gsd-sdk frontmatter.validate --schema plan .planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-01-PLAN.md`
  - `gsd-sdk frontmatter.validate --schema plan .planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-02-PLAN.md`
  - `gsd-sdk verify.plan-structure .planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-01-PLAN.md`
  - `gsd-sdk verify.plan-structure .planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-02-PLAN.md`
- Grep the patched files for the four fixes above.
- Then proceed to Step 13 coverage gates:
  - LIVE-02 covered by 02-01.
  - SNOW-01 and SNOW-02 covered by 02-02.
  - D-01 through D-07 represented in plans, scope guards, task actions, and/or threat models.
  - Update STATE/ROADMAP with `gsd-sdk query state.planned-phase` and the roadmap dependency annotation.
- Step 14: present "PHASE 2 PLANNED" and point to `/gsd:execute-phase 2 go-live`.

</remaining_work>

<decisions_made>

- Phase 2 remains document-and-validate only: no prod `terraform apply`, no prod application deploy, no prod Snowflake DDL.
- The successful plan-checker result supersedes the older "plan-checker unavailable" handoff. Treat the checker output as authoritative content feedback.
- Fixing the plans is the next step. Do not proceed to coverage gates until the plan-checker blockers are addressed.
- The Snowflake wrapper smoke test can remain part of the plan only with an explicit preflight/static-order guard.
- The dev dbt requirement must not be downgraded silently. The plan needs a credential checkpoint and concrete dev-target dbt commands.

</decisions_made>

<blockers>

- Content blocker: plan-checker found four issues listed under Remaining Work.
- External blocker carried from Phase 1/Phase 2 context: prod AWS passive infrastructure and `infra/aws-prod-application.json` do not exist yet.
- External blocker: no prod Snowflake connection exists.
- Human-action blocker for execution, not planning: dev Snowflake/dbt credentials must be supplied if the dev-precedent dbt validation is to run during execution.

</blockers>

## Required Reading (in order)

1. `.planning/workstreams/go-live/.continue-here.md` - this handoff.
2. `.planning/HANDOFF.json` - machine-readable structured handoff.
3. `.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-CONTEXT.md` - D-01 through D-07 decisions.
4. `.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-01-PLAN.md` - AWS-side plan requiring D-07 MDM secret identifier patch.
5. `.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-02-PLAN.md` - Snowflake/dbt plan requiring SNOW-01 and D-03/SNOW-02 patches.
6. `.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-VALIDATION.md` - Nyquist map requiring task updates if task IDs change.
7. `.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-RESEARCH.md` - open questions requiring resolution text.
8. `infra/scripts/deploy-snowflake-stack.sh` - source for the static-order guard in SNOW-01.

## Infrastructure State

- No dev server, Streamlit dashboard, watcher, or background process is running for this work.
- No prod AWS/Snowflake mutation was performed in this side conversation.
- Worktree was clean before this handoff update.
- Current handoff update changes only:
  - `.planning/workstreams/go-live/.continue-here.md`
  - `.planning/HANDOFF.json`

## Pre-Execution Critique Required

- Design artifact: Phase 2 plan docs after the four patches above.
- Critique focus: D-03 dev dbt requirement, SNOW-01 non-mutating guard, D-07 MDM secret identifiers, and resolved research questions.
- Gate: Do not begin Phase 2 execution until the patched plans pass direct validation and either a focused plan-checker retry or an explicit direct-validation fallback is recorded.

<context>
The main thread was interrupted just before patching the plan docs. The intent was to apply those plan-only fixes, not to execute Phase 2. This side conversation should leave Claude a precise handoff and should not continue the implementation.
</context>

<next_action>
Start with:

```bash
cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform
git status --short --branch
printf 'active workstream: '; cat .planning/active-workstream
```

Then patch only the Phase 2 planning docs for the four plan-checker findings above. Do not update STATE/ROADMAP or mark Phase 2 planned until those patches pass validation.
</next_action>
