---
workstream: go-live
phase: "08"
phase_name: production-mdm-secrets-and-connectivity
branch: claude/go-live-v1.6-phase7
head: 8e4d945
status: phase_8_planning_complete_pending_plan_checker
last_updated: "2026-06-20T16:21:06.000Z"
---

# Resume: Phase 8 — Production MDM Secrets And Connectivity

## BLOCKING CONSTRAINTS

- Phase 8 plans must scope to exactly 2 MDM secrets: `postgres_dsn` and `snowflake`.
  Do NOT include `neo4j` or `api_keys` — both confirmed out of scope (neo4j is legacy,
  superseded by the Snowflake-hosted graph; api_keys belongs to the unrelated MDM
  FastAPI auth layer).
- Whichever plan populates `postgres_dsn` MUST include a precondition task that
  verifies the production Postgres instance actually exists before writing any DSN
  to Secrets Manager. No Terraform/SQL in this repo provisions a prod MDM Postgres
  instance — existence is unconfirmed. Never fabricate a DSN.
- Append exactly ONE new TODOS.md entry (do not edit existing entries) recording a
  follow-up to remove the unused `edgartools-prod/mdm/neo4j` Secrets Manager
  Terraform resource. This is a separate, NOT-yet-scheduled task — do not fold it
  into a Phase 8 execution task.
- Never commit secrets, DSNs, tokens, raw connector errors, or generated Terraform
  state to git (D-10, workstream-wide).

## Critical Anti-Patterns

| Don't | Do instead |
|---|---|
| Re-run gsd-phase-researcher for Phase 8 | 08-RESEARCH.md already exists (HIGH confidence) — re-running plan-phase should detect and skip re-research |
| Assume prod Postgres instance exists | Add a discovery/precondition task; stop if unconfirmed |
| Bundle neo4j Terraform removal into Phase 8 | Track it as a separate TODOS.md follow-up only |
| Write to /Users/aneenaananth/projects/edgartools-platform | Wrong checkout — all Phase 8 file ops belong in this go-live worktree (/Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform) |

<current_state>
Phase 7 (Production Snowflake Native Pull And Gold) is fully closed: SNOW-03 and
SNOW-04 both PASS, 16/16 dbt gold models built, 47/47 tests pass against production.

Phase 8 planning is DONE. Research is written to
`.planning/workstreams/go-live/phases/08-production-mdm-secrets-and-connectivity/08-RESEARCH.md`.
The `gsd-planner` agent's tool call returned a Claude session-limit message with no
`## PLANNING COMPLETE` marker, but both PLAN.md files were in fact written to disk in
full before the interruption: `08-01-populate-prod-mdm-secrets-PLAN.md` (230 lines, 3
tasks) and `08-02-verify-prod-mdm-connectivity-PLAN.md` (210 lines, 2 tasks). Both were
read end-to-end this session and confirmed correctly scoped: exactly postgres_dsn +
snowflake, a precondition task verifying the prod Postgres instance exists before any
DSN write, explicit exclusion of neo4j/api_keys, full STRIDE threat models. Only
`gsd-plan-checker` has not yet run against them.
</current_state>

<completed_work>
- Phase 7: dbt deps/run/test --target prod run to completion. Fixed a genuine
  PERIOD_START schema-drift bug on the live SEC_FINANCIAL_FACT table via a
  non-destructive 3-step migration. 16/16 models, 47/47 tests pass. SNOW-04 PASS.
- Phase 8 research: resolved a real scope discrepancy — HANDOFF.json had listed 4
  MDM secrets as blockers, but ROADMAP.md only requires 2. Confirmed via codebase
  research that neo4j and api_keys are legitimately out of scope.
- Phase 8 research surfaced a new gap: no Terraform/SQL in this repo provisions a
  production Postgres MDM instance — its existence is unconfirmed.
- Phase 8 planning: both 08-01 and 08-02 PLAN.md files exist, are complete, and
  correctly enforce every scope guardrail (see BLOCKING CONSTRAINTS above).
- Branch hygiene (this session, same conversation but not go-live-specific):
  `claude/go-live-v1.6-phase7` was rebased onto `origin/main` (now at PR #78's
  merge commit) and force-pushed with `--force-with-lease` after verifying via
  `git log origin/<branch> --not HEAD` that no commits would be lost. Branch is
  0/0 in sync with origin.
</completed_work>

<remaining_work>
1. Spawn `gsd-plan-checker` against the existing `08-01-populate-prod-mdm-secrets-PLAN.md`
   and `08-02-verify-prod-mdm-connectivity-PLAN.md`. Do NOT re-spawn gsd-planner —
   planning is already complete.
2. If the checker passes, proceed directly to `/gsd:execute-phase 8 --ws go-live`
   (08-01 secret population, then 08-02 connectivity/migrate/counts verification).
3. Continue to Phases 9 (hosted graph E2E), 10 (dashboard UAT), 11 (final GO decision).
</remaining_work>

<decisions_made>
- Phase 8 secret scope: exactly `postgres_dsn` + `snowflake`; `neo4j` and `api_keys`
  excluded.
- neo4j Terraform cleanup: doc-only now (one new TODOS.md entry), execution deferred
  — not a Phase 8 task.
- postgres_dsn population requires an existence-verification precondition task; never
  fabricate a DSN.
- `claude/go-live-v1.6-phase7` rebased onto main through PR #78, safely force-pushed.
</decisions_made>

<blockers>
- BLOCKING (human_action): confirm whether the production Snowflake-hosted Postgres
  MDM instance exists, and if so supply its connection details directly into Secrets
  Manager (never through git or chat). 08-01's Task 1 handles this check at execution
  time and will legitimately stop with a 5-whys BLOCKED if the instance doesn't exist.
- BLOCKING (human_action): owner/reviewer sign-off required later in Phases 7-11
  before the go/no-go packet can flip to GO.
- technical (resolved): the apparent gsd-planner session-limit interruption did not
  actually lose any planning output — both PLAN.md files were already on disk.
</blockers>

## Required Reading

- `.planning/workstreams/go-live/phases/08-production-mdm-secrets-and-connectivity/08-RESEARCH.md`
- `.planning/workstreams/go-live/STATE.md`
- `.planning/HANDOFF.json` (this file's machine-readable counterpart)

<context>
This go-live worktree (`/Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform`,
branch `claude/go-live-v1.6-phase7`) is the only place Phase 8 file operations should
happen. A separate, unrelated repo checkout (`/Users/aneenaananth/projects/edgartools-platform`)
is shared with Codex and had an uncommitted TODOS.md edit rescued and committed onto
`claude/financial-derived-lag-tiebreaker` (commit `d9b5111`) this same session — that
work is unrelated to go-live and requires no follow-up here.
</context>

<next_action>
Spawn `gsd-plan-checker` against `08-01-populate-prod-mdm-secrets-PLAN.md` and
`08-02-verify-prod-mdm-connectivity-PLAN.md`. If it passes, run
`/gsd:execute-phase 8 --ws go-live` directly — do not re-run the planner.
</next_action>
