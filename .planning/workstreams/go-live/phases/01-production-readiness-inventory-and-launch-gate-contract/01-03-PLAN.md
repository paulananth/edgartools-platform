---
phase: 01-production-readiness-inventory-and-launch-gate-contract
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/mdm-hosted-graph.md
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md
autonomous: true
requirements: [LIVE-01, SEC-01, ISO-01, ISO-02]

must_haves:
  truths:
    - "Operator can read an MDM/hosted-graph evidence file that reconciles the dev verify-graph + AWS MDM E2E precedent against production launch needs, with the strict verify-graph full-acceptance run marked as a Phase 3 / matrix BLOCKED pointer, not a Phase 1 pass."
    - "Operator can read a dashboard-security UAT evidence file that states the read-only/inspection-only guarantee, the secret-safe loading convention, and classifies the stale NEO4J_* README cleanup as a matrix BLOCKED item rather than a dashboard pass."
    - "Both evidence files contain only commands actually run or verified during Phase 1; --skip-preflight debug runs are labeled non-acceptance; every not-yet-runnable production step points to a BLOCKED row in 01-LAUNCH-GATE-MATRIX.md."
    - "Both evidence files contain no DSNs, passwords, tokens, Native App job logs, raw connector exceptions, or unbounded exports."
  artifacts:
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/mdm-hosted-graph.md"
      provides: "MDM/hosted-graph production-readiness evidence template: secret-safe header, verify-graph non-secret payload summary shape + parity table shape, --status-only Step Functions evidence actually run, dev-precedent reconciliation, and Phase 3 / matrix BLOCKED pointers for the full strict acceptance run."
      contains: "dev precedent only — prod proof required separately"
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md"
      provides: "Dashboard UAT + security evidence template: what-NOT-to-include preamble, read-only guarantee, secret-safe DSN-loading convention, text UAT note shape (screenshots optional), and NEO4J_* README cleanup classified as a matrix BLOCKED pointer."
      contains: "inspection only"
  key_links:
    - from: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md"
      to: "evidence/mdm-hosted-graph.md, evidence/dashboard-security.md"
      via: "relative-path links from matrix Required-Rerun-Proof / evidence-source cells (01-01 links TO these files)"
      pattern: "evidence/(mdm-hosted-graph|dashboard-security)\\.md"
    - from: "evidence/mdm-hosted-graph.md, evidence/dashboard-security.md"
      to: "01-LAUNCH-GATE-MATRIX.md BLOCKED rows"
      via: "back-pointer text for every not-yet-runnable prod step (D-13)"
      pattern: "01-LAUNCH-GATE-MATRIX\\.md"
---

<objective>
Create the MDM/hosted-graph and dashboard-security production-readiness evidence files for go-live
Phase 1. These are evidence TEMPLATE + dev-reconciliation documents, not filled-in production logs:
they capture the secret-safe verify-graph / Step Functions / dashboard-UAT structure, record only the
read-only checks actually run during Phase 1, reconcile dev proof against production launch needs, and
point every not-yet-runnable production step (especially the strict prod verify-graph acceptance run,
which is Phase 3 work) to a BLOCKED row in the gate matrix.

Purpose: ROADMAP success criterion 2 requires existing dev proof to be reconciled against production
launch needs (hosted graph E2E, dashboard UAT, MDM DSN source, unresolved workstream closeout items).
These two files are the MDM/hosted-graph and dashboard halves of that reconciliation, linked from the
gate matrix (01-LAUNCH-GATE-MATRIX.md) created by plan 01-01.

Output: `evidence/mdm-hosted-graph.md` and `evidence/dashboard-security.md` in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/PROJECT.md
@.planning/workstreams/go-live/ROADMAP.md
@.planning/workstreams/go-live/REQUIREMENTS.md
@.planning/workstreams/go-live/STATE.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-01-PLAN.md
@.planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-LIVE-DEV-RUN.md
</context>

<critical_constraint>
These two files are NOT filled-in production evidence. During Phase 1 the strict production verify-graph
acceptance run does NOT happen (it is Phase 3 work per D-22). DO NOT fabricate prod verify-graph results,
fake `phase3_acceptance: true`, fake parity counts, or invented dashboard UAT passes. Each file must contain:
1. A secret-safe header + disclaimer sentence (D-07).
2. The relevant non-secret summary STRUCTURE (verify-graph payload field list / parity table / Step Functions
   table for MDM; UAT-note shape for dashboard), ready to fill when prod runs actually happen.
3. Dev precedent reconciled from `03-LIVE-DEV-RUN.md` and dashboard UAT, EACH labeled
   "dev precedent only — prod proof required separately" (D-18).
4. ONLY read-only Phase 1 checks actually run (D-13/D-21) — e.g. `--status-only` Step Functions reports.
   The strict prod verify-graph full-acceptance run is a Phase 3 / matrix BLOCKED pointer, not a Phase 1 entry.
5. `--skip-preflight` debug runs labeled "emergency/debug — non-acceptance" and never recorded as a passing
   gate (D-23). Every not-yet-runnable prod step points to a `01-LAUNCH-GATE-MATRIX.md` BLOCKED row (D-13).
</critical_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Create evidence/mdm-hosted-graph.md (MDM + hosted graph evidence template)</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/mdm-hosted-graph.md</files>
  <read_first>
    - .planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-LIVE-DEV-RUN.md (canonical MDM/hosted-graph evidence shape: verify-graph payload summary lines 52-72 and 133-156; relationship parity table lines 74-89; Step Functions status table lines 99-109, 169-178)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md (D-09, D-10, D-13, D-18, D-21, D-22, D-23 — verify-graph preflight gate, status-only read-only, skip-preflight non-acceptance)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md (lines 184-243 evidence/mdm-hosted-graph.md patterns: verify-graph field list, parity table, Step Functions table, --status-only/--skip-preflight semantics)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-01-PLAN.md (Task 1 action — the verify-graph and AWS MDM E2E gate rows + BLOCKED rows this file must point back to)
    - infra/scripts/run-aws-mdm-e2e.sh (--snow-connection / --snowflake-database / --status-only / --skip-preflight flag semantics, local verify-graph preflight, Native App compute-pool selector)
  </read_first>
  <action>
    Create `evidence/mdm-hosted-graph.md` (create `evidence/` subdir if absent). Header following the
    `03-LIVE-DEV-RUN.md` lines 1-10 convention: title `# MDM + Hosted Graph Evidence — Phase 1 Production Readiness`,
    then `Date:`, `Environment:` (prod required per D-18, dev rows precedent only), `Snowflake connection:` /
    `Snowflake database:` / `AWS profile:` label placeholders, and the secret-safety disclaimer sentence:
    "This artifact captures non-secret evidence only. It omits passwords, tokens, DSNs, full task logs, and raw
    Native App job logs." (D-07).

    Add a "Preflight gate note" stating D-22: before any AWS MDM E2E starts, Phase 1 requires the LOCAL
    `edgar-warehouse mdm verify-graph` against the target Snowflake connection/database and Native App compute
    pool to be runnable; the actual strict full-acceptance run and its `phase3_acceptance: true` summary is
    Phase 3 work and must appear as a Phase 3 / matrix BLOCKED pointer, NOT as a Phase 1 evidence pass.

    Add a "Phase 1 read-only checks actually run" section using the command/result/summary block shape, ONLY
    for read-only checks genuinely run (D-21). The executor MUST run what it records and report the real outcome:
      - `bash infra/scripts/run-aws-mdm-e2e.sh --env <env> --aws-profile <profile> --status-only` IF the AWS
        profile is available — summarize using the Step Functions status table column shape
        `| Workflow | Latest status | Latest execution name |` covering mdm_migrate / mdm_run /
        mdm_backfill_relationships / mdm_sync_graph / mdm_verify_graph / mdm_counts. If the profile is NOT
        available in this environment, do NOT fabricate — record it as a not-yet-runnable step pointing to a matrix row.
    State explicitly (D-23): any `--skip-preflight` run is "emergency/debug — non-acceptance" and must never be
    recorded as a passing gate.

    Add the non-secret structures ready to fill (do NOT fabricate values):
      - The verify-graph "Non-secret verification payload summary" field list (Overall status, Snowflake graph
        nodes/edges, node/relationship parity status, missing/extra diagnostics, Native App status, Native App
        compute pool, Native App checks app_installation/compute_pool/graph_info/bfs/wcc, phase3_acceptance) —
        as an empty/placeholder template, marked "to be filled by Phase 3 prod run".
      - The relationship parity table column shape `| Relationship type | MDM active | Snowflake graph | Delta |`.

    Add a "Dev precedent reconciliation" section: summarize the dev verify-graph SUCCESS and dev AWS MDM E2E
    chain from `03-LIVE-DEV-RUN.md` (overall status ok, 15 nodes / 4 edges, compute pool CPU_X64_XS, all six
    Step Functions SUCCEEDED, phase3_acceptance true in DEV). Label the entire block "dev precedent only —
    prod proof required separately" (D-18). State the production proof still required (prod connection/database,
    prod Native App compute pool, prod strict verify-graph, prod AWS MDM E2E) and that each is a matrix BLOCKED/
    pending row pointing to Phase 3.

    For every not-yet-runnable production step add a back-pointer "BLOCKED — see 01-LAUNCH-GATE-MATRIX.md row
    '<gate name>'" (D-13). Do not add evidence entries for them.

    Do not paste any DSN, password, token, raw Native App job log, or secret value.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && test -f .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/mdm-hosted-graph.md && grep -q 'dev precedent only — prod proof required separately' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/mdm-hosted-graph.md && grep -q '01-LAUNCH-GATE-MATRIX.md' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/mdm-hosted-graph.md && grep -qi 'verify-graph' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/mdm-hosted-graph.md && grep -qi 'skip-preflight' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/mdm-hosted-graph.md && echo MDM_EVIDENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `evidence/mdm-hosted-graph.md` exists in the phase `evidence/` directory.
    - Header includes Date, Environment (prod-required/dev-precedent note), connection/database/profile labels, and the secret-safety disclaimer (D-07).
    - A "Preflight gate note" states D-22 (local verify-graph preflight is the Phase 1 requirement; strict full-acceptance run is Phase 3 / matrix BLOCKED).
    - The verify-graph non-secret payload field list and relationship parity table column shape are present as fill-ready templates, NOT fabricated with values.
    - `--status-only` evidence is recorded only if actually run (real outcome), and `--skip-preflight` is labeled "emergency/debug — non-acceptance" (D-23).
    - A "Dev precedent reconciliation" section summarizes the dev verify-graph + E2E success and is labeled "dev precedent only — prod proof required separately" (D-18).
    - Every not-yet-runnable production step points to a `01-LAUNCH-GATE-MATRIX.md` BLOCKED row (D-13); the matrix is referenced.
    - No embedded-credential DSN appears: `grep -v '^#' evidence/mdm-hosted-graph.md | grep -Eic 'postgres(ql)?://[^ ]+:[^ ]+@'` returns 0.
  </acceptance_criteria>
  <done>evidence/mdm-hosted-graph.md exists as a secret-safe template with the verify-graph/parity/Step-Functions structures ready to fill, the dev verify-graph + E2E precedent reconciled and labeled dev-only, the strict prod acceptance run pointed to a Phase 3 / matrix BLOCKED row, and --skip-preflight marked non-acceptance — with no secrets or job logs.</done>
</task>

<task type="auto">
  <name>Task 2: Create evidence/dashboard-security.md (dashboard UAT + security evidence template)</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md</files>
  <read_first>
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md (D-07, D-13, D-14, D-26, D-27 — dashboard screenshots optional, inspection-only-does-not-define-acceptance, CLI/dbt-first routing)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md (lines 247-295 evidence/dashboard-security.md patterns: what-NOT-to-include preamble, read-only guarantee, secret-loading-without-printing, the stale NEO4J_* README cleanup BLOCKED classification)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-01-PLAN.md (Task 1 action — the dashboard gate row + the NEO4J_* README cleanup BLOCKED row this file must point back to)
    - examples/mdm_graph_dashboard/README.md (read-only guarantee framing lines 9-12; secret-safe DSN-loading via aws secretsmanager get-secret-value lines 28-37; stale NEO4J_* setup steps lines 18-21 and ~80-106 that must be classified, not inherited)
    - .planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-03-PLAN.md (Task 2 spec lines 79-97 — intended dashboard verification evidence shape / what-NOT-to-include list)
  </read_first>
  <action>
    Create `evidence/dashboard-security.md`. Header following the shared convention: title
    `# Dashboard UAT + Security Evidence — Phase 1 Production Readiness`, then `Date:`, `Environment:`
    (prod-like read-only; dev UAT is precedent only per D-18), and the secret-safety disclaimer (D-07).

    Add a "What this file must NOT include" preamble (from 04-03-PLAN.md Task 2 spec intent): no DSNs, passwords,
    tokens, raw connector exceptions/stack traces, full logs, account-secret values, mutation controls, or
    unbounded exports; bounded samples are diagnostics, not exhaustive exports. Add D-14: dashboard screenshots
    are optional if secret-safe; text UAT notes are sufficient for Phase 1.

    Add a "Read-only / inspection-only guarantee" section (reusing the `examples/mdm_graph_dashboard/README.md`
    lines 9-12 framing): state DASH-03/D-27 — the dashboard does not run sync, repair, migrate, load, or write
    actions; `Refresh metrics` only rereads cached read-only payloads; the dashboard is inspection only after
    CLI/dbt/Native App gates and explains issues but does NOT define acceptance.

    Add a "Routing policy" note (D-26): operators start with CLI verification and dbt tests, then inspect the
    dashboard for explanation.

    Add a "Secret-safe loading convention" section: describe operators loading `MDM_DATABASE_URL` via
    `aws secretsmanager get-secret-value ... --query SecretString --output text` into an env var WITHOUT ever
    printing the value (reference the README lines 28-37 pattern by description; do NOT paste any actual secret
    output). State this is the only sanctioned way DSNs enter the dashboard runtime.

    Add a "Phase 1 UAT notes" section as a TEXT UAT-note template, ready to fill (D-14) — view-by-view note rows
    for MDM overview, hosted graph overview, mismatch diagnostics, manual refresh timestamps, and bounded samples,
    with a pass/fail note column. Do NOT fabricate prod UAT passes; where dev UAT precedent exists (dashboard UAT
    passed locally after loading MDM config from Secrets Manager without printing the DSN — per STATE.md Known
    Inputs), summarize it and label it "dev precedent only — prod proof required separately" (D-18).

    Add a "Known cleanup item — classify, do not inherit" note: the current `examples/mdm_graph_dashboard/README.md`
    still documents `NEO4J_URI`/`NEO4J_USER`/`NEO4J_PASSWORD`/`NEO4J_DATABASE`/`NEO4J_SECRET_JSON` and
    `check-connectivity --neo4j` as active setup steps (the stale `NEO4J_*` assumptions). Per D-02 this is an
    incomplete upstream `neo4j-snowflake` Phase 4 (`04-03-PLAN.md`) closeout item that blocks go-live dashboard
    docs — record it as a back-pointer "BLOCKED — see 01-LAUNCH-GATE-MATRIX.md row 'Dashboard README NEO4J_* cleanup'",
    NOT as a passed dashboard item (D-13).

    Do not paste any DSN, password, token, raw connector exception, or unbounded export.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && test -f .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md && grep -qi 'inspection only' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md && grep -q '01-LAUNCH-GATE-MATRIX.md' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md && grep -q 'NEO4J' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md && grep -qi 'get-secret-value' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md && echo DASH_EVIDENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `evidence/dashboard-security.md` exists in the phase `evidence/` directory.
    - A "What this file must NOT include" preamble forbids DSNs, passwords, tokens, raw exceptions, full logs, mutation controls, and unbounded exports, and states D-14 (screenshots optional, text UAT sufficient).
    - A "Read-only / inspection-only guarantee" section states the dashboard is inspection only and does not define acceptance (D-27).
    - A "Routing policy" note states CLI/dbt-first then dashboard (D-26).
    - A "Secret-safe loading convention" section describes `aws secretsmanager get-secret-value` loading without printing the value (no actual secret output pasted).
    - A "Phase 1 UAT notes" text template is present (D-14); any dev UAT precedent is labeled "dev precedent only — prod proof required separately" (D-18).
    - The stale `NEO4J_*` README cleanup is classified as a back-pointer to the matrix BLOCKED row, not a dashboard pass (D-02/D-13).
    - No embedded-credential DSN appears: `grep -v '^#' evidence/dashboard-security.md | grep -Eic 'postgres(ql)?://[^ ]+:[^ ]+@'` returns 0.
  </acceptance_criteria>
  <done>evidence/dashboard-security.md exists as a secret-safe UAT template with the what-NOT-to-include preamble, read-only/inspection-only guarantee (does not define acceptance), secret-safe DSN-loading convention, fill-ready text UAT notes with dev precedent labeled dev-only, and the stale NEO4J_* README cleanup classified as a matrix BLOCKED pointer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator memory / live MDM+hosted-graph env → evidence markdown | Operators transcribe `verify-graph` / Step Functions output into `evidence/mdm-hosted-graph.md`; DSNs, tokens, and raw Native App job logs can leak if pasted verbatim. |
| dashboard runtime (Secrets Manager DSN) → UAT notes | The dashboard loads `MDM_DATABASE_URL` from Secrets Manager; UAT notes risk capturing the DSN, raw connector exceptions, or unbounded sample exports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `evidence/mdm-hosted-graph.md` verify-graph / Step Functions blocks | mitigate | Secret-safety disclaimer header (D-07); action forbids raw Native App job logs and DSNs; only non-secret payload field summaries recorded; acceptance grep asserts 0 embedded-credential DSN matches. |
| T-03-02 | Information Disclosure | `evidence/dashboard-security.md` UAT notes | mitigate | "What must NOT include" preamble forbids DSNs/tokens/raw exceptions/unbounded exports; secret-safe loading convention describes `get-secret-value` without printing; acceptance grep asserts 0 embedded-credential DSN matches. |
| T-03-03 | Information Disclosure | stale `NEO4J_*` README credentials | mitigate | The NEO4J_* cleanup is classified as a matrix BLOCKED pointer (not inherited as a dashboard pass); ISO-02 keeps the AWS-only path, deprecating external Neo4j credential assumptions. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — documentation-only evidence templates, no dependency changes. No supply-chain surface. |
</threat_model>

<verification>
- `evidence/mdm-hosted-graph.md` and `evidence/dashboard-security.md` exist in the phase `evidence/` directory.
- MDM file has the verify-graph/parity/Step-Functions structures, dev precedent labeled dev-only (D-18), --skip-preflight marked non-acceptance (D-23), and strict prod acceptance pointed to Phase 3 / matrix BLOCKED (D-22/D-13).
- Dashboard file states inspection-only-does-not-define-acceptance (D-27), the secret-safe loading convention, and the NEO4J_* cleanup as a matrix BLOCKED pointer (D-02).
- No embedded-credential DSN, raw Native App job log, raw connector exception, or unbounded export appears in either file.
</verification>

<success_criteria>
- Existing dev hosted-graph E2E + dashboard UAT proof is reconciled against production launch needs and labeled dev-precedent-only (ROADMAP success criterion 2).
- The strict prod verify-graph acceptance run and other not-yet-runnable prod steps are matrix BLOCKED-row pointers, not evidence entries (D-13); --skip-preflight is non-acceptance (D-23).
- The unresolved NEO4J_* README closeout item is classified as a matrix BLOCKED row, not silently inherited (D-02, success criterion 2's "unresolved workstream closeout items").
- Secret-safety rules honored: no DSNs, passwords, tokens, raw Native App job logs, raw connector exceptions, or unbounded exports (SEC-01, D-07).
- Both files live under `.planning/workstreams/go-live/` (ISO-01); no non-AWS deployment paths or external Neo4j credential dependencies introduced (ISO-02).
- LIVE-01: the read-only preflight checks (local verify-graph runnability, --status-only, secret-safe DSN loading) are the documented preflight evidence shape.
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-03-SUMMARY.md` when done.
</output>
