---
phase: 01-production-readiness-inventory-and-launch-gate-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
autonomous: true
requirements: [LIVE-01, SEC-01, ISO-01, ISO-02]

must_haves:
  truths:
    - "Operator can read one launch gate matrix that lists every required AWS, Snowflake, MDM, hosted graph, dashboard, and dbt check with command or evidence source and a BLOCKED/PASS/WARNING status."
    - "Every unresolved launch-impacting item appears as a BLOCKED row with owner/source, required fix, and required rerun proof."
    - "Operator can read explicit secret-safety rules forbidding DSNs, passwords, tokens, Terraform state, raw connector traces, and sensitive generated deployment values in evidence."
    - "Operator can read the list of required production identifiers that must be supplied before Phase 2 execution planning."
    - "Operator can route a launch data issue by layer to a likely source, evidence to check, owner, blocker status, and next action."
  artifacts:
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md"
      provides: "Launch gate matrix, blocker classification, secret-safety rules, required production identifiers checklist, and per-layer data-issue triage table"
      contains: "| Gate | Owner/Source | Required Fix | Required Rerun Proof | Status |"
  key_links:
    - from: "01-LAUNCH-GATE-MATRIX.md"
      to: "evidence/aws.md, evidence/snowflake.md, evidence/mdm-hosted-graph.md, evidence/dashboard-security.md"
      via: "relative-path links in matrix Required Rerun Proof / evidence-source cells"
      pattern: "evidence/(aws|snowflake|mdm-hosted-graph|dashboard-security)\\.md"
---

<objective>
Create the canonical launch gate matrix for go-live, including blocker classification rules,
the secret-safety contract for all evidence, the required production identifiers checklist
that gates Phase 2, and the per-layer data-issue triage table.

Purpose: Operators need a single go/no-go contract that lists every launch gate, classifies
every unresolved item as launch-blocking / warning-only / deferred, and forbids secret leakage
before any expensive or state-changing launch run begins. This is the source-of-truth document
that Phase 2 and Phase 3 planning consume to decide what is safe to run.

Output: `01-LAUNCH-GATE-MATRIX.md` in the phase directory, routing to the four `evidence/*.md`
files created by plans 01-02 and 01-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/PROJECT.md
@.planning/workstreams/go-live/ROADMAP.md
@.planning/workstreams/go-live/REQUIREMENTS.md
@.planning/workstreams/go-live/STATE.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the launch gate matrix with blocker classification</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md</files>
  <read_first>
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md (decisions D-01 through D-29 are the source of truth for every rule)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md (lines 26-93 matrix patterns; lines 326-348 BLOCKED row shape + owner-role vocabulary)
    - .planning/workstreams/go-live/REQUIREMENTS.md (traceability table format lines 64-83; IDs LIVE-01, SEC-01, ISO-01, ISO-02; per-phase requirement lists)
    - .planning/workstreams/go-live/ROADMAP.md (Phase 1 success criteria lines 39-46; per-phase requirement lists for the gates to enumerate)
    - docs/aws-mdm-snowflake-postgres-cutover.md (hazard call-out format Symptom/Root cause/Fix; the two named deploy hazards)
    - TODOS.md (blocker entry format; the EDGARTOOLS_DEV_DEPLOYER direct-SELECT grant gap)
  </read_first>
  <action>
    Create `01-LAUNCH-GATE-MATRIX.md` with a top-of-file environment header (Date, Environment label, secret-safety disclaimer sentence per D-07/D-18). Build the central gate matrix with exactly these columns per D-12 and PATTERNS line 332: `| Gate | Owner/Source | Required Fix | Required Rerun Proof | Status |`. Constrain `Status` to exactly `BLOCKED`, `PASS`, `WARNING` — no `WAIVED` value per D-06. `WARNING` is allowed only for cleanup that does not affect operator commands, acceptance gates, security, or evidence per D-05.

    Enumerate one gate row per required launch check across all domains: AWS passive infrastructure outputs, AWS active application deploy (`infra/scripts/deploy-aws-application.sh`), Snowflake native S3 pull stack (integration / stage / source-mirror tables / pipe / stream / procedures / tasks / grants), dbt compile/run/test, `EDGARTOOLS_GOLD_STATUS` + dynamic-table freshness, MDM Snowflake Postgres connectivity/migration/counts, `mdm sync-graph`, strict `mdm verify-graph` (SQL parity, Native App grants, compute pool, GRAPH_INFO, BFS, WCC), AWS MDM E2E (mdm_migrate / mdm_run / mdm_backfill_relationships / mdm_sync_graph / mdm_verify_graph / mdm_counts), dashboard inspection views, and the secret-safety scrub gate. Each gate row names the command or evidence source.

    Add these BLOCKED rows verbatim in intent per PATTERNS lines 333-337: (1) Production AWS application manifest `infra/aws-prod-application.json` — Owner = AWS operator; Required Fix = live discovery or successful prod deploy; Required Rerun Proof = non-secret summary of file presence + state machine names + image refs per D-15/D-17 (this file does NOT exist in the repo today). (2) Dashboard README `NEO4J_*` cleanup (neo4j-snowflake Phase 4 `04-03-PLAN.md` closeout) — Owner = dashboard reviewer / release owner, per D-02. (3) Stale `edgar-identity` secret ARN mitigation — Owner = AWS operator; Required Fix = runbook requires `--edgar-identity-secret-arn` with a freshly looked-up ARN before every deploy, per D-03. (4) ECR cleanup deleting an in-flight image digest — Owner = AWS operator; Required Fix = runbook requires re-resolving digests immediately before deploy after any cleanup step, per D-03.

    Use only the owner-role vocabulary from D-29: AWS operator, Snowflake operator, MDM operator, dashboard reviewer, release owner.

    Add a "Blocker classification rules" section stating D-01 through D-08 as operating rules: strict gate definition (D-01), upstream-closeout-blocks-launch (D-02), hazards-with-workarounds-are-blockers-until-runbook-enforced (D-03), missing-artifacts-are-blockers (D-04), warning-only allowance (D-05), no-waivers (D-06), secret-safety failures are hard blockers (D-07), a blocker is fixed only after rerun + non-secret pass summary (D-08).

    Add a "Dev vs prod distinction" note per D-18: every row referencing the dev hosted-graph proof from `03-LIVE-DEV-RUN.md` is marked "dev precedent only — prod proof required separately" using the environment-label header convention. Link each matrix Required-Rerun-Proof or evidence-source cell to the relative paths `evidence/aws.md`, `evidence/snowflake.md`, `evidence/mdm-hosted-graph.md`, `evidence/dashboard-security.md` (created by plans 01-02 and 01-03) per D-11.

    Add a closing "Requirement traceability" subsection mapping LIVE-01, SEC-01, ISO-01, ISO-02 to the matrix sections that satisfy them, using a `| Requirement | Satisfied by | Status |` column shape. ISO-01 row asserts all phase artifacts live under `.planning/workstreams/go-live/`; ISO-02 row asserts no non-AWS deployment paths appear in the matrix.

    Do not paste secret values, DSNs, tokens, Terraform state, or full generated JSON anywhere.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && test -f .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && grep -Fq '| Gate | Owner/Source | Required Fix | Required Rerun Proof | Status |' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && grep -q 'aws-prod-application.json' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && grep -Eq 'BLOCKED' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && echo MATRIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `01-LAUNCH-GATE-MATRIX.md` exists in the phase directory.
    - Contains the exact header row `| Gate | Owner/Source | Required Fix | Required Rerun Proof | Status |`.
    - The four named BLOCKED rows are present: `aws-prod-application.json`, dashboard `NEO4J_*` cleanup, stale `edgar-identity` ARN, ECR cleanup in-flight digest. Each has a non-empty Owner/Source, Required Fix, and Required Rerun Proof cell.
    - The only Status values used in the matrix are `BLOCKED`, `PASS`, `WARNING`. Verify no `WAIVED` token appears in matrix rows: `grep -v '^#' 01-LAUNCH-GATE-MATRIX.md | grep -c 'WAIVED'` returns 0.
    - Gate rows exist for each domain: AWS, Snowflake native pull, dbt, gold freshness, MDM, sync-graph, verify-graph, AWS MDM E2E, dashboard, secret-safety scrub.
    - Owner cells use only the five D-29 roles (AWS operator, Snowflake operator, MDM operator, dashboard reviewer, release owner).
    - A "Blocker classification rules" section references D-01 through D-08.
    - A "Requirement traceability" subsection maps LIVE-01, SEC-01, ISO-01, ISO-02 to satisfying sections.
  </acceptance_criteria>
  <done>The gate matrix lists every launch domain check with command/evidence source and BLOCKED/PASS/WARNING status, includes the four mandatory BLOCKED rows with full owner/fix/rerun-proof cells, states the D-01..D-08 classification rules, and traces all four phase requirement IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Add secret-safety rules and required production identifiers checklist</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md</files>
  <read_first>
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md (the file Task 1 created — append to it)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md (D-07, D-09, D-10, D-13, D-15 evidence/secret rules; D-16 through D-24 source-of-truth, identifiers, preflight/spend rules)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md (lines 299-348 shared secret-safety + BLOCKED patterns)
    - infra/scripts/deploy-aws-application.sh (image-ref inputs, MDM flags, secret ARN resolution, app summary output)
    - infra/scripts/run-aws-mdm-e2e.sh (Snowflake connection/database inputs, Native App compute-pool selector, --status-only / --skip-preflight semantics)
    - infra/scripts/deploy-snowflake-stack.sh (Snowflake connection/database/dbt inputs)
  </read_first>
  <action>
    Append two sections to `01-LAUNCH-GATE-MATRIX.md`.

    Section "Secret-safety rules" (satisfies SEC-01, D-07): state that no DSNs, passwords, tokens, Terraform state, raw connector traces/exceptions, or sensitive generated deployment values may appear in any evidence file or runbook. State the D-10 evidence-entry rule (exact command, environment label, pass/fail, key counts or statuses, sanitized evidence links; full logs not pasted). State the D-15 generated-JSON rule (summarize `infra/aws-*-application.json` as file presence + top-level keys + state-machine name list + image-ref format only; never paste the JSON body). State the D-13 rule (evidence files contain only commands actually run or verified; planned-but-blocked commands belong as BLOCKED matrix rows, not evidence entries). State the D-14 rule (dashboard screenshots optional if secret-safe; text UAT notes sufficient). State the secret-safe loading convention: secrets are loaded via `aws secretsmanager get-secret-value` into env vars without ever printing the value. State the D-20/D-21 spend boundary: local/static readiness checks, production identifier validation, secret-safety scan, live discovery, and strict hosted-graph preflight must pass before any paid or state-changing execution; only pure read-only status/metadata checks (e.g. `--status-only`) are allowed before all gates are green, and only if they start no workloads and reveal no secret values. State the D-23 rule: `--skip-preflight` debug runs are non-acceptance and cannot satisfy go-live gates. State the D-24 rule: bounded production runs require explicit limits, target scope, and stop conditions before execution.

    Section "Required production identifiers (input contract for Phase 2)" (satisfies D-19, LIVE-01): a checklist of identifiers operators MUST supply before execution planning — production AWS profile/account; production Snowflake connection and database; deploy image references (warehouse and MDM, digest form); generated app summary path (`infra/aws-prod-application.json`); MDM secret names (Secrets Manager secret IDs, names only, never values); and Native App app name + compute-pool selector. Mark each as a checkbox item; note (per D-17) that the prod app summary is currently a BLOCKED gate until live discovery or a successful prod deploy supplies it.

    Do not paste secret values or full JSON.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && grep -qi 'Secret-safety rules' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && grep -qi 'Required production identifiers' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && grep -q 'compute-pool' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && echo SECTIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - A "Secret-safety rules" section forbids DSNs, passwords, tokens, Terraform state, raw connector traces, and sensitive generated deployment values, and states the D-10, D-13, D-14, D-15 evidence rules.
    - The spend-boundary text references read-only-before-green (D-21) and the `--skip-preflight` non-acceptance rule (D-23).
    - A "Required production identifiers" section lists all seven D-19 items: AWS profile/account, Snowflake connection, Snowflake database, deploy image refs, app summary path, MDM secret names, Native App app + compute-pool selector.
    - The section notes the prod app summary is BLOCKED per D-17.
    - No actual secret value, DSN string, or full JSON body appears: `grep -v '^#' 01-LAUNCH-GATE-MATRIX.md | grep -Eic 'postgres(ql)?://[^ ]+:[^ ]+@'` returns 0 (no DSN with embedded credentials).
  </acceptance_criteria>
  <done>The matrix file contains a secret-safety rules section enforcing D-07/D-10/D-13/D-14/D-15 and the D-20/D-21/D-23/D-24 spend boundary, plus a required-production-identifiers checklist covering all seven D-19 items with the app-summary BLOCKED note.</done>
</task>

<task type="auto">
  <name>Task 3: Add the per-layer data-issue triage table</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md</files>
  <read_first>
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md (the file Tasks 1-2 created — append to it)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md (D-25 through D-29 data-issue routing rules)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md (lines 352-356 No-Analog note: triage table structure must be designed fresh from the D-25 column set)
  </read_first>
  <action>
    Append a "Data-issue triage table" section per D-25 with exactly these columns: `| Layer | Symptom | Likely source | Evidence to check | Owner | Blocker status | Next action |`. Add one row per layer covering all eight D-25 layers: ingestion, bronze/silver, MDM, hosted graph, dbt/gold, Native App, dashboard, permissions. Each Owner cell uses only the D-29 role vocabulary (AWS operator, Snowflake operator, MDM operator, dashboard reviewer, release owner). Each Blocker-status cell uses `BLOCKED` / `PASS` / `WARNING` consistent with the gate matrix.

    Add a short preamble stating the D-26/D-27/D-28 routing policy: operators start with CLI verification and dbt tests, then inspect the dashboard; the dashboard is inspection only after CLI/dbt/Native App gates and explains issues but does not define acceptance (D-27); failed CLI/dbt/Native App gates are launch-blocking, and dashboard-only warnings block only when they point to a failed gate or a launch-impacting data gap (D-28).
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && grep -Fq '| Layer | Symptom | Likely source | Evidence to check | Owner | Blocker status | Next action |' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && grep -qi 'Native App' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && echo TRIAGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - A "Data-issue triage table" section exists with the exact header `| Layer | Symptom | Likely source | Evidence to check | Owner | Blocker status | Next action |`.
    - The table has one row for each of the eight layers: ingestion, bronze/silver, MDM, hosted graph, dbt/gold, Native App, dashboard, permissions.
    - Each row's Owner cell uses only the five D-29 roles.
    - A preamble states the CLI/dbt-first then dashboard routing policy (D-26) and that the dashboard does not define acceptance (D-27).
  </acceptance_criteria>
  <done>The matrix file includes a per-layer triage table covering all eight D-25 layers with the full D-25 column set, role-based owners (D-29), and a routing-policy preamble (D-26/D-27/D-28).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator memory / live env → evidence markdown | Operators transcribe live AWS/Snowflake/MDM command output into documentation; secret values can leak across this boundary if pasted verbatim. |
| generated artifacts (`infra/aws-*-application.json`, Terraform state) → committed docs | Summarizing generated deployment values risks copying secret fields (ARNs with embedded tokens, connection strings) into git history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `01-LAUNCH-GATE-MATRIX.md` evidence-source cells | mitigate | Secret-safety rules section (Task 2) forbids DSNs/tokens/passwords/Terraform state; acceptance criteria grep for embedded-credential DSN patterns (values, not type-names) and assert 0 matches. |
| T-01-02 | Information Disclosure | required-identifiers checklist | mitigate | D-19 checklist names secret IDs/connection names only, never secret values; action explicitly forbids pasting values. |
| T-01-03 | Tampering | matrix status integrity | accept | Single-author doc in an isolated workstream branch; no automated status mutation. Low risk: launch decisions require human review per D-06 no-waivers rule. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — documentation-only artifacts, no dependency changes. No supply-chain surface. |
</threat_model>

<verification>
- `01-LAUNCH-GATE-MATRIX.md` exists and contains the gate matrix header, secret-safety rules, required-identifiers checklist, and triage table.
- No embedded-credential DSN or full JSON body appears in the file.
- All four phase requirement IDs (LIVE-01, SEC-01, ISO-01, ISO-02) are traced.
</verification>

<success_criteria>
- Single launch gate matrix lists every launch-domain check with command/evidence source and BLOCKED/PASS/WARNING status (ROADMAP success criterion 1).
- The four mandatory BLOCKED rows are present with full owner/fix/rerun-proof cells (success criterion 4).
- Secret-safety rules explicitly forbid DSNs, passwords, tokens, Terraform state, raw connector traces, and sensitive generated deployment values (success criterion 3).
- Required production identifiers checklist (D-19) and per-layer triage table (D-25) present.
- All artifacts under `.planning/workstreams/go-live/` (ISO-01); no non-AWS deployment paths referenced (ISO-02).
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-01-SUMMARY.md` when done.
</output>
