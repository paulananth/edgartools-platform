---
phase: 01-production-readiness-inventory-and-launch-gate-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md
autonomous: true
requirements: [LIVE-01, SEC-01, ISO-01, ISO-02]

must_haves:
  truths:
    - "Operator can read an AWS evidence file that reconciles existing dev AWS/Step Functions proof against production launch needs, with every dev row labeled dev-precedent-only and prod proof marked required separately."
    - "Operator can read a Snowflake evidence file that reconciles existing dev native-pull / dbt / gold proof against production launch needs, with dev rows labeled dev-precedent-only."
    - "Both evidence files contain only commands actually run or verified during Phase 1; every not-yet-runnable production step points to a BLOCKED row in 01-LAUNCH-GATE-MATRIX.md instead of appearing as an evidence entry."
    - "Both evidence files contain no DSNs, passwords, tokens, Terraform state, raw connector traces, or full generated JSON bodies."
  artifacts:
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md"
      provides: "AWS production-readiness evidence template: secret-safe header, command/result/summary blocks for read-only AWS checks actually run, dev-precedent reconciliation from 03-LIVE-DEV-RUN.md, and pointers to matrix BLOCKED rows for unrunnable prod steps."
      contains: "dev precedent only — prod proof required separately"
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md"
      provides: "Snowflake production-readiness evidence template: secret-safe header, command/result/summary blocks for read-only Snowflake/dbt checks actually run, dev-precedent reconciliation, gold-status/freshness summary shape, and pointers to matrix BLOCKED rows."
      contains: "dev precedent only — prod proof required separately"
  key_links:
    - from: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md"
      to: "evidence/aws.md, evidence/snowflake.md"
      via: "relative-path links from matrix Required-Rerun-Proof / evidence-source cells (01-01 links TO these files)"
      pattern: "evidence/(aws|snowflake)\\.md"
    - from: "evidence/aws.md, evidence/snowflake.md"
      to: "01-LAUNCH-GATE-MATRIX.md BLOCKED rows"
      via: "back-pointer text for every not-yet-runnable prod step (D-13)"
      pattern: "01-LAUNCH-GATE-MATRIX\\.md"
---

<objective>
Create the AWS and Snowflake production-readiness evidence files for go-live Phase 1. These are
evidence TEMPLATE + dev-reconciliation documents, not filled-in production logs: they capture the
secret-safe command/result/summary structure, record only the read-only checks actually run during
Phase 1, reconcile existing dev proof against production launch needs, and point every not-yet-runnable
production step to a BLOCKED row in the gate matrix.

Purpose: ROADMAP success criterion 2 requires existing dev proof to be reconciled against production
launch needs (Snowflake native pull, dbt gold, gold freshness, AWS deploy/Step Functions). These two
files are the AWS and Snowflake domain halves of that reconciliation, linked from the gate matrix
(01-LAUNCH-GATE-MATRIX.md) created by plan 01-01.

Output: `evidence/aws.md` and `evidence/snowflake.md` in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/PROJECT.md
@.planning/workstreams/go-live/ROADMAP.md
@.planning/workstreams/go-live/REQUIREMENTS.md
@.planning/workstreams/go-live/STATE.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-01-PLAN.md
@.planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-LIVE-DEV-RUN.md
</context>

<critical_constraint>
These two files are NOT filled-in production evidence. During Phase 1 almost nothing runs against
production. DO NOT fabricate prod command results, fake `SUCCEEDED` rows, or invented counts. Each
file must contain:
1. A secret-safe header + disclaimer sentence (D-07).
2. The command/result/summary STRUCTURE, ready to fill when prod runs actually happen.
3. Dev precedent reconciled from `03-LIVE-DEV-RUN.md`, EACH row explicitly labeled
   "dev precedent only — prod proof required separately" (D-18). This reconciliation is the deliverable.
4. ONLY read-only Phase 1 checks that were actually run (D-13/D-21) — e.g. `ls infra/aws-prod-application.json`
   (which is absent today), `dbt compile`, `--status-only`. Use the real result of the check actually run.
5. A back-pointer to a `01-LAUNCH-GATE-MATRIX.md` BLOCKED row for every not-yet-runnable prod step.
   Not-yet-runnable steps do NOT appear as evidence entries (D-13).
</critical_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Create evidence/aws.md (AWS production-readiness evidence template)</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md</files>
  <read_first>
    - .planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-LIVE-DEV-RUN.md (header lines 1-10; command/result/summary shape lines 90-118; Step Functions status table lines 99-109, 169-178 — this is the exact analog to mirror)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md (D-09, D-10, D-13, D-15, D-16, D-17, D-18, D-21 — evidence-format, source-of-truth, dev-vs-prod, read-only rules)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md (lines 97-148 evidence/aws.md patterns; lines 299-325 shared header + command/result blocks)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-01-PLAN.md (frontmatter key_links + Task 1 action — so this file matches what the matrix links to and the BLOCKED rows it must point back to)
    - infra/scripts/deploy-aws-application.sh (image-ref inputs, MDM flags, secret ARN resolution, generated app summary output, known cleanup hazards — command surface to reference, not run)
    - infra/scripts/run-aws-mdm-e2e.sh (--status-only read-only semantics; --skip-preflight debug-only semantics)
  </read_first>
  <action>
    Create `evidence/aws.md` (the executor must create the `evidence/` subdirectory if absent). Start with
    a header block following the `03-LIVE-DEV-RUN.md` lines 1-10 convention: title `# AWS Evidence — Phase 1 Production Readiness`,
    then `Date:`, `Environment:` (state that `prod` is required per D-18 and `dev` rows below are precedent only),
    `AWS profile:` / `AWS account:` label placeholders, and the secret-safety disclaimer sentence verbatim in
    intent: "This artifact captures non-secret evidence only. It omits passwords, tokens, DSNs, full task logs,
    secret ARNs, and raw Native App job logs." (D-07).

    Add a "Source-of-truth note" stating D-16: live AWS discovery and command checks are authoritative for
    production readiness; manifests and docs are supporting evidence only.

    Add a "Phase 1 read-only checks actually run" section. Include ONLY commands genuinely run during Phase 1
    using the `03-LIVE-DEV-RUN.md` command/result/summary block shape (fenced command -> `Result: <succeeded|failed>.`
    -> non-secret bullets/table). The executor MUST actually run these and record the real outcome:
      - `ls infra/aws-dev-application.json` and `ls infra/aws-prod-application.json` — record that the dev manifest
        exists and the prod manifest is ABSENT (do not paste the JSON body; per D-15 summarize presence only).
      - Optionally `aws stepfunctions list-state-machines` / `aws ecs list-task-definitions` ONLY if they run
        read-only without revealing secrets (D-21); if not run, omit them rather than fabricate. Never run
        `aws secretsmanager get-secret-value`.
    For each not-yet-runnable production step (production deploy via `deploy-aws-application.sh`, production
    `infra/aws-prod-application.json` summary, production AWS MDM E2E full run), DO NOT add an evidence entry —
    instead add a one-line back-pointer: "BLOCKED — see 01-LAUNCH-GATE-MATRIX.md row '<gate name>'" (D-13),
    referencing the four matrix BLOCKED rows established by 01-01 (prod app manifest per D-17, stale `edgar-identity`
    ARN, ECR cleanup in-flight digest, dashboard README cleanup as applicable).

    Add a "Dev precedent reconciliation" section. Summarize the dev AWS Step Functions proof from
    `03-LIVE-DEV-RUN.md` (the `--status-only` table: mdm_migrate / mdm_run / mdm_backfill_relationships /
    mdm_sync_graph / mdm_verify_graph / mdm_counts all SUCCEEDED in dev) using the Step Functions status table
    column shape `| Workflow | Latest status | Latest execution name |`. Label this block explicitly:
    "dev precedent only — prod proof required separately" (D-18). State what production proof is still required
    (a production-env run with production profile/account) and that it is tracked as a matrix BLOCKED/pending row.

    Add a "Generated-JSON summary rule" reminder per D-15: when the prod app summary exists, it will be
    summarized as file presence + top-level keys + state-machine name list + image-ref format (digest vs tag)
    only — never the JSON body.

    Do not paste any secret value, DSN, secret ARN, token, Terraform state, or full JSON anywhere.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && test -f .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md && grep -q 'dev precedent only — prod proof required separately' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md && grep -q '01-LAUNCH-GATE-MATRIX.md' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md && grep -q 'aws-prod-application.json' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md && echo AWS_EVIDENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `evidence/aws.md` exists in the phase `evidence/` directory.
    - Header includes Date, Environment (with prod-required/dev-precedent note), AWS profile/account labels, and the secret-safety disclaimer sentence (D-07).
    - A "Source-of-truth note" states D-16 (live discovery authoritative).
    - At least one read-only check actually run is recorded with the command/result/summary shape, including the `infra/aws-prod-application.json` absence (D-15/D-17), and no JSON body is pasted.
    - A "Dev precedent reconciliation" section summarizes the dev Step Functions proof and is labeled "dev precedent only — prod proof required separately" (D-18).
    - Every not-yet-runnable production step points to a `01-LAUNCH-GATE-MATRIX.md` BLOCKED row instead of being recorded as an evidence entry (D-13). Verify the matrix is referenced: file contains `01-LAUNCH-GATE-MATRIX.md`.
    - No embedded-credential DSN appears: `grep -v '^#' evidence/aws.md | grep -Eic 'postgres(ql)?://[^ ]+:[^ ]+@'` returns 0.
    - No `get-secret-value` output or secret ARN value is pasted (the command name may be referenced as a forbidden action).
  </acceptance_criteria>
  <done>evidence/aws.md exists as a secret-safe template that records the read-only AWS checks actually run in Phase 1, reconciles the dev Step Functions precedent labeled dev-only, and back-points every not-yet-runnable prod step to a matrix BLOCKED row — with no secrets or full JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Create evidence/snowflake.md (Snowflake production-readiness evidence template)</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md</files>
  <read_first>
    - .planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-LIVE-DEV-RUN.md (header + command/result/summary shape — same analog as Task 1)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-CONTEXT.md (D-09, D-10, D-13, D-15, D-16, D-18, D-21 — evidence rules; dbt/gold readiness context)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-PATTERNS.md (lines 151-181 evidence/snowflake.md patterns: dbt compile command shape, EDGARTOOLS_GOLD_STATUS, the EDGARTOOLS_DEV_DEPLOYER --full-refresh grant gap)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-01-PLAN.md (Task 1/2 actions — so the gold-freshness and grant-gap items here match the matrix BLOCKED rows 01-01 owns)
    - infra/scripts/deploy-snowflake-stack.sh (--env / --snow-connection / --run-validation / --run-dbt / --upload-dashboard inputs — command surface to reference, not run against prod)
    - TODOS.md (the EDGARTOOLS_DEV_DEPLOYER lacks direct SELECT on EDGARTOOLS_SOURCE grant gap blocking dynamic-table --full-refresh)
  </read_first>
  <action>
    Create `evidence/snowflake.md`. Start with the same `03-LIVE-DEV-RUN.md` header convention: title
    `# Snowflake Evidence — Phase 1 Production Readiness`, then `Date:`, `Environment:` (prod required per D-18,
    dev rows are precedent only), `Snowflake connection:` / `Snowflake database:` label placeholders, and the
    secret-safety disclaimer sentence (D-07).

    Add a "Source-of-truth note" stating D-16: live Snowflake command checks are authoritative; manifests/docs
    are supporting evidence.

    Add a "Phase 1 read-only checks actually run" section using the command/result/summary block shape, ONLY
    for non-state-changing checks genuinely run during Phase 1 (D-21). The executor MUST actually run what it
    records and report the real outcome. Candidate read-only checks (include only those actually run; omit any
    not run rather than fabricate):
      - `cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt compile` IF Snowflake
        credentials/connection are available in this environment; summarize as models-compiled count and whether
        the `EDGARTOOLS_GOLD_STATUS` model is present. If credentials are NOT available, do NOT fabricate a result —
        record it as a not-yet-runnable step pointing to a matrix row instead.
      - Static presence/structure checks that need no Snowflake connection (e.g. listing dbt model files under
        `infra/snowflake/dbt/edgartools_gold/models/gold/`).
    For not-yet-runnable production steps (production `deploy-snowflake-stack.sh --run-validation`, production
    `dbt run`/`dbt test`, production gold dynamic-table freshness, production `EDGARTOOLS_GOLD_STATUS`), do NOT
    add evidence entries — add a back-pointer "BLOCKED — see 01-LAUNCH-GATE-MATRIX.md row '<gate name>'" (D-13).

    Add a "Known grant-gap" note referencing the `EDGARTOOLS_DEV_DEPLOYER` lacks-direct-SELECT-on-EDGARTOOLS_SOURCE
    issue from TODOS.md: state that if the production deployer role has the analogous gap it blocks
    `dbt run --full-refresh` for any gold dynamic table, and that this is recorded as a matrix BLOCKED row
    (not as an evidence pass) per D-04/D-17 reasoning. Do not duplicate the full matrix row here — point to it.

    Add a "Dev precedent reconciliation" section: summarize the dev Snowflake/native-pull/dbt/gold precedent
    available (e.g. dev dbt models compile, gold tables built in EDGARTOOLS_DEV) using bullet summaries, and label
    the block "dev precedent only — prod proof required separately" (D-18). State the production proof still
    required (prod connection + prod database validation, prod dbt run/test, prod gold freshness).

    Add the D-15 generated-JSON reminder and the gold-status/freshness summary SHAPE (a `| Model/Table | Status |
    Last refresh |`-style table left ready to fill, not fabricated).

    Do not paste any DSN, password, token, Terraform state, or full generated JSON.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && test -f .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md && grep -q 'dev precedent only — prod proof required separately' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md && grep -q '01-LAUNCH-GATE-MATRIX.md' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md && grep -qi 'EDGARTOOLS_GOLD_STATUS' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md && echo SNOW_EVIDENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `evidence/snowflake.md` exists in the phase `evidence/` directory.
    - Header includes Date, Environment (prod-required/dev-precedent note), Snowflake connection/database labels, and the secret-safety disclaimer sentence (D-07).
    - A "Source-of-truth note" states D-16.
    - The "Phase 1 read-only checks actually run" section records only non-state-changing checks genuinely run (D-21), with real outcomes — no fabricated dbt run/test results.
    - A "Known grant-gap" note references the EDGARTOOLS_DEV_DEPLOYER direct-SELECT gap and points to a matrix BLOCKED row rather than recording a pass.
    - A "Dev precedent reconciliation" section is labeled "dev precedent only — prod proof required separately" (D-18).
    - Every not-yet-runnable production step points to a `01-LAUNCH-GATE-MATRIX.md` BLOCKED row (D-13); the matrix is referenced.
    - `EDGARTOOLS_GOLD_STATUS` and a gold-freshness summary shape are present (left ready to fill, not fabricated).
    - No embedded-credential DSN appears: `grep -v '^#' evidence/snowflake.md | grep -Eic 'postgres(ql)?://[^ ]+:[^ ]+@'` returns 0.
  </acceptance_criteria>
  <done>evidence/snowflake.md exists as a secret-safe template recording only the read-only Snowflake/dbt checks actually run, reconciling dev native-pull/dbt/gold precedent labeled dev-only, noting the deployer grant gap as a matrix pointer, and back-pointing every not-yet-runnable prod step to a matrix BLOCKED row — with no secrets or full JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator memory / live AWS+Snowflake env → evidence markdown | Operators transcribe live command output into `evidence/aws.md` and `evidence/snowflake.md`; secret values (DSNs, tokens, secret ARNs) can leak across this boundary if pasted verbatim. |
| generated artifacts (`infra/aws-*-application.json`, dbt artifacts, Terraform state) → committed evidence | Summarizing generated deployment/Snowflake values risks copying secret fields (connection strings, ARNs with embedded tokens) into git history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `evidence/aws.md` command/result blocks | mitigate | Secret-safety disclaimer header (D-07); action forbids `get-secret-value` output and secret ARNs; acceptance grep asserts 0 embedded-credential DSN matches; only read-only checks recorded (D-21). |
| T-02-02 | Information Disclosure | `evidence/snowflake.md` dbt/SQL output | mitigate | Disclaimer header (D-07); D-15 summarize-only rule for generated JSON; no DSN/password/token pasted; acceptance grep asserts 0 embedded-credential DSN matches. |
| T-02-03 | Information Disclosure | `infra/aws-*-application.json` summary | mitigate | D-15 rule enforced in action — file presence + top-level keys + state-machine names + image-ref format only; never paste JSON body; acceptance forbids JSON body. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. `dbt compile` runs via the already-locked `uv run --with dbt-snowflake` toolchain (no new dependency added to the repo). Documentation-only outputs; no supply-chain surface introduced. |
</threat_model>

<verification>
- `evidence/aws.md` and `evidence/snowflake.md` exist in the phase `evidence/` directory.
- Each has the secret-safe header + disclaimer (D-07), a dev-precedent-reconciliation block labeled "dev precedent only — prod proof required separately" (D-18), and only read-only checks actually run (D-13/D-21).
- Each back-points every not-yet-runnable prod step to `01-LAUNCH-GATE-MATRIX.md` (D-13).
- No embedded-credential DSN, secret ARN value, or full JSON body appears in either file.
</verification>

<success_criteria>
- Existing dev AWS + Snowflake proof is reconciled against production launch needs and labeled dev-precedent-only (ROADMAP success criterion 2).
- Evidence files contain only commands actually run/verified; planned-but-blocked prod commands are matrix BLOCKED-row pointers, not evidence entries (D-13).
- Secret-safety rules honored: no DSNs, passwords, tokens, Terraform state, raw connector traces, or full generated JSON (SEC-01, D-07/D-15).
- Both files live under `.planning/workstreams/go-live/` (ISO-01); no non-AWS deployment paths referenced (ISO-02).
- LIVE-01: the AWS/Snowflake read-only preflight checks (manifest presence, dbt compile, connections/secret containers without printing values) are the documented preflight evidence shape.
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-02-SUMMARY.md` when done.
</output>
