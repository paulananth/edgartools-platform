---
phase: 04-operator-dashboard-and-data-issue-triage
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - .planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/runbook/data-issue-triage.md
autonomous: true
requirements: [DASH-02]

must_haves:
  truths:
    - "DASH-02: An operator has a single triage guide that maps data-issue symptoms to the likely layer across all 8 layers: ingestion, bronze/silver, MDM, hosted graph, dbt/gold, Native App, dashboard, permissions."
    - "DASH-02: Each layer entry includes 1-2 non-destructive, read-only diagnostic CLI commands so the operator can follow the guide start-to-finish without looking up other docs (D-06)."
    - "DASH-02: Each layer entry names an owner and an escalation/next-action path."
    - "DASH-02: The guide cross-references (does not duplicate) the launch gate matrix 'Data-Issue Triage Table' (rows 90-100) and routes operators to CLI/dbt acceptance gates first, dashboard inspection second."
    - "DASH-03: The guide contains no secrets, DSNs, passwords, tokens, raw connector exceptions, or stack traces, and every diagnostic command is read-only (no put-secret-value, no get-secret-value SecretString, no mutation commands) (D-14)."
  artifacts:
    - path: .planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/runbook/data-issue-triage.md
      provides: "Operator data-issue triage guide covering all 8 layers with read-only diagnostic commands, owners, and escalation paths."
      contains: "verify-graph"
      min_lines: 80
  key_links:
    - from: ".planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/runbook/data-issue-triage.md"
      to: "01-LAUNCH-GATE-MATRIX.md Data-Issue Triage Table (rows 90-100)"
      via: "cross-phase relative markdown link"
      pattern: "01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md"
---

<objective>
Create a new operator-facing data-issue triage guide at
`.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/runbook/data-issue-triage.md`
(per D-05) that covers all 8 DASH-02 layers — ingestion, bronze/silver, MDM,
hosted graph, dbt/gold, Native App, dashboard, permissions — with symptom
description, likely source, 1-2 non-destructive read-only diagnostic CLI
commands per layer (D-06/D-07), owner, and escalation/next-action path.

Purpose: when an operator sees a data issue during launch, this guide is the
first inspection path. It extends the existing launch gate matrix "Data-Issue
Triage Table" (rows 90-100) by adding runnable read-only diagnostics per layer,
rather than duplicating it. It reinforces the routing policy: CLI / dbt /
Native App gates are the acceptance gates; the dashboard is inspection only.

Output: a new `runbook/data-issue-triage.md`. Documentation-only plan — no live
commands are run. Every diagnostic command must be read-only and non-destructive
(D-14): no `put-secret-value`, no `get-secret-value --query SecretString`, no
sync/repair/migrate/load/derive/grant/deploy commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/04-CONTEXT.md

# The existing triage table this guide EXTENDS (rows 90-100) — read it, link to
# it, do NOT duplicate it. Also read the per-layer owners/next-actions there:
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md

# Phase 3 runbook format to mirror (header disclaimer, numbered sections,
# References section):
@.planning/workstreams/go-live/phases/03-mdm-hosted-graph-e2e-acceptance/runbook/mdm-secrets.md

<interfaces>
<!-- Read-only diagnostic command vocabulary already established in this repo. -->
<!-- Use ONLY read-only / non-destructive commands. Suggested per layer: -->
<!--   ingestion: aws stepfunctions list-executions / describe-execution --query -->
<!--     status (read-only); bronze S3 presence via `aws s3 ls s3://.../warehouse/bronze/...` -->
<!--   bronze/silver: `aws s3 ls` on the warehouse storage root; silver shard listing (read-only) -->
<!--   MDM: `edgar-warehouse mdm counts` (read-only entity/relationship counts) -->
<!--   hosted graph: `edgar-warehouse mdm verify-graph` (read-only parity + Native App checks) -->
<!--   dbt/gold: `SELECT * FROM <DB>.EDGARTOOLS_GOLD.EDGARTOOLS_GOLD_STATUS LIMIT 10;` -->
<!--     via `snow sql --connection <conn> -q ...` (read-only); dynamic-table freshness query -->
<!--   Native App: the Native App check section of strict `edgar-warehouse mdm verify-graph` -->
<!--     (GRAPH_INFO / BFS / WCC); `snow sql ... SHOW TASKS LIKE 'SNOWFLAKE_RUN_MANIFEST_TASK'` (read-only) -->
<!--   dashboard: launch read-only dashboard (uv run --extra dashboard --extra mdm-runtime -->
<!--     streamlit run examples/mdm_graph_dashboard/streamlit_app.py) and inspect views; -->
<!--     `uv run pytest tests/mdm/test_dashboard_readonly.py -q` (read-only helper coverage) -->
<!--   permissions: `aws secretsmanager describe-secret --query {Name,ARN,LastChangedDate, -->
<!--     VersionIdsToStages}` (METADATA only, never get-secret-value SecretString); -->
<!--     `snow sql ... SHOW GRANTS TO ROLE <role>` (read-only) -->
<!-- FORBIDDEN in this guide: put-secret-value, get-secret-value --query SecretString, -->
<!-- mdm sync-graph / migrate / run / derive / load, dbt run/build, terraform apply, -->
<!-- and any aws/snow mutation verb. Owners/next-actions mirror rows 90-100. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author runbook/data-issue-triage.md covering all 8 layers with read-only diagnostics</name>
  <files>.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/runbook/data-issue-triage.md</files>
  <read_first>
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
    - .planning/workstreams/go-live/phases/03-mdm-hosted-graph-e2e-acceptance/runbook/mdm-secrets.md
    - .planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/04-CONTEXT.md
  </read_first>
  <action>
Create the new file
`.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/runbook/data-issue-triage.md`,
mirroring the Phase 3 `runbook/mdm-secrets.md` format (non-secret header
disclaimer, numbered sections, closing References section).

Required structure, in order:

1. Header disclaimer: state this guide is the operator first-inspection path for
   launch data issues (DASH-02); all diagnostic commands are read-only and
   non-destructive; no secrets, DSNs, passwords, tokens, raw connector
   exceptions, or stack traces appear here (D-12/D-14).

2. Routing policy (short): operators run the relevant CLI / dbt / Native App
   acceptance gate FIRST and capture pass/fail evidence, THEN use the dashboard
   to inspect/explain (Overview, MDM Overview, Neo4j Overview, Mismatch
   Diagnostics). The dashboard is inspection only; it does not define
   acceptance. Cross-reference the launch gate matrix "Data-Issue Triage Table"
   (rows 90-100) with a cross-phase relative link
   `../01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md`
   and state this guide EXTENDS that table with runnable read-only diagnostics —
   it does not duplicate the owner/blocker columns.

3. One `### ` section per layer, for ALL 8 layers in this order: ingestion,
   bronze/silver, MDM, hosted graph, dbt/gold, Native App, dashboard,
   permissions. Each section MUST contain: (a) Symptom — one line matching the
   rows-90-100 symptom; (b) Likely source — one line; (c) Diagnostic — 1-2
   read-only CLI commands in a fenced bash block (per D-06); (d) Owner — mirror
   the rows-90-100 owner; (e) Escalation — the rows-90-100 "Next action" plus
   where to record evidence.

   Layer treatment (launch-critical layers MDM, hosted graph, dbt/gold, Native
   App get priority/fuller treatment; ingestion, dashboard, permissions may be
   briefer per D-07 but are still required):
   - ingestion: AWS Step Functions read-only status checks
     (`aws stepfunctions list-executions ... --query` and/or
     `describe-execution ... --query status`) and bronze S3 presence via
     `aws s3 ls s3://.../warehouse/bronze/...`. Owner: AWS operator.
   - bronze/silver: `aws s3 ls` on the warehouse storage root / silver shard
     listing (read-only). Owner: AWS operator.
   - MDM: `edgar-warehouse mdm counts` (read-only counts). Owner: MDM operator.
   - hosted graph: `edgar-warehouse mdm verify-graph` (read-only parity +
     Native App checks). Owner: MDM operator.
   - dbt/gold: `SELECT * FROM <DB>.EDGARTOOLS_GOLD.EDGARTOOLS_GOLD_STATUS LIMIT
     10;` via `snow sql --connection <conn> -q ...` (read-only freshness/status).
     Owner: Snowflake operator.
   - Native App: the Native App check section of strict
     `edgar-warehouse mdm verify-graph` (GRAPH_INFO / BFS / WCC) and
     `snow sql --connection <conn> -q "SHOW TASKS LIKE 'SNOWFLAKE_RUN_MANIFEST_TASK'"`
     (read-only). Owner: Snowflake operator.
   - dashboard: launch the read-only dashboard
     (`uv run --extra dashboard --extra mdm-runtime streamlit run examples/mdm_graph_dashboard/streamlit_app.py`)
     and inspect the four views; `uv run pytest tests/mdm/test_dashboard_readonly.py -q`
     for read-only helper coverage. Owner: dashboard reviewer. Note this layer
     is WARNING (not BLOCKED) unless it points to a failed acceptance gate.
   - permissions: `aws secretsmanager describe-secret --secret-id <id> --query
     '{Name:Name,ARN:ARN,LastChangedDate:LastChangedDate,VersionIdsToStages:VersionIdsToStages}'`
     (METADATA only — NEVER `get-secret-value --query SecretString`) and
     `snow sql --connection <conn> -q "SHOW GRANTS TO ROLE <role>"` (read-only).
     Owner: Snowflake operator.

   Use `<DB>`, `<conn>`, `<role>`, and `<id>` as placeholder tokens — never real
   prod identifiers, ARNs, or DSNs.

4. Secret-safety note: restate that no mutation command, no
   `put-secret-value`, and no `get-secret-value --query SecretString` appears in
   this guide; only `describe-secret` metadata is recordable; bounded samples
   are diagnostics, not exhaustive exports.

5. References section: link the launch gate matrix triage table
   (`../01-.../01-LAUNCH-GATE-MATRIX.md`), the Phase 3 MDM secrets runbook
   (`../03-mdm-hosted-graph-e2e-acceptance/runbook/mdm-secrets.md`), the
   dashboard README (`examples/mdm_graph_dashboard/README.md`), and
   `infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql`.

Do NOT include any of: `put-secret-value`, `get-secret-value --query
SecretString`, `mdm sync-graph`, `mdm migrate`, `mdm run`, `mdm derive`,
`mdm load`, `dbt run`, `dbt build`, `terraform apply`, or any other mutation
verb as a diagnostic command (D-14). The string `get-secret-value` may appear
only inside prohibition prose, never as a runnable diagnostic.
  </action>
  <verify>
    <automated>f=.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/runbook/data-issue-triage.md; test -f "$f" && for L in ingestion "bronze/silver" MDM "hosted graph" "dbt/gold" "Native App" dashboard permissions; do grep -qi "$L" "$f" || { echo "MISSING LAYER $L"; exit 1; }; done && grep -q 'mdm counts' "$f" && grep -q 'verify-graph' "$f" && grep -q 'EDGARTOOLS_GOLD_STATUS' "$f" && grep -q 'describe-secret' "$f" && grep -q '01-LAUNCH-GATE-MATRIX.md' "$f" && echo PASS</automated>
    <automated>f=.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/runbook/data-issue-triage.md; test "$(grep -vE '^[[:space:]]*(>|#|-|\*)' "$f" | grep -cE 'put-secret-value|get-secret-value[[:space:]]+--query[[:space:]]+SecretString|mdm (sync-graph|migrate|run|derive|load)|dbt (run|build)|terraform apply')" -eq 0 && echo NO_MUTATIONS</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact path under the phase `runbook/` directory.
    - All 8 layer tokens (ingestion, bronze/silver, MDM, hosted graph, dbt/gold, Native App, dashboard, permissions) appear as section content (grep gate passes).
    - Each layer section names an owner and an escalation/next-action.
    - The read-only diagnostic commands `edgar-warehouse mdm counts`, `edgar-warehouse mdm verify-graph`, an `EDGARTOOLS_GOLD_STATUS` SELECT, and `describe-secret` metadata query are present.
    - The guide links the launch gate matrix triage table via the cross-phase path `../01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md` and states it extends (not duplicates) it.
    - No mutation command appears outside prohibition prose: the second grep gate (excluding comment/prose lines) returns 0 for `put-secret-value`, `get-secret-value --query SecretString`, `mdm sync-graph/migrate/run/derive/load`, `dbt run/build`, `terraform apply`.
    - No real prod identifiers, ARNs, DSNs, passwords, or tokens appear — only `<DB>`, `<conn>`, `<role>`, `<id>` placeholders.
  </acceptance_criteria>
  <done>
`runbook/data-issue-triage.md` exists covering all 8 layers, each with symptom,
likely source, 1-2 read-only diagnostic CLI commands, owner, and escalation
path. It cross-references and extends the launch gate matrix triage table (rows
90-100) without duplicating it, routes operators to CLI/dbt acceptance gates
first and dashboard inspection second, and contains no mutation commands,
secrets, DSNs, ARNs, or stack traces.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → committed runbook | Diagnostic commands run by the operator resolve real identifiers/secrets at runtime; the guide commits only placeholder tokens and read-only command shapes. |
| AWS Secrets Manager → triage guide | Only `describe-secret` metadata is a sanctioned diagnostic; `get-secret-value --query SecretString` is forbidden as a runnable command. |
| diagnostic command → system state | Every documented command must be read-only; the guide must not enable sync/repair/migrate/load/grant/deploy from the triage path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Diagnostic command blocks | mitigate | All identifiers are `<DB>`/`<conn>`/`<role>`/`<id>` placeholders; no ARNs, DSNs, passwords, or tokens. Permissions-layer secret diagnostic is restricted to `describe-secret` metadata; `get-secret-value --query SecretString` appears only in prohibition prose. Verified by the second grep gate (prose lines excluded). |
| T-04-02-02 | Tampering | Triage diagnostic path | mitigate | The guide documents only read-only/non-destructive commands; `mdm sync-graph/migrate/run/derive/load`, `dbt run/build`, and `terraform apply` are forbidden as diagnostics and enforced by the mutation grep gate. |
| T-04-02-03 | Repudiation | Layer ownership / escalation | mitigate | Each layer names an owner and escalation/next-action mirroring launch-gate-matrix rows 90-100, so a triaged issue has a clear accountable owner and evidence-capture target. |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — documentation-only plan. No supply-chain checkpoint required. |
</threat_model>

<verification>
- File exists with all 8 layer sections (first grep gate passes).
- Read-only diagnostics present per layer (`mdm counts`, `verify-graph`,
  `EDGARTOOLS_GOLD_STATUS`, `describe-secret`).
- No mutation command outside prohibition prose (second grep gate returns 0).
- Cross-phase link to launch gate matrix triage table present; guide states it
  extends rather than duplicates that table.
- Secret-safety scrub: no DSNs, ARNs, passwords, tokens, or stack traces.
</verification>

<success_criteria>
- `runbook/data-issue-triage.md` covers all 8 DASH-02 layers with 1-2 read-only
  diagnostic commands, an owner, and an escalation path per layer.
- The guide extends (links, does not duplicate) the launch gate matrix triage
  table and reinforces CLI-first / dashboard-inspection-second routing.
- No secrets, DSNs, ARNs, raw exceptions, stack traces, or mutation commands.
- `<threat_model>` present covering Information Disclosure and Tampering.
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/04-02-SUMMARY.md` when done.
</output>
