---
phase: 04-operator-dashboard-and-data-issue-triage
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
autonomous: false
requirements: [DASH-01, DASH-03]

must_haves:
  truths:
    - "DASH-01: An operator launches the read-only dashboard locally with dev credentials (MDM_DATABASE_URL from dev Secrets Manager loaded via env per D-08/D-13, dev Snowflake connection) and inspects the four views (Overview, MDM Overview, Neo4j Overview, Mismatch Diagnostics)."
    - "DASH-01: The 5 existing UAT rows in evidence/dashboard-security.md (MDM overview, Hosted graph overview, Mismatch diagnostics, Manual refresh, Bounded samples) are filled with dev-run pass/fail notes (D-10)."
    - "DASH-03: The UAT evidence is annotated 'dev precedent only — prod proof required separately' (D-09) and contains no credentials, DSNs, raw connector exceptions, stack traces, or unbounded exports (D-12)."
    - "DASH-01/DASH-03: 01-LAUNCH-GATE-MATRIX.md rows 26-27 are updated to point at the filled dev UAT evidence and the completed README cleanup, with the dev-precedent annotation, while remaining BLOCKED on prod proof."
  artifacts:
    - path: .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md
      provides: "Filled 5-row dashboard UAT evidence (dev precedent) with pass/fail notes and dev-precedent annotation."
      contains: "dev precedent only"
    - path: .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
      provides: "Updated dashboard rows 26-27 referencing filled dev UAT evidence and completed README cleanup."
      contains: "dashboard-security.md"
  key_links:
    - from: "01-LAUNCH-GATE-MATRIX.md rows 26-27"
      to: "evidence/dashboard-security.md filled UAT rows"
      via: "same-directory relative markdown link"
      pattern: "evidence/dashboard-security.md"
---

<objective>
Run the dashboard UAT with dev credentials and capture dev-precedent evidence:
(a) Claude automates the secret-safe dashboard launch and read-only helper
coverage; (b) the operator visually verifies the four dashboard views in the
browser (a `checkpoint:human-verify` — Claude cannot inspect a live Streamlit
UI and must not fabricate the result); (c) Claude fills the 5 existing UAT rows
in `evidence/dashboard-security.md` with the operator's pass/fail notes and
updates `01-LAUNCH-GATE-MATRIX.md` rows 26-27.

Purpose: prove the dashboard is a usable read-only inspection surface against
dev (production-like) configuration, establishing dev precedent for DASH-01 and
recording it as DASH-03-compliant evidence. This does NOT satisfy production
dashboard UAT — it is annotated "dev precedent only — prod proof required
separately" (D-09), and rows 26-27 stay BLOCKED on prod proof.

Output: filled 5-row UAT table in `evidence/dashboard-security.md` and updated
rows 26-27 in `01-LAUNCH-GATE-MATRIX.md`. Depends on 04-01 (the README cleanup
that unblocks the row-27 "NEO4J_* cleanup" reference). No secrets, DSNs, raw
exceptions, stack traces, or unbounded exports in any file (D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/04-CONTEXT.md

# The evidence file with the 5 pending UAT rows to fill (D-10):
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md

# Rows 26-27 to update (Dashboard operator inspection views; README NEO4J_* cleanup):
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md

# The dashboard under test and its (post-04-01) README:
@examples/mdm_graph_dashboard/streamlit_app.py
@examples/mdm_graph_dashboard/README.md

# Phase 3 secret-safe dev-credential loading precedent (mirror the env-only DSN load):
@.planning/workstreams/go-live/phases/03-mdm-hosted-graph-e2e-acceptance/runbook/mdm-secrets.md

<interfaces>
<!-- The 5 UAT table rows to fill in evidence/dashboard-security.md (D-10), under -->
<!-- "## Phase 1 UAT Notes Template", columns: View | Evidence to inspect | Result | Notes -->
<!--   MDM overview | Entity counts, relationship counts, setup guidance -->
<!--   Hosted graph overview | Snowflake graph node/edge counts and Native App failure-only detail -->
<!--   Mismatch diagnostics | Missing/extra node and edge samples with bounded row limit -->
<!--   Manual refresh | Refresh timestamp and cached read-only payload behavior -->
<!--   Bounded samples | Row limit behavior and no unbounded export path -->
<!-- The file already carries the line "dev precedent only — prod proof required separately". -->

<!-- Secret-safe dev load (D-08/D-13): MDM_DATABASE_URL from dev Secrets Manager -->
<!-- edgartools-dev/mdm/postgres_dsn via `aws secretsmanager get-secret-value -->
<!-- ... --query SecretString --output text` into an env var; value never printed. -->
<!-- Launch command (D-11): -->
<!--   uv run --extra dashboard --extra mdm-runtime streamlit run examples/mdm_graph_dashboard/streamlit_app.py -->

<!-- Rows 26-27 in 01-LAUNCH-GATE-MATRIX.md: -->
<!--   row 26 "Dashboard operator inspection views" — link filled dev UAT evidence -->
<!--   row 27 "Dashboard README NEO4J_* cleanup ... 04-03-PLAN.md closeout" — 04-01 -->
<!--     completed the cleanup; reference it. Both stay BLOCKED on prod proof per the -->
<!--     Dev-Vs-Prod rule (dev UAT does not flip to PASS). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automate secret-safe dashboard launch and read-only helper coverage</name>
  <files>.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/04-03-SUMMARY.md</files>
  <read_first>
    - examples/mdm_graph_dashboard/README.md
    - examples/mdm_graph_dashboard/streamlit_app.py
    - .planning/workstreams/go-live/phases/03-mdm-hosted-graph-e2e-acceptance/runbook/mdm-secrets.md
  </read_first>
  <action>
Prepare the dashboard for the human-verify checkpoint, using dev credentials and
secret-safe loading (D-08/D-13):

1. Load `MDM_DATABASE_URL` from dev Secrets Manager into the environment WITHOUT
   printing the value, mirroring the Phase 3 convention:
   `export MDM_DATABASE_URL="$(aws secretsmanager get-secret-value --profile
   <dev-profile> --region us-east-1 --secret-id edgartools-dev/mdm/postgres_dsn
   --query SecretString --output text)"`. If the secret is JSON, extract the
   `dsn`/`url`/`MDM_DATABASE_URL` field first. Confirm only that the variable is
   set (e.g. `test -n "$MDM_DATABASE_URL" && echo SET`) — never echo the value.
   Set the dev Snowflake connection context (`SNOWFLAKE_CONNECTION` /
   `DBT_SNOWFLAKE_*`) for the hosted-graph view.

2. Run the read-only helper coverage suite to confirm the dashboard's read-only
   payloads function before the operator opens the browser:
   `uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py -q`.
   Record only a non-secret PASS/FAIL summary and counts.

3. Launch the dashboard for the operator (D-11):
   `uv run --extra dashboard --extra mdm-runtime streamlit run examples/mdm_graph_dashboard/streamlit_app.py`.
   If a local optional dependency is missing or dev credentials are unavailable,
   document the exact missing dependency / blocked step in the summary (non-
   secret) and route to the checkpoint with that limitation noted — do NOT
   fabricate a successful UAT.

Record in `04-03-SUMMARY.md` (non-secret): that MDM_DATABASE_URL was loaded via
env only (not printed), the helper-suite pass/fail summary, and the launch
command issued. No DSN, password, token, raw exception, or stack trace.
  </action>
  <verify>
    <automated>uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `MDM_DATABASE_URL` is set in the environment via `get-secret-value --query SecretString --output text` and is never printed/echoed (the summary states "loaded via env, not printed").
    - `uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py -q` result (pass/fail + counts) is recorded; if blocked by a missing optional dependency, the exact dependency and command are documented.
    - The launch command `uv run --extra dashboard --extra mdm-runtime streamlit run examples/mdm_graph_dashboard/streamlit_app.py` is issued (or the blocking reason is documented).
    - `04-03-SUMMARY.md` contains no DSN, password, token, raw connector exception, or stack trace.
  </acceptance_criteria>
  <done>
Dashboard launched with dev credentials loaded via env only (never printed),
read-only helper suite run with a recorded non-secret pass/fail summary, and the
launch handed to the operator for visual verification. Any missing-dependency or
credential blocker is documented non-secret.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies the four dashboard views</name>
  <what-built>
The read-only dashboard launched locally with dev credentials (Task 1). It
exposes four views: Overview, MDM Overview, Neo4j Overview (Snowflake-hosted
graph), and Mismatch Diagnostics.
  </what-built>
  <how-to-verify>
In the browser tab Streamlit opened:
1. Overview — confirm blocking failures / coverage warnings render before
   snapshot metrics. Note pass/fail.
2. MDM Overview — confirm registered entity counts and relationship counts
   render. Note pass/fail.
3. Neo4j Overview — confirm Snowflake-hosted graph node/edge counts render, and
   that any Native App check failures show as failure-only detail (no raw logs).
   Note pass/fail.
4. Mismatch Diagnostics — confirm missing/extra node and edge samples render
   bounded by the Row limit selector (25/50/100/250, default 50). Note pass/fail.
5. Manual refresh — click `Refresh metrics`; confirm the refresh timestamp
   updates and only cached read-only payloads re-read (no write control). Note
   pass/fail.
6. Bounded samples — change the Row limit; confirm sample tables stay bounded
   and there is no unbounded-export control. Note pass/fail.

Report pass/fail and one short note per view as TEXT only. Do NOT paste DSNs,
credentials, raw exceptions, stack traces, or full table dumps. Screenshots are
optional only if secret-safe.
  </how-to-verify>
  <resume-signal>Provide the 5 UAT-row pass/fail results (MDM overview, Hosted graph overview, Mismatch diagnostics, Manual refresh, Bounded samples) as secret-safe text, or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fill the 5 UAT evidence rows and update launch gate matrix rows 26-27</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md, .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md</files>
  <read_first>
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
  </read_first>
  <action>
Using only the operator's secret-safe pass/fail text from the Task 2 checkpoint:

1. Fill the 5 rows under "## Phase 1 UAT Notes Template" in
   `evidence/dashboard-security.md`: replace each "pending production proof"
   Result cell with the dev-run result (pass/fail) and a short secret-safe note
   for MDM overview, Hosted graph overview, Mismatch diagnostics, Manual refresh,
   and Bounded samples. Keep the existing line "dev precedent only — prod proof
   required separately" (D-09) and add a dated dev-run header noting dev
   credentials were used (MDM_DATABASE_URL from dev Secrets Manager, env-only).
   Do NOT alter the "What This File Must NOT Include", "Read-Only / Inspection-
   Only Guarantee", "Routing Policy", or "Secret-Safe Loading Convention"
   sections. Do NOT add DSNs, raw exceptions, stack traces, or unbounded exports.

2. Update `01-LAUNCH-GATE-MATRIX.md` in place, preserving the column structure
   and touching only rows 26-27:
   - Row 26 "Dashboard operator inspection views": Required Rerun Proof cell
     references the now-filled dev UAT rows in
     `[evidence/dashboard-security.md](evidence/dashboard-security.md)`,
     annotated "dev precedent only — prod proof required separately". Status
     stays BLOCKED (dev UAT does not flip to PASS per the Dev-Vs-Prod rule).
   - Row 27 "Dashboard README NEO4J_* cleanup (...04-03-PLAN.md closeout)":
     reference that the README cleanup completed in go-live Phase 4 plan 04-01
     (README no longer instructs external NEO4J_*/Bolt/Aura/check-connectivity
     --neo4j) and link the dashboard-security evidence README-recheck note.
     Status stays BLOCKED only if prod proof is still required; if the row's
     acceptance is purely the cleanup (no prod dependency), it MAY move to PASS —
     follow the existing Blocker Classification Rules (D-01/D-02) and keep the
     dev-precedent framing. Do not invent a prod pass.

Reference only evidence section names and the relative evidence link in matrix
cells — never raw DSNs, ARNs, or secret values.
  </action>
  <verify>
    <automated>f=.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md; grep -q 'dev precedent only' "$f" && test "$(grep -c 'pending production proof' "$f")" -le 0 && for v in 'MDM overview' 'Hosted graph overview' 'Mismatch diagnostics' 'Manual refresh' 'Bounded samples'; do grep -q "$v" "$f" || { echo "MISSING $v"; exit 1; }; done && echo PASS</automated>
    <automated>f=.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/dashboard-security.md; test "$(grep -cE 'postgresql://|password=|bolt://|neo4j://|Traceback|RuntimeError\(' "$f")" -eq 0 && echo SECRET_SAFE</automated>
    <automated>f=.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md; grep -q 'evidence/dashboard-security.md' "$f" && grep -q '04-01' "$f" && echo MATRIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - All 5 UAT rows in `evidence/dashboard-security.md` have a filled dev-run Result (no remaining "pending production proof" in the UAT table) with secret-safe notes; the "dev precedent only — prod proof required separately" line is retained.
    - The evidence file contains no `postgresql://`, `password=`, `bolt://`, `neo4j://`, `Traceback`, or `RuntimeError(` (secret-safe grep gate returns 0).
    - `01-LAUNCH-GATE-MATRIX.md` row 26 references the filled dev UAT evidence with the dev-precedent annotation; row 27 references the 04-01 README cleanup completion. Only rows 26-27 changed.
    - No DSNs, ARNs, raw exceptions, stack traces, or unbounded exports introduced in either file.
  </acceptance_criteria>
  <done>
The 5 UAT evidence rows are filled with dev-run pass/fail notes and the dev-
precedent annotation, secret-safe. Launch gate matrix rows 26-27 reference the
filled dev UAT evidence and the completed 04-01 README cleanup, with prod-proof
framing preserved. No secrets, DSNs, raw exceptions, or unbounded exports.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev Secrets Manager → dashboard runtime | `MDM_DATABASE_URL` enters the runtime via env only (`get-secret-value --query SecretString --output text`); the value is never printed or committed (D-13). |
| dashboard runtime → committed evidence | Only secret-safe pass/fail text and bounded-sample observations cross into evidence; raw connector exceptions, stack traces, full tables, and unbounded exports do not (D-12). |
| operator browser inspection → evidence | The human-verify result is the only source for the UAT row results; Claude must not fabricate pass/fail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | dev credential load + UAT evidence | mitigate | `MDM_DATABASE_URL` loaded via env only, never echoed (Task 1 confirms with `test -n`/`echo SET`). Evidence secret-safe grep gate (Task 3) forbids `postgresql://`, `password=`, `bolt://`, `neo4j://`, `Traceback`, `RuntimeError(`. No DSNs/ARNs in matrix cells. |
| T-04-03-02 | Repudiation | UAT pass/fail provenance | mitigate | The `checkpoint:human-verify` (Task 2) is the recorded source of the 5 UAT-row results; Task 3 fills rows only from the operator's text. Evidence annotated "dev precedent only — prod proof required separately" (D-09) so dev results are not mistaken for prod acceptance. |
| T-04-03-03 | Tampering | dashboard inspection surface | mitigate | The dashboard is read-only; the verify steps confirm `Refresh metrics` only re-reads cached payloads with no write control and no unbounded-export path. No mutation command is run. |
| T-04-03-04 | Elevation of Privilege | dev-vs-prod gate status | mitigate | Rows 26-27 stay BLOCKED on prod proof (Dev-Vs-Prod rule); a dev UAT pass does not flip prod acceptance. Any row-27 move follows existing Blocker Classification Rules, not an invented prod pass. |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — UAT uses existing locked deps via `uv run --extra dashboard --extra mdm-runtime`. No supply-chain checkpoint required. |
</threat_model>

<verification>
- Task 1: `uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py -q` recorded; MDM_DATABASE_URL loaded via env only.
- Task 2: operator returns 5 secret-safe UAT-row pass/fail results.
- Task 3: 5 UAT rows filled (no remaining "pending production proof"); evidence
  secret-safe grep gate returns 0; matrix rows 26-27 reference filled evidence
  and 04-01 cleanup; only rows 26-27 changed.
- Dev-precedent annotation retained; rows stay BLOCKED on prod proof.
</verification>

<success_criteria>
- The dashboard ran locally with dev credentials (env-only DSN) and the operator
  verified all four views.
- The 5 UAT rows in `evidence/dashboard-security.md` are filled with dev-run
  pass/fail notes, annotated "dev precedent only — prod proof required
  separately", and are secret-safe.
- `01-LAUNCH-GATE-MATRIX.md` rows 26-27 reference the filled dev UAT evidence and
  the completed 04-01 README cleanup, preserving prod-proof framing.
- `<threat_model>` present covering Information Disclosure, Repudiation, and
  Tampering.
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/04-operator-dashboard-and-data-issue-triage/04-03-SUMMARY.md` when done.
</output>
