---
phase: 02-aws-and-snowflake-production-deployment-dry-run
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/snowflake-native-pull.md
  - .planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/dbt-gold.md
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
autonomous: false
requirements: [SNOW-01, SNOW-02]
must_haves:
  truths:
    - "Operator can read the exact deploy-snowflake-stack.sh --env prod command and understands it structurally cannot proceed today (dies at the backend.hcl existence check across 3 Terraform roots, before touching Snowflake), with the 3 missing backend.hcl files + native_pull resource list documented as the required fix."
    - "The SNOW-01 structural-blocker has been proven as a repeatable smoke check (script exits non-zero with a backend.hcl message) and the result is recorded in evidence/snowflake.md as the proof the row stays BLOCKED."
    - "Operator can read the exact prod-target dbt commands (dbt run/test --target prod with DBT_SNOWFLAKE_* placeholders, D-04) AND Phase 2 runs the dev-target dbt compile/run/test gate when non-committed dev credentials are supplied; any missing dev credentials are recorded as BLOCKED/failed evidence, not silently downgraded to docs-only."
    - "The Snowflake-side BLOCKED matrix rows (6-9) reflect Phase 2 findings: structural-blocker proof for native-pull, documented placeholder/grant/freshness commands; all stay BLOCKED with documented required-fix commands."
  artifacts:
    - path: ".planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/snowflake-native-pull.md"
      provides: "deploy-snowflake-stack.sh prod invocation + backend.hcl prerequisite + native_pull resource list"
      min_lines: 40
    - path: ".planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/dbt-gold.md"
      provides: "dev-precedent + prod-target placeholder dbt commands + gold-status query"
      min_lines: 40
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md"
      provides: "SNOW-01 structural-blocker smoke-test evidence + Phase 2 dispositions"
      contains: "backend.hcl"
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md"
      provides: "Updated Snowflake-side gate rows 6-9"
      contains: "Snowflake native S3 pull stack"
  key_links:
    - from: "runbook/snowflake-native-pull.md"
      to: "01-LAUNCH-GATE-MATRIX.md row `Snowflake native S3 pull stack (infra/scripts/deploy-snowflake-stack.sh)`"
      via: "BLOCKED-row reference string (exact Gate-column name)"
      pattern: "Snowflake native S3 pull stack"
    - from: "evidence/snowflake.md"
      to: "01-LAUNCH-GATE-MATRIX.md row `Snowflake native S3 pull stack (infra/scripts/deploy-snowflake-stack.sh)`"
      via: "structural-blocker smoke-test result summary"
      pattern: "backend\\.hcl"
---

<objective>
Produce the Snowflake/dbt-side production deployment dry-run artifacts for Phase 2: a
non-secret runbook for the native-pull stack (`deploy-snowflake-stack.sh --env prod`,
SNOW-01) and dbt gold (SNOW-02), a repeatable guarded smoke-test proving the native-pull
structural blocker, a credential-gated dev-target dbt validation run for D-03, and the
corresponding updates to `evidence/snowflake.md` and the Snowflake-side rows (6-9) of
`01-LAUNCH-GATE-MATRIX.md`.

Purpose: Satisfy SNOW-01 (operator can deploy/validate the Snowflake native S3 pull
stack — documented command + proven structural blocker) and SNOW-02 (operator can run
dbt compile/run/test for the production target — documented prod-target placeholder
commands + dev-target precedent evidence + gold-status/freshness query). No prod
Snowflake DDL or prod dbt command is executed; no prod Snowflake connection exists
(D-01/D-04).

Output: `runbook/snowflake-native-pull.md` (new), `runbook/dbt-gold.md` (new), appended
sections in `evidence/snowflake.md`, edited rows 6-9 in `01-LAUNCH-GATE-MATRIX.md`. No
production state is changed; nothing under `infra/` is committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-CONTEXT.md
@.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-RESEARCH.md
@.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-PATTERNS.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md

<scope_guards>
- This phase is document-and-validate only (D-01). No Snowflake DDL against any prod
  target, no `terraform apply` for the Snowflake roots, no real `dbt run`/`dbt test`
  against prod (D-04 — no prod Snowflake connection exists).
- SNOW-01 smoke test runs `deploy-snowflake-stack.sh --env prod` ONLY after the guard
  proves the 3 prod `backend.hcl` files are absent and the script still checks those
  files before any `terraform apply`, `snow sql`, dbt, or dashboard-upload action. It
  MUST exit non-zero with a `backend.hcl` message. If the static guard fails, do not run
  the wrapper; record SNOW-01 as BLOCKED with the guard failure. Do NOT create the 3
  `backend.hcl` files; do NOT supply real tfvars/credentials.
- SNOW-02 has a credential-gated dev validation and a prod documentation path. If the
  operator supplies dev `DBT_SNOWFLAKE_*` values outside git, run `dbt compile`, `dbt run`,
  and `dbt test` against `--target dev` and record pass/fail as dev-precedent evidence
  (D-03). If the credentials are unavailable, record the dev-precedent gate as BLOCKED/
  failed evidence; do not silently narrow D-03 to documentation-only. Prod-target dbt
  remains documentation-only (D-04).
- Secret-safety (Phase 1 D-13/D-15): never paste raw connector errors, DSNs, account
  locators, passwords, secret ARNs, compiled SQL, or full generated JSON. Summarize as
  counts/names/format/pass-fail only. Use `export VAR="<placeholder>"` style for all
  prod credentials.
</scope_guards>

<interfaces>
<!-- Exact formats to replicate — see 02-PATTERNS.md for full detail. -->

Matrix row format (01-LAUNCH-GATE-MATRIX.md, table header at line 10-11):
| Gate | Owner/Source | Required Fix | Required Rerun Proof | Status |

Allowed Status values are ONLY `BLOCKED` and `PASS` (Phase 1 D-06). All 4 Snowflake-side
rows stay `BLOCKED`; refine the Required Fix / Required Rerun Proof cells only.

The 4 Snowflake-side Gate-column names (copy VERBATIM for BLOCKED-row reference strings):
- `Snowflake native S3 pull stack (infra/scripts/deploy-snowflake-stack.sh)`
- `Snowflake deployer direct grants for gold dynamic tables`
- `dbt compile/run/test for production target`
- `EDGARTOOLS_GOLD_STATUS and dynamic-table freshness`

BLOCKED-row reference string (reuse verbatim in evidence bullets):
- BLOCKED - see `01-LAUNCH-GATE-MATRIX.md` row `<exact Gate-column name copied verbatim>`.

Command-evidence block structure (every new evidence/snowflake.md entry):
```bash
<exact command>
```
Result: <succeeded|failed> <one-line summary>.

- <non-secret fact 1>.
- <BLOCKED reference OR "dev precedent only — prod proof required separately" caveat>.

Dev-precedent label (reuse VERBATIM as a standalone line before any dev-target result):
- dev precedent only — prod proof required separately

dbt env-var names (Pattern 4 / docs/runbook.md lines 426-441 — placeholders only):
- DBT_SNOWFLAKE_ACCOUNT, DBT_SNOWFLAKE_USER, DBT_SNOWFLAKE_PASSWORD,
  DBT_SNOWFLAKE_ROLE, DBT_SNOWFLAKE_DATABASE, DBT_SNOWFLAKE_WAREHOUSE

Gold-status freshness summary-table header (reuse from evidence/snowflake.md lines 81-84):
| Model/Table | Status | Last refresh |
| --- | --- | --- |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02-02-01: Prove SNOW-01 structural blocker and write the native-pull runbook</name>
  <files>.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/snowflake-native-pull.md, .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md</files>
  <action>
Before running the SNOW-01 structural-blocker smoke test, prove the smoke cannot become
state-changing:

1. Assert the three prod backend files are absent:
   `infra/terraform/access/aws/accounts/prod/backend.hcl`,
   `infra/terraform/snowflake/accounts/prod/backend.hcl`, and
   `infra/terraform/access/snowflake/accounts/prod/backend.hcl`.
2. Statically inspect `infra/scripts/deploy-snowflake-stack.sh` and confirm the
   `backend.hcl` checks occur before the first state-changing call site:
   `terraform_apply`, `terraform_apply_storage_integration_only`,
   `terraform_apply_root`, `snow sql`, dbt run/test, or dashboard upload. Command
   existence checks and local temp-file setup may occur earlier; that is acceptable
   because they do not touch prod state.

If either guard fails, do NOT invoke the wrapper. Record SNOW-01 as BLOCKED with a
non-secret guard-failure summary in `evidence/snowflake.md`.

If the guard passes, run the smoke test exactly once to prove the documented failure mode
(02-RESEARCH.md Pitfall 2): `bash infra/scripts/deploy-snowflake-stack.sh --env prod
--snow-connection edgartools-prod --run-validation` — capture the exit code and confirm
it exits non-zero with a `backend.hcl` message before any state-changing Terraform,
Snowflake SQL, dbt, or dashboard action. If the message text differs from the expected
`backend.hcl` die (e.g. the script structure changed since research), record the actual
observed first-failure honestly.

Create `runbook/snowflake-native-pull.md` (new file) documenting, in the
CLAUDE.md known-issue framing (02-PATTERNS.md Analog 3):
1. The exact prod invocation `bash infra/scripts/deploy-snowflake-stack.sh --env prod
   --snow-connection edgartools-prod --run-validation --run-dbt` (documented, NOT run
   beyond the structural-blocker smoke test).
2. A bold lead-in stating the structural blocker (the script is a 3-root
   `terraform_init`+`terraform_apply` orchestrator FIRST — AWS bootstrap trust ->
   Snowflake storage integration -> Snowflake full apply -> Snowflake access apply —
   with `--run-validation`/`--run-dbt`/`--upload-dashboard` as opt-in tails AFTER that
   preamble), then the root cause (it `die`s at the `backend.hcl` existence check
   before any flag-gated logic runs), then the required fix: create the 3 real
   `backend.hcl` files (`infra/terraform/{access/aws,snowflake,access/snowflake}/accounts/prod/backend.hcl`
   — only `.example` files exist) plus real `terraform.tfvars` with Snowflake
   account/org identifiers and an `edgartools-prod-tfstate`-equivalent S3 backend
   bucket — all of which require a production Snowflake account to exist (out of scope,
   D-01).
3. The `native_pull` module target-state resource list (from
   `infra/terraform/snowflake/modules/native_pull/main.tf`): storage integration, file
   formats, stage, source mirror tables, pipe, stream, 3 stored procedures, 1 task —
   names/categories only, no values.

Then append to `evidence/snowflake.md` (under the existing header — do NOT duplicate
the header) a new "Phase 2 Read-Only Checks Actually Run" section following the
command-evidence block structure: a fenced bash block with the smoke-test command, a
`Result: failed (structural blocker proven) ...` line, and bulleted non-secret facts —
that the script exited non-zero, that the first state-changing blocker was the
`backend.hcl` existence check (reached no Terraform apply/Snowflake SQL/dbt/dashboard
action), and the BLOCKED reference string for the
`Snowflake native S3 pull stack (infra/scripts/deploy-snowflake-stack.sh)` row. Never
paste the raw script output or any connector trace (D-13/D-15).
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && for f in infra/terraform/access/aws/accounts/prod/backend.hcl infra/terraform/snowflake/accounts/prod/backend.hcl infra/terraform/access/snowflake/accounts/prod/backend.hcl; do test ! -f "$f"; done && backend_line=$(rg -n '^\\[\\[ -f "\\$\\{AWS_ROOT\\}/backend\\.hcl"' infra/scripts/deploy-snowflake-stack.sh | head -n1 | cut -d: -f1) && apply_line=$(rg -n '^(terraform_apply "\\$\\{AWS_ROOT\\}"|terraform_apply_storage_integration_only "\\$\\{SNOWFLAKE_ROOT\\}"|terraform_apply "\\$\\{SNOWFLAKE_ROOT\\}"|terraform_apply_root "\\$\\{SNOWFLAKE_ACCESS_ROOT\\}")|snow sql --connection|dbt run --target|streamlit/deploy\\.sh' infra/scripts/deploy-snowflake-stack.sh | head -n1 | cut -d: -f1) && test -n "$backend_line" && test -n "$apply_line" && test "$backend_line" -lt "$apply_line" && out=$(bash infra/scripts/deploy-snowflake-stack.sh --env prod --snow-connection edgartools-prod --run-validation 2>&1); rc=$?; RB=.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/snowflake-native-pull.md && printf '%s' "$out" | grep -qi 'backend.hcl' && [ "$rc" -ne 0 ] && test -f "$RB" && grep -q "deploy-snowflake-stack.sh" "$RB" && grep -qi 'backend.hcl' "$RB" && grep -qi 'native_pull\|storage integration' "$RB" && grep -qi 'backend.hcl' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md && echo "SNOW01-CONFIRMED rc=$rc"</automated>
  </verify>
  <done>
The SNOW-01 smoke test exited non-zero with a `backend.hcl` message (proven, not
declined). `runbook/snowflake-native-pull.md` exists (≥40 lines) documenting the exact
prod command, the structural-blocker root cause + required fix (3 missing `backend.hcl`
files + real tfvars + prod Snowflake account), and the `native_pull` target-state
resource list. `evidence/snowflake.md` has a new Phase 2 section recording the
static guard, structural-blocker smoke result, and the BLOCKED reference — all
non-secret, no raw script output pasted.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 02-02-02: Confirm dev dbt credentials and run dev-target dbt validation</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md</files>
  <action>
Secret-safe checkpoint: confirm whether the operator has supplied dev Snowflake/dbt
credentials as environment variables outside git. Required variable names:
`DBT_SNOWFLAKE_ACCOUNT`, `DBT_SNOWFLAKE_USER`, `DBT_SNOWFLAKE_PASSWORD`,
`DBT_SNOWFLAKE_ROLE`, `DBT_SNOWFLAKE_DATABASE`, and `DBT_SNOWFLAKE_WAREHOUSE`.
Do not print or commit values.

If all variables are set, run:

```bash
cd infra/snowflake/dbt/edgartools_gold
uv run --with dbt-snowflake dbt deps
uv run --with dbt-snowflake dbt compile --target dev
uv run --with dbt-snowflake dbt run --target dev
uv run --with dbt-snowflake dbt test --target dev
```

Append a `dev precedent only — prod proof required separately` evidence entry to
`evidence/snowflake.md` with pass/fail for `dbt compile --target dev`,
`dbt run --target dev`, and `dbt test --target dev`. Summarize failures without raw
connector traces, account locators, compiled SQL, passwords, or DSNs.

If any required variable is missing, do not run dbt. Append a BLOCKED/failed evidence
entry naming only the missing variable names and stating that SNOW-02 remains blocked
until the dev-target dbt gate can run. This is a failed prerequisite, not a
documentation-only downgrade of D-03.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && SE=.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md && grep -qF 'dev precedent only — prod proof required separately' "$SE" && grep -q 'dbt compile --target dev' "$SE" && grep -q 'dbt run --target dev' "$SE" && grep -q 'dbt test --target dev' "$SE" && echo OK</automated>
  </verify>
  <done>
`evidence/snowflake.md` records the dev-target dbt gate outcome with the required
dev-precedent label. If credentials were supplied, compile/run/test were executed and
summarized. If credentials were missing, the evidence explicitly records the missing
variable names and keeps SNOW-02 blocked. No credential values, DSNs, connector traces,
or compiled SQL are committed.
  </done>
</task>

<task type="auto">
  <name>Task 02-02-03: Write dbt-gold runbook and update Snowflake-side matrix rows</name>
  <files>.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/dbt-gold.md, .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md, .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md</files>
  <action>
Create `runbook/dbt-gold.md` (new file) documenting, in the
`export VAR="<placeholder>"` style of CLAUDE.md / `docs/runbook.md` lines 426-441
(02-PATTERNS.md Analog 2/4) — DO NOT run prod dbt commands (Pitfall 4: `dbt compile`
opens a live Snowflake connection; D-04: no prod credentials/connection exist):
1. The dev-precedent block (D-03): `cd infra/snowflake/dbt/edgartools_gold`, the
	   `DBT_SNOWFLAKE_{ACCOUNT,USER,PASSWORD,WAREHOUSE}` env exports with
	   `<dev-...>` placeholders, and `uv run --with dbt-snowflake dbt {compile,run,test}
	   --target dev`. Label this block with the verbatim line `dev precedent only — prod
	   proof required separately`, and cross-reference Task 02-02-02 as the Phase 2
	   credential-gated execution/evidence source.
2. The prod-target block (D-04): the same env exports plus `DBT_SNOWFLAKE_ROLE=
   EDGARTOOLS_PROD_DEPLOYER`, `DBT_SNOWFLAKE_DATABASE=EDGARTOOLS_PROD`,
   `DBT_SNOWFLAKE_WAREHOUSE=EDGARTOOLS_PROD_REFRESH_WH` (placeholders), then `dbt deps`,
   `dbt run --target prod`, `dbt test --target prod`. Documented as the BLOCKED row's
   required-fix command.
3. A known-issue callout (Pitfall 4) in the CLAUDE.md bold-lead-in framing: even `dbt
   compile --target prod` needs live prod Snowflake credentials — there is no
   "lighter"/placeholder logic-only check (it fails with a live `404 ... login-request`
   error).
4. The gold-status / freshness query `SELECT * FROM
   EDGARTOOLS_PROD.EDGARTOOLS_GOLD.EDGARTOOLS_GOLD_STATUS LIMIT 10;` (or `snow sql
   --connection edgartools-prod -q ...`) documented as the matrix-row-9 required-fix
   command, plus the `EDGARTOOLS_GOLD_STATUS` column list (from
   `infra/snowflake/dbt/edgartools_gold/models/gold/edgartools_gold_status.sql`:
   environment, source_workflow, run_id, business_date, status, source_load_status,
   refresh_status, source_row_count, tables_loaded, last_successful_refresh_at,
   updated_at) — names only, no values.

Then append to `evidence/snowflake.md` a short "Known Grant Gap" required-fix sub-bullet
documenting the `SHOW GRANTS TO ROLE EDGARTOOLS_PROD_DEPLOYER;` check (matrix row 7
required fix, parallel to the resolved `EDGARTOOLS_DEV_DEPLOYER` dev gap recorded in
`TODOS.md`), following the existing "recorded as a matrix blocker, not a pass" framing.
Reuse the existing "Gold Status And Freshness Summary Shape" table header for any future
prod freshness result (do NOT fabricate values — leave `pending production proof`).

Then edit rows 6-9 of the `## Gate Matrix` table in `01-LAUNCH-GATE-MATRIX.md` (the
"Snowflake native S3 pull stack" through "`EDGARTOOLS_GOLD_STATUS` and dynamic-table
freshness" rows). Per the 02-RESEARCH.md disposition table: row 6 — refine Required Fix
/ Required Rerun Proof to cite the proven structural blocker (3 missing `backend.hcl`
files) and the `native_pull` resource list; keep `BLOCKED`. Row 7 — note the
`SHOW GRANTS TO ROLE EDGARTOOLS_PROD_DEPLOYER;` required-fix command; keep `BLOCKED`.
Row 8 — note the documented prod-target dbt command + the `dbt compile` live-credential
requirement (Pitfall 4); keep `BLOCKED`. Row 9 — note the documented gold-status query +
the existing freshness summary-table shape; keep `BLOCKED`. Keep the existing relative
`[evidence/snowflake.md](evidence/snowflake.md)` link syntax. Do NOT invent a new status
word and do NOT rewrite the table or other sections.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && RB=.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/dbt-gold.md && SE=.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/snowflake.md && MX=.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && test -f "$RB" && grep -q 'target dev' "$RB" && grep -q 'target prod' "$RB" && grep -q 'DBT_SNOWFLAKE_' "$RB" && grep -q 'EDGARTOOLS_GOLD_STATUS' "$RB" && grep -qF 'dev precedent only — prod proof required separately' "$RB" && grep -qF 'dev precedent only — prod proof required separately' "$SE" && grep -q 'dbt compile --target dev' "$SE" && grep -q 'SHOW GRANTS TO ROLE EDGARTOOLS_PROD_DEPLOYER' "$SE" && grep -q 'EDGARTOOLS_PROD_DEPLOYER' "$MX" && echo OK</automated>
  </verify>
  <done>
`runbook/dbt-gold.md` exists (≥40 lines) documenting the dev-precedent dbt block
(labeled `dev precedent only — prod proof required separately` and cross-referenced to
Task 02-02-02 evidence), the prod-target placeholder block (`dbt run/test --target prod` with
`DBT_SNOWFLAKE_*` placeholders), the Pitfall 4 `dbt compile` live-credential callout,
and the gold-status query + `EDGARTOOLS_GOLD_STATUS` column list. `evidence/snowflake.md`
has the `SHOW GRANTS TO ROLE EDGARTOOLS_PROD_DEPLOYER;` required-fix sub-bullet under
"Known Grant Gap". `01-LAUNCH-GATE-MATRIX.md` Snowflake rows 6-9 are refined with
documented required-fix commands and all stay `BLOCKED`. No prod dbt was executed; no
secrets/locators/passwords/compiled SQL in any artifact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → committed planning docs (`evidence/snowflake.md`, `runbook/{snowflake-native-pull,dbt-gold}.md`, matrix) | Command output (the `deploy-snowflake-stack.sh` smoke test) and documented dbt/Snowflake commands cross into version-controlled docs. Raw connector traces, account locators, passwords, DSNs, or compiled SQL must never be committed. |
| operator shell → `deploy-snowflake-stack.sh --env prod` | The SNOW-01 smoke test invokes the prod deploy wrapper. It must reach only the `backend.hcl` existence check (first action in `main()`) and stop — never any `terraform apply`, real Snowflake DDL, or credential use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Denial of Service | SNOW-01 structural-blocker smoke test (`deploy-snowflake-stack.sh --env prod`) | mitigate | The smoke test is bounded by a preflight guard: Task 02-02-01 first asserts the 3 prod `backend.hcl` files are absent and statically verifies the backend checks precede the first state-changing call sites (`terraform_apply*`, `snow sql`, dbt, dashboard upload). Only then does it run the wrapper, asserting BOTH a non-zero exit (`rc != 0`) AND a `backend.hcl` message (rc captured before the pipe to avoid a masked false-green). The 3 `backend.hcl` files are NOT created; no real tfvars/credentials are supplied. |
| T-02-03 | Information Disclosure | `runbook/dbt-gold.md` + `evidence/snowflake.md` (Snowflake connection / dbt credential leakage) | mitigate | dbt prod commands use `export VAR="<placeholder>"` only and are not executed. The dev-target dbt gate may execute only after the operator supplies `DBT_SNOWFLAKE_*` values outside git; evidence records command names and pass/fail only, never values, account locators, raw connector traces, DSNs, or compiled SQL. Snowflake object names (`EDGARTOOLS_PROD_DEPLOYER`, `EDGARTOOLS_GOLD_STATUS`, native_pull resources, column names) are non-secret identifiers only. Apply the Generated-JSON/output Summary Rule (D-13/D-15): record presence/counts/format/pass-fail, never raw values. |
| T-02-03b | Information Disclosure | SNOW-01 smoke-test output captured into `evidence/snowflake.md` | mitigate | Only summarize the smoke result (exited non-zero, first failure = `backend.hcl` check) — never paste the raw `deploy-snowflake-stack.sh` stdout/stderr or any connector trace. Verified by the Phase-gate secret-safety review (02-VALIDATION.md). |
</threat_model>

<verification>
- Task 02-02-01 `<automated>` proves the SNOW-01 static preflight guard passes and the
  structural blocker is real and repeatable (`SNOW01-CONFIRMED rc=...` with `rc != 0`
  AND a `backend.hcl` message, rc captured before the pipe), and that
  `runbook/snowflake-native-pull.md` + `evidence/snowflake.md` document it.
- Task 02-02-02 `<automated>` proves `evidence/snowflake.md` records the dev-target dbt
  gate outcome with the required dev-precedent label and compile/run/test command names.
- Task 02-02-03 `<automated>` proves `runbook/dbt-gold.md` contains the dev + prod dbt
  command surfaces (`target dev`, `target prod`, `DBT_SNOWFLAKE_`, `EDGARTOOLS_GOLD_STATUS`,
  the verbatim dev-precedent label), the `EDGARTOOLS_PROD_DEPLOYER` grant-check in
  `evidence/snowflake.md`, and that the matrix still carries its `BLOCKED` rows.
- Phase-gate manual review (per 02-VALIDATION.md): no DSNs/account-locators/passwords/
  secret-ARNs/raw connector traces/compiled SQL in `evidence/snowflake.md`,
  `runbook/{snowflake-native-pull,dbt-gold}.md`, or the matrix diff; all 4 Snowflake-side
  rows use only `BLOCKED`/`PASS` status values.
</verification>

<success_criteria>
- SNOW-01: an operator can read the exact `deploy-snowflake-stack.sh --env prod`
  command and the proven structural blocker (dies at `backend.hcl` check), with the 3
  missing `backend.hcl` files + real tfvars + `native_pull` resource list documented as
  the required fix; the structural blocker is recorded as a repeatable smoke result in
  `evidence/snowflake.md`.
- SNOW-02: an operator can read the exact prod-target dbt commands (placeholders,
  D-04), the dev-precedent commands (D-03, labeled), the Phase 2 dev-target dbt gate
  outcome, the `dbt compile` live-credential requirement (Pitfall 4), and the gold-status/
  freshness query — with no prod dbt executed and no secrets.
- Snowflake-side matrix rows 6-9 reflect Phase 2 findings (documented required-fix
  commands; all `BLOCKED` until real prod Snowflake state/credentials exist).
- No production state changed; nothing under `infra/` committed; all evidence
  non-secret.
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-02-SUMMARY.md` when done
</output>
