---
phase: 02-aws-and-snowflake-production-deployment-dry-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/aws-deploy.md
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
autonomous: true
requirements: [LIVE-02]
must_haves:
  truths:
    - "Operator can read an exact, runnable production AWS deploy command (deploy-aws-application.sh --env prod ...) with image-ref placeholders, --enable-mdm, and a freshly-resolved --edgar-identity-secret-arn, without any secret values pasted."
    - "Operator can follow a documented ECR image-promotion procedure (re-tag edgartools-dev-{warehouse,mdm}:prod in place) and capture immutable digests for --image-ref/--mdm-image-ref."
    - "A read-only terraform plan against infra/terraform/accounts/prod/ has been run via Pattern 1 (temp versions.tf edit + override.tf local backend), fully reverted (git status --short clean), and its resource-add count + output-name list recorded in evidence/aws.md."
    - "The versions.tf ~> version-constraint bug is recorded as a required-fix note (not fixed) in evidence/aws.md and the matrix."
    - "The four MDM secret names required by --enable-mdm are recorded by name only as required-identifier BLOCKED items in the matrix/evidence, with no ARNs or values."
    - "The 5 AWS-side BLOCKED matrix rows reflect Phase 2 findings: row 1 plan-validated, rows 2-5 stay BLOCKED with documented commands/mitigations."
  artifacts:
    - path: ".planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/aws-deploy.md"
      provides: "Production AWS deploy + ECR image-promotion runbook"
      min_lines: 60
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md"
      provides: "Pattern 1 terraform-plan evidence entry + image-promotion describe-images evidence"
      contains: "Plan:"
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md"
      provides: "Updated AWS-side gate rows 1-5"
      contains: "AWS passive infrastructure outputs"
  key_links:
    - from: "runbook/aws-deploy.md"
      to: "01-LAUNCH-GATE-MATRIX.md"
      via: "BLOCKED-row reference strings (exact Gate-column names)"
      pattern: "AWS active application deploy"
    - from: "evidence/aws.md"
      to: "01-LAUNCH-GATE-MATRIX.md row `AWS passive infrastructure outputs`"
      via: "terraform plan result summary"
      pattern: "Plan: [0-9]+ to add"
---

<objective>
Produce the AWS-side production deployment dry-run artifacts for Phase 2: a non-secret
runbook for the active-application deploy (`deploy-aws-application.sh --env prod`) and
ECR image promotion (D-05/D-06), the one executable read-only `terraform plan` check
for `infra/terraform/accounts/prod/` (Pattern 1, edit-then-revert), and the
corresponding updates to `evidence/aws.md` and the 5 AWS-side rows of
`01-LAUNCH-GATE-MATRIX.md`.

Purpose: Satisfy LIVE-02 — an operator can deploy/update active AWS application
components through the existing deploy script with explicit image refs and `--enable-mdm`,
with no Terraform-owned runtime commands or secret values — and move the "AWS passive
infrastructure outputs" gate from untested to plan-validated.

Output: `runbook/aws-deploy.md` (new), appended sections in `evidence/aws.md`, edited
rows 1-5 in `01-LAUNCH-GATE-MATRIX.md`. No production state is changed; nothing under
`infra/` is committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-CONTEXT.md
@.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-RESEARCH.md
@.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-PATTERNS.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md

<scope_guards>
- This phase is document-and-validate only (D-01). No `terraform apply`, no
  `deploy-aws-application.sh --env prod` execution, no ECR `put-image` mutation, no
  Snowflake DDL.
- Pattern 1 ONLY for the terraform check — temporary `versions.tf` edit + `override.tf`,
  fully reverted. Do NOT commit any change under `infra/` (Pattern 0 is out of scope per
  ISO-01; no authorization checkpoint was requested for this phase).
- ECR image-promotion interpretation is A1: re-tag `edgartools-dev-warehouse:prod` and
  `edgartools-dev-mdm:prod` in place (the only repos that exist — D-05 confirms prod is
  same-account, prefix-distinguished, no separate prod ECR). Document this as the only
  interpretation consistent with D-05+D-01+D-02. The `aws ecr put-image` re-tag step is
  operator-executed at cutover, NOT run during Phase 2.
- Secret-safety (Phase 1 D-13/D-15): never paste raw `terraform plan` output, ECR
  `describe-images` JSON, secret ARNs, or generated application JSON. Summarize as
  counts/names/format/pass-fail only.
</scope_guards>

<interfaces>
<!-- Exact formats to replicate — see 02-PATTERNS.md for full detail. -->

Matrix row format (01-LAUNCH-GATE-MATRIX.md, table header at line 10-11):
| Gate | Owner/Source | Required Fix | Required Rerun Proof | Status |

Allowed Status values are ONLY `BLOCKED` and `PASS` (Phase 1 D-06: no waiver/third
status). If a row cannot reach PASS, keep `BLOCKED` and add the new plan-validated /
documented-command detail in the Required Fix / Required Rerun Proof cell text.

BLOCKED-row reference string (reuse verbatim in evidence bullets):
- BLOCKED - see `01-LAUNCH-GATE-MATRIX.md` row `<exact Gate-column name copied verbatim>`.

Command-evidence block structure (every new evidence/aws.md entry):
```bash
<exact command>
```
Result: <succeeded|failed> <one-line summary>.

- <non-secret fact 1>.
- <non-secret fact 2>.
- <BLOCKED reference OR "context only, not production proof" caveat>.

Pattern 1 procedure (verified working this session — 02-RESEARCH.md lines 246-274):
- TEMP edit (do NOT commit): required_version = "~> 1.14.7" -> ">= 1.14.7"
- write override.tf with a backend "local" block at /tmp/edgartools-prod-plan/terraform.tfstate
- terraform init -input=false -no-color ; terraform plan -input=false -no-color
- git checkout -- versions.tf ; rm -rf override.tf .terraform .terraform.lock.hcl terraform.tfstate* /tmp/edgartools-prod-plan
- git status --short  # MUST be clean
Previously observed: Plan: 37 to add, 0 to change, 0 to destroy against account
077127448006. Record COUNT + output-name list (from outputs.tf) only — never the body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02-01-01: Run Pattern 1 terraform plan and record AWS infra evidence</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md</files>
  <action>
Execute the read-only `terraform plan` for `infra/terraform/accounts/prod/` using the
Pattern 1 procedure exactly (02-RESEARCH.md lines 246-274; the values are confirmed
live this session). Steps: (1) temporarily edit `required_version = "~> 1.14.7"` to
`">= 1.14.7"` in `infra/terraform/accounts/prod/versions.tf` — do NOT commit; (2) write
`infra/terraform/accounts/prod/override.tf` with a `backend "local"` block pointing at
`/tmp/edgartools-prod-plan/terraform.tfstate`; (3) run `terraform init -input=false
-no-color` then `terraform plan -input=false -no-color` from that directory; (4) revert
EVERYTHING — `git checkout -- versions.tf`, then `rm -rf override.tf .terraform
.terraform.lock.hcl terraform.tfstate* /tmp/edgartools-prod-plan`; (5) confirm `git
status --short` is clean.

If the live `terraform plan` cannot complete (network/credential drift, or the resource
count differs from the previously-observed `Plan: 37 to add`), record the actual
observed result honestly — the evidence entry captures what ran, not an expected value.

Then append to `evidence/aws.md` (under the existing header — do NOT duplicate the file
header) a new "Phase 2 Read-Only Checks Actually Run" section following the
command-evidence block structure: a fenced bash block with the Pattern 1 commands, a
`Result: succeeded ...` line, and bulleted non-secret facts — the resource-add COUNT
(e.g. `Plan: N to add, 0 to change, 0 to destroy`), the output-name list read from
`infra/terraform/accounts/prod/outputs.tf` (names only, no values), the account label
`077127448006` (per D-05, non-secret), and a note that the revert left `git status
--short` clean. Add a required-fix note that the prod-side `versions.tf` carries a `~>`
version-constraint bug (Pitfall 1) discovered during this check — recorded for a future
task to fix permanently, NOT fixed by Phase 2. NEVER paste the full plan body
(Generated-JSON Summary Rule / D-15).

Also append a short "Image-Promotion Digest Capture (read-only)" sub-entry documenting
the immediately-runnable `aws ecr describe-images ... --image-ids imageTag=dev --query
'imageDetails[0].imageDigest'` reads for `edgartools-dev-warehouse` and
`edgartools-dev-mdm` (the digest FORMAT only — `@sha256:` prefix presence — never the
full digest string or describe-images JSON), labeled as the read-only half of the
image-promotion procedure (the `put-image` re-tag is operator-executed at cutover).
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform/infra/terraform/accounts/prod && sed -i '' 's/"~> 1.14.7"/">= 1.14.7"/' versions.tf && printf 'terraform {\n  backend "local" {\n    path = "/tmp/edgartools-prod-plan/terraform.tfstate"\n  }\n}\n' > override.tf && terraform init -input=false -no-color && terraform plan -input=false -no-color; rc=$?; git checkout -- versions.tf; rm -rf override.tf .terraform .terraform.lock.hcl terraform.tfstate* /tmp/edgartools-prod-plan; cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && test -z "$(git status --short infra/terraform/accounts/prod)" && echo "REVERT-CLEAN rc=$rc"</automated>
  </verify>
  <done>
`terraform plan` for `accounts/prod` ran via Pattern 1 and was fully reverted (`git
status --short` clean for `infra/terraform/accounts/prod`). `evidence/aws.md` has a new
Phase 2 section recording the resource-add count, output-name list, account label, the
revert-clean confirmation, the `versions.tf` required-fix note, and the read-only
image-digest-format capture — all non-secret, no raw plan/JSON body pasted.
  </done>
</task>

<task type="auto">
  <name>Task 02-01-02: Write aws-deploy runbook and update AWS-side matrix rows</name>
  <files>.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/aws-deploy.md, .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md, .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md</files>
  <action>
Create `runbook/aws-deploy.md` (new file) documenting, in the numbered-step-with-`#`-
comments style of CLAUDE.md "Manual AWS build and deploy" (02-PATTERNS.md Analog 1):

1. The ECR image-promotion procedure (Pattern 3 / A1): read current `:dev` digests via
   `aws ecr describe-images` (read-only), re-tag in place as `edgartools-dev-warehouse:prod`
   / `edgartools-dev-mdm:prod` via `aws ecr batch-get-image` + `aws ecr put-image`
   (operator-executed at cutover, clearly marked state-changing), and capture the
   immutable `@sha256:` digest form for `--image-ref` / `--mdm-image-ref`. State
   explicitly that A1 (in-place `:prod` tag on the dev repos) is the only interpretation
   consistent with D-05 (same account, no separate prod ECR) + D-01/D-02.
2. The exact prod deploy command (Pattern 2) with image-ref/mdm-image-ref `<DIGEST>`
   placeholders, `--enable-mdm`, `--skip-build`, and `--edgar-identity-secret-arn
   "$(aws secretsmanager describe-secret --secret-id edgartools-prod-edgar-identity
   --query ARN --output text)"` freshly resolved (per LIVE-02 / matrix row 4). Document
   the script's parameter resolution order (CLI flag -> `infra/aws-prod-application.json`
   -> AWS discovery / `edgartools-prod-*` naming), the `--skip-build` REQUIRES
   `--image-ref` hard-fail, the 4 MDM secret ARNs `--enable-mdm` requires
   (`edgartools-prod/mdm/{postgres_dsn,neo4j,api_keys,snowflake}` — names only, no
   values), and the list of identifiers the script hard-fails on today because prod
   passive infra is not yet applied (cluster ARN, ECR URL, 3 buckets, edgar-identity
   secret ARN, 3 IAM role ARNs, subnet/SG) — this is the structural reason row 3 stays
   BLOCKED.
3. A "Pre-deploy ordering requirement" callout (Pitfall 3, matrix row 5) in the
   CLAUDE.md known-issue framing: the deploy script unconditionally runs
   `cleanup-ecr-images.sh --apply` before deploy, so digests MUST be re-resolved
   immediately before the deploy invocation, in the same session, AFTER cleanup runs —
   never reuse a digest captured earlier.
4. A note that the documented generated `infra/aws-prod-application.json` manifest, once
   it exists, is summarized only (top-level keys, state-machine names, image-ref format)
   per the Generated-JSON Summary Rule (matrix row 2).

Use `export VAR="<placeholder-description>"` / `<DIGEST>` placeholder style. No secret
values, no real ARNs.

Then append a "Required MDM Secret Names" subsection to `evidence/aws.md` and list
the same names in `01-LAUNCH-GATE-MATRIX.md` under `## Required Production Identifiers`
as unchecked/BLOCKED required identifiers:
- `edgartools-prod/mdm/postgres_dsn`
- `edgartools-prod/mdm/neo4j`
- `edgartools-prod/mdm/api_keys`
- `edgartools-prod/mdm/snowflake`
These are names only. Do not include ARNs, secret values, DSNs, or JSON bodies.

Then edit rows 1-5 of the `## Gate Matrix` table in `01-LAUNCH-GATE-MATRIX.md` (the
"AWS passive infrastructure outputs" through "ECR cleanup deleting in-flight image
digest mitigation" rows). Per the 02-RESEARCH.md disposition table: row 1 — refine
Required Fix / Required Rerun Proof to note the Pattern 1 plan validated the resource
count + output names (link `[evidence/aws.md](evidence/aws.md)`) and that the
`versions.tf` `~>` bug is a required fix; keep Status `BLOCKED` (no real backend/state
yet — do NOT invent a new status word). Rows 2-5 — keep `BLOCKED`, refine the Required
Fix cells to point at the documented runbook command/mitigation, keeping the existing
relative `[evidence/aws.md](evidence/aws.md)` link syntax. Also, in `## Required
Production Identifiers`, check the `- [x]` box for "Production AWS profile and AWS
account label" with a one-line note citing D-05 (account `077127448006`, IAM user
`cli-access`, same-account prefix-distinguished prod), and add/retain the four MDM
secret-name identifiers as unchecked/BLOCKED required items. Do NOT rewrite the table or
other sections.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform && RB=.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/aws-deploy.md && AW=.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md && MX=.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md && test -f "$RB" && grep -q "deploy-aws-application.sh" "$RB" && grep -q "enable-mdm" "$RB" && grep -q "edgar-identity-secret-arn" "$RB" && grep -q "skip-build" "$RB" && grep -q "edgartools-dev-warehouse" "$RB" && grep -q "edgartools-dev-mdm" "$RB" && grep -qi "cleanup" "$RB" && grep -qF '[x] Production AWS profile and AWS account label' "$MX" && for s in edgartools-prod/mdm/postgres_dsn edgartools-prod/mdm/neo4j edgartools-prod/mdm/api_keys edgartools-prod/mdm/snowflake; do grep -q "$s" "$RB" && grep -q "$s" "$AW" && grep -q "$s" "$MX"; done && echo OK</automated>
  </verify>
  <done>
`runbook/aws-deploy.md` exists (≥60 lines) documenting the ECR image-promotion
procedure (A1), the exact `deploy-aws-application.sh --env prod` command with
`--enable-mdm` / `--skip-build` / freshly-resolved `--edgar-identity-secret-arn` and
`<DIGEST>` placeholders, the parameter resolution order, the `--skip-build` requires
`--image-ref` hard-fail, the 4 MDM secret names, the prod-infra hard-fail identifier
list, the Pitfall 3 pre-deploy digest re-resolution callout, and the
generated-manifest summary rule — all non-secret. `01-LAUNCH-GATE-MATRIX.md` AWS rows
1-5 are refined (row 1 plan-validated text + versions.tf required-fix note; rows 2-5
documented-command/mitigation, all still `BLOCKED`), the "Production AWS profile and
AWS account label" identifier checkbox is checked `- [x]` with a D-05 note, and the
four MDM secret names appear as unchecked/BLOCKED required identifiers in both
`evidence/aws.md` and the matrix. No table structure or other rows rewritten.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → committed planning docs (`evidence/aws.md`, `runbook/aws-deploy.md`, matrix) | Command output (terraform plan, `aws ecr describe-images`, `aws secretsmanager describe-secret`) crosses into version-controlled documentation. Raw output may contain secret ARNs, account internals, or full resource bodies that must never be committed. |
| operator shell → `infra/terraform/accounts/prod/` source tree | Pattern 1 temporarily mutates `versions.tf` and writes `override.tf`/local state. These must be fully reverted; any leaked prod backend config, real tfstate, or committed source change is a process-integrity (ISO-01) and state-leak hazard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | `evidence/aws.md` + `runbook/aws-deploy.md` (secret/credential leakage) | mitigate | Apply the Generated-JSON Summary Rule (Phase 1 D-13/D-15): record only resource-add COUNT, output-NAME list, account label `077127448006`, digest FORMAT (`@sha256:` prefix presence), and pass/fail. Never paste raw `terraform plan` body, `aws ecr describe-images` JSON, full image digests, secret ARN values, or generated application JSON. Runbook uses `export VAR="<placeholder>"` / `<DIGEST>` placeholders only — no real ARNs/passwords/DSNs. Verified by the Phase-gate secret-safety review in 02-VALIDATION.md. |
| T-02-04b | Tampering | Pattern 1 local-backend override + `versions.tf` temp edit (state/credential leak into repo) | mitigate | Pattern 1 is strictly edit-then-revert: `git checkout -- versions.tf` plus `rm -rf override.tf .terraform .terraform.lock.hcl terraform.tfstate* /tmp/edgartools-prod-plan`, then `git status --short` for `infra/terraform/accounts/prod` MUST be empty (asserted in task 02-01-01's `<automated>` REVERT-CLEAN gate). No prod backend.hcl/tfvars are created; the local backend writes only to `/tmp`, which is removed. No `infra/` change is committed (Pattern 0 out of scope per ISO-01). |
| T-02-04c | Tampering / Process integrity | Out-of-scope source-tree commit during a document-and-validate phase | mitigate | `files_modified` is restricted to `.planning/...` docs only; the `infra/` edit is transient and reverted (T-02-04b). No `terraform apply`, no `deploy-aws-application.sh` execution, no `aws ecr put-image` — the `put-image` re-tag is documented as operator-executed at cutover, not run in Phase 2 (D-01). |
</threat_model>

<verification>
- Task 02-01-01 `<automated>` proves the Pattern 1 `terraform plan` ran and reverted
  cleanly (`REVERT-CLEAN rc=...`, `git status --short infra/terraform/accounts/prod`
  empty).
- Task 02-01-02 `<automated>` proves `runbook/aws-deploy.md` exists with the required
  command surfaces (`deploy-aws-application.sh`, `--enable-mdm`, `--skip-build`,
  `--edgar-identity-secret-arn`, both ECR repos, cleanup callout) and that the AWS
  profile/account identifier checkbox is checked in the matrix.
- Phase-gate manual review (per 02-VALIDATION.md): no DSNs/ARNs/tokens/raw
  plan-or-JSON bodies in `evidence/aws.md`, `runbook/aws-deploy.md`, or the matrix
  diff; all 5 AWS-side rows use only `BLOCKED`/`PASS` status values.
</verification>

<success_criteria>
- LIVE-02: an operator can read an exact, runnable `deploy-aws-application.sh --env
  prod` command (image-ref/mdm-image-ref placeholders, `--enable-mdm`, `--skip-build`,
  freshly-resolved `--edgar-identity-secret-arn`) plus the ECR `:dev`→`:prod`
  promotion + digest-capture procedure — with no Terraform-owned runtime commands and
  no secret values.
- `infra/terraform/accounts/prod/` plan validated read-only via Pattern 1 and fully
  reverted; resource-add count + output-name list + account label recorded in
  `evidence/aws.md`; the `versions.tf` `~>` bug recorded as a required-fix note.
- AWS-side matrix rows 1-5 reflect Phase 2 findings (row 1 plan-validated; rows 2-5
  documented-command/mitigation; all `BLOCKED` until real prod state exists) and the
  "Production AWS profile and AWS account label" identifier is checked.
- No production state changed; nothing under `infra/` committed; all evidence
  non-secret.
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/02-01-SUMMARY.md` when done
</output>
