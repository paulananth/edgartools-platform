---
context: phase
workstream: go-live
phase: 02-aws-and-snowflake-production-deployment-dry-run
task: bronze-reuse-runbook-handoff
total_tasks: 2
status: paused
last_updated: 2026-06-14T22:44:18.021Z
branch: workspace/go-live
head: 4316732
handoff_target: Claude
---

# Phase 2 Handoff To Claude

## Current State

Codex just added a production bronze reuse step at the user's request. The
intent is to reuse already downloaded dev bronze SEC artifacts for prod because
SEC filing artifacts in bronze are additive and immutable after capture.

No state-changing command was run. The worktree has uncommitted documentation
and handoff changes only.

## Completed Work

- Added prod bronze seeding instructions to `docs/runbook.md`.
- Added prod bronze seeding instructions to `runbook/aws-deploy.md`.
- Added a `Production bronze data reuse from dev bronze` launch gate row.
- Added the dev/prod bronze prefix evidence requirements to `evidence/aws.md`.
- Verified whitespace with `git diff --check`.
- Verified the edited gate-matrix rows still have the expected table shape.

## Remaining Work

- Claude should inspect the uncommitted diff.
- If the operator wants, commit the docs + handoff changes.
- Do not run `aws s3 sync`; prod storage is not applied yet.
- If continuing the broader Phase 2 workflow, resume the existing post-execution gates from the previous Phase 2 state.

## Critical Constraints

- AWS path only. Do not introduce non-AWS storage or deployment paths.
- Copy bronze only; do not copy dev warehouse, silver, gold, generated JSON, Terraform state, or secrets into prod.
- Do not use `--delete` on prod bronze.
- Keep loader defaults idempotent; `--force` is repair-only.
- Evidence should record prefixes, object count, and total bytes only; no full object listings.

## Next Action

```bash
cd /Users/aneenaananth/gsd-workspaces/go-live/edgartools-platform
git status --short --branch
cat .planning/active-workstream
git diff --stat
```

Then review the four documentation diffs:

- `docs/runbook.md`
- `.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/aws-deploy.md`
- `.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md`
- `.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md`
