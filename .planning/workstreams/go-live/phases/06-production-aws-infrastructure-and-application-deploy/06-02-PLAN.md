---
phase: 06-production-aws-infrastructure-and-application-deploy
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md
  - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
autonomous: true
requirements: [LIVE-05]
user_setup: []

must_haves:
  truths:
    - "The :dev warehouse and MDM images are promoted to :prod by registry-side re-tag within the same account (077127448006), and image digests are re-resolved AFTER the script's pre-deploy ECR cleanup runs, in the same session (matrix rows 16/17 mitigation)."
    - "The EDGAR identity secret ARN is freshly resolved immediately before deploy via describe-secret, never a cached/stale value (matrix row 16 mitigation)."
    - "deploy-aws-application.sh runs with --env prod, explicit --image-ref and --mdm-image-ref digest forms, --enable-mdm, --skip-build, and --edgar-identity-secret-arn (LIVE-05)."
    - "infra/aws-prod-application.json is produced and summarized non-secretly: file presence, top-level keys, state-machine name list, and image-ref @sha256: digest-vs-tag format only — never the JSON body (D-10, Generated-JSON Summary Rule)."
    - "Launch gate matrix rows 12, 14, 15, 16, 17 flip from BLOCKED toward PASS with concrete Phase 6 evidence citations; row 13 (dev->prod bronze sync) stays BLOCKED with only its prerequisite note updated since the sync is out of scope."
    - "No secrets, secret ARNs, Terraform state, image digest values, or generated JSON body appear in any committed file (D-10)."
  artifacts:
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md"
      provides: "Non-secret active-deploy evidence: ECR promotion (format only), deploy command, aws-prod-application.json summary"
      contains: "aws-prod-application.json"
    - path: ".planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md"
      provides: "Rows 12,14,15,16,17 updated with Phase 6 evidence; row 13 prerequisite-only update"
      contains: "PASS"
  key_links:
    - from: "06-01 applied infrastructure (cluster, buckets, roles, subnets, SGs, secret ARNs)"
      to: "deploy-aws-application.sh --env prod identifier resolution"
      via: "AWS API discovery of edgartools-prod-* resources"
      pattern: "deploy-aws-application.sh"
    - from: "deploy-aws-application.sh --env prod"
      to: "infra/aws-prod-application.json"
      via: "script writes generated manifest on success"
      pattern: "aws-prod-application.json"
---

<objective>
Promote the warehouse and MDM container images to `:prod`, run the active AWS application deploy (`deploy-aws-application.sh --env prod`) against the infrastructure applied in 06-01, capture a non-secret summary of the generated `infra/aws-prod-application.json`, and flip the launch gate matrix rows for active deploy from BLOCKED toward PASS.

Purpose: Completes the active half of Phase 6, satisfies LIVE-05, and produces the production application manifest that downstream phases and the final go/no-go audit depend on.

Output: A successful prod deploy, a non-secret `aws-prod-application.json` summary appended to the phase-01 `evidence/aws.md`, and updated `01-LAUNCH-GATE-MATRIX.md` rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-CONTEXT.md
@.planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-PATTERNS.md
@.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/aws-deploy.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote images to :prod and run the production active application deploy</name>
  <files>(state-changing AWS deploy; no repo source files modified)</files>
  <read_first>
    - .planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/aws-deploy.md sections 1, 2, 4 (command-of-record: ECR re-tag, deploy flags, pre-deploy cleanup ordering — do not re-derive)
    - .planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-CONTEXT.md decisions D-05, D-06, D-10 and the canonical_refs deployment-script section
    - .planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-01-SUMMARY.md (confirms passive infra is applied and the edgar-identity secret is populated — hard prerequisite)
  </read_first>
  <action>
    Verify 06-01 is complete (passive infra applied, edgartools-prod-edgar-identity populated) before any deploy step. Follow runbook/aws-deploy.md as the script-of-record; remove only the dry-run/reverted framing since these are now real.

    Promote images in-place within account 077127448006 (no separate prod ECR exists): resolve the current :dev digests for edgartools-dev-warehouse and edgartools-dev-mdm with aws ecr describe-images, then re-tag each as :prod via aws ecr batch-get-image plus aws ecr put-image (registry-side manifest copy, no docker pull/push).

    Because deploy-aws-application.sh unconditionally runs cleanup-ecr-images.sh --env prod --apply BEFORE deploy (retention = :dev/:prod plus 2 newest :sha-* per repo), an in-flight digest can be deleted if it falls outside the keep window. Mitigate per matrix row 17: re-resolve the warehouse and MDM image digests in the SAME shell session immediately before invoking deploy, AFTER cleanup has had its chance to run, and use the @sha256:<digest> immutable form for --image-ref / --mdm-image-ref. Never reuse a digest captured in an earlier session or from a stale manifest.

    Mitigate matrix row 16: freshly resolve the EDGAR identity secret ARN with aws secretsmanager describe-secret --secret-id edgartools-prod-edgar-identity --query ARN immediately before deploy; never reuse a cached ARN.

    Run bash infra/scripts/deploy-aws-application.sh with: --env prod, --image-ref (warehouse @sha256 digest URL), --mdm-image-ref (MDM @sha256 digest URL), --enable-mdm, --skip-build, and --edgar-identity-secret-arn (the freshly resolved ARN). --skip-build requires --image-ref (supplied). --enable-mdm requires all 4 MDM secret ARNs to resolve — note these are empty containers created in 06-01; container existence (not value) satisfies the ARN lookup, so the deploy is consistent with D-05 leaving them unpopulated. The deploy writes infra/aws-prod-application.json on success.
  </action>
  <acceptance_criteria>
    - CLI assertion: aws ecr describe-images shows a :prod tag on both edgartools-dev-warehouse and edgartools-dev-mdm.
    - Behavior assertion: the image digests passed to --image-ref/--mdm-image-ref were re-resolved in the same session after cleanup, and the edgar-identity ARN was resolved via describe-secret immediately before deploy (not reused).
    - CLI assertion: deploy-aws-application.sh --env prod exits 0 and infra/aws-prod-application.json exists afterward.
  </acceptance_criteria>
  <verify>
    <automated>test -f infra/aws-prod-application.json && aws ecr describe-images --region us-east-1 --repository-name edgartools-dev-warehouse --image-ids imageTag=prod --query 'imageDetails[0].imageDigest' --output text | grep -q sha256</automated>
  </verify>
  <done>Both images carry a :prod tag; deploy ran with explicit digest image refs, --enable-mdm, --skip-build, and a freshly resolved edgar-identity ARN; infra/aws-prod-application.json is written.</done>
</task>

<task type="auto">
  <name>Task 2: Summarize the manifest non-secretly and flip launch gate matrix rows 12-17</name>
  <files>.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md, .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md</files>
  <read_first>
    - infra/aws-prod-application.json (read for top-level keys and state_machines names — summarize only, never paste the body)
    - .planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-PATTERNS.md (Generated-JSON Summary Rule; matrix-row rewrite pattern for rows 12-17, including row 13 out-of-scope and rows 16/17 documentation-gate handling)
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md rows 12-17 and the row-27 PASS analog
    - .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md (append the deploy/manifest subsections to the Phase 6 section started in 06-01)
    - .planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-CONTEXT.md D-10
  </read_first>
  <action>
    Append to the Phase 6 section of the phase-01 evidence/aws.md (following the existing append-only command+result+bullets convention) two subsections: (1) the active deploy step (ECR promotion FORMAT only — confirm sha256:<64-hex> shape, never the digest value; deploy command with placeholder digests; result + exit-code bullet), and (2) the aws-prod-application.json summary per the Generated-JSON Summary Rule — file presence, top-level keys, the state_machines name list, and the --image-ref/--mdm-image-ref @sha256: digest-vs-mutable-tag format only. Do not paste the JSON body, any secret ARN, or any digest value (D-10).

    Update 01-LAUNCH-GATE-MATRIX.md preserving each row's Owner/Source column. Flip rows 12 (AWS passive infrastructure outputs — cite the real apply + versions.tf fix), 14 (Production AWS application manifest — cite the summarized aws-prod-application.json), and 15 (AWS active application deploy — cite the successful deploy with explicit image refs) from BLOCKED to PASS, citing the concrete Phase 6 evidence subsection and concrete proof (resource counts, exit codes, digest format) like the row-27 analog rather than a vague "done." Flip rows 16 (stale edgar-identity ARN mitigation) and 17 (ECR cleanup mitigation) to PASS on the documentation-gate model: the runbook ordering exists and the fresh-ARN / post-cleanup-digest-re-resolution was followed during the real deploy. Leave row 13 (dev->prod bronze sync) BLOCKED — the sync is out of scope this phase — updating only its prerequisite note to record that the prod bronze bucket now exists. The matrix's relative evidence links use the bare form (evidence/aws.md) since the matrix and evidence file share the phase-01 directory — preserve that convention.
  </action>
  <acceptance_criteria>
    - Source assertion: the phase-01 evidence/aws.md Phase 6 section contains an aws-prod-application.json summary listing top-level keys and state_machines names; grep confirms no JSON body, no secret ARN value, and no sha256 digest value is pasted.
    - Source assertion: 01-LAUNCH-GATE-MATRIX.md rows 12, 14, 15, 16, 17 show PASS with a Phase 6 evidence citation; row 13 still shows BLOCKED with an updated prerequisite note.
    - Behavior assertion: each flipped row cites concrete proof (counts / exit code / digest format), not a subjective claim.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'aws-prod-application.json' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/evidence/aws.md && grep -A1 'Production AWS application manifest' .planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md | grep -q PASS</automated>
  </verify>
  <done>The phase-01 evidence/aws.md has a non-secret deploy + manifest summary; matrix rows 12/14/15/16/17 are PASS with concrete Phase 6 citations; row 13 stays BLOCKED with an updated note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → ECR | re-tagging images to :prod changes what the production deploy pulls. |
| deploy script → AWS control plane | deploy-aws-application.sh creates/updates ECS task definitions and Step Functions state machines in production. |
| generated manifest → committed evidence | infra/aws-prod-application.json contains ARNs and resource identifiers that must not be committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | ECR pre-deploy cleanup deletes in-flight image manifest | mitigate | Re-resolve image digests in the same session AFTER cleanup, immediately before deploy, using @sha256 immutable form (matrix row 17). |
| T-06-07 | Spoofing | stale/wrong edgar-identity secret ARN bound at deploy | mitigate | Freshly resolve the ARN via describe-secret immediately before deploy; never reuse a cached value (matrix row 16). |
| T-06-08 | Information Disclosure | infra/aws-prod-application.json summarized into committed evidence | mitigate | Generated-JSON Summary Rule: file presence, top-level keys, state_machines names, image-ref format only — no JSON body, no ARN, no digest value (D-10). |
| T-06-09 | Elevation | --enable-mdm wiring against empty MDM secret containers | accept | Container existence (not value) satisfies ARN lookup; values remain unpopulated and owned by Phase 8 / MDM-02 (D-05). No MDM value is written here. |
| T-06-SC | Tampering | npm/pip/cargo package installs | accept | N/A — this plan installs no packages; it runs aws-cli and the existing deploy script only. No package-legitimacy gate applicable. |
</threat_model>

<verification>
- Both images carry a :prod tag; deploy ran with explicit digest image refs and a freshly resolved edgar-identity ARN.
- infra/aws-prod-application.json exists and is summarized non-secretly in the phase-01 evidence/aws.md.
- Matrix rows 12/14/15/16/17 are PASS with concrete citations; row 13 stays BLOCKED (out of scope) with an updated prerequisite note.
- No secrets, ARNs, digests, Terraform state, or JSON body committed (D-10).
</verification>

<success_criteria>
- LIVE-05 satisfied: active AWS application deployed with explicit image references; aws-prod-application.json summary evidence produced without committing sensitive generated JSON.
- D-05, D-06, D-10 honored; matrix rows 16/17 mitigations demonstrably followed during the real deploy.
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-02-SUMMARY.md` when done.
</output>
