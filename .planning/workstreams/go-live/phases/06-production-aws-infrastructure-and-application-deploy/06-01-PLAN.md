---
phase: 06-production-aws-infrastructure-and-application-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/terraform/accounts/prod/versions.tf
  - infra/terraform/accounts/prod/backend.hcl
  - infra/terraform/accounts/prod/terraform.tfvars
  - .planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/evidence/aws.md
autonomous: false
requirements: [LIVE-04]
user_setup: []

must_haves:
  truths:
    - "The edgartools-prod-tfstate S3 bucket exists with versioning, SSE encryption, and a public-access block (all four flags true) before terraform init runs (D-01)."
    - "infra/terraform/accounts/prod/versions.tf required_version reads >= 1.14.7 (not ~> 1.14.7) (D-09/D-09a)."
    - "D-02 is satisfied with no edit: use_lockfile = true is already present in versions.tf backend \"s3\" block; backend.hcl is intentionally a 4-line copy of the .example with no use_lockfile line."
    - "A terraform plan is saved to tfplan and its add/change/destroy counts and resource list are shown to the user, who gives explicit go-ahead before any apply (D-03)."
    - "terraform apply runs against the saved tfplan file, never a fresh re-plan (D-04)."
    - "All 5 secret containers are created as fresh empty shells with no edgar_identity_secret_arn or MDM override variables (D-05)."
    - "The edgartools-prod-edgar-identity secret value is populated via aws secretsmanager put-secret-value with the EDGAR_IDENTITY user-agent string; no MDM secret gets a put-secret-value call (D-06)."
    - "terraform.tfvars sets pipeline_notifications_enabled = true and pipeline_failure_subscriber_email = thepaulananth@gmail.com (D-07/D-08)."
    - "No secrets, Terraform state, secret ARNs/values, or the EDGAR_IDENTITY string appear in any committed file; evidence is non-secret summary only (D-10)."
  artifacts:
    - path: "infra/terraform/accounts/prod/versions.tf"
      provides: "Terraform version constraint fixed to >= 1.14.7"
      contains: 'required_version = ">= 1.14.7"'
    - path: ".planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/evidence/aws.md"
      provides: "Non-secret Phase 6 passive-apply evidence section (bucket bootstrap, terraform plan/apply counts, output names only, edgar-identity value-set confirmation)"
      contains: "Phase 6"
  key_links:
    - from: "edgartools-prod-tfstate bucket (created by CLI)"
      to: "terraform init -backend-config=backend.hcl"
      via: "S3 state backend"
      pattern: "edgartools-prod-tfstate"
    - from: "terraform apply tfplan"
      to: "edgartools-prod-edgar-identity secret container"
      via: "Terraform creates empty shell, then put-secret-value populates it"
      pattern: "edgartools-prod-edgar-identity"
---

<objective>
Bootstrap the production Terraform S3 state backend, fix the versions.tf constraint bug, run the first real production `terraform apply` for `infra/terraform/accounts/prod/` behind an explicit human approval gate, populate the `edgartools-prod-edgar-identity` secret value, and capture non-secret passive-infrastructure evidence.

Purpose: This is the first state-changing production action in the entire go-live workstream. It remediates Blocker 1 (prod AWS infrastructure not yet applied) from the v1.5 go/no-go packet and satisfies LIVE-04.

Output: Live `edgartools-prod-tfstate` bucket, applied prod passive infrastructure (VPC, subnets, security groups, S3/KMS, ECR, ECS cluster/logs, 5 empty secret containers), a populated EDGAR identity secret, and a non-secret evidence section in `evidence/aws.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-CONTEXT.md
@.planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-PATTERNS.md
@.planning/workstreams/go-live/phases/02-aws-and-snowflake-production-deployment-dry-run/runbook/aws-deploy.md
@.planning/workstreams/go-live/phases/01-production-readiness-inventory-and-launch-gate-contract/01-LAUNCH-GATE-MATRIX.md
@infra/terraform/accounts/prod/versions.tf
@infra/terraform/accounts/dev/versions.tf
@infra/terraform/accounts/prod/backend.hcl.example
@infra/terraform/accounts/prod/terraform.tfvars.example
@infra/terraform/accounts/prod/variables.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap tfstate backend, fix versions.tf, configure backend/tfvars, and produce a saved terraform plan</name>
  <files>infra/terraform/accounts/prod/versions.tf, infra/terraform/accounts/prod/backend.hcl, infra/terraform/accounts/prod/terraform.tfvars</files>
  <read_first>
    - infra/terraform/accounts/prod/versions.tf (the file being modified — confirm line 2 reads required_version = "~> 1.14.7")
    - infra/terraform/accounts/dev/versions.tf (the correct analog — line 2 already reads required_version = ">= 1.14.7")
    - infra/terraform/accounts/prod/backend.hcl.example (4-line template to copy verbatim)
    - infra/terraform/accounts/prod/terraform.tfvars.example (tfvars template)
    - infra/terraform/accounts/prod/variables.tf (declarations for pipeline_notifications_enabled and pipeline_failure_subscriber_email)
    - .planning/.../06-CONTEXT.md decisions D-01, D-02, D-05, D-07, D-08, D-09, D-09a
    - .planning/.../02-.../runbook/aws-deploy.md (command-of-record; do not re-derive command shapes)
  </read_first>
  <action>
    Edit infra/terraform/accounts/prod/versions.tf changing only line 2 from required_version = "~> 1.14.7" to required_version = ">= 1.14.7" (D-09a). Leave the provider pin version = "= 6.39.0" and the backend "s3" block with use_lockfile = true untouched — use_lockfile is already present, so D-02 needs no edit anywhere.

    Bootstrap the Terraform state bucket BEFORE terraform init, using the runbook-of-record command shapes (no DynamoDB lock table; S3-native locking via the existing use_lockfile). Run aws s3api create-bucket for bucket edgartools-prod-tfstate in region us-east-1, then put-bucket-versioning with status Enabled, then put-bucket-encryption with SSE (AES256 server-side encryption), then put-public-access-block with all four flags true (BlockPublicAcls, IgnorePublicAcls, BlockPublicPolicy, RestrictPublicBuckets) since this bucket holds sensitive Terraform state and is created out-of-band by raw CLI (Terraform does not manage or harden it later). Confirm creation with aws s3api head-bucket on edgartools-prod-tfstate returning success.

    Create infra/terraform/accounts/prod/backend.hcl as a 4-line verbatim copy of backend.hcl.example: bucket = edgartools-prod-tfstate, key = accounts/prod/terraform.tfstate, region = us-east-1, encrypt = true. Do NOT add a fifth use_lockfile line — that key lives in versions.tf and D-02 (corrected) confirms backend.hcl stays at 4 lines. This file is gitignored / never committed.

    Create infra/terraform/accounts/prod/terraform.tfvars from the .example, leaving edgar_identity_secret_arn and all MDM override variables commented out / absent (fresh empty-shell creation per D-05), and adding two lines: pipeline_notifications_enabled = true (D-07) and pipeline_failure_subscriber_email = "thepaulananth@gmail.com" (D-08). This file is gitignored / never committed.

    From infra/terraform/accounts/prod/, run terraform init -backend-config=backend.hcl, then terraform plan -out=tfplan. Capture the add/change/destroy counts and the resource-type list (names only, no output values) for presentation in Task 2.
  </action>
  <acceptance_criteria>
    - Source assertion: grep -n 'required_version' infra/terraform/accounts/prod/versions.tf shows exactly >= 1.14.7 and no occurrence of ~> 1.14.7.
    - CLI assertion: aws s3api head-bucket --bucket edgartools-prod-tfstate exits 0; aws s3api get-bucket-versioning shows Status Enabled; aws s3api get-public-access-block shows all four flags true.
    - File assertion: backend.hcl exists and is exactly 4 lines (bucket/key/region/encrypt) with no use_lockfile line.
    - File assertion: terraform.tfvars contains pipeline_notifications_enabled = true and pipeline_failure_subscriber_email = "thepaulananth@gmail.com", and has no uncommented edgar_identity_secret_arn or MDM override line.
    - CLI assertion: terraform init succeeds against the S3 backend; terraform plan -out=tfplan writes a tfplan file and reports a purely-additive count (0 to destroy).
  </acceptance_criteria>
  <verify>
    <automated>grep -c '>= 1.14.7' infra/terraform/accounts/prod/versions.tf | grep -qv '^0$' && aws s3api head-bucket --bucket edgartools-prod-tfstate && test -f infra/terraform/accounts/prod/tfplan</automated>
  </verify>
  <done>tfstate bucket exists (versioned, SSE, public-access-blocked); versions.tf reads >= 1.14.7; backend.hcl is a 4-line copy; terraform.tfvars has the two pipeline lines and no overrides; terraform init succeeded and tfplan is saved.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: First-apply approval gate — present saved plan and wait for explicit go-ahead (D-03/D-04)</name>
  <read_first>
    - .planning/.../06-CONTEXT.md decisions D-03 and D-04
    - infra/terraform/accounts/prod/tfplan (the saved plan from Task 1 — the exact artifact that will be applied)
  </read_first>
  <decision>Approve the first-ever real production terraform apply against infra/terraform/accounts/prod/ using the saved tfplan file.</decision>
  <context>This is the project's first state-changing production infrastructure action. The plan was saved with terraform plan -out=tfplan in Task 1. Per D-04, apply will use that exact saved file (terraform apply tfplan), so what is approved here is byte-identical to what executes — there is no drift window. Present to the user: the add/change/destroy counts (expected purely additive, 0 to destroy), and the resource-type list captured in Task 1 (VPC, subnets, security groups, S3/KMS, ECR, ECS cluster/logs, 5 empty secret containers). Do not proceed to apply without an explicit go-ahead message.</context>
  <options>
    <option id="approve">
      <name>Approve apply</name>
      <pros>Unblocks LIVE-04, prod passive infrastructure, and all downstream phases (7/8 depend on these outputs/containers).</pros>
      <cons>First real production state change; creates billable AWS resources.</cons>
    </option>
    <option id="hold">
      <name>Hold / revise</name>
      <pros>Allows the operator to inspect the plan further or adjust tfvars before any state change.</pros>
      <cons>LIVE-04 stays blocked; downstream phases remain gated.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Behavior assertion: the executor has presented add/change/destroy counts and the resource-type list to the user before this gate resolves.
    - Behavior assertion: Task 3 (apply) does not begin until the user sends an explicit go-ahead message; a hold response stops the plan without applying.
  </acceptance_criteria>
  <resume-signal>Type "approved" to run terraform apply tfplan, or describe the hold/revision needed.</resume-signal>
  <done>User has explicitly approved (resume) or held. No apply runs without explicit approval.</done>
</task>

<task type="auto">
  <name>Task 3: Apply saved plan, populate EDGAR identity secret, and capture non-secret evidence</name>
  <files>.planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/evidence/aws.md</files>
  <read_first>
    - infra/terraform/accounts/prod/tfplan (the approved saved plan — apply this exact file, do not re-plan)
    - infra/terraform/accounts/prod/outputs.tf (the 22 output names to capture by name only)
    - .planning/.../06-CONTEXT.md decisions D-04, D-05, D-06, D-10
    - .planning/.../06-PATTERNS.md evidence/aws.md section (append-only command+result+bullets convention; Generated-JSON / non-secret summary discipline)
    - CLAUDE.md "Image management" — the EDGAR_IDENTITY value shape "EdgarTools Platform <email>"
    - .planning/.../evidence/aws.md (existing file — append a new section, do not duplicate the header/preamble)
  </read_first>
  <action>
    Run terraform apply tfplan from infra/terraform/accounts/prod/ against the saved plan file approved in Task 2 — never a fresh terraform apply that re-plans (D-04). This creates the passive infrastructure and all 5 secret containers as fresh empty shells (D-05): edgartools-prod-edgar-identity plus the 4 MDM containers (edgartools-prod/mdm/postgres_dsn, /neo4j, /api_keys, /snowflake).

    Immediately after apply, populate ONLY the EDGAR identity secret: run aws secretsmanager put-secret-value with secret-id edgartools-prod-edgar-identity and the secret-string "EdgarTools Platform thepaulananth@gmail.com" (the SEC EDGAR User-Agent header value, same shape as the dev env var in CLAUDE.md). This value is non-sensitive (no password/token/key) but must NEVER be pasted into any committed file (D-06/D-10). Do NOT call put-secret-value on any of the 4 MDM containers — they remain empty shells owned by Phase 8 / MDM-02 (D-06 guardrail).

    Append a new "## Phase 6 Production Apply" section to evidence/aws.md following the existing append-only convention (do not modify the file header/preamble). Include: a bucket-bootstrap subsection (create-bucket/versioning/encryption/public-access-block/head-bucket commands and a succeeded result with 2-3 non-secret bullets), a terraform plan/apply subsection reporting the REAL non-reverted add/change/destroy counts and the 22 output NAMES only from outputs.tf (never output values — extend the non-secret-summary discipline to Terraform outputs), a one-line "EDGAR_IDENTITY value set, not pasted" confirmation bullet for the put-secret-value step, and a closing status-pointer bullet. Paste no secret ARNs, no Terraform state, no output values, and not the EDGAR_IDENTITY string (D-10).
  </action>
  <acceptance_criteria>
    - CLI assertion: terraform apply tfplan completes successfully; terraform output (names only) lists the 22 documented output names.
    - CLI assertion: aws secretsmanager describe-secret --secret-id edgartools-prod-edgar-identity resolves an ARN, and the secret reports a current version (value populated); each of the 4 MDM containers exists but has no put-secret-value applied in this plan.
    - Source assertion: evidence/aws.md contains a "Phase 6" section with bucket-bootstrap, plan/apply counts, and output names only; grep confirms no EDGAR_IDENTITY value string, no secret ARN value, and no Terraform output value appears in the committed evidence.
  </acceptance_criteria>
  <verify>
    <automated>cd infra/terraform/accounts/prod && terraform output >/dev/null 2>&1 && aws secretsmanager describe-secret --secret-id edgartools-prod-edgar-identity --query ARN --output text | grep -q edgartools-prod-edgar-identity</automated>
  </verify>
  <done>Prod passive infrastructure applied from the approved tfplan; 5 secret containers exist; only edgartools-prod-edgar-identity is populated; non-secret Phase 6 evidence section appended to evidence/aws.md with output names only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → AWS control plane | Raw aws-cli and terraform commands create real, billable, hard-to-reverse production resources. |
| Terraform state → S3 backend | tfstate contains sensitive resource metadata; stored in a CLI-bootstrapped bucket Terraform does not manage. |
| operator → Secrets Manager | put-secret-value writes a secret container value directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | edgartools-prod-tfstate S3 bucket (Terraform state) | mitigate | Task 1 applies put-public-access-block with all four flags true, plus SSE encryption and versioning, before terraform init; bucket is private with no public policy path. |
| T-06-02 | Tampering / Elevation | terraform apply blast radius (first prod apply) | mitigate | D-03 explicit human approval gate (Task 2) + D-04 apply from saved tfplan only (no drift between review and apply); plan expected purely additive (0 to destroy). |
| T-06-03 | Information Disclosure | edgartools-prod-edgar-identity put-secret-value | mitigate | Value is a non-sensitive public SEC User-Agent string; set via CLI only, never pasted into any committed file (D-06/D-10); evidence records "value set, not pasted" only. |
| T-06-04 | Information Disclosure | evidence/aws.md committed artifact | mitigate | Non-secret-summary discipline: output names only (no values), no secret ARNs, no Terraform state, no EDGAR_IDENTITY string (D-10). |
| T-06-05 | Spoofing / over-grant | 4 MDM secret containers | accept | Created as empty shells with no values (D-05); no value population or grant in this phase — ownership deferred to Phase 8 / MDM-02. |
| T-06-SC | Tampering | npm/pip/cargo package installs | accept | N/A — this plan installs no packages; it runs only terraform and aws-cli against pinned provider version = 6.39.0. No package-legitimacy gate applicable. |
</threat_model>

<verification>
- terraform apply completed from the approved saved tfplan (D-04); 0 resources destroyed.
- edgartools-prod-tfstate bucket is versioned, SSE-encrypted, and public-access-blocked.
- versions.tf required_version = ">= 1.14.7".
- 5 secret containers exist; only edgartools-prod-edgar-identity has a value.
- evidence/aws.md Phase 6 section is non-secret summary only.
</verification>

<success_criteria>
- LIVE-04 satisfied: prod AWS passive infrastructure applied and required Terraform outputs captured as non-secret evidence (output names only).
- All of D-01, D-02 (no-op), D-03, D-04, D-05, D-06, D-07, D-08, D-09/D-09a, D-10 are honored.
- Blocker 1 from the v1.5 go/no-go packet is remediated.
</success_criteria>

<output>
Create `.planning/workstreams/go-live/phases/06-production-aws-infrastructure-and-application-deploy/06-01-SUMMARY.md` when done.
</output>
