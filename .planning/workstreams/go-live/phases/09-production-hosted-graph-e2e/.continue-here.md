---
context: phase
phase: 09-production-hosted-graph-e2e
task: Blocker 4 production retry
status: pass
last_updated: 2026-06-25T04:20:32.130Z
---

# Blocker 4 PASS

Blocker 4 is resolved. The production `bronze_seed_silver_gold` chain
completed end-to-end on 2026-06-25 after running from the PR #95-capable
warehouse image and the fixed MDM image.

Final execution:

- Execution name: `bronze-seed-silver-gold-1782351277`
- Input: `{"batch_size": 100}`
- Started: 2026-06-25T01:34:39.738Z
- Succeeded: 2026-06-25T04:20:32.130Z
- Parent status: `SUCCEEDED`
- BatchSilver map run: 81/81 children succeeded, 0 failed, 0 aborted

Validated live application state:

- Warehouse task definition: `edgartools-prod-medium:19`
- Warehouse image digest: `sha256:036b7487...`
- MDM task definition: `edgartools-prod-mdm-medium:19`
- MDM image digest: `sha256:50af1f66...`
- BatchSilver command includes `--artifact-policy skip --parser-policy skip`.
- BatchSilver `MaxConcurrency=2` is the validated production setting.
- The later source edit that raises BatchSilver concurrency to 5 was not part
  of this pass and should not be treated as production-proven.

## Required Log Evidence

Do not reopen Blocker 4 unless these facts become invalid because a new image,
task definition, or state-machine definition is deployed.

| Gate | Result |
| --- | --- |
| SeedFromBronze discovers prod bronze CIKs | PASS: `seed_bronze_batches_completed` reported `cik_count=8006`, `batch_count=81`. |
| BatchSilver reuses bronze | PASS: child commands used `--artifact-policy skip --parser-policy skip`; logs show monolith/shard fallback hydration from existing warehouse silver. |
| No live SEC refetch | PASS: CloudWatch search from execution start found 0 `sec_pull_started` events. |
| No parser fanout during BatchSilver | PASS: CloudWatch search from execution start found 0 `filing_artifact_pipeline_started` events. |
| BatchSilver speed | PASS: 81 `bronze_capture_completed` events; min 1.240s, median 12.514s, max 30.742s, average 14.544s. |
| MDM run | PASS: `mdm run` completed exit 0; `companies_processed=8006`; optional ADV/ownership-derived entities were 0 because parser fanout was intentionally skipped. |
| MDM backfill | PASS: `backfill-relationships` completed exit 0. |
| MDM graph sync | PASS: `sync-graph` completed exit 0; 10 graph nodes materialized/synced, 0 edges. |
| MDM verify graph | PASS: `verify-graph` completed exit 0; Native App checks, BFS, WCC, node parity, and relationship parity all `ok`. |
| Gold refresh | PASS: `gold_refresh` completed exit 0; 22 gold tables built and Snowflake export manifest written. |

Gold refresh wrote the expected production-scale company and filing exports:

- `dim_company=8006`
- `dim_filing=4017296`
- `fact_filing_activity=4017296`
- Snowflake export manifest:
  `workflow_name=gold_refresh/business_date=2026-06-25/run_id=bronze-seed-silver-gold-1782351277/run_manifest.json`

## What Changed During The Retry

- Used the PR #95 warehouse optimization path in the production warehouse
  image. This is the bulk filings merge improvement that made BatchSilver
  practical on the copied bronze set.
- Kept BatchSilver pinned to the verified no-refetch mode:
  `--artifact-policy skip --parser-policy skip`.
- Rebuilt and deployed a separate MDM image so MDM tasks no longer inherit the
  warehouse-only runtime image by default.
- Relaxed MDM bulk-run preflight so `mdm run --entity-type all` requires the
  parser-backed optional source tables to exist, but does not require ownership
  and ADV rows when parser fanout was intentionally skipped.

## Superseded Findings

The older notes in this phase described these blockers:

- Legacy `edgartools-prod/mdm/neo4j` and `edgartools-prod/mdm/api_keys` task
  secret injection.
- Prod bucket-name mismatch and empty prod warehouse path.
- Missing `bronze_seed_silver_gold` path for a cold start from existing bronze.
- BatchSilver SEC refetch risk.
- MDM image/runtime mismatch.
- Empty optional parser tables blocking `mdm run --entity-type all`.

Those findings are now resolved for the validated production path above. The
standing constraints remain:

- Never populate `edgartools-prod/mdm/neo4j`.
- Never populate `edgartools-prod/mdm/api_keys`.
- Do not commit generated `infra/aws-prod-application.json`.
- Always use AWS region `us-east-1`.

## Next Work

Blocker 5 is now the active go-live blocker: run production dashboard UAT
against a production or production-like read-only configuration and capture the
evidence. Do not rerun Blocker 4 unless the warehouse image, MDM image, task
definitions, or `bronze_seed_silver_gold` definition change.
