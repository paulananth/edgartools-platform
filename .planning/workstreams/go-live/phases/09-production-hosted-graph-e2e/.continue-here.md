---
context: phase
phase: 09-production-hosted-graph-e2e
task: Blocker 4 fix — root cause found for SEC re-fetch bug, fix in PR #91, NOT yet deployed/retested
status: in_progress
last_updated: 2026-06-23T23:10:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: `deploy-aws-application.sh --enable-mdm` silently defaults MDM ECS task
  definitions to the *warehouse* image ref when `--mdm-image-ref` is omitted (MDM needs
  `.[s3,mdm-runtime]` deps, warehouse only has `.[s3]`). Hit this live against prod this
  session. Structural mitigation now in place: `infra/scripts/go-live.sh` (PR #90,
  `claude/go-live-one-click-fixes`, not yet merged) always publishes both images and
  always passes `--mdm-image-ref` + `--enable-mdm` explicitly. If you redeploy prod
  manually (not via go-live.sh), you MUST pass `--mdm-image-ref` explicitly every time —
  never omit it.
- [ ] CONSTRAINT: `cleanup-ecr-images.sh` only protects image digests referenced by
  *currently active* ECS task definitions at the moment it runs. If a task definition is
  pointing at a digest that ISN'T yet "active" in the sense the cleanup script checks
  (e.g. you just redeployed with a wrong ref, haven't fixed it yet), running cleanup can
  delete the digest you actually need. Sequence matters: get the correct image ref
  deployed and verified via `describe-task-definition` BEFORE running ECR cleanup, not
  after.
- [ ] CONSTRAINT: `bootstrap-batch`'s idempotency check only consulted the silver
  checkpoint table, NOT S3 bronze existence. On a fresh silver DB (any environment that
  never locally processed a CIK before — e.g. prod, which only ever had bronze synced in
  from dev) this means EVERY CIK triggers a live SEC fetch even though the bronze JSON
  already exists in S3. `--artifact-policy skip` does NOT prevent this — it only gates
  ownership-XML/ADV attachment fetches, not the core submissions.json/pagination capture.
  Confirmed live: the `bronze_seed_silver_gold` execution this session emitted thousands
  of real `sec_pull_started`/`sec_pull_completed` events to `data.sec.gov` during
  `BatchSilver`, at ~1 batch/hour (would have taken ~80h for all 81 batches). Execution was
  stopped (`aws stepfunctions stop-execution`) once this was confirmed in CloudWatch logs.
  Fix is in **PR #91** (`claude/bootstrap-batch-existing-bronze-reuse`, NOT yet merged,
  NOT yet deployed, NOT yet retested live): adds a CIK-prefix glob fallback
  (`StorageLocation.find_existing`, `_read_bronze_by_glob_if_present`) so bronze already in
  S3 is found and reused even with no local silver checkpoint. 426 tests pass locally
  (194 unit/application + new regression tests + 424 broader suite). **Do not re-run
  `bronze_seed_silver_gold` against prod until this PR is merged AND a new warehouse image
  is built from it AND prod is redeployed onto that image** — re-running with the current
  prod image will reproduce the exact same SEC-call bug.

**Do not proceed until all three boxes are checked.**

# UPDATE (this session, later, 2026-06-23 23:10 UTC): root cause found, fix written, NOT yet live-verified

After the `bronze_seed_silver_gold` execution (`bronze-seed-silver-gold-1782251900`) ran
~1 hour and completed only 1/81 `BatchSilver` batches, inspected the running ECS task's
CloudWatch logs directly (`/aws/ecs/edgartools-prod-warehouse`) instead of trusting the
Step Function's `RUNNING` status — found `bronze_capture_started` /
`sec_pull_started`/`sec_pull_completed` events firing for every CIK in the batch, hitting
`data.sec.gov/submissions/CIK....json` live, despite those CIKs already having bronze in
S3 from the earlier `aws s3 sync` (145,908 objects). This violates the explicit "zero
`sec_pull_started`" pass bar for Blocker 4 — **stopped the execution**
(`aws stepfunctions stop-execution`, now `ABORTED`) rather than let it run ~80 hours making
live SEC calls for no benefit.

Root-caused via a research agent + direct code verification (see BLOCKING CONSTRAINT
above for the mechanism). Wrote the fix, added regression tests, ran the full local suite
(424 passed), and opened **PR #91**:
`https://github.com/paulananth/edgartools-platform/pull/91`
(`claude/bootstrap-batch-existing-bronze-reuse` → `main`).

## NOT done yet — this is exactly where to resume
1. PR #91 not reviewed/merged.
2. No warehouse image rebuilt from the PR #91 fix.
3. Prod NOT redeployed onto that new image (prod is currently still running the rev-11
   image from before this fix — i.e. the SEC-re-fetch bug is still live in prod right now).
4. `bronze_seed_silver_gold` has NOT been re-run since the fix. The `ABORTED` execution
   from this session proved the bug exists; it does NOT prove the fix works — that's the
   next live test.
5. PR #90 (`go-live.sh` one-click fixes) also still unmerged — independent of #91, can
   land in either order.

## Next steps on resume — DO THIS FIRST
1. Review/merge PR #91 (and #90, independently).
2. Rebuild + push the warehouse image from the new `main` HEAD (after merge), per
   CLAUDE.md's Colima recipe or CI.
3. Redeploy prod onto that image — **always pass `--mdm-image-ref` explicitly** (see first
   BLOCKING CONSTRAINT), verify via `describe-task-definition` before proceeding.
4. Start a fresh `bronze_seed_silver_gold` execution:
   ```bash
   aws stepfunctions start-execution --region us-east-1 \
     --state-machine-arn arn:aws:states:us-east-1:077127448006:stateMachine:edgartools-prod-bronze-seed-silver-gold \
     --input '{"batch_size": 100}'
   ```
5. This time, check CloudWatch logs for the FIRST batch's ECS task within minutes of
   start (don't wait an hour) — confirm `bronze_capture_started`/`sec_pull_started` do
   NOT appear for these CIKs, and that the batch completes fast (no live SEC round-trips
   means this should take seconds, not ~1 hour, per batch of 100).
6. Only if that holds across all 81 batches AND `MdmRun`/`MdmBackfill`/`MdmSync`/
   `MdmVerify`/`GoldRefresh` all reach `SUCCEEDED`: update this file, `evidence/aws-mdm-e2e.md`,
   `STATE.md` Blocker 4 (flip to PASS), `TODOS.md` (5-whys for this bug, the MDM-image bug,
   and the ECR-cleanup-ordering hazard).
7. If the fix doesn't fully eliminate SEC calls (e.g. some CIKs in the batch genuinely
   never had bronze synced from dev, or the glob pattern misses an edge case), that's new
   information — investigate via the same direct-log-inspection method, don't assume.

---

# UPDATE (this session, 2026-06-23): prod redeployed correctly, bronze_seed_silver_gold execution IN PROGRESS — do not flip Blocker 4 yet

## What happened, in order
1. Picked up Codex's handoff (`codex/go-live-wizard-handoff` branch, commit `ebb9018`):
   main had PR #89 merged (`5d3141b`), warehouse image built from that commit
   (`sha-5d3141b4ca2b`, digest `bae6d354...`). Next step per handoff: redeploy prod with
   that image, then rerun `bronze_seed_silver_gold`.
2. Confirmed via `describe-task-definition` that prod's `edgartools-prod-medium` was still
   on an OLD digest (`23ce46b6...`) — redeploy genuinely hadn't happened yet.
3. **First redeploy attempt had a bug** (see BLOCKING CONSTRAINT above): omitted
   `--mdm-image-ref`, which silently pointed MDM task defs at the warehouse image. Caught
   it via `describe-task-definition` before triggering anything — did NOT let an MDM ECS
   task run against the wrong image.
4. Re-ran the redeploy with `--mdm-image-ref` pinned to the previously-deployed MDM digest
   (`ab5197f2...`). This attempt hit a transient `Could not connect to the endpoint URL`
   AWS network error mid-deploy (exit code reported 0 despite the errors in the log body —
   another instance of "never trust the exit code, read the actual log"). Retried once
   connectivity was confirmed via `aws sts get-caller-identity`; the retry went through
   clean. Prod task defs at **revision 10**, warehouse + MDM images both correct, confirmed
   via `describe-task-definition`.
5. Started the live execution:
   `arn:aws:states:us-east-1:077127448006:execution:edgartools-prod-bronze-seed-silver-gold:bronze-seed-silver-gold-1782251900`
   at 17:58 ET.
6. **Ran ECR cleanup** (`infra/scripts/cleanup-ecr-images.sh --env dev --apply`) to clear
   accumulated stale images (65 images / 15.1 GB freed from `edgartools-dev-warehouse` and
   `edgartools-dev-mdm`). This deleted the MDM digest (`ab5197f2...`) that revision 10 was
   using — it wasn't tagged with anything the script's active-task-def scan recognized as
   protected at the time of the scan (see second BLOCKING CONSTRAINT). Caught this
   immediately by re-checking `describe-task-definition` after cleanup and finding the
   digest gone.
7. Found that CI (`build-images.yml`) had already built a *separate, valid, current* MDM
   image from the same `5d3141b` commit: `edgartools-dev-mdm:5d3141b` /
   `edgartools-dev-mdm:dev`, digest `a069ccab93d8addc295f1546ceda3f15a8c96c12d4c91f32684df1eb037ec3e3`
   (confirmed distinct from the warehouse image's digest, pushed at the same timestamp).
   Redeployed prod onto this digest — **revision 11**, confirmed correct via
   `describe-task-definition`. This happened BEFORE the execution reached `MdmRun`, so no
   MDM ECS task ever attempted to pull the deleted digest.
8. Opened **PR #90** (`claude/go-live-one-click-fixes` — separate branch from Codex's
   `codex/go-live-wizard-handoff`, per the HARD branch-ownership rule; this branch has
   ZERO commits from Codex) encoding both bugs found above into `infra/scripts/go-live.sh`:
   the wizard now always publishes + pins both images, and a new
   `AWS: bronze_seed_silver_gold (one-click data refresh)` stage starts the Step Function
   and polls to a terminal state as part of the one-click path. **NOT YET MERGED.**

## Live execution status as of this handoff (2026-06-23 22:22 UTC)
- Execution ARN: `arn:aws:states:us-east-1:077127448006:execution:edgartools-prod-bronze-seed-silver-gold:bronze-seed-silver-gold-1782251900`
- Status: `RUNNING`
- Progress observed: `SeedFromBronze` completed, `BatchSilver` (Distributed Map) entered
  and still in progress ~24 minutes in. This is EXPECTED to be slow on a first-time
  copied-bronze load — per the existing known-notes section below, there's no
  `shard-manifest.json` yet, so `BatchSilver` runs sequentially through the monolith
  fallback rather than in parallel, across the ~81 batches discovered from ~8,006 CIKs in
  S3 bronze (per the prior session's `SeedFromBronze` discovery count). Has NOT yet
  reached `MdmRun`/`MdmBackfill`/`MdmSync`/`MdmVerify`/`GoldRefresh` — none of these have
  been observed to start in this run yet.
- A background Monitor task was streaming state-transition events to the session that
  started this run; that monitor does not survive into a new session. **On resume, you
  must re-poll the execution directly** — do not assume any monitor is still active.

## Next steps on resume — DO THIS FIRST
1. Check current status:
   ```bash
   aws stepfunctions describe-execution --region us-east-1 \
     --execution-arn arn:aws:states:us-east-1:077127448006:execution:edgartools-prod-bronze-seed-silver-gold:bronze-seed-silver-gold-1782251900
   ```
   - If still `RUNNING`: keep polling (e.g. every 30-60s) and/or check
     `get-execution-history` for the latest `TaskStateEntered`/`MapStateEntered` events to
     see what stage it's in.
   - If `SUCCEEDED`: **do NOT immediately flip Blocker 4 to PASS.** Pull CloudWatch logs
     (ECS task logs under `/aws/ecs/edgartools-prod-warehouse`, Step Functions logs under
     `/aws/states/edgartools-prod-warehouse`) and verify:
     a. `seed_bronze_batches_completed` reports `cik_count` > 0 (expect ~8,006).
     b. Zero `sec_pull_started` events anywhere during the `BatchSilver` map (proves no new
        SEC calls were made).
     c. `MdmRun`, `MdmBackfill`, `MdmSync`, `MdmVerify`, `GoldRefresh` all show their own
        `SUCCEEDED` terminal states in the execution history — none of these have ever
        succeeded in prod before this run, so treat a clean pass with real scrutiny, not
        just relief.
   - If `FAILED`/`ABORTED`/`TIMED_OUT`: this is new failure surface for this saga (further
     than any previous attempt reached). Pull the failing state's ECS task logs via
     `get-execution-history` (look for `TaskFailed`/`ExecutionFailed` events with
     `cause`/`error` fields) before assuming anything about root cause.
2. Only after the full chain is confirmed `SUCCEEDED` with the log verification above:
   update this file, `evidence/aws-mdm-e2e.md`, `STATE.md` Blocker 4 (flip to PASS), and
   `TODOS.md` (5-whys for the MDM-image-default bug and the ECR-cleanup-ordering hazard,
   both documented in PR #90's commit message and `go-live.sh`'s
   `known_go_live_notes_markdown`).
3. Get PR #90 (`claude/go-live-one-click-fixes`) reviewed/merged — it is unrelated to
   whether the live execution passes or fails, but should land regardless since it fixes a
   real, demonstrated bug class.
4. Blocker 5 (dashboard UAT) — still entirely unstarted, lower priority than finishing
   Blocker 4.

## CONSTRAINT (unchanged from prior sessions)
Never populate/reference `edgartools-prod/mdm/neo4j` or `edgartools-prod/mdm/api_keys`.

---

# Prior session history (older context below, still useful background)

# Blocker 4 — FOUR bugs found this saga. 3 fixed+verified. 4th (prod has no data) is NOT a quick fix.

## Confirmed working (do not re-investigate)
1. Neo4j secret injection removed (`d625358`) — verified live.
2. api_keys secret injection removed (`e526caf`) — verified live.
3. Postgres table ownership (`05ba7aa`, branch `claude/go-live-phase9-merge-followup`):
   `REASSIGN OWNED BY snowflake_admin TO application;` added to
   `bootstrap-prod-mdm.sh` and `mdm_post_restore.sql`. **Verified**: `mdm_migrate`
   SUCCEEDED on two separate live E2E re-runs since this fix landed.
4. Prod S3 bucket-naming mismatch — FIXED this session (not yet committed,
   see below): `deploy-aws-application.sh` was defaulting bucket names to the
   dev convention (`${NAME_PREFIX}-<purpose>-${ACCOUNT_ID}`) unconditionally,
   but prod Terraform (`infra/terraform/accounts/prod/main.tf`) provisions
   buckets WITHOUT the account-id suffix (`edgartools-prod-bronze`,
   `edgartools-prod-warehouse`, `edgartools-prod-snowflake-export`). The wrong
   suffixed names had been cached into `infra/aws-prod-application.json`
   (the deploy manifest) by an earlier deploy, so every subsequent deploy
   kept perpetuating the bug. Added `resolve_bucket_name()` helper
   (checks which name actually exists in S3 via head-bucket, prefers that)
   and switched all three bucket resolutions to use it instead of the
   hardcoded suffix. Redeployed all 5 prod ECS task defs + 16 Step
   Functions state machines with `--bronze-bucket-name edgartools-prod-bronze
   --warehouse-bucket-name edgartools-prod-warehouse
   --snowflake-export-bucket-name edgartools-prod-snowflake-export`
   (existing pinned images, no rebuild). Manifest now correctly shows
   unsuffixed names; `MDM_SILVER_DUCKDB` in the new `mdm-medium:4` task def
   now correctly points at `s3://edgartools-prod-warehouse/...`.
   **NOT YET COMMITTED** — the edit is live in
   `infra/scripts/deploy-aws-application.sh` on disk but no `git add`/commit
   has happened yet this session.

## UPDATE (this session, later): Option B bronze sync done, but won't unblock silver
Bronze sync (dev → prod, `aws s3 sync`) completed successfully: prod bronze now
has exactly 145,908 objects / 3,777,512,484 bytes, matching dev exactly
(`SYNC_EXIT=0` in `/tmp/bronze-sync.log`).

**However, starting `edgartools-prod-silver-mdm-gold` would NOT build silver
from this copied bronze.** Traced the actual code
(`edgar_warehouse/application/warehouse_orchestrator.py:1326-1345`):
`seed-silver-batches` (the 2nd state in that pipeline, "SeedSilverBatches")
calls `db.get_ciks_with_bronze(...)`, which queries silver DuckDB's
`sec_company_sync_state` JOIN `sec_source_checkpoint` (source_name=
'submissions_main') tables — **not** anything in S3/bronze. On a fresh prod
silver DuckDB (auto-created empty by `SilverStore.__init__`,
`silver_store.py:585-589` — does NOT error on missing file), those tables are
empty, so the query returns 0 rows. The orchestrator handles this explicitly
(line 1337-1345): emits `seed_silver_batches_completed` with `cik_count=0,
batch_count=0` and **returns early — no error, no batch file written**.
Net effect: the whole Step Function would "succeed" while doing nothing.

Root cause: `sec_company_sync_state`/`sec_source_checkpoint` rows are written
as a *side effect* of a CIK's bronze+silver having been processed through the
normal bootstrap path at least once — they are not derivable from bronze
object existence in S3. Copying bronze files into prod's bucket does not
backfill this silver-side bookkeeping. This pipeline
(`silver_mdm_gold`/`SeedSilverBatches`) is designed to *reprocess* CIKs silver
already knows about from cached bronze — it is not a bootstrap-from-bronze
mechanism for a brand-new environment.

**Decision needed from user before proceeding**: Option B as approved does
not achieve "prod builds silver from copied bronze with zero SEC calls" via
this state machine. Did not start the execution — would have been a silent
no-op, wasting an opaque run and creating false-positive evidence. Options to
discuss next session: (a) run a real `load_history`/`bootstrap` flow scoped to
the actual prod target universe (incurs real SEC calls, contradicts the
"reuse dev's already-fetched data" goal), (b) find/write a one-off command
that backfills `sec_company_sync_state`/`sec_source_checkpoint` directly from
bronze object listing (no such command currently exists — would need new
code), or (c) revisit Option A (copy dev's already-built `silver.duckdb`
directly) despite its earlier-flagged downside (reflects dev's test universe,
not necessarily prod's intended universe).

## NEW finding (5th, much bigger): prod has never had a bronze→silver load
After the bucket-name fix, `mdm_run` FAILED again — but with a DIFFERENT,
much more fundamental error:
```
mdm run: cannot open MDM_SILVER_DUCKDB — edgartools-prod-warehouse/warehouse/silver/sec/shard-manifest.json.
Check that MDM_SILVER_DUCKDB is set to a valid local path or s3:// URI.
```
The bucket now resolves correctly, but it's **empty** — confirmed via
`s3api list-objects-v2` on `edgartools-prod-bronze` and
`edgartools-prod-warehouse` returning zero `Contents` (checked earlier this
session, before the redeploy — still true after, since the redeploy only
touches ECS/Step-Functions config, not data).

**This means no ECS-run pipeline stage (bootstrap, daily_incremental,
gold_refresh, or any MDM stage) has ever successfully persisted bronze or
silver data to the correct prod S3 location.** The bucket-naming bug
explains *why* — every prior attempt was silently writing to a bucket that
doesn't exist (or failing outright), so even pipeline runs that reported
some success were not landing data where the runtime now looks for it.

Note: this does NOT necessarily contradict Phase 7's native-pull/dbt PASS
evidence — Snowflake's native S3 pull is configured via its own
Terraform/stage definitions, independent of these ECS task env vars, so
that path could have been validated against different (correct) bucket
references the whole time. Not independently re-verified this session;
flagged as a thing to check if it matters later.

## 5-whys for the bucket-naming bug (bug #4)
1. **Symptom**: `mdm_run` ECS task exits 1 with `MDM_SILVER_DUCKDB` bucket-not-found.
2. **Why**: task def env var pointed at `edgartools-prod-warehouse-077127448006`.
3. **Why**: `deploy-aws-application.sh` defaults bucket names with
   `${NAME_PREFIX}-<purpose>-${ACCOUNT_ID}` unconditionally.
4. **Why**: that convention is correct for dev (Terraform appends account id
   there) but prod's Terraform root does not append it — the script assumed
   one fixed naming convention across environments instead of asking AWS
   or Terraform what the real name is.
5. **Why it recurred across multiple deploys**: the first wrong value got
   written into `infra/aws-prod-application.json`, and the script's
   parameter-resolution order prefers the cached manifest value over
   re-deriving it, so the bug self-perpetuated until someone passed an
   explicit override (as done this session) or fixed the default logic.
   **Root-cause fix applied**: derive the bucket name from what actually
   exists in S3 (`resolve_bucket_name()`), not from a hardcoded convention.

## Next steps on resume
1. Commit the `deploy-aws-application.sh` fix (`resolve_bucket_name`) — not
   yet committed. Branch: `claude/go-live-phase9-merge-followup` (confirm
   this is still the right branch / no Codex collision before committing).
2. Decide and execute a real prod bronze→silver load. Per CLAUDE.md's
   phased pipeline, the correct mechanism is the `load_history` Step
   Function (NOT manual `bootstrap-next`) for any non-trivial company
   count. This is a materially bigger, longer-running operation than
   anything done so far in this saga (15+ min for 100 companies) and should
   get explicit user sign-off on scope (how many companies, which tracking
   status) before kicking off — do not start it unilaterally.
3. Once prod has real silver data, re-run the same E2E command:
   ```
   bash infra/scripts/run-aws-mdm-e2e.sh --env prod \
     --aws-profile sec_platform_deployer --aws-region us-east-1 \
     --snow-connection edgartools-prod --snowflake-database EDGARTOOLS_PROD \
     --native-app-compute-pool CPU_X64_XS --mdm-run-limit 5 --graph-limit 100
   ```
   Watch for the full chain (`mdm_migrate → mdm_run → mdm_backfill_relationships
   → mdm_sync_graph → mdm_verify_graph → mdm_counts`) all reaching SUCCEEDED.
   None of these later stages have ever been reached in any run this saga.
4. Only after the full chain SUCCEEDS: update `evidence/aws-mdm-e2e.md`,
   `STATE.md` Blocker 4 (flip to PASS), `TODOS.md` (5-whys for all 4 bugs:
   neo4j secret, api_keys secret, postgres ownership, S3 bucket-naming).
5. Merge/finalize PR #82 — check status, not yet checked this session.
6. Separately worth checking (not blocking): whether Phase 7's native-pull
   PASS evidence is still valid given prod bronze/warehouse buckets are
   empty — likely explained by native pull using its own Snowflake stage
   config rather than these ECS env vars, but not independently confirmed.
7. Blocker 5 (dashboard UAT) — still entirely unstarted.

## CONSTRAINT (unchanged)
Never populate/reference `edgartools-prod/mdm/neo4j` or
`edgartools-prod/mdm/api_keys`.

## Infra/branch state right now
- Prod warehouse task defs at new revisions with corrected bucket env vars
  (redeployed this session, exit 0). MDM task def now `edgartools-prod-mdm-medium:4`.
- `infra/aws-prod-application.json` (untracked, never commit) now has correct
  unsuffixed bucket names.
- `infra/scripts/deploy-aws-application.sh` has an uncommitted fix
  (`resolve_bucket_name`) — commit this before doing anything else.
- Most recent E2E run (`aws-mdm-e2e-1782164795-*`): `mdm_migrate` SUCCEEDED,
  `mdm_run` FAILED with shard-manifest-not-found (empty bucket, not a config bug).
- Branch `claude/go-live-phase9-merge-followup`, PR #82 open — status not
  re-checked this session.
