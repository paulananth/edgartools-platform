---
context: phase
phase: 09-production-hosted-graph-e2e
task: Blocker 4 fix (post-Phase-9-merge follow-up)
status: in_progress
last_updated: 2026-06-22T06:32:00.000Z
---

# Blocker 4 — second secret-wiring bug found and fixed; redeploy #2 in flight

## What happened this session
1. Removed legacy `NEO4J_SECRET_JSON` secret injection from MDM ECS task-def
   template (`infra/scripts/deploy-aws-application.sh`), commit `d625358`.
2. Redeployed prod MDM task defs (`edgartools-prod-mdm-small/medium:2`).
   Verified live via `aws ecs describe-task-definition` — NEO4J_SECRET_JSON
   confirmed gone.
3. Re-ran `run-aws-mdm-e2e.sh` from `mdm_migrate`. Got PAST the Neo4j
   failure, but hit a NEW failure: `edgartools-prod/mdm/api_keys` secret
   has no AWSCURRENT version either (same `ResourceInitializationError`
   class). Root cause: `MDM_API_KEYS` is only consumed by
   `edgar_warehouse/mdm/api/auth.py` (a separate API-server component not
   deployed by this script at all), but was unconditionally injected into
   every MDM CLI task including `migrate`.
4. Per the standing constraint, `edgartools-prod/mdm/api_keys` must NEVER
   be populated — same remediation as Neo4j: remove the injection, don't
   populate the secret.
5. Applied the same removal pattern (CLI flag, var, ARN resolution,
   mandatory check, container injection, summary metadata) to
   `MDM_API_KEYS_SECRET_ARN`. Commit `e526caf` on
   `claude/go-live-phase9-merge-followup`. `bash -n` syntax OK, the 10
   architecture tests in
   `tests/architecture/test_mdm_snowflake_postgres_cutover.py` pass.
6. Pushed commit, started redeploy #2 (same command/image-refs as before,
   `deploy-aws-application.sh --env prod --skip-build ...`) — **this was
   STILL RUNNING (PID tree alive, log file present but empty) when context
   ran out.** Background task ID was `bulkugg7z`, log at `/tmp/redeploy2.log`.

## CONSTRAINT (unchanged, re-confirm before any further action)
Never populate/reference `edgartools-prod/mdm/neo4j` or
`edgartools-prod/mdm/api_keys`. Both fixes are "remove the injection,"
never "populate the secret."

## Immediate next steps on resume
1. Check `/tmp/redeploy2.log` content (not exit code — pipe/tee can mask
   real exit code) for the deployment-summary JSON; confirm `task_definitions.mdm_small`/`mdm_medium` revision bumped again (should be `:3`).
2. Verify live: `aws ecs describe-task-definition --region us-east-1
   --task-definition edgartools-prod-mdm-small:3 --query
   'taskDefinition.containerDefinitions[0].secrets'` — should show only
   `MDM_DATABASE_URL`, `MDM_SNOWFLAKE_SECRET_JSON`, `EDGAR_IDENTITY` (no
   NEO4J_SECRET_JSON, no MDM_API_KEYS).
3. Re-run the same E2E command as before:
   ```
   bash infra/scripts/run-aws-mdm-e2e.sh --env prod \
     --aws-profile sec_platform_deployer --aws-region us-east-1 \
     --snow-connection edgartools-prod --snowflake-database EDGARTOOLS_PROD \
     --native-app-compute-pool CPU_X64_XS --mdm-run-limit 5 --graph-limit 100
   ```
4. Watch for `mdm_migrate` reaching SUCCEEDED this time (it should, since
   the only two unconditionally-injected-but-unpopulated secrets are now
   both removed: neo4j and api_keys). If it fails again, repeat the same
   diagnostic pattern (check StoppedReason, identify which secret ARN
   failed `GetSecretValue`, confirm it's a secret on the standing
   never-populate list before removing its injection — do NOT populate any
   secret as a workaround).
5. If `mdm_migrate` succeeds, the chain auto-continues through
   `mdm_check_connectivity → mdm_run → mdm_backfill_relationships →
   mdm_sync_graph → mdm_verify_graph → mdm_counts`. Confirm all six reach
   SUCCEEDED before updating any evidence/STATE.md to PASS.
6. Only after full chain SUCCEEDED: update
   `evidence/aws-mdm-e2e.md`, `STATE.md` Blocker 4, and `TODOS.md`'s Neo4j
   section to reflect the real, verified fix (now covering both
   neo4j AND api_keys).
7. Open/merge a PR for commits `d625358` + `e526caf` (currently sitting on
   `claude/go-live-phase9-merge-followup`, same branch as the still-open
   PR #82 docs followup — they'll ride together in #82, or split if you
   prefer a separate PR for the infra fix).
8. Blocker 5 (dashboard UAT): not started at all, still pending per the
   prior handoff's task list.

## Infra state right now
- Prod MDM task defs at revision `:2` (post-Neo4j-fix) when api_keys
  failure was hit; redeploy #2 (api_keys fix) was in flight, likely
  produced `:3` by the time you resume — verify, don't assume.
- `edgartools-prod/mdm/neo4j` and `edgartools-prod/mdm/api_keys` secrets:
  still exist, still intentionally unpopulated, still must never be
  populated.
- Branch `claude/go-live-phase9-merge-followup`: commits `1052785` (wip
  pause), `d625358` (neo4j fix), `e526caf` (api_keys fix) — all pushed to
  origin. PR #82 open, not yet merged, now also carries these two infra
  fixes.
