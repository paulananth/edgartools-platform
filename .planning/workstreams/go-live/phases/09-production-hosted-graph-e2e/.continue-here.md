---
context: phase
phase: 09-production-hosted-graph-e2e
task: Blocker 4 fix (post-Phase-9-merge follow-up)
status: in_progress
last_updated: 2026-06-22T10:10:00.000Z
---

# Blocker 4 — both secret-wiring bugs fixed and verified live; E2E re-run in flight

## What happened this session (continuation)
1. Neo4j fix (`d625358`) — done, deployed, verified live earlier.
2. api_keys fix (`e526caf`) — root cause: `MDM_API_KEYS` only consumed by
   `edgar_warehouse/mdm/api/auth.py` (separate, undeployed API-server
   component), unconditionally injected into every MDM CLI task anyway.
   Removed the injection (never populate — same standing constraint as
   Neo4j).
3. Redeploy #2 completed successfully. Verified via `/tmp/redeploy2.log`
   content (not exit code): `task_definitions.mdm_small`/`mdm_medium` now
   at revision `:3`, manifest's `mdm.secrets` shows only `postgres_dsn` and
   `snowflake` (no `api_keys`).
4. Verified live via:
   ```
   aws ecs describe-task-definition --region us-east-1 \
     --task-definition edgartools-prod-mdm-small:3 \
     --query 'taskDefinition.containerDefinitions[0].secrets' \
     --profile sec_platform_deployer
   ```
   Confirmed exactly 3 secrets: `MDM_DATABASE_URL`,
   `MDM_SNOWFLAKE_SECRET_JSON`, `EDGAR_IDENTITY`. No `NEO4J_SECRET_JSON`,
   no `MDM_API_KEYS`. Both bugs are now fully remediated in the live
   task definition.
5. Started E2E re-run #3 in the background:
   ```
   bash infra/scripts/run-aws-mdm-e2e.sh --env prod \
     --aws-profile sec_platform_deployer --aws-region us-east-1 \
     --snow-connection edgartools-prod --snowflake-database EDGARTOOLS_PROD \
     --native-app-compute-pool CPU_X64_XS --mdm-run-limit 5 --graph-limit 100
   ```
   Background task ID `bjpcl2t66`, log at `/tmp/mdm-e2e-rerun2.log`.
   **STATUS UNKNOWN AT HANDOFF TIME — was still running when context ran
   low. Do not assume success or failure; read the log content.**

## CONSTRAINT (unchanged, reconfirm before any further action)
Never populate/reference `edgartools-prod/mdm/neo4j` or
`edgartools-prod/mdm/api_keys`. Both fixes are "remove the injection,"
never "populate the secret." If E2E re-run hits a THIRD secret of this
same failure class, apply the identical pattern: identify the failing
secret ARN from `StoppedReason`, confirm it's safe/intended to be
unpopulated for the failing subcommand, remove the injection from
`infra/scripts/deploy-aws-application.sh` — do not populate as a
workaround.

## Immediate next steps on resume
1. Read `/tmp/mdm-e2e-rerun2.log` content in full (not exit code).
2. Check whether `mdm_migrate` reached SUCCEEDED this time. Both known
   unpopulated-secret bugs (neo4j, api_keys) are now fixed, so it should
   pass — but verify, don't assume.
3. If `mdm_migrate` SUCCEEDED, confirm the chain continues through
   `mdm_check_connectivity → mdm_run → mdm_backfill_relationships →
   mdm_sync_graph → mdm_verify_graph → mdm_counts`, all reaching SUCCEEDED.
4. If it fails again with the same `ResourceInitializationError` /
   `GetSecretValue` / `ResourceNotFoundException for staging label:
   AWSCURRENT` pattern for some other secret ARN, repeat the standard
   diagnostic: identify the ARN from `StoppedReason`, confirm it's not
   needed for the failing subcommand, remove its injection from
   `deploy-aws-application.sh` (apply the same 9-location removal pattern:
   help text, var init, CLI arg parsing, ARN resolution, mandatory-value
   check, container-definition secrets array + positional heredoc args,
   deployment-summary writer), run `bash -n` + the architecture test suite
   (`tests/architecture/test_mdm_snowflake_postgres_cutover.py`, expect
   10 passed), commit, push, redeploy, re-verify live via
   `describe-task-definition`, re-run E2E.
5. Only after the full six-state-machine chain SUCCEEDS: update
   `evidence/aws-mdm-e2e.md` (sanitized — no raw ARNs/cause JSON beyond
   existing pattern), `STATE.md` Blocker 4 (flip to PASS only on real
   verified success), and `TODOS.md`'s secret-wiring section (mark both
   neo4j and api_keys fixes done).
6. Merge/finalize PR #82 — carries commits `1052785`, `d625358`, `e526caf`,
   `d907683` (prior handoff) on top of its original docs content. Check CI,
   merge once Blocker 4 work is fully verified, or split into a separate
   PR if preferred (not yet decided).
7. Blocker 5 (dashboard UAT) — entirely unstarted. Identify dashboard entry
   point (Streamlit-in-Snowflake vs. standalone), reuse Phase 4/5 UAT
   checklist if one exists, load prod config read-only with no secrets
   printed, walk through key views, write sanitized evidence, update
   STATE.md only after real evidence.
8. "Fix go-live.sh" one-click-deployment scope (user-selected, not started):
   `go-live.sh` line ~544 requires manually exporting `WAREHOUSE_IMAGE_REF`
   before the "AWS: ECS task definitions and Step Functions" stage can run.
   Should auto-resolve from latest ECR `:prod`/`:dev` tag or the existing
   deployment summary (`infra/aws-prod-application.json`'s `image_ref`)
   instead of requiring a manual env var.

## Infra state right now
- Prod MDM task defs at revision `:3`. Confirmed live: only
  `MDM_DATABASE_URL`, `MDM_SNOWFLAKE_SECRET_JSON`, `EDGAR_IDENTITY`
  injected. Both `neo4j` and `api_keys` injections removed and verified.
- `edgartools-prod/mdm/neo4j` and `edgartools-prod/mdm/api_keys` secrets:
  still exist as Terraform-managed containers, still intentionally
  unpopulated, still must never be populated.
- Branch `claude/go-live-phase9-merge-followup`: commits `1052785`,
  `d625358`, `e526caf`, `d907683` all pushed to origin. PR #82 open, not
  yet merged.
- E2E re-run #3 (`bjpcl2t66` / `/tmp/mdm-e2e-rerun2.log`) status unknown at
  handoff — check this first on resume.
