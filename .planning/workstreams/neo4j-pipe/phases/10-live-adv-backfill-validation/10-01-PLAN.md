---
phase: 10-live-adv-backfill-validation
plan: "10-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md
autonomous: false
requirements:
  - MDM-ADV-01
user_setup: []

must_haves:
  truths:
    - "One real Form ADV XML for Vanguard Group Inc (CRD 105958) exists in dev S3 bronze."
    - "The S3 bronze ADV object is readable via the warehouse object-storage adapter."
    - "The exact accession, CIK, and S3 storage path are recorded for the Wave 2 --artifact run."
  artifacts:
    - path: ".planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md"
      provides: "Acquisition evidence: source URL, accession, CIK, S3 path, readability proof"
      contains: "ADV Bronze Acquisition"
  key_links:
    - from: "IAPD / SEC ADV source"
      to: "s3 bronze path filings/sec/cik={CIK}/accession={ACCESSION}/primary_doc.xml"
      via: "aws s3 cp upload"
      pattern: "primary_doc.xml"
---

<objective>
Acquire one real Form ADV XML filing for Vanguard Group Inc (CRD 105958, SEC file 801-11953)
and place it in dev S3 bronze at the canonical primary-document path, so Wave 2 can parse it
into silver ADV tables via the parse-adv-bronze --artifact explicit path.

Purpose: The dev S3 bronze contains ZERO ADV filing artifacts (confirmed in 10-RESEARCH.md: 0
rows in sec_adv_filing, 0 sec_company_filing rows matching ADV%). ADV forms are NOT in EDGAR —
they live in IAPD (FINRA-operated). Standard edgar-warehouse bootstrap cannot capture ADV bronze.
This plan performs the user-approved one-time IAPD ingestion (Fork A, extended) to unblock live
validation. This is the single precondition for the entire phase.

Output: One ADV XML object in S3 at the canonical bronze path, plus the acquisition section of
10-VALIDATION-NOTES.md recording source, accession, CIK, and S3 path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/neo4j-pipe/STATE.md
@.planning/workstreams/neo4j-pipe/ROADMAP.md
@.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-RESEARCH.md

Critical: read the IAPD Findings appendix in 10-RESEARCH.md (section "Appendix: IAPD Findings
(Fork A — Extended)") for the target adviser, the accepted XML formats, and the 403-Forbidden
pitfall on the firm-specific IAPD API.

<interfaces>
The --artifact value the executor must produce for Wave 2 (Phase 9 CLI).
Format: ACCESSION,FORM,STORAGE_PATH[,CIK]

Canonical bronze path shape (from CLAUDE.md WAREHOUSE_BRONZE_ROOT + RESEARCH.md):
  s3://edgartools-dev-bronze-077127448006/warehouse/bronze/filings/sec/cik={CIK}/accession={ACCESSION}/primary_doc.xml

ADV parser accepts (parsers/adv.py, verified in RESEARCH.md): XML rooted at edgarsubmission,
advfiling/advFiling, or generic xml; the advFiling tag triggers XML parse mode. Also accepts
HTML, plain text, PDF.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Choose and confirm the ADV XML acquisition method</name>
  <decision>Which acquisition method produces the Vanguard ADV XML to upload to S3 bronze.</decision>
  <action>
    Decide which acquisition method (iapd-browser, sec-bulk-csv, or minimal-xml) will
    produce the Vanguard Group Inc (CRD 105958) ADV XML, then record the chosen method id
    and its source URL/origin in 10-VALIDATION-NOTES.md under "## ADV Bronze Acquisition".
    Prefer a real download (iapd-browser or sec-bulk-csv) so the silver rows are representative
    and a private-fund schedule is more likely present; fall back to minimal-xml only if real
    download is blocked, and if so record that the MDM-ADV-01 fund condition is not expected to
    be satisfiable from this sample. No upload happens in this task; this task only fixes the method.
  </action>
  <context>
    api.adviserinfo.sec.gov firm-specific endpoints return 403 Forbidden (RESEARCH.md Pitfall 6).
    The IAPD Search API at https://api.adviserinfo.sec.gov/search/firm?query=...&wt=json is
    publicly accessible and confirmed Vanguard active (CRD 105958). A real ADV download requires
    one of the methods below. The executor must pick one before any upload happens, because the
    method determines how representative the silver rows will be and whether a private-fund
    schedule is present (the discriminating fact for the MDM-ADV-01 fund condition).
  </context>
  <options>
    <option id="iapd-browser">
      <name>IAPD website download via /browse skill (Chrome automation)</name>
      <pros>Real, complete ADV Part 1 data; most representative silver rows; highest chance the filing carries a private-fund schedule (Schedule D 7.B) satisfying the MDM-ADV-01 fund condition.</pros>
      <cons>Requires browser automation against adviserinfo.sec.gov; download format must be confirmed parseable by parsers/adv.py.</cons>
    </option>
    <option id="sec-bulk-csv">
      <name>SEC bulk Form ADV dataset converted to minimal XML</name>
      <pros>Structured, stable source; no browser session needed.</pros>
      <cons>CSV must be wrapped into parser-accepted XML; private-fund detail may be lost in wrapping, risking an empty sec_adv_private_fund.</cons>
    </option>
    <option id="minimal-xml">
      <name>Minimal synthetic ADV XML (advFiling root) for Vanguard identity</name>
      <pros>Guaranteed parseable (advFiling triggers XML mode); fastest; proves the S3 to parse to silver to MDM path end-to-end.</pros>
      <cons>Not representative; almost certainly produces zero private-fund rows, so the MDM-ADV-01 fund condition and the Wave 2 fund branch will be marked "not satisfiable from this sample". Fallback only if real download is blocked.</cons>
    </option>
  </options>
  <read_first>
    - .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - A single method id (iapd-browser, sec-bulk-csv, or minimal-xml) is recorded in 10-VALIDATION-NOTES.md under an "## ADV Bronze Acquisition" heading.
    - The chosen method's source URL or origin is written verbatim in 10-VALIDATION-NOTES.md.
    - If minimal-xml is chosen, the note explicitly states the private-fund (MDM-ADV-01 fund) condition is expected NOT to be satisfiable from this sample.
  </acceptance_criteria>
  <resume-signal>Select: iapd-browser, sec-bulk-csv, or minimal-xml</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Obtain the ADV XML and upload it to the canonical S3 bronze path</name>
  <files>.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md</files>
  <read_first>
    - .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-RESEARCH.md
    - CLAUDE.md
  </read_first>
  <action>
    Using the method chosen in Task 1, obtain one Form ADV XML for Vanguard Group Inc (CRD 105958).
    Choose a deterministic accession identifier: pattern ADV-105958-YYYYMMDD where YYYYMMDD is the
    filing/effective date if known else today's date; record it as ACCESSION. Use CIK value 105958
    and record the exact value used. Export the env vars from CLAUDE.md (EDGAR_IDENTITY,
    WAREHOUSE_RUNTIME_MODE=bronze_capture, WAREHOUSE_BRONZE_ROOT, WAREHOUSE_STORAGE_ROOT,
    AWS_DEFAULT_REGION=us-east-1). Upload the XML with aws s3 cp to exactly
    s3://edgartools-dev-bronze-077127448006/warehouse/bronze/filings/sec/cik=105958/accession=ACCESSION/primary_doc.xml.
    Record the full S3 URI, ACCESSION, CIK, FORM=ADV, and byte size in 10-VALIDATION-NOTES.md under
    "## ADV Bronze Acquisition". Do NOT call any SEC EDGAR bootstrap or download_sec_bytes path —
    acquisition is from IAPD/manual only, per the locked no-EDGAR-fetch constraint. Do NOT add this
    CIK to the tracked universe; the --artifact path (Wave 2) reads the object directly and needs no
    registry row.
  </action>
  <verify>
    <automated>aws s3 ls "s3://edgartools-dev-bronze-077127448006/warehouse/bronze/filings/sec/cik=105958/" --recursive | grep -c "primary_doc.xml"</automated>
  </verify>
  <acceptance_criteria>
    - aws s3 ls of the cik=105958 accession path lists exactly one primary_doc.xml object with nonzero Size.
    - 10-VALIDATION-NOTES.md "## ADV Bronze Acquisition" contains the full S3 URI, ACCESSION, CIK=105958, FORM=ADV, and byte size.
    - The uploaded object is XML whose root tag is one of edgarsubmission, advFiling/advfiling, or xml (parser-accepted per RESEARCH.md).
  </acceptance_criteria>
  <done>One ADV XML object exists at the canonical S3 bronze path; accession/CIK/path/size recorded in 10-VALIDATION-NOTES.md.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the S3 bronze ADV object is readable via the warehouse storage adapter</name>
  <files>.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md</files>
  <read_first>
    - edgar_warehouse/infrastructure/object_storage.py
    - .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md
  </read_first>
  <action>
    Confirm the uploaded object is readable through the same read_bytes(storage_path) path that
    parse-adv-bronze uses in Wave 2 (NOT a raw boto call) — this proves the protocol allowlist and
    credentials are correct before the Wave 2 parse. Run a one-line uv run python snippet that
    imports read_bytes from edgar_warehouse.infrastructure.object_storage and reads the exact S3 URI
    recorded in Task 2, printing the returned byte length. Record the printed byte length and the
    assembled Wave 2 --artifact string ACCESSION,ADV,S3_URI,105958 verbatim in 10-VALIDATION-NOTES.md
    so Wave 2 can copy it directly.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/neo4j-pipe/edgartools-platform && uv run --extra s3 python -c "from edgar_warehouse.infrastructure.object_storage import read_bytes; import sys; b=read_bytes(sys.argv[1]); print('bytes', len(b)); assert len(b) > 0" "$(grep -oE 's3://edgartools-dev-bronze-077127448006[^ ]*primary_doc.xml' .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md | head -1)"</automated>
  </verify>
  <acceptance_criteria>
    - The read_bytes snippet exits 0 and prints a byte count > 0 for the recorded S3 URI.
    - 10-VALIDATION-NOTES.md records the printed byte length and the verbatim Wave 2 --artifact string ACCESSION,ADV,S3_URI,105958.
  </acceptance_criteria>
  <done>The S3 ADV object reads cleanly through the warehouse storage adapter; the Wave 2 --artifact string is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IAPD/internet -> local disk | Untrusted ADV XML downloaded from an external SEC/FINRA surface |
| local disk -> S3 bronze | Operator-uploaded artifact crosses into the dev bronze trust zone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Uploaded ADV XML content | mitigate | Restrict upload to the single cik=105958 accession path; parser in Wave 2 is read-only and writes typed columns only (no eval of XML). |
| T-10-02 | Spoofing | Source authenticity (is this really Vanguard ADV) | accept | One-time validation sample; identity recorded (CRD 105958, file 801-11953) for audit. Not production data. |
| T-10-03 | Information disclosure | S3 bronze write scope | mitigate | Write only under the documented dev bronze prefix; no other prefixes touched; no secrets in artifact. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; uv sync uses the existing locked deps only. |
</threat_model>

<verification>
- aws s3 ls confirms exactly one primary_doc.xml under cik=105958.
- read_bytes via the warehouse adapter returns nonzero bytes for the recorded URI.
- 10-VALIDATION-NOTES.md contains the "## ADV Bronze Acquisition" section with source, accession, CIK, path, size, and the Wave 2 --artifact string.
</verification>

<success_criteria>
- One ADV XML for Vanguard (CRD 105958) exists at the canonical S3 bronze path.
- The object reads cleanly through edgar_warehouse.infrastructure.object_storage.read_bytes.
- The Wave 2 --artifact string is recorded verbatim for 10-02 to consume.
</success_criteria>

<output>
Append the "## ADV Bronze Acquisition" section to
`.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md`
and create
`.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-01-SUMMARY.md` when done.
</output>
