---
phase: 10-live-adv-backfill-validation
plan: "10-02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - tests/mdm/test_adv_preflight.py
  - .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md
autonomous: false
requirements:
  - MDM-ADV-01
  - MDM-ADV-02
user_setup: []

must_haves:
  truths:
    - "MDM-ADV-01: After parse-adv-bronze runs against the Wave 1 --artifact, sec_adv_filing has > 0 rows in silver DuckDB."
    - "MDM-ADV-01 (fund condition): sec_adv_private_fund row count is recorded; if 0, the fund branch is explicitly deferred to Phase 5 with the reason that the single Fork-A sample carried no private-fund schedule."
    - "MDM-ADV-02: A fixture-based preflight test proves _require_silver_reader transitions from FAIL (empty sec_adv_filing) to PASS (populated sec_adv_filing) for entity-type adviser, and likewise for fund on sec_adv_private_fund."
    - "MDM-ADV-02 (live): mdm run --entity-type adviser exits 0 and writes mdm_adviser rows to MDM Postgres against the parse-adv-bronze-populated silver (D-07, D-12)."
    - "A deliberate malformed --artifact emits a non-fatal unreadable/error event and the run continues (D-25)."
    - "Rollback commands for silver and MDM Postgres are recorded so the sample can be undone (D-27)."
    - "The Neo4j graph pipeline (backfill-relationships, sync-graph, verify-graph) and Neo4j startup are deferred UNCONDITIONALLY to Phase 5 (orchestrator re-scope) and are not run in this plan."
  artifacts:
    - path: "tests/mdm/test_adv_preflight.py"
      provides: "Fixture-based fail->pass preflight tests for adviser (sec_adv_filing) and fund (sec_adv_private_fund) entity types."
      contains: "_require_silver_reader"
      min_lines: 40
    - path: ".planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md"
      provides: "Parse run output, silver counts, deliberate-failure evidence, MDM run commands + Postgres counts, rollback section."
      contains: "Silver Backfill"
  key_links:
    - from: "Wave 1 --artifact string (ACCESSION,ADV,S3_URI,105958)"
      to: "edgar-warehouse parse-adv-bronze --artifact"
      via: "explicit-artifact parse run"
      pattern: "parse-adv-bronze --artifact"
    - from: "silver sec_adv_filing (populated)"
      to: "mdm run --entity-type adviser preflight"
      via: "_require_silver_reader / _validate_silver_tables"
      pattern: "entity-type adviser"
---

<objective>
Run parse-adv-bronze against the Wave 1 ADV artifact to populate silver ADV tables, prove the row
counts are nonzero, prove the MDM adviser/fund preflight transitions from fail to pass with a
fixture test, and perform the live MDM adviser load (and fund load when private-fund rows exist)
into local Colima Postgres.

Purpose: This is the requirement-completing wave. MDM-ADV-01 is proven by a nonzero sec_adv_filing
count after backfill. MDM-ADV-02 is proven two ways: a deterministic fixture preflight test
(automated, repeatable) AND a live mdm run against the populated silver (operational evidence).
The fund branch is data-dependent on the single Fork-A sample carrying a private-fund schedule:
this plan runs the fund load when sec_adv_private_fund > 0 and defers it (data-reachability) when the
sample has none. The Neo4j relationship/graph pipeline (backfill-relationships, sync-graph,
verify-graph) is OUT OF Phase 10 scope and deferred UNCONDITIONALLY to the Phase 5 resume per the
orchestrator re-scope; it is not run here.

Output: tests/mdm/test_adv_preflight.py (new), and the "## Silver Backfill" + "## MDM Load" +
"## Rollback" sections of 10-VALIDATION-NOTES.md with parse output, silver counts, MDM exit codes,
and Postgres counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/neo4j-pipe/STATE.md
@.planning/workstreams/neo4j-pipe/ROADMAP.md
@.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-RESEARCH.md
@.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-CONTEXT.md
@CLAUDE.md

The Wave 1 artifact string (ACCESSION,ADV,S3_URI,105958) is recorded in the "## ADV Bronze
Acquisition" section of 10-VALIDATION-NOTES.md. Read it before running parse-adv-bronze.

<interfaces>
parse-adv-bronze CLI (Phase 9, edgar_warehouse/cli.py + warehouse_orchestrator.py):
  edgar-warehouse parse-adv-bronze --artifact "ACCESSION,FORM,STORAGE_PATH[,CIK]"
  Emits structured JSON events. The completion event parse_adv_bronze_completed carries
  metrics including parsed, skipped, missing_artifacts, unreadable_artifacts, errors,
  rows_written. A malformed/unreadable path emits parse_adv_bronze_unreadable_artifact (per
  10-CONTEXT.md specifics) and the run continues to the next artifact.

MDM preflight (edgar_warehouse/mdm/cli.py, VERIFIED):
  _REQUIRED_TABLES_RUN = { "adviser": {"sec_adv_filing": True}, "fund": {"sec_adv_private_fund": True}, ... }
  _required_tables_for_run(entity_type) -> dict   (line 371)
  _validate_silver_tables(reader, required_tables) -> list[str]  (line 393; nonempty list = failures)
  _require_silver_reader(required_tables, command_name) -> (reader, rc)  (line 423; rc 0 = pass)
  _silver_reader() reads the MDM_SILVER_DUCKDB env var (line 274), NOT WAREHOUSE_SILVER_ROOT.
  mdm run --entity-type {adviser|fund} runs preflight via _handle_run before opening a session.

Existing preflight test patterns to mirror: tests/mdm/test_source_to_mdm_load_path.py
(uses monkeypatch on MDM_SILVER_DUCKDB and patches _session; lines ~339-445).

Silver ADV table DDL: edgar_warehouse/silver_store.py lines 175-214 (sec_adv_filing,
sec_adv_private_fund column definitions — read before writing fixture INSERTs and count queries).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixture-based MDM adviser/fund preflight fail->pass test</name>
  <files>tests/mdm/test_adv_preflight.py</files>
  <read_first>
    tests/mdm/test_source_to_mdm_load_path.py
    tests/mdm/conftest.py
    edgar_warehouse/mdm/cli.py
    edgar_warehouse/silver_store.py
  </read_first>
  <behavior>
    - adviser FAIL: a silver DuckDB whose sec_adv_filing table exists but is empty makes
      _require_silver_reader(_required_tables_for_run("adviser"), "mdm run") return rc == 1, and
      _validate_silver_tables returns a nonempty failures list naming sec_adv_filing.
    - adviser PASS: after inserting one sec_adv_filing row, the same call returns rc == 0 and an
      empty failures list.
    - fund FAIL: empty sec_adv_private_fund makes _required_tables_for_run("fund") preflight return
      rc == 1 naming sec_adv_private_fund.
    - fund PASS: after inserting one sec_adv_private_fund row, rc == 0.
  </behavior>
  <action>
    Create tests/mdm/test_adv_preflight.py mirroring the monkeypatch/MDM_SILVER_DUCKDB conventions in
    tests/mdm/test_source_to_mdm_load_path.py. Build minimal silver DuckDB fixtures with duckdb in a
    tmp_path: CREATE the sec_adv_filing and sec_adv_private_fund tables using the column shapes from
    silver_store.py DDL (only the columns the preflight reader counts are required — keep the fixture
    minimal). Point MDM_SILVER_DUCKDB at the fixture directory/path via monkeypatch.setenv exactly as
    _silver_reader expects (it reads MDM_SILVER_DUCKDB as a directory/path per cli.py lines 274-311).
    Drive the fail->pass transition by inserting one row, not by recreating the DB. Assert through the
    real _require_silver_reader and _validate_silver_tables seams (import from edgar_warehouse.mdm.cli)
    rather than re-implementing count logic. Do NOT open an MDM Postgres session — patch _session if any
    code path would reach it, following the existing test's _spy_session pattern. Cover adviser and fund
    entity types. This is the automated proof for MDM-ADV-02; it must pass deterministically with no S3,
    no Postgres, and no network.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/neo4j-pipe/edgartools-platform && uv run --extra s3 --with pytest pytest tests/mdm/test_adv_preflight.py -q</automated>
    <automated>cd /Users/aneenaananth/gsd-workspaces/neo4j-pipe/edgartools-platform && rg -n "_require_silver_reader|sec_adv_filing|sec_adv_private_fund|entity_type|MDM_SILVER_DUCKDB" tests/mdm/test_adv_preflight.py</automated>
  </verify>
  <acceptance_criteria>
    Behavior: pytest tests/mdm/test_adv_preflight.py exits 0.
    Source: test_adv_preflight.py imports and calls _require_silver_reader (or _validate_silver_tables) from edgar_warehouse.mdm.cli — does not re-implement the count check.
    Source: test_adv_preflight.py covers both an empty-table FAIL assertion and a populated-table PASS assertion for adviser (sec_adv_filing) and fund (sec_adv_private_fund).
    Behavior: the test uses no network, no live S3, and no live Postgres (MDM_SILVER_DUCKDB points at a tmp fixture; _session is patched if reached).
  </acceptance_criteria>
  <done>Fixture preflight test proves the adviser and fund preflight gates transition fail->pass; MDM-ADV-02 has a deterministic automated proof.</done>
</task>

<task type="auto">
  <name>Task 2: Run parse-adv-bronze against the Wave 1 artifact and record silver counts</name>
  <files>.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md</files>
  <read_first>
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-CONTEXT.md
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-RESEARCH.md
    edgar_warehouse/silver_store.py
    CLAUDE.md
  </read_first>
  <action>
    Export the env vars from CLAUDE.md (EDGAR_IDENTITY, WAREHOUSE_RUNTIME_MODE=bronze_capture,
    WAREHOUSE_BRONZE_ROOT, WAREHOUSE_STORAGE_ROOT, AWS_DEFAULT_REGION=us-east-1). Read the verbatim
    Wave 2 --artifact string (ACCESSION,ADV,S3_URI,105958) from the "## ADV Bronze Acquisition" section
    of 10-VALIDATION-NOTES.md.

    Run: edgar-warehouse parse-adv-bronze --artifact "<that string>". Capture the full terminal output
    including the parse_adv_bronze_completed event. Per D-09, errors must be 0 — if errors > 0,
    investigate. If the error is confirmed data-specific (not a parser bug, per Claude's Discretion in
    10-CONTEXT.md), re-acquire/repair the artifact; do not edit the parser.

    Deliberate failure test (D-25): run a second invocation with a known-bad artifact
    --artifact "FAKE-ACCESSION-0,ADV,s3://invalid-path/fake.xml" and confirm a
    parse_adv_bronze_unreadable_artifact (or equivalent non-fatal) event is emitted and the run exits
    without aborting on the bad artifact. Record the event line.

    Then query silver counts. The silver DuckDB lives under WAREHOUSE_STORAGE_ROOT (D-20); use the path
    the CLI uses (RESEARCH.md: {WAREHOUSE_STORAGE_ROOT}/silver/sec/silver.duckdb unless overridden).
    Record: SELECT COUNT(*) FROM sec_adv_filing and SELECT COUNT(*) FROM sec_adv_private_fund. Append a
    "## Silver Backfill" section to 10-VALIDATION-NOTES.md with: the exact parse command, the
    parse_adv_bronze_completed metrics line, the deliberate-failure event line, and both count queries
    with their numeric results.

    Append a "## Rollback" section (D-27) recording the exact undo commands:
    duckdb <silver> "DELETE FROM sec_adv_filing; DELETE FROM sec_adv_office; DELETE FROM
    sec_adv_disclosure_event; DELETE FROM sec_adv_private_fund" and the MDM Postgres deletes used in
    Task 3. Do not run rollback now — only record it.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/neo4j-pipe/edgartools-platform && rg -n "Silver Backfill|sec_adv_filing|parse_adv_bronze_completed|Rollback" .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    Behavior: parse-adv-bronze --artifact run completes with a parse_adv_bronze_completed event whose errors metric is 0.
    Behavior: a SELECT COUNT(*) FROM sec_adv_filing against the CLI silver DuckDB returns > 0 (MDM-ADV-01 core proof).
    Behavior: the deliberate-failure invocation emits a non-fatal unreadable/error event and the process does not abort the batch (D-25).
    Source: 10-VALIDATION-NOTES.md "## Silver Backfill" contains the exact command, the completion metrics line, both count queries with numeric results, and the deliberate-failure event line.
    Source: 10-VALIDATION-NOTES.md "## Rollback" records the silver DELETE and MDM Postgres DELETE commands.
  </acceptance_criteria>
  <done>parse-adv-bronze populated silver from the Wave 1 artifact; sec_adv_filing > 0 (MDM-ADV-01); deliberate-failure non-fatal handling proven; counts and rollback recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Start local Colima Postgres and run live MDM adviser/fund load</name>
  <what-built>
    Wave 1 placed an ADV XML in S3 bronze; Task 2 parsed it into silver (sec_adv_filing > 0). This
    task starts local Colima Postgres, runs the live MDM adviser load against the populated silver,
    runs the fund load conditionally, and records Postgres row counts as the operational MDM-ADV-02
    evidence. It is a checkpoint because it requires a local Docker/Colima daemon and writes to a
    local Postgres the executor brings up by hand.
  </what-built>
  <read_first>
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-CONTEXT.md
    edgar_warehouse/mdm/cli.py
    CLAUDE.md
  </read_first>
  <action>
    Bring up local services and run the live MDM load. The executor must perform these steps and
    record outcomes in 10-VALIDATION-NOTES.md under a "## MDM Load" section.

    1. Start Colima and local Postgres (D-21, D-24): colima start; docker run a postgres container
       exposing 5432 with database mdm and password test, matching
       MDM_DATABASE_URL=postgresql://postgres:test@localhost:5432/mdm. Use raw docker run, not a
       compose file (D-24). Wait until the container accepts connections.
    2. Point the MDM preflight at the silver Task 2 populated. CRITICAL path mapping: parse-adv-bronze
       (Task 2) writes the CLI silver DuckDB, but mdm preflight reads MDM_SILVER_DUCKDB, which
       edgar_warehouse/mdm/cli.py (_silver_reader, lines 274-311) treats as a SHARDED DIRECTORY path,
       not a single .duckdb file. Do NOT trust either literal in D-20 ($WAREHOUSE_STORAGE_ROOT/silver.duckdb)
       or RESEARCH.md (.../silver/sec/silver.duckdb) — resolve the actual CLI silver location from the
       warehouse config/CLI output produced in Task 2, then set MDM_SILVER_DUCKDB to the DIRECTORY that
       contains that DuckDB (sync locally if the CLI path is an s3:// URI). Prove the mapping is correct
       by running edgar-warehouse mdm run --entity-type adviser and confirming preflight does NOT report
       an empty sec_adv_filing; if it does, the directory mapping is wrong — fix it before proceeding.
    3. Adviser load (D-07, D-12): run edgar-warehouse mdm run --entity-type adviser. It must exit 0
       (preflight passes because sec_adv_filing > 0). Then verify
       psql "$MDM_DATABASE_URL" -c "SELECT COUNT(*) FROM mdm_adviser" returns > 0. Record command,
       exit code, and count.
    4. Fund load (conditional on data reachability, D-11/D-13): read the sec_adv_private_fund count
       recorded in Task 2.
       - If sec_adv_private_fund > 0: run edgar-warehouse mdm run --entity-type fund; it must exit 0;
         verify SELECT COUNT(*) FROM mdm_fund > 0. Record command, exit code, and count.
       - If sec_adv_private_fund == 0: do NOT run the fund load. Record "Fund load DEFERRED — no
         private-fund rows" in the "## MDM Load" section, stating the single Fork-A Vanguard sample
         carried no Schedule D private-fund schedule. This matches MDM-ADV-01's own "when source data
         contains private funds" wording — it is a data-reachability deferral, and the fund command
         path is still proven by the fixture fund preflight test in Task 1.
    5. Graph relationship pipeline (D-14/D-15/D-16/D-22) is OUT OF PHASE 10 SCOPE — deferred
       UNCONDITIONALLY (regardless of fund count) to the v1.1 Phase 5 resume. Reason: the orchestrator
       planning_context Wave structure and quality_gate re-scoped backfill-relationships -> sync-graph
       -> verify-graph and the Neo4j startup to Phase 5 after the Fork-A re-scope; CONTEXT.md D-17 also
       defers full-scale graph sync to Phase 5. Do NOT start Neo4j or run the graph commands in this
       plan. Record one line in "## MDM Load": "Graph pipeline (backfill-relationships, sync-graph,
       verify-graph) and Neo4j startup deferred to Phase 5 resume per orchestrator re-scope." 10-03
       documents this resume path.
    6. Reference the "## Rollback" section from Task 2 for undoing mdm_adviser/mdm_fund rows.
  </action>
  <how-to-verify>
    1. docker ps shows a running postgres container on 5432.
    2. MDM_SILVER_DUCKDB points at the directory containing the Task 2 silver DuckDB, and
       edgar-warehouse mdm run --entity-type adviser preflight does NOT report an empty sec_adv_filing.
    3. edgar-warehouse mdm run --entity-type adviser exits 0.
    4. psql "$MDM_DATABASE_URL" -c "SELECT COUNT(*) FROM mdm_adviser" prints a count > 0.
    5. If sec_adv_private_fund > 0: mdm run --entity-type fund exits 0 and mdm_fund count > 0; if 0,
       10-VALIDATION-NOTES.md records "Fund load DEFERRED — no private-fund rows".
    6. 10-VALIDATION-NOTES.md "## MDM Load" records a line deferring the graph pipeline + Neo4j to
       Phase 5 (unconditional), and records every command, exit code, and Postgres count.
  </how-to-verify>
  <acceptance_criteria>
    Behavior: mdm run --entity-type adviser exits 0 against the parse-adv-bronze-populated silver.
    Behavior: SELECT COUNT(*) FROM mdm_adviser in MDM Postgres returns > 0.
    Source: 10-VALIDATION-NOTES.md "## MDM Load" records the adviser command, exit code, and mdm_adviser count.
    Source: the fund branch is either executed with mdm_fund > 0 recorded, OR a "Fund load DEFERRED — no private-fund rows" note with the no-private-fund reason is recorded.
    Source: 10-VALIDATION-NOTES.md "## MDM Load" records the graph pipeline + Neo4j as unconditionally deferred to Phase 5 (no backfill-relationships/sync-graph/verify-graph run in this plan).
  </acceptance_criteria>
  <resume-signal>Type "approved" once mdm_adviser count > 0 is recorded (and the fund branch is either done or explicitly deferred), or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| S3 bronze -> silver | Untrusted ADV XML parsed into typed silver columns |
| silver -> MDM Postgres | Parsed silver rows resolved into MDM entities written to local Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | ADV XML -> silver parse | mitigate | Reuse the Phase 9 parse_adv path only; no parser edits; typed-column merge, no eval. |
| T-10-02-02 | Denial of service | Malformed --artifact aborting the batch | mitigate | D-25 deliberate-failure test confirms unreadable artifacts emit a non-fatal event and the run continues. |
| T-10-02-03 | Information disclosure | MDM Postgres credentials | accept | Local Colima Postgres with a throwaway test password; not production; no secrets committed. |
| T-10-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed; uv run uses existing locked deps only. |
</threat_model>

<verification>
- pytest tests/mdm/test_adv_preflight.py exits 0 (MDM-ADV-02 automated proof).
- parse-adv-bronze --artifact completes with errors=0 and sec_adv_filing > 0 (MDM-ADV-01).
- Deliberate-failure invocation emits a non-fatal event; the batch continues (D-25).
- mdm run --entity-type adviser exits 0 and mdm_adviser > 0 in Postgres (MDM-ADV-02 live).
- Fund branch executed (mdm_fund > 0) or deferred for no private-fund rows (data-reachability).
- Graph pipeline + Neo4j NOT run; recorded as unconditionally deferred to Phase 5.
- 10-VALIDATION-NOTES.md has "## Silver Backfill", "## MDM Load", and "## Rollback" sections.
</verification>

<success_criteria>
- MDM-ADV-01: sec_adv_filing > 0 after parse-adv-bronze backfill; sec_adv_private_fund count recorded.
- MDM-ADV-02: fixture preflight test passes AND live mdm adviser load exits 0 with mdm_adviser > 0.
- Fund load is conditional on private-fund rows; the graph pipeline + Neo4j are unconditionally deferred to Phase 5.
- No parser, gold/dbt, Snowflake, or non-AWS changes (ISO-01/02/03 preserved).
</success_criteria>

<output>
Append "## Silver Backfill", "## MDM Load", and "## Rollback" sections to
`.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md`
and create
`.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-02-SUMMARY.md` when done.
</output>
