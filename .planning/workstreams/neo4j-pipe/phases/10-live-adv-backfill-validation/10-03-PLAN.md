---
phase: 10-live-adv-backfill-validation
plan: "10-03"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - docs/aws-mdm-source-to-mdm.md
  - .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md
  - .planning/workstreams/neo4j-pipe/STATE.md
autonomous: true
requirements:
  - MDM-ADV-03
user_setup: []

must_haves:
  truths:
    - "MDM-ADV-03: docs/aws-mdm-source-to-mdm.md has a 'Step 1b: Parse ADV Bronze' section documenting that ADV bronze comes from IAPD (not standard EDGAR bootstrap) and how to run parse-adv-bronze."
    - "MDM-ADV-03: the docs identify the exact Phase 5 resume path with the required silver counts (sec_adv_filing > 0 for adviser; sec_adv_private_fund > 0 for fund) before running MDM adviser/fund loaders."
    - "10-VALIDATION-NOTES.md records the actual Wave 2 silver/MDM counts as Phase 5 resume evidence and a Phase 10 COMPLETE declaration."
    - "STATE.md reflects Phase 10 completion with a row-count summary (D-18)."
  artifacts:
    - path: "docs/aws-mdm-source-to-mdm.md"
      provides: "Operator runbook with the ADV bronze parse step and the Phase 5 resume path."
      contains: "parse-adv-bronze"
    - path: ".planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md"
      provides: "Phase 10 COMPLETE declaration, Next Steps (Phase 5 resume), and the recorded counts."
      contains: "Phase 10 COMPLETE"
    - path: ".planning/workstreams/neo4j-pipe/STATE.md"
      provides: "Updated milestone state reflecting Phase 10 completion."
      contains: "Phase 10"
  key_links:
    - from: "docs/aws-mdm-source-to-mdm.md Step 1b"
      to: "mdm run --entity-type adviser / fund"
      via: "documented required silver counts before MDM load"
      pattern: "sec_adv_filing"
---

<objective>
Document the ADV backfill operator step and the exact Phase 5 resume path in
docs/aws-mdm-source-to-mdm.md, finalize 10-VALIDATION-NOTES.md with the Phase 10 COMPLETE
declaration and the recorded counts, and update STATE.md to reflect Phase 10 completion.

Purpose: MDM-ADV-03 is the requirement that unblocks the paused v1.1 Phase 5 live checkpoint by
telling an operator exactly how to populate ADV silver and what counts must be nonzero before
running the MDM adviser/fund loaders. The runbook currently has a "Step 1: Parse Ownership Bronze"
but no ADV equivalent; this wave adds "Step 1b: Parse ADV Bronze" and a Phase 5 resume section,
correctly noting that ADV bronze originates from IAPD (FINRA), not standard EDGAR bootstrap.

Output: docs/aws-mdm-source-to-mdm.md with the new ADV step + resume path; finalized
10-VALIDATION-NOTES.md; updated STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/neo4j-pipe/STATE.md
@.planning/workstreams/neo4j-pipe/ROADMAP.md
@.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-RESEARCH.md
@.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-CONTEXT.md

<interfaces>
docs/aws-mdm-source-to-mdm.md current structure (VERIFIED):
  "## Step 1: Parse Ownership Bronze" (line ~73) — mirror this section's shape for the new ADV step.
  "## Step 2: Run MDM Entity Loaders" (line ~102) — adviser/fund mdm run commands.
  "## Silver Readiness Diagnostics" (line ~160) — table already lists sec_adv_filing (adviser, fund, min 1)
  and sec_adv_private_fund (fund, min 1); the "Interpreting Results" table already says
  "Zero sec_adv_filing = ADV data was not loaded". Cross-link the new Step 1b from these rows.

Key fact for the docs (10-RESEARCH.md, VERIFIED): ADV forms are NOT in EDGAR submissions.json;
they are filed through IARD/IAPD (FINRA). Standard edgar-warehouse bootstrap does NOT capture ADV
bronze. ADV bronze must be obtained from IAPD (the Phase 10 Fork-A path) and parsed via
parse-adv-bronze. Do not document a standard-bootstrap path for ADV — it does not work.

parse-adv-bronze CLI (Phase 9): supports --accession-list, --limit, and explicit
--artifact "ACCESSION,FORM,STORAGE_PATH[,CIK]" (the path used when no registry rows exist).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the ADV bronze parse step and Phase 5 resume path to the operator runbook</name>
  <files>docs/aws-mdm-source-to-mdm.md</files>
  <read_first>
    docs/aws-mdm-source-to-mdm.md
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-RESEARCH.md
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md
  </read_first>
  <action>
    Add a "## Step 1b: Parse ADV Bronze (Required for Adviser and Fund Entity Types)" section
    immediately after the existing "## Step 1: Parse Ownership Bronze", mirroring its formatting.

    The section must state:
    - ADV forms are NOT in EDGAR; they originate from IAPD/IARD (FINRA). Standard
      edgar-warehouse bootstrap does NOT capture ADV bronze. ADV bronze must be obtained from IAPD
      and placed in S3 bronze before parsing (reference the Phase 10 Fork-A acquisition as the
      precedent; do not claim standard bootstrap works for ADV).
    - The parse commands: edgar-warehouse parse-adv-bronze (registry path),
      parse-adv-bronze --limit N, parse-adv-bronze --accession-list ..., and the explicit
      parse-adv-bronze --artifact "ACCESSION,ADV,STORAGE_PATH,CIK" path used when no registry rows
      exist (the path Phase 10 used).
    - The required silver counts before MDM adviser/fund load: sec_adv_filing > 0 (required for
      mdm run --entity-type adviser) and sec_adv_private_fund > 0 (required for
      mdm run --entity-type fund). Note that mdm run --entity-type all does NOT enforce these tables.

    Add a "## Phase 5 Resume Path" section (or extend the existing readiness/scope sections) that
    states the exact resume sequence for the blocked v1.1 Phase 5 live checkpoint:
    1. Ensure ADV bronze exists in S3 (IAPD source — see Step 1b).
    2. Run parse-adv-bronze.
    3. Confirm sec_adv_filing > 0 and (for fund) sec_adv_private_fund > 0.
    4. Run mdm run --entity-type adviser then --entity-type fund.
    5. Run backfill-relationships -> sync-graph -> verify-graph for the full adviser/fund graph
       (this is the Phase 5 full-scale graph sync; Phase 10 validated only a single-sample slice).
    Cross-reference the existing "Silver Readiness Diagnostics" rows for sec_adv_filing /
    sec_adv_private_fund so the operator who hits a zero-count diagnostic is pointed at Step 1b.
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/neo4j-pipe/edgartools-platform && rg -n "Step 1b: Parse ADV Bronze|parse-adv-bronze|IAPD|Phase 5 Resume" docs/aws-mdm-source-to-mdm.md</automated>
    <automated>cd /Users/aneenaananth/gsd-workspaces/neo4j-pipe/edgartools-platform && grep -v '^#' docs/aws-mdm-source-to-mdm.md | grep -c "sec_adv_private_fund"</automated>
  </verify>
  <acceptance_criteria>
    Source: docs/aws-mdm-source-to-mdm.md contains a "Step 1b: Parse ADV Bronze" heading.
    Source: the section states ADV bronze comes from IAPD/IARD and that standard EDGAR bootstrap does not capture ADV.
    Source: the docs document the explicit parse-adv-bronze --artifact path and the registry parse-adv-bronze path.
    Source: the docs state the required counts sec_adv_filing > 0 (adviser) and sec_adv_private_fund > 0 (fund) before MDM load, and a Phase 5 resume sequence ending at verify-graph.
  </acceptance_criteria>
  <done>The runbook documents how to populate ADV silver from IAPD bronze and the exact Phase 5 resume path with required counts; MDM-ADV-03 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize VALIDATION-NOTES and update STATE.md for Phase 10 completion</name>
  <files>
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md
    .planning/workstreams/neo4j-pipe/STATE.md
  </files>
  <read_first>
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md
    .planning/workstreams/neo4j-pipe/STATE.md
    .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-CONTEXT.md
  </read_first>
  <action>
    Finalize 10-VALIDATION-NOTES.md (D-17): ensure it carries the acquisition (Wave 1), the silver
    backfill counts and deliberate-failure evidence (Wave 2 Task 2), the MDM load commands + Postgres
    counts (Wave 2 Task 3), then add:
    - a "## Phase 10 COMPLETE" declaration listing the gates that passed (sec_adv_filing > 0,
      mdm_adviser > 0, fixture preflight test green) and which were deferred (fund + graph pipeline,
      if sec_adv_private_fund == 0) with the reason.
    - a "## Next Steps" section pointing at the Phase 5 resume sequence
      (parse-adv-bronze -> sec_adv_filing/sec_adv_private_fund counts ->
      mdm adviser/fund -> backfill-relationships -> sync-graph -> verify-graph), noting the Phase 10
      run covered a single Fork-A sample and full-scale graph sync is Phase 5 resume (D-17).

    Update STATE.md (D-18): set Phase 10 status to Complete with a one-line row-count summary
    (sec_adv_filing, sec_adv_private_fund, mdm_adviser, and mdm_fund-or-deferred). Update the
    progress block (completed_phases, percent) and the Phase Summary table row for Phase 10. Move the
    v1.1 Phase 5 blocker note to reflect that the ADV backfill path and resume evidence now exist
    (the checkpoint is unblocked pending the full-scale Phase 5 run). Do not edit the loader-fix or
    other-runtime sections (ISO-01).
  </action>
  <verify>
    <automated>cd /Users/aneenaananth/gsd-workspaces/neo4j-pipe/edgartools-platform && rg -n "Phase 10 COMPLETE|Next Steps" .planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md</automated>
    <automated>cd /Users/aneenaananth/gsd-workspaces/neo4j-pipe/edgartools-platform && rg -n "Phase 10|sec_adv_filing|mdm_adviser" .planning/workstreams/neo4j-pipe/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    Source: 10-VALIDATION-NOTES.md contains a "## Phase 10 COMPLETE" declaration and a "## Next Steps" Phase 5 resume pointer.
    Source: STATE.md shows Phase 10 status Complete with a row-count summary and an updated progress block.
    Source: STATE.md Phase 5 blocker reflects that the ADV backfill/resume evidence now exists.
    Source: no edits to loader-fix or other-runtime sections of STATE.md (ISO-01 preserved).
  </acceptance_criteria>
  <done>10-VALIDATION-NOTES.md declares Phase 10 complete with a Phase 5 resume pointer; STATE.md reflects completion with counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> runbook | Documentation guides a privileged operator who runs MDM loaders against real data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Repudiation | Runbook accuracy | mitigate | Document the verified IAPD-source fact and the exact required counts so an operator cannot run MDM adviser/fund against empty ADV silver without a documented gate. |
| T-10-03-02 | Information disclosure | Docs leaking secrets | accept | No credentials or secrets are written to docs; only command shapes and count thresholds. |
| T-10-03-SC | Tampering | npm/pip/cargo installs | mitigate | Docs/state-only plan; no packages installed. |
</threat_model>

<verification>
- docs/aws-mdm-source-to-mdm.md has a "Step 1b: Parse ADV Bronze" section and a Phase 5 resume path.
- The docs state ADV bronze is IAPD-sourced and that standard bootstrap does not capture ADV.
- The docs state required counts sec_adv_filing > 0 (adviser) and sec_adv_private_fund > 0 (fund).
- 10-VALIDATION-NOTES.md has a Phase 10 COMPLETE declaration and a Phase 5 Next Steps pointer.
- STATE.md reflects Phase 10 completion with a row-count summary.
</verification>

<success_criteria>
- MDM-ADV-03: the runbook documents the ADV backfill step (IAPD source) and the exact Phase 5 resume
  path with required silver counts.
- Phase 10 evidence is finalized and STATE.md reflects completion.
- ISO-01/02/03 preserved: no loader-fix, gold/dbt, Snowflake, or non-AWS edits.
</success_criteria>

<output>
Update `docs/aws-mdm-source-to-mdm.md`, finalize
`.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-VALIDATION-NOTES.md`,
update `.planning/workstreams/neo4j-pipe/STATE.md`, and create
`.planning/workstreams/neo4j-pipe/phases/10-live-adv-backfill-validation/10-03-SUMMARY.md` when done.
</output>
