---
phase: 05-source-to-mdm-load-path
plan: 04
type: execute
wave: 2
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - edgar_warehouse/mdm/pipeline.py
  - tests/mdm/test_source_to_mdm_load_path.py
  - docs/aws-mdm-source-to-mdm.md
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - ISO-01
  - ISO-02
must_haves:
  truths:
    - "D-12: Running entity resolution twice against the same silver fixture leaves company, adviser, person, security, and fund counts stable."
    - "D-12: Company loading uses current silver sync-state semantics or nullable tracking, not stale sec_tracked_universe."
    - "D-03, D-09, D-10, D-11: Operators have AWS-focused local and S3-backed MDM_SILVER_DUCKDB guidance that avoids SEC re-fetch and Neo4j sync scope."
    - "D-04, D-05: Documentation and idempotency repairs stay inside the neo4j-pipe worktree and avoid loader-fix artifacts, generated deployment JSON, gold/dbt, and unrelated loader code."
  artifacts:
    - path: "edgar_warehouse/mdm/pipeline.py"
      provides: "current-schema entity loading and stable all-domain entity counts"
    - path: "tests/mdm/test_source_to_mdm_load_path.py"
      provides: "green all-domain idempotency coverage"
    - path: "docs/aws-mdm-source-to-mdm.md"
      provides: "operator documentation for local/S3-backed silver source validation and bounded artifact checks"
  key_links:
    - from: "edgar_warehouse/mdm/pipeline.py"
      to: "sec_company_sync_state"
      via: "company tracking lookup"
      pattern: "sec_company_sync_state"
    - from: "tests/mdm/test_source_to_mdm_load_path.py"
      to: "edgar_warehouse/mdm/pipeline.py"
      via: "two-pass domain-count assertion"
      pattern: "mdm_(company|adviser|person|security|fund)"
    - from: "docs/aws-mdm-source-to-mdm.md"
      to: "edgar_warehouse/mdm/cli.py"
      via: "documented MDM_SILVER_DUCKDB preflight behavior"
      pattern: "MDM_SILVER_DUCKDB"
---

<objective>
Complete Phase 5 by proving idempotent MDM entity loading and documenting the operator path.

Purpose: Satisfy PIPE-02 and close the phase-level operator contract: local or S3-backed silver can feed MDM entity rows repeatedly without count drift, SEC re-fetch, or scope leakage into Phase 6/7.
Output: Current-schema MDM pipeline repair, green all-domain idempotency tests, and AWS-focused operator documentation.
</objective>

<decision_coverage>
- D-03: Keep relationship coverage and Neo4j sync downstream of silver/MDM source readiness; Phase 6 and Phase 7 own those behaviors.
- D-04: Keep implementation in the `workspace/neo4j-pipe` worktree and `neo4j-pipe` planning workstream.
- D-05: Do not edit loader-fix artifacts, generated deployment JSON, gold/dbt assets, Step Functions observability work, or unrelated loader code.
- D-09: Document that already-captured bronze primary XML is a prerequisite and absent XML is reported.
- D-10: Document that SEC fetch or artifact-capture repair is outside the default Phase 5 path.
- D-11: Document MDM silver-source preflight behavior for local and S3-backed `MDM_SILVER_DUCKDB`.
- D-12: Prove and document required nonzero source rows before company-person graph validation.
</decision_coverage>

<execution_context>
@/Users/aneenaananth/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aneenaananth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/neo4j-pipe/STATE.md
@.planning/workstreams/neo4j-pipe/ROADMAP.md
@.planning/workstreams/neo4j-pipe/REQUIREMENTS.md
@.planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-CONTEXT.md
@.planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-RESEARCH.md
@.planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-VALIDATION.md
@.planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-PATTERNS.md
@AGENTS.md
@tests/mdm/test_source_to_mdm_load_path.py

<interfaces>
From `edgar_warehouse/mdm/pipeline.py`:
- `MDMPipeline.run_companies(limit)` reads `sec_company`, `sec_company_ticker`, and currently stale `sec_tracked_universe`.
- `MDMPipeline.run_advisers`, `run_persons`, `run_securities`, and `run_funds` already use silver table inputs for the other four entity domains.
- `run_all(limit)` calls all five entity loaders, then derives relationships, and syncs graph only when a Neo4j client is present. Phase 5 tests must not require Neo4j.

From current silver DDL:
- `sec_company_sync_state(cik, tracking_status, ...)` is the current tracking source table.
- `sec_tracked_universe` is not created by current DDL and must not be required.

From Phase 5 validation:
- Assert idempotency on domain tables only: `mdm_company`, `mdm_adviser`, `mdm_person`, `mdm_security`, and `mdm_fund`. Do not assert staging/change-log tables are unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove stable all-domain MDM entity counts</name>
  <read_first>
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-CONTEXT.md
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-RESEARCH.md
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-PATTERNS.md
    edgar_warehouse/mdm/pipeline.py
    edgar_warehouse/mdm/resolvers/base.py
    edgar_warehouse/mdm/resolvers/company.py
    edgar_warehouse/mdm/resolvers/adviser.py
    edgar_warehouse/mdm/resolvers/person.py
    edgar_warehouse/mdm/resolvers/security.py
    edgar_warehouse/mdm/resolvers/fund.py
    edgar_warehouse/mdm/database.py
    tests/mdm/test_source_to_mdm_load_path.py
  </read_first>
  <files>edgar_warehouse/mdm/pipeline.py, tests/mdm/test_source_to_mdm_load_path.py</files>
  <behavior>
    - `MDMPipeline.run_companies` reads current `sec_company_sync_state` when tracking metadata exists and tolerates absent sync-state/ticker rows.
    - `sec_tracked_universe` is not required by the Phase 5 fixture or implementation.
    - Running the five entity loaders twice against the same silver fixture keeps `mdm_company`, `mdm_adviser`, `mdm_person`, `mdm_security`, and `mdm_fund` counts unchanged.
    - Tests do not require `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD`, or graph sync.
  </behavior>
  <action>Update `MDMPipeline.run_companies` so the tracking lookup uses `sec_company_sync_state` and gracefully proceeds when no tracking row exists. Keep ticker lookup optional. Do not touch resolver identity keys unless the idempotency test exposes a current duplicate-count defect. Complete the all-domain fixture in `tests/mdm/test_source_to_mdm_load_path.py` so it runs company, adviser, person, security, and fund loaders twice in the same SQLite MDM schema and asserts only domain-table counts stay stable. Keep relationship derivation and Neo4j sync out of the idempotency assertion except where existing `run_all` requires a no-client path.</action>
  <verify>
    <automated>uv run --extra mdm-runtime --extra s3 --with pytest pytest tests/mdm/test_source_to_mdm_load_path.py::test_entity_load_is_idempotent_for_domain_counts -q</automated>
    <automated>uv run --extra mdm-runtime --extra s3 --with pytest pytest tests/mdm/test_source_to_mdm_load_path.py -q</automated>
  </verify>
  <acceptance_criteria>
    Behavior: domain counts for `mdm_company`, `mdm_adviser`, `mdm_person`, `mdm_security`, and `mdm_fund` are equal after first and second load.
    Source: `rg -n "sec_tracked_universe" edgar_warehouse/mdm/pipeline.py tests/mdm/test_source_to_mdm_load_path.py` returns no active production/test dependency.
    Isolation: no Neo4j credentials are required or read in the idempotency test.
  </acceptance_criteria>
  <done>PIPE-02 is proven for all five MDM entity domains against the local silver fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Document local and S3-backed source-to-MDM operations</name>
  <read_first>
    .planning/workstreams/neo4j-pipe/ROADMAP.md
    .planning/workstreams/neo4j-pipe/REQUIREMENTS.md
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-CONTEXT.md
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-RESEARCH.md
    AGENTS.md
    docs/aws-mdm-source-to-mdm.md
    docs/neo4j.md
    scripts/ops/check-neo4j-e2e.py
  </read_first>
  <files>docs/aws-mdm-source-to-mdm.md, tests/mdm/test_source_to_mdm_load_path.py</files>
  <action>Create `docs/aws-mdm-source-to-mdm.md` with AWS-focused operator guidance for Phase 5. Document the local path using `MDM_SILVER_DUCKDB=/path/to/silver.duckdb`, the S3-backed path using `MDM_SILVER_DUCKDB=s3://.../silver.duckdb`, the optional local cache path `MDM_LOCAL_SILVER_DUCKDB`, and the commands `edgar-warehouse parse-ownership-bronze --limit ...`, `edgar-warehouse mdm run --entity-type ...`, `edgar-warehouse mdm derive-relationships ...`, and `edgar-warehouse mdm load-relationships --skip-graph-sync ...`. Include an explicit diagnostic section for required silver readiness counts: Forms 3/4/5 in `sec_company_filing`, primary rows in `sec_filing_attachment`, raw objects in `sec_raw_object`, nonzero `sec_ownership_reporting_owner`, and all five entity-domain source groups. State that absent bronze primary XML is reported and not re-fetched per D-09/D-10, relationship coverage is Phase 6, Neo4j sync is Phase 7, and generated deployment JSON plus loader-fix workstream artifacts are protected per D-04/D-05 and ISO-01/ISO-02. Keep docs AWS/local only; do not add non-AWS, retired analytics platform, non-AWS storage, new secret-management, or Terraform rollout steps.</action>
  <verify>
    <automated>uv run --extra mdm-runtime --extra s3 --with pytest pytest tests/mdm/test_source_to_mdm_load_path.py tests/application/test_parse_ownership_bronze.py -q</automated>
    <automated>bash -lc 'test -f docs/aws-mdm-source-to-mdm.md && rg -n "MDM_SILVER_DUCKDB|MDM_LOCAL_SILVER_DUCKDB|parse-ownership-bronze|--skip-graph-sync|sec_ownership_reporting_owner|sec_filing_attachment|sec_raw_object" docs/aws-mdm-source-to-mdm.md'</automated>
    <automated>bash -lc 'git status --short --untracked-files=all | grep -Ev "^(.. |\\?\\? )(.planning/workstreams/neo4j-pipe/|edgar_warehouse/mdm/pipeline.py$|tests/mdm/test_source_to_mdm_load_path.py$|docs/aws-mdm-source-to-mdm.md$|edgar_warehouse/cli.py$|edgar_warehouse/application/warehouse_orchestrator.py$|tests/application/test_parse_ownership_bronze.py$)" | test "$(wc -l)" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    Documentation: the doc names local and S3-backed `MDM_SILVER_DUCKDB`, required source tables/counts, no SEC re-fetch behavior, and Phase 6/7 boundaries.
    CLI-output: quick Phase 5 tests pass together.
    Static: changed files exclude loader-fix artifacts, generated deployment JSON, gold/dbt assets, and Step Functions observability files.
  </acceptance_criteria>
  <done>Operators can follow documented local/S3-backed Phase 5 commands and diagnostics, and the repo proves all five MDM entity domains are idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Silver DuckDB -> MDM resolvers | Source rows become mutable MDM entity rows. |
| Operator docs -> live AWS operation | Documentation guides commands that can read S3 and mutate MDM SQL. |
| Phase 5 -> later phases | Relationship derivation coverage and Neo4j sync must not be pulled into Phase 5 implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | Entity loader idempotency | mitigate | Tests assert stable counts for `mdm_company`, `mdm_adviser`, `mdm_person`, `mdm_security`, and `mdm_fund` across repeated runs. |
| T-05-19 | Tampering | Current silver schema | mitigate | Replace stale `sec_tracked_universe` dependency with `sec_company_sync_state` or nullable tracking lookup. |
| T-05-20 | Information Disclosure | Operator documentation | mitigate | Document variable names and paths only; do not include secret values or command examples that print passwords. |
| T-05-21 | Tampering | Scope leakage into graph sync | mitigate | Keep tests credential-free and document `--skip-graph-sync` for Phase 5 source-readiness validation. |
| T-05-22 | Repudiation | Live artifact readiness | mitigate | Documentation lists bounded readiness-count checks for `sec_company_filing`, `sec_filing_attachment`, `sec_raw_object`, and ownership rows. |
| T-05-SC | Tampering | Package installs | accept | No new project dependencies or package-manager install tasks are planned. |
</threat_model>

<verification>
Run `uv run --extra mdm-runtime --extra s3 --with pytest pytest tests/mdm/test_source_to_mdm_load_path.py tests/application/test_parse_ownership_bronze.py -q`, then run `git status --short --untracked-files=all` and confirm only Phase 5 source/test/docs/planning files changed.
</verification>

<success_criteria>
Phase 5 is complete when local/S3-backed silver preflight is documented, all Phase 5 quick tests pass, MDM entity domain counts stay stable across repeated runs, missing source failures remain non-mutating, and changed files preserve neo4j-pipe isolation.
</success_criteria>

<output>
Create `.planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-04-SUMMARY.md` when done.
</output>
