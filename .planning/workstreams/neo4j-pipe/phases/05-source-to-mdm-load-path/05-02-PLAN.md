---
phase: 05-source-to-mdm-load-path
plan: 02
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - edgar_warehouse/cli.py
  - edgar_warehouse/application/warehouse_orchestrator.py
  - tests/application/test_parse_ownership_bronze.py
autonomous: true
requirements:
  - PIPE-01
  - ISO-01
  - ISO-02
must_haves:
  truths:
    - "D-01, D-06, D-09, D-10: Operator can run the existing parse-ownership-bronze command against already-captured bronze artifacts without adding a second command or re-fetching SEC data."
    - "D-02, D-03, D-07, D-08: Ownership parser output is written into the current silver ownership tables from artifact-registry XML and accessions already parsed are skipped."
    - "D-04, D-05: Parser repair stays inside the neo4j-pipe worktree and avoids loader-fix artifacts, generated deployment JSON, gold/dbt, and unrelated loader code."
    - "D-09, D-10: Absent primary XML artifacts are reported clearly and do not trigger SEC re-fetch."
  artifacts:
    - path: "edgar_warehouse/cli.py"
      provides: "bounded arguments for the existing parse-ownership-bronze command"
    - path: "edgar_warehouse/application/warehouse_orchestrator.py"
      provides: "current-schema ownership bronze-to-silver parser loop"
    - path: "tests/application/test_parse_ownership_bronze.py"
      provides: "green regression coverage for parser command behavior"
  key_links:
    - from: "edgar_warehouse/cli.py"
      to: "edgar_warehouse/application/warehouse_orchestrator.py"
      via: "run_command('parse-ownership-bronze', args)"
      pattern: "parse-ownership-bronze"
    - from: "edgar_warehouse/application/warehouse_orchestrator.py"
      to: "edgar_warehouse/parsers/ownership.py"
      via: "parse_ownership(accession, xml_content, form)"
      pattern: "parse_ownership"
    - from: "edgar_warehouse/application/warehouse_orchestrator.py"
      to: "edgar_warehouse/infrastructure/object_storage.py"
      via: "read_bytes(storage_path)"
      pattern: "read_bytes"
---

<objective>
Repair the existing `parse-ownership-bronze` command so it populates silver ownership rows from already-captured bronze XML.

Purpose: Satisfy D-01 through D-10 and PIPE-01 by making ownership source rows available to MDM without SEC re-fetch or loader-fix workstream overlap.
Output: Green parser-command tests and repaired warehouse command behavior.
</objective>

<decision_coverage>
- D-01: Use the existing edgartools-backed ownership parser path to populate missing Forms 3/4/5 silver tables from bronze primary XML artifacts.
- D-02: Write parser output into `sec_ownership_reporting_owner`, `sec_ownership_non_derivative_txn`, and `sec_ownership_derivative_txn`.
- D-03: Keep MDM and Neo4j downstream of silver ownership rows; do not derive MDM relationships directly from bronze XML.
- D-04: Keep implementation in the `workspace/neo4j-pipe` worktree and `neo4j-pipe` planning workstream.
- D-05: Do not edit loader-fix artifacts, generated deployment JSON, gold/dbt assets, Step Functions observability work, or unrelated loader code.
- D-06: Repair and use the existing `parse-ownership-bronze` command rather than adding a second ownership backfill command.
- D-07: Fix `parse-ownership-bronze` to use `sec_company_filing.form` and `report_date`, not stale `form_type` and `period_of_report`.
- D-08: Prefer artifact-registry reads through `sec_filing_attachment` plus `sec_raw_object` before any path fallback.
- D-09: Treat already-captured bronze primary XML as the prerequisite and report missing artifacts clearly.
- D-10: Do not add SEC fetch or artifact-capture repair behavior to the default Phase 5 parser path.
</decision_coverage>

<execution_context>
@/Users/aneenaananth/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aneenaananth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/neo4j-pipe/STATE.md
@.planning/workstreams/neo4j-pipe/ROADMAP.md
@.planning/workstreams/neo4j-pipe/REQUIREMENTS.md
@.planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-CONTEXT.md
@.planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-RESEARCH.md
@.planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-PATTERNS.md
@AGENTS.md
@tests/application/test_parse_ownership_bronze.py

<interfaces>
Existing command surface:
- `edgar_warehouse/cli.py` registers `parse-ownership-bronze` and routes to `_handle_parse_ownership_bronze`.
- `edgar_warehouse/application/warehouse_orchestrator.py` dispatches command name `parse-ownership-bronze` to `_run_parse_ownership_bronze(...)`.

Current silver DDL from `edgar_warehouse/silver_store.py`:
- `sec_company_filing(accession_number, cik, form, filing_date, report_date, primary_document, ...)`.
- `sec_ownership_reporting_owner`, `sec_ownership_non_derivative_txn`, and `sec_ownership_derivative_txn` are merge targets per D-02.
- `sec_raw_object(storage_path, ...)` and `sec_filing_attachment(is_primary, raw_object_id, ...)` form the artifact registry per D-08.

Parser adapter:
- `edgar_warehouse/parsers/ownership.py` imports `from edgar.ownership import Ownership` and calls `Ownership.from_xml(content)`. Do not alter this import without a changelog check.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make parse-ownership-bronze tests green</name>
  <read_first>
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-CONTEXT.md
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-RESEARCH.md
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-PATTERNS.md
    edgar_warehouse/cli.py
    edgar_warehouse/application/warehouse_orchestrator.py
    edgar_warehouse/silver_store.py
    edgar_warehouse/infrastructure/object_storage.py
    edgar_warehouse/parsers/ownership.py
    tests/application/test_parse_ownership_bronze.py
  </read_first>
  <files>edgar_warehouse/cli.py, edgar_warehouse/application/warehouse_orchestrator.py, tests/application/test_parse_ownership_bronze.py</files>
  <behavior>
    - Current-schema query: Form 3/4/5 accessions are selected from `sec_company_filing.form` and ordered with `report_date`, not stale `form_type` or `period_of_report` per D-07.
    - Registry-first read: Primary XML bytes are read through `sec_filing_attachment` -> `sec_raw_object` -> `read_bytes(storage_path)` per D-08.
    - Existing command only: implementation repairs `parse-ownership-bronze`; it does not add a second ownership backfill command per D-06.
    - No re-fetch: no SEC API/download helper is called when a primary artifact is missing per D-09 and D-10.
  </behavior>
  <action>Update `parse-ownership-bronze` registration in `edgar_warehouse/cli.py` to add bounded operator controls `--limit` and `--accession-list` while keeping the command name unchanged per D-06. Update `_run_parse_ownership_bronze(...)` to read `accession_number`, `cik`, and `form` from `sec_company_filing`; filter to Forms `3`, `3/A`, `4`, `4/A`, `5`, and `5/A`; apply `--accession-list` and `--limit` from command arguments; skip accessions already present in `sec_ownership_reporting_owner`; call `_read_primary_artifact_bytes(db, accession_number)` before any path fallback; parse via `parse_ownership(accession, xml_content, form)`; merge into the three D-02 ownership tables; and emit metrics/events for parsed, skipped, missing-artifact, error, and rows-written counts. Remove the direct `fsspec.filesystem("s3")` listing from the default registry path. Keep any path fallback bounded to derived primary-document storage only and do not add SEC client calls.</action>
  <verify>
    <automated>uv run --extra s3 --with pytest pytest tests/application/test_parse_ownership_bronze.py -q</automated>
  </verify>
  <acceptance_criteria>
    Behavior: parser tests pass and prove D-01 through D-10.
    Source: `rg -n "form_type|period_of_report|fsspec\\.filesystem\\(\"s3\"\\)|download_sec_bytes|download_sec_submission" edgar_warehouse/application/warehouse_orchestrator.py` shows no matches inside `_run_parse_ownership_bronze`.
    CLI: `edgar-warehouse parse-ownership-bronze --help` includes `--limit` and `--accession-list` on the existing command.
  </acceptance_criteria>
  <done>`parse-ownership-bronze` reads current silver schema, prefers artifact registry, parses existing XML into silver ownership tables, and never re-fetches SEC artifacts in the default path.</done>
</task>

<task type="auto">
  <name>Task 2: Harden parse diagnostics and static isolation checks</name>
  <read_first>
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-CONTEXT.md
    .planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-VALIDATION.md
    edgar_warehouse/cli.py
    edgar_warehouse/application/warehouse_orchestrator.py
    tests/application/test_parse_ownership_bronze.py
  </read_first>
  <files>edgar_warehouse/application/warehouse_orchestrator.py, tests/application/test_parse_ownership_bronze.py</files>
  <action>Extend the parser tests and implementation to make operator diagnostics explicit: when registry rows are absent or a raw object is missing, increment a named missing-artifact metric, emit a `parse_ownership_bronze_missing_artifact` or equivalent structured event with accession number and run id, continue to the next accession, and return nonzero parsed rows only for successfully merged accessions. Add static assertions in the test file that the Phase 5 path does not import SEC download helpers and does not touch loader-fix workstream or generated deployment JSON. Do not change gold/dbt, Step Functions observability, or deployment JSON surfaces per D-04, D-05, ISO-01, and ISO-02.</action>
  <verify>
    <automated>uv run --extra s3 --with pytest pytest tests/application/test_parse_ownership_bronze.py -q</automated>
    <automated>bash -lc 'git status --short --untracked-files=all | grep -Ev "^(.. |\\?\\? )(.planning/workstreams/neo4j-pipe/|edgar_warehouse/cli.py$|edgar_warehouse/application/warehouse_orchestrator.py$|tests/application/test_parse_ownership_bronze.py$|docs/aws-mdm-source-to-mdm.md$)" | test "$(wc -l)" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    CLI-output: missing artifacts are visible through structured event payloads or command metrics with accession numbers.
    Static: changed files stay within this plan's declared file set plus allowed planning summaries created by execution.
    Source: no new ownership parser implementation exists outside `edgar_warehouse/parsers/ownership.py`.
  </acceptance_criteria>
  <done>Operators can run a bounded ownership parse repair, see missing-artifact diagnostics, and trust that the command does not silently re-fetch SEC data or edit unrelated workstream surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI -> warehouse command | `--limit` and `--accession-list` bound execution but must not alter command identity or bypass idempotency. |
| Silver filing registry -> object storage | Registry storage paths cross into local/S3 object reads through `read_bytes`. |
| Bronze XML -> silver ownership rows | Parsed XML becomes mutable silver rows through merge methods. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | `_run_parse_ownership_bronze` SQL | mitigate | Use fixed Forms 3/4/5 constants and parameterized/accession-list filtering; do not interpolate arbitrary operator identifiers. |
| T-05-08 | Information Disclosure | Artifact storage path reads | mitigate | Prefer `_read_primary_artifact_bytes` and `object_storage.read_bytes()` so unsupported protocols are rejected by existing allowlisting. |
| T-05-09 | Repudiation | Missing artifact handling | mitigate | Emit structured missing-artifact events and named metrics with accession numbers and run id. |
| T-05-10 | Tampering | Parser implementation | mitigate | Keep `edgar_warehouse.parsers.ownership.parse_ownership` and `Ownership.from_xml(content)` as the sole Form 3/4/5 parser path per AGENTS.md and D-01. |
| T-05-11 | Denial of Service | Unbounded live repair | mitigate | Add `--limit` and `--accession-list` to the existing command so operator validation can be bounded. |
| T-05-12 | Tampering | SEC re-fetch | mitigate | Remove direct SEC client/download calls from the parser repair path; missing artifacts are reported, not fetched. |
| T-05-SC | Tampering | Package installs | accept | No new project dependency install is planned; no package-manager install tasks exist. |
</threat_model>

<verification>
`uv run --extra s3 --with pytest pytest tests/application/test_parse_ownership_bronze.py -q` passes, and static grep confirms no stale schema columns or SEC download helpers remain in the repaired parser path.
</verification>

<success_criteria>
PIPE-01 is satisfied for the ownership source path when `parse-ownership-bronze` consumes current silver metadata, reads existing primary XML artifacts, writes the three ownership silver tables, skips already parsed accessions, reports missing artifacts, and avoids loader-fix/generated deployment changes.
</success_criteria>

<output>
Create `.planning/workstreams/neo4j-pipe/phases/05-source-to-mdm-load-path/05-02-SUMMARY.md` when done.
</output>
