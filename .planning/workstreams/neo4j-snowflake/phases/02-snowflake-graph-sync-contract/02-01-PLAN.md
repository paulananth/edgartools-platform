---
phase: 02-snowflake-graph-sync-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - edgar_warehouse/mdm/snowflake_graph.py
  - tests/mdm/test_snowflake_graph_migration.py
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SNOW-02
  - SNOW-04
must_haves:
  truths:
    - "`generate_snowflake_graph_migration` emits idempotent Snowflake graph projection SQL for `EDGARTOOLS_DEV.NEO4J_GRAPH_MIGRATION` without external Neo4j credentials."
    - "Generated SQL materializes active MDM entities into `MDM_GRAPH_NODES` and Native App-compatible `GRAPH_NODE_*` projections with stable `NODEID`, labels, source metadata, and timestamps."
    - "Generated SQL materializes active MDM relationships into `MDM_GRAPH_EDGES` and Native App-compatible `GRAPH_EDGE_*` projections with stable `SOURCENODEID`, `TARGETNODEID`, relationship type, properties, and sync status."
    - "Generated SQL reconciles the Phase 1 `MDM_GRAPH_NODES` / `MDM_GRAPH_EDGES` contract with live operator table naming conventions `GRAPH_NODE_*` / `GRAPH_EDGE_*`."
  artifacts:
    - path: edgar_warehouse/mdm/snowflake_graph.py
      provides: "Snowflake graph projection SQL renderer for MDM nodes, edges, validation views, and governed Native App output guidance."
      exports:
        - SnowflakeGraphMigrationConfig
        - generate_snowflake_graph_migration
        - render_graph_tables
        - render_validation
        - render_readme
    - path: tests/mdm/test_snowflake_graph_migration.py
      provides: "Credential-free SQL generation assertions for graph projection naming, idempotency, and Native App table conventions."
  key_links:
    - from: edgar_warehouse/mdm/snowflake_graph.py
      to: EDGARTOOLS_DEV.NEO4J_GRAPH_MIGRATION
      via: "SnowflakeGraphMigrationConfig default target schema and rendered fully qualified identifiers"
      pattern: "NEO4J_GRAPH_MIGRATION"
    - from: edgar_warehouse/mdm/snowflake_graph.py
      to: MDM_GRAPH_NODES
      via: "CREATE OR REPLACE TABLE or VIEW generated from Snowflake MDM mirror tables"
      pattern: "MDM_GRAPH_NODES"
    - from: edgar_warehouse/mdm/snowflake_graph.py
      to: GRAPH_NODE_*
      via: "per-label compatibility projections over `MDM_GRAPH_NODES`"
      pattern: "GRAPH_NODE_COMPANY"
    - from: edgar_warehouse/mdm/snowflake_graph.py
      to: GRAPH_EDGE_*
      via: "per-relationship compatibility projections over `MDM_GRAPH_EDGES`"
      pattern: "GRAPH_EDGE_IS_INSIDER"
---

<objective>
Create the Snowflake graph projection SQL contract that Phase 2 execution will materialize.

Purpose: `edgar-warehouse mdm sync-graph` needs a deterministic Snowflake-hosted graph shape before the CLI can stop depending on `NEO4J_URI`, `NEO4J_USER`, `NEO4J_USERNAME`, `NEO4J_PASSWORD`, `NEO4J_DATABASE`, or `NEO4J_SECRET_JSON`.

Output: Updated SQL generation and tests for `MDM_GRAPH_NODES`, `MDM_GRAPH_EDGES`, `GRAPH_NODE_*`, and `GRAPH_EDGE_*` in `EDGARTOOLS_DEV.NEO4J_GRAPH_MIGRATION`.
</objective>

<execution_context>
@/Users/aneenaananth/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aneenaananth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/neo4j-snowflake/PROJECT.md
@.planning/workstreams/neo4j-snowflake/ROADMAP.md
@.planning/workstreams/neo4j-snowflake/REQUIREMENTS.md
@.planning/workstreams/neo4j-snowflake/STATE.md
@.planning/workstreams/neo4j-snowflake/SNOWFLAKE-GRAPH-ANALYTICS-AGENT-INSTRUCTIONS.md
@.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-ARCHITECTURE-DECISION.md
@.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-GRAPH-PROJECTION-CONTRACT.md
@.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-PLAN-REVIEW-QUESTIONS.md
@.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-VERIFICATION.md
@edgar_warehouse/mdm/snowflake_graph.py
@edgar_warehouse/mdm/migrations/runtime.py
@edgar_warehouse/mdm/migrations/002_seed_data.sql
@tests/mdm/test_snowflake_graph_migration.py

<interfaces>
From `edgar_warehouse/mdm/snowflake_graph.py`:
- `SnowflakeGraphMigrationConfig(env, output_dir, target_database=None, target_schema="NEO4J_GRAPH_MIGRATION", mdm_database=None, mdm_schema="MDM", silver_path=None)`
- `generate_snowflake_graph_migration(config) -> dict[str, Path]`
- `render_graph_tables(context) -> str`
- `render_validation(context) -> str`
- `render_hosted_neo4j_e2e(context) -> str`
- `run_snowflake_graph_sql(files, snow_connection) -> list[str]`

Existing generated artifacts:
- `00_graph_tables.sql`
- `01_validation.sql`
- `02_hosted_neo4j_e2e.sql`
- `README.md`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the projection contract in SQL generation tests</name>
  <files>tests/mdm/test_snowflake_graph_migration.py</files>
  <read_first>
    - tests/mdm/test_snowflake_graph_migration.py
    - .planning/workstreams/neo4j-snowflake/SNOWFLAKE-GRAPH-ANALYTICS-AGENT-INSTRUCTIONS.md
    - .planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-GRAPH-PROJECTION-CONTRACT.md
  </read_first>
  <action>Add failing assertions before implementation for the Phase 2 contract: generated SQL must contain `EDGARTOOLS_DEV.NEO4J_GRAPH_MIGRATION`, `MDM_GRAPH_NODES`, `MDM_GRAPH_EDGES`, `GRAPH_NODE_COMPANY`, `GRAPH_NODE_PERSON`, `GRAPH_NODE_SECURITY`, `GRAPH_NODE_ADVISER`, `GRAPH_NODE_FUND`, `GRAPH_EDGE_IS_INSIDER`, `GRAPH_EDGE_HOLDS`, `GRAPH_EDGE_COMPANY_HOLDS`, `GRAPH_EDGE_ISSUED_BY`, `GRAPH_EDGE_IS_ENTITY_OF`, `GRAPH_EDGE_HAS_PARENT_COMPANY`, `GRAPH_EDGE_MANAGES_FUND`, `GRAPH_EDGE_IS_PERSON_OF`, `NODEID`, `SOURCENODEID`, `TARGETNODEID`, stable source metadata, timestamps, and sync status. Assert the generated SQL and README do not require `NEO4J_URI`, `NEO4J_USER`, `NEO4J_USERNAME`, `NEO4J_PASSWORD`, `NEO4J_DATABASE`, or `NEO4J_SECRET_JSON`. Keep tests credential-free and do not invoke Snowflake.</action>
  <verify>
    <automated>uv run pytest tests/mdm/test_snowflake_graph_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before `snowflake_graph.py` is updated because the new Phase 2 contract names and columns are absent.
    - Tests explicitly cover `MDM_GRAPH_NODES` / `MDM_GRAPH_EDGES` and `GRAPH_NODE_*` / `GRAPH_EDGE_*` reconciliation.
    - Tests include a no-external-Neo4j credential assertion for generated graph sync SQL and README text.
  </acceptance_criteria>
  <done>The test file defines the exact Snowflake graph projection contract expected from this plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render idempotent Snowflake graph projection SQL</name>
  <files>edgar_warehouse/mdm/snowflake_graph.py, tests/mdm/test_snowflake_graph_migration.py</files>
  <read_first>
    - edgar_warehouse/mdm/snowflake_graph.py
    - edgar_warehouse/mdm/migrations/runtime.py
    - edgar_warehouse/mdm/migrations/002_seed_data.sql
    - tests/mdm/test_snowflake_graph_migration.py
  </read_first>
  <action>Update `render_graph_tables`, `render_validation`, and `render_readme` so `00_graph_tables.sql` creates or replaces the governed target schema, materializes `MDM_GRAPH_NODES` and `MDM_GRAPH_EDGES`, and then exposes Native App-compatible `GRAPH_NODE_*` and `GRAPH_EDGE_*` projections over those canonical tables. Use uppercase unquoted Snowflake identifiers for `NODEID`, `SOURCENODEID`, and `TARGETNODEID` to match the live `EDGARTOOLS_DEV.NEO4J_GRAPH_MIGRATION` convention. Preserve stable node identity from `mdm_entity.entity_id`, skip quarantined entities, include labels from `mdm_entity_type_definition.neo4j_label`, include entity type and source/timestamp metadata, and include properties without secrets. Preserve stable edge identity from `mdm_relationship_instance.instance_id`, active relationship filtering, seeded relationship type names, endpoint ids, provenance fields, `GRAPH_SYNC_STATUS`, and `GRAPH_SYNCED_AT`. Use `CREATE OR REPLACE` or deterministic `MERGE` semantics only; do not append into graph tables. Document in README that `NEO4J_GRAPH_ANALYTICS.GRAPH.*` procedures consume the generated graph tables but are not invoked by this SQL generation step.</action>
  <verify>
    <automated>uv run pytest tests/mdm/test_snowflake_graph_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `00_graph_tables.sql` contains `MDM_GRAPH_NODES`, `MDM_GRAPH_EDGES`, per-label `GRAPH_NODE_*`, and per-type `GRAPH_EDGE_*` projections.
    - Node rows are active, non-quarantined MDM entities with stable `NODEID`, label, entity type, source metadata, timestamps, and properties.
    - Edge rows are active MDM relationships with stable edge id, `SOURCENODEID`, `TARGETNODEID`, relationship type, properties, provenance, and sync status.
    - Validation SQL reports node counts, edge counts, active MDM relationship parity, and missing endpoint diagnostics.
    - README states algorithm outputs from `NEO4J_GRAPH_ANALYTICS.GRAPH.*` belong in governed `EDGARTOOLS_DEV.NEO4J_GRAPH_MIGRATION` tables with operator cleanup ownership, satisfying `SNOW-04`.
  </acceptance_criteria>
  <done>Generated graph SQL passes the focused tests and expresses the complete Phase 2 projection contract without live Snowflake credentials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| MDM mirror tables -> Snowflake graph schema | Existing MDM data becomes graph-readable projection rows. |
| Generated SQL -> Snowflake execution | SQL text will later be executed against a live Snowflake account. |
| Operator config -> identifier rendering | Database and schema names become Snowflake identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-02-01-01 | Tampering | `SnowflakeGraphMigrationConfig` identifiers | mitigate | Keep `_ident` validation for database, schema, and table identifiers; tests must not introduce unvalidated identifier interpolation. |
| T-02-01-02 | Information Disclosure | Node and edge `PROPERTIES` payloads | mitigate | Include only MDM domain/provenance fields; assert generated SQL does not reference `NEO4J_URI`, `NEO4J_USER`, `NEO4J_USERNAME`, `NEO4J_PASSWORD`, `NEO4J_DATABASE`, or `NEO4J_SECRET_JSON`. |
| T-02-01-03 | Denial of Service | Graph table rebuild SQL | accept | Phase 2 generates deterministic SQL only; live scheduling and warehouse sizing are operator concerns in later phases. |
| T-02-01-SC | Tampering | Package installs | accept | No package installs are planned in this plan. |
</threat_model>

<verification>
Run `uv run pytest tests/mdm/test_snowflake_graph_migration.py -q`. Review generated SQL snippets in the test temp output only if failures occur. Do not run live Snowflake SQL in this plan.
</verification>

<success_criteria>
- `MDM_GRAPH_NODES` and `MDM_GRAPH_EDGES` are first-class generated graph inputs.
- `GRAPH_NODE_*` and `GRAPH_EDGE_*` compatibility projections reconcile Phase 1 names with the live operator naming convention in `EDGARTOOLS_DEV.NEO4J_GRAPH_MIGRATION`.
- The generated graph contract is idempotent and credential-free.
- `SNOW-02` and `SNOW-04` are represented in executable SQL and README text.
</success_criteria>

<source_audit>
GOAL covered: graph-ready Snowflake node and edge state from MDM data.
REQ covered: SYNC-01, SYNC-02, SNOW-02, SNOW-04.
RESEARCH/PHASE-1 covered: `MDM_GRAPH_NODES`, `MDM_GRAPH_EDGES`, `GRAPH_NODE_*`, `GRAPH_EDGE_*`, `NODEID`, `SOURCENODEID`, `TARGETNODEID`, `NEO4J_GRAPH_ANALYTICS.GRAPH.*`.
CONTEXT covered: AWS/Snowflake-only, no external Neo4j validation target, no `NEO4J_*` credential dependency.
</source_audit>

<output>
Create `.planning/workstreams/neo4j-snowflake/phases/02-snowflake-graph-sync-contract/02-01-SUMMARY.md` when done.
</output>
