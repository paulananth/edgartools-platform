---
phase: 01-snowflake-native-app-feasibility-and-architecture-decision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-ARCHITECTURE-DECISION.md
requirements:
  - DISC-02
  - DISC-03
  - ISO-01
  - ISO-02
autonomous: true
must_haves:
  truths:
    - "D-02: This milestone is a production migration path with Phase 1 feasibility first."
    - "D-03: edgar-warehouse mdm sync-graph remains the graph sync command surface."
    - "D-04: External Neo4j is not retained as a parallel validation target for this milestone."
    - "D-05: Graph access comes from Snowflake-managed app roles, grants, database roles, and connection context rather than external NEO4J_* secrets."
    - "Current Neo4j runtime inputs are NEO4J_URI, NEO4J_USER/NEO4J_USERNAME, NEO4J_PASSWORD, NEO4J_DATABASE, and NEO4J_SECRET_JSON in edgar_warehouse/mdm/cli.py."
    - "Current Snowflake runtime/export inputs include MDM_SNOWFLAKE_* or DBT_SNOWFLAKE_* in edgar_warehouse/mdm/export.py and SERVING_EXPORT_ROOT/SNOWFLAKE_EXPORT_ROOT in warehouse settings."
  artifacts:
    - path: ".planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-ARCHITECTURE-DECISION.md"
      provides: "Architecture decision record for replacing external Neo4j with the Snowflake Native App path"
      contains: "Status: Accepted for milestone planning"
  key_links:
    - from: "01-ARCHITECTURE-DECISION.md"
      to: ".planning/workstreams/neo4j-snowflake/ROADMAP.md"
      via: "Phase 2 through Phase 4 dependency assumptions"
      pattern: "Phase 2|Phase 3|Phase 4"
---

<objective>
Create the architecture decision record that makes the Native App the milestone graph
target, keeps `edgar-warehouse` as the graph sync owner, and rejects external Neo4j
credentials and dual external validation for this milestone.

Purpose: Satisfy DISC-02 and DISC-03 by converting the user's decisions into a durable
planning contract that later implementation phases must follow.

Output: `01-ARCHITECTURE-DECISION.md` in the Phase 1 workstream directory.
</objective>

<execution_context>
@/Users/aneenaananth/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aneenaananth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/neo4j-snowflake/PROJECT.md
@.planning/workstreams/neo4j-snowflake/REQUIREMENTS.md
@.planning/workstreams/neo4j-snowflake/ROADMAP.md
@.planning/workstreams/neo4j-snowflake/STATE.md
@.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-CONTEXT.md
@.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-RESEARCH.md
@.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-PATTERNS.md
@edgar_warehouse/mdm/cli.py
@edgar_warehouse/mdm/export.py
@edgar_warehouse/infrastructure/warehouse_settings.py
@edgar_warehouse/serving/targets/snowflake.py
@infra/terraform/access/snowflake/modules/account_access/main.tf
@infra/scripts/deploy-aws-application.sh
@infra/scripts/run-aws-mdm-e2e.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the migration architecture decision</name>
  <files>.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-ARCHITECTURE-DECISION.md</files>
  <read_first>
    - `.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-CONTEXT.md` for D-02 through D-07.
    - `.planning/workstreams/neo4j-snowflake/PROJECT.md` for milestone architecture and scope boundaries.
    - `.planning/workstreams/neo4j-snowflake/ROADMAP.md` for downstream phase dependencies.
    - `edgar_warehouse/mdm/cli.py` for `_neo4j_client()`, `check-connectivity --neo4j`, `sync-graph`, `verify-graph`, `load-relationships`, and `backfill-relationships` behavior that currently depends on `NEO4J_URI`, `NEO4J_USER`/`NEO4J_USERNAME`, `NEO4J_PASSWORD`, `NEO4J_DATABASE`, and `NEO4J_SECRET_JSON`.
    - `edgar_warehouse/mdm/export.py` for the existing Snowflake connector configuration surface: `MDM_SNOWFLAKE_ACCOUNT`, `MDM_SNOWFLAKE_USER`, `MDM_SNOWFLAKE_PASSWORD`, `MDM_SNOWFLAKE_DATABASE`, `MDM_SNOWFLAKE_SCHEMA`, `MDM_SNOWFLAKE_WAREHOUSE`, `MDM_SNOWFLAKE_ROLE`, and `DBT_SNOWFLAKE_*` fallbacks.
    - `edgar_warehouse/infrastructure/warehouse_settings.py` for `SERVING_EXPORT_ROOT`, `SNOWFLAKE_EXPORT_ROOT`, and `MDM_DATABASE_URL` runtime validation.
    - `edgar_warehouse/serving/targets/snowflake.py` for the current Snowflake export writer boundary.
    - `infra/terraform/access/snowflake/modules/account_access/main.tf` for existing Snowflake account-role, schema, warehouse, and future-object grant patterns.
    - `infra/scripts/deploy-aws-application.sh` and `infra/scripts/run-aws-mdm-e2e.sh` for AWS MDM deployment and validation entry points that Phase 2/3 must not break.
  </read_first>
  <action>Create `01-ARCHITECTURE-DECISION.md` with sections named exactly `# ADR: Snowflake Native App Neo4j Target`, `## Status`, `## Context`, `## Current Runtime Mapping`, `## Decision`, `## Consequences`, `## Credential And Configuration Model`, `## Downstream Phase Contract`, `## Rejected Alternatives`, `## Open Questions`, and `## Sources`. Set `## Status` to `Accepted for milestone planning`. In `## Current Runtime Mapping`, name the exact current external Neo4j inputs `NEO4J_URI`, `NEO4J_USER`, `NEO4J_USERNAME`, `NEO4J_PASSWORD`, `NEO4J_DATABASE`, and `NEO4J_SECRET_JSON`; name the handlers that currently consume `_neo4j_client()` (`check-connectivity --neo4j`, `sync-graph`, `verify-graph`, `load-relationships`, and `backfill-relationships`); and name the existing Snowflake configuration surfaces `MDM_SNOWFLAKE_*`, `DBT_SNOWFLAKE_*`, `SERVING_EXPORT_ROOT`, `SNOWFLAKE_EXPORT_ROOT`, and Snowflake access Terraform account roles/grants. In `## Decision`, state that the Snowflake Marketplace Neo4j Graph Analytics Native App replaces the external Neo4j target for this milestone, `edgar-warehouse mdm sync-graph` remains the command surface, no external Neo4j parallel validation target is planned, and milestone validation must not depend on `NEO4J_URI`, `NEO4J_USER`, or `NEO4J_PASSWORD`. In `## Credential And Configuration Model`, specify that Phase 2 must replace the `_neo4j_client()`/Bolt credential path for graph sync with a Snowflake execution context using Snowflake-managed app roles, database roles, table/view grants, warehouse or app warehouse context, and application role assignment, while preserving existing AWS/Snowflake runtime settings for MDM database access and Snowflake export where they remain valid.</action>
  <verify>
    <automated>test -f .planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-ARCHITECTURE-DECISION.md</automated>
    <automated>rg -n "Accepted for milestone planning|Current Runtime Mapping|edgar-warehouse mdm sync-graph|_neo4j_client|NEO4J_URI|NEO4J_USER|NEO4J_USERNAME|NEO4J_PASSWORD|NEO4J_DATABASE|NEO4J_SECRET_JSON|MDM_SNOWFLAKE_|DBT_SNOWFLAKE_|SERVING_EXPORT_ROOT|SNOWFLAKE_EXPORT_ROOT|no external Neo4j parallel validation" .planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-ARCHITECTURE-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - ADR explicitly covers D-02, D-03, D-04, and D-05.
    - ADR maps the current `NEO4J_*` and `NEO4J_SECRET_JSON` CLI path to the future Snowflake-managed app role, database role, grant, warehouse/app warehouse, and Snowflake connection context path.
    - ADR identifies which current code paths Phase 2 must change: `_neo4j_client()`, `sync-graph`, `verify-graph`, graph connectivity checks, and graph sync call sites that expect `Neo4jGraphClient`.
    - ADR states which existing AWS/Snowflake settings remain in scope for this migration: MDM database access, `MDM_SNOWFLAKE_*`/`DBT_SNOWFLAKE_*` export settings, `SERVING_EXPORT_ROOT`, and Snowflake access grants.
    - ADR lists rejected alternatives: external Neo4j Aura/Bolt as validation target, self-hosted Neo4j, dual-write validation, non-AWS/non-aws-app revival, and dashboard-owned graph writes.
    - ADR states downstream Phase 2, Phase 3, and Phase 4 obligations without requiring Phase 1 source code changes.
  </acceptance_criteria>
  <done>Architecture decision is durable enough for plan-review convergence and Phase 2 implementation planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ECS runtime -> Snowflake | `edgar-warehouse` will use Snowflake context rather than external Neo4j secrets. |
| Planning docs -> future implementation | Later phases will treat the ADR as the source of truth for graph target and credential model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | Graph target configuration | mitigate | ADR rejects ambiguous fallback to external Bolt targets and requires Snowflake app context for milestone validation. |
| T-01-05 | Information Disclosure | Legacy external credentials | mitigate | ADR states `NEO4J_URI`, `NEO4J_USER`, and `NEO4J_PASSWORD` are not milestone validation dependencies. |
| T-01-06 | Repudiation | Cutover decision | mitigate | ADR records accepted decision, rejected alternatives, consequences, and open questions in a committed planning artifact. |
</threat_model>

<verification>
Run:

- `rg -n "Accepted for milestone planning|Current Runtime Mapping|edgar-warehouse mdm sync-graph|_neo4j_client|NEO4J_URI|NEO4J_USER|NEO4J_USERNAME|NEO4J_PASSWORD|NEO4J_DATABASE|NEO4J_SECRET_JSON|MDM_SNOWFLAKE_|DBT_SNOWFLAKE_|SERVING_EXPORT_ROOT|SNOWFLAKE_EXPORT_ROOT|no external Neo4j parallel validation" .planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-ARCHITECTURE-DECISION.md`
- `rg -n "non-AWS|non-AWS app runtime|dual-write|external Neo4j" .planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-ARCHITECTURE-DECISION.md` and confirm matches are rejected alternatives or out-of-scope statements.
</verification>

<success_criteria>
- `01-ARCHITECTURE-DECISION.md` exists and covers DISC-02, DISC-03, ISO-01, and ISO-02.
- Future plans can cite the ADR for graph target, command ownership, credential model, and rejected alternatives.
- No source code, Terraform, dashboard files, generated JSON, or sibling workstream artifacts are edited.
</success_criteria>

<output>
Create `.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-02-SUMMARY.md` when done.
</output>
