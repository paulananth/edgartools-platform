---
phase: 04-dashboard-hosted-graph-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - examples/mdm_graph_dashboard/README.md
  - tests/architecture/test_dashboard_foundation_boundaries.py
  - .planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-DASHBOARD-VERIFICATION.md
requirements:
  - VERIFY-04
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "D-01: Phase 4 docs and tests cover dashboard, shared helpers, tests, and operator docs."
    - "D-03: Docs preserve the `Neo4j Overview` route name but explain it as Snowflake-hosted Neo4j Graph Analytics."
    - "D-04: Operator documentation points to hosted `verify-graph`, Native App prerequisites, and AWS hosted graph E2E expectations."
    - "D-07: Documentation must not describe dashboard sync, repair, migrate, load, derive, write, grant, activation, or deploy controls."
    - "D-08: Documentation describes manual refresh and visible timestamps."
    - "D-09: Documentation distinguishes dashboard inspection from the CLI acceptance gate."
    - "D-10: Documentation explains Native App proof is quiet when healthy."
    - "D-12: Documentation and tests forbid secrets, raw exceptions, DSNs, passwords, tokens, stack traces, and external Neo4j credential assumptions."
    - "D-14: Documentation states bounded samples are diagnostics, not exhaustive exports."
    - "D-15: Documentation preserves route labels, row-limit choices, and filter behavior."
  artifacts:
    - path: examples/mdm_graph_dashboard/README.md
      provides: "Hosted graph dashboard runbook and validation commands."
    - path: .planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-DASHBOARD-VERIFICATION.md
      provides: "Non-secret Phase 4 verification checklist and evidence capture target."
---

<objective>
Update dashboard documentation and guardrails so Phase 4 closes with hosted graph dashboard comparison plus strict CLI verification evidence.
</objective>

<context>
@AGENTS.md
@.planning/workstreams/neo4j-snowflake/REQUIREMENTS.md
@.planning/workstreams/neo4j-snowflake/ROADMAP.md
@.planning/workstreams/neo4j-snowflake/STATE.md
@.planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-CONTEXT.md
@.planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-UI-SPEC.md
@.planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-RESEARCH.md
@.planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-LIVE-DEV-RUN.md
@examples/mdm_graph_dashboard/README.md
@examples/mdm_graph_dashboard/streamlit_app.py
@tests/architecture/test_dashboard_foundation_boundaries.py
@infra/scripts/run-aws-mdm-e2e.sh
@infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace dashboard runbook external Neo4j assumptions</name>
  <files>examples/mdm_graph_dashboard/README.md, tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    - examples/mdm_graph_dashboard/README.md
    - .planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-UI-SPEC.md
    - tests/architecture/test_dashboard_foundation_boundaries.py
  </read_first>
  <action>Update the README to describe the dashboard as read-only MDM plus Snowflake-hosted graph inspection. Document required MDM configuration, Snowflake connection context accepted by the helper (`MDM_SNOWFLAKE_*`, `DBT_SNOWFLAKE_*`, Snowflake CLI connection via `SNOWFLAKE_CONNECTION`), Native App grant/compute-pool prerequisites, and the fact that `verify-graph` remains the acceptance gate. Remove active setup instructions for `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD`, `NEO4J_DATABASE`, `NEO4J_SECRET_JSON`, external Bolt, Aura, and `check-connectivity --neo4j`. Update architecture tests to enforce the new runbook contract.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - README tells operators how to launch the dashboard locally with `uv`.
    - README points to `edgar-warehouse mdm verify-graph`, `infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql`, and the AWS hosted graph E2E command as documentation text only.
    - README states the dashboard has no destructive or write controls.
    - README contains no active external `NEO4J_*`, Bolt, Aura, or read-only `MATCH` setup path.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture Phase 4 verification checklist</name>
  <files>.planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-DASHBOARD-VERIFICATION.md, tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    - .planning/workstreams/neo4j-snowflake/REQUIREMENTS.md
    - .planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-CONTEXT.md
    - .planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-LIVE-DEV-RUN.md
    - infra/scripts/run-aws-mdm-e2e.sh
  </read_first>
  <action>Create `04-DASHBOARD-VERIFICATION.md` as the Phase 4 evidence target. It should list non-secret evidence to capture during execution: focused test commands, dashboard helper payload status, screenshot/manual dashboard comparison notes if performed, `edgar-warehouse mdm verify-graph` result summary, Native App failure detail behavior, and AWS hosted graph E2E reference status. The file must make clear that unit tests use fixtures and that live credentials or secrets must not be pasted.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Verification checklist maps `VERIFY-04`, `DASH-01`, `DASH-02`, and `DASH-03` to concrete evidence.
    - Checklist includes dashboard comparison plus CLI verification, satisfying the roadmap success criterion.
    - Checklist excludes DSNs, passwords, tokens, raw connector exceptions, full logs, and account-secret values.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run focused final validation</name>
  <files>tests/mdm/test_graph_readonly.py, tests/mdm/test_dashboard_readonly.py, tests/architecture/test_dashboard_foundation_boundaries.py, examples/mdm_graph_dashboard/streamlit_app.py</files>
  <read_first>
    - tests/mdm/test_graph_readonly.py
    - tests/mdm/test_dashboard_readonly.py
    - tests/architecture/test_dashboard_foundation_boundaries.py
    - examples/mdm_graph_dashboard/streamlit_app.py
  </read_first>
  <action>Run the focused credential-free suite and syntax checks after docs and guardrails are updated. If local optional dependencies are missing, document the exact missing dependency and command that could not run in the Phase 4 summary. Do not substitute a live Snowflake run for unit coverage.</action>
  <verify>
    <automated>python3 -m py_compile examples/mdm_graph_dashboard/streamlit_app.py</automated>
    <automated>uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
    <manual>Optional live acceptance reference: run `edgar-warehouse mdm verify-graph` with the dev Snowflake connection and record non-secret pass/fail summary in `04-DASHBOARD-VERIFICATION.md`.</manual>
  </verify>
  <acceptance_criteria>
    - Focused tests pass without live Snowflake or AWS credentials.
    - Dashboard syntax check passes.
    - Final Phase 4 evidence states whether live CLI verification was run, skipped, or blocked, with non-secret detail only.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-04-03-01 | Information Disclosure | Docs and evidence | Forbid secrets, DSNs, tokens, raw connector errors, stack traces, and full logs in README and verification evidence. |
| T-04-03-02 | Tampering | Operator runbook | Keep runbook commands external and read-only; do not add dashboard controls for sync, repair, grants, Native App activation, AWS execution, or Terraform. |
| T-04-03-03 | Repudiation | Phase 4 acceptance | Capture exact test commands, pass/fail summaries, and whether live CLI verification was run. |
| T-04-03-04 | Elevation of Privilege | Native App prerequisites | Reference grant SQL and operator activation tasks as documentation, not dashboard automation. |
</threat_model>

<verification>
Run `python3 -m py_compile examples/mdm_graph_dashboard/streamlit_app.py` and `uv run pytest tests/mdm/test_dashboard_readonly.py tests/mdm/test_graph_readonly.py tests/architecture/test_dashboard_foundation_boundaries.py -q`. For live final acceptance, record a non-secret `edgar-warehouse mdm verify-graph` summary if credentials are available.
</verification>

<success_criteria>
- README and tests no longer encode external Neo4j credential assumptions.
- Phase 4 has an explicit non-secret verification checklist for dashboard comparison and CLI verification.
- Focused tests and syntax checks define the closeout gate for dashboard migration.
</success_criteria>

