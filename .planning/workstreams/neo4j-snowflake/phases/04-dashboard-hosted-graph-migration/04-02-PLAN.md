---
phase: 04-dashboard-hosted-graph-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - examples/mdm_graph_dashboard/streamlit_app.py
  - tests/architecture/test_dashboard_foundation_boundaries.py
requirements:
  - VERIFY-04
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "D-03: Preserve the `Neo4j Overview` route label, but replace misleading external Neo4j/Bolt/credential copy in dashboard UI."
    - "D-05: Streamlit renders helper payloads only and does not shell out to `verify-graph`."
    - "D-06: Dashboard comparison views show the same conceptual parity and diagnostics as `verify-graph`."
    - "D-07: The dashboard exposes no sync, repair, migrate, load, derive, write, grant, activation, deploy, or AWS execution controls."
    - "D-08: Refresh is manual and clears only cached read-only payloads; timestamps remain visible."
    - "D-09: Page load and refresh do not imply a fresh Phase 4 acceptance gate."
    - "D-10: Healthy Native App proof is quiet."
    - "D-11: Native App failure detail includes compute pool, `GRAPH_INFO`, `BFS`, and `WCC` when relevant."
    - "D-12: Dashboard failure copy is secret-safe."
    - "D-13: Mismatch Diagnostics include bounded samples for missing/extra nodes, missing/extra edges, and missing edge endpoints."
    - "D-15: Preserve `Overview`, `MDM Overview`, `Neo4j Overview`, `Mismatch Diagnostics`, row limits, and single-select filters."
  artifacts:
    - path: examples/mdm_graph_dashboard/streamlit_app.py
      provides: "Streamlit operator dashboard rendering hosted graph comparison and mismatch diagnostics."
---

<objective>
Migrate the existing Streamlit dashboard in place from external Neo4j assumptions to Snowflake-hosted graph comparison.
</objective>

<context>
@AGENTS.md
@.planning/workstreams/neo4j-snowflake/REQUIREMENTS.md
@.planning/workstreams/neo4j-snowflake/ROADMAP.md
@.planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-CONTEXT.md
@.planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-UI-SPEC.md
@.planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-PATTERNS.md
@examples/mdm_graph_dashboard/streamlit_app.py
@edgar_warehouse/mdm/dashboard_readonly.py
@edgar_warehouse/mdm/graph_readonly.py
@tests/architecture/test_dashboard_foundation_boundaries.py

<ui_contract>
Use Streamlit native primitives only. Keep route labels and order:
1. `Overview`
2. `MDM Overview`
3. `Neo4j Overview`
4. `Mismatch Diagnostics`

Sidebar must keep title `EdgarTools MDM`, section radio, row-limit selectbox with `[25, 50, 100, 250]` and default `50`, and `Refresh metrics`. Sidebar copy should say `Read-only MDM and Snowflake-hosted graph status`.
</ui_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update dashboard architecture/UI guardrails for hosted graph copy</name>
  <files>tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    - .planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-UI-SPEC.md
    - tests/architecture/test_dashboard_foundation_boundaries.py
    - examples/mdm_graph_dashboard/streamlit_app.py
  </read_first>
  <action>Revise architecture tests to assert Phase 4 dashboard behavior: exact route labels remain; sidebar caption names Snowflake-hosted graph status; forbidden external `NEO4J_*`, Bolt, Aura, and read-only `MATCH` copy is absent from active UI copy; Streamlit contains no raw SQL, raw Cypher, subprocess, stdout parsing, credential entry fields, or mutation controls; Native App failure detail table columns are present; required mismatch sample table column labels are present; healthy Native App proof is not always rendered in the overview.</action>
  <verify>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before dashboard migration because active UI still contains external Neo4j copy.
    - Tests continue to preserve dense Streamlit-native operator ergonomics from the approved UI spec.
    - Tests do not require Streamlit to be installed beyond the existing fake module pattern.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render hosted graph overview, comparison, and diagnostics</name>
  <files>examples/mdm_graph_dashboard/streamlit_app.py</files>
  <read_first>
    - examples/mdm_graph_dashboard/streamlit_app.py
    - edgar_warehouse/mdm/dashboard_readonly.py
    - edgar_warehouse/mdm/graph_readonly.py
    - .planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-UI-SPEC.md
  </read_first>
  <action>Replace the external graph metric path with the hosted graph helper from Plan 04-01. Rename rendering variables and copy to Snowflake-hosted graph terminology while preserving `Neo4j Overview` as the route label. Render overview metrics `MDM entities`, `MDM relationships`, `Snowflake graph nodes`, `Snowflake graph edges`, and `Pending sync`. Render entity and relationship comparison tables with the exact UI-spec columns. Render mismatch tables for missing graph nodes, extra graph nodes, missing graph edges, extra graph edges, and missing graph edge endpoints, including direction as `source -> target`. Keep `MDM Overview` usable when Snowflake graph metrics are unavailable.</action>
  <verify>
    <automated>python3 -m py_compile examples/mdm_graph_dashboard/streamlit_app.py</automated>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard startup no longer depends on an absent helper or external Neo4j connection settings.
    - `Overview` shows blocking failures and coverage warnings before summary metrics.
    - `Neo4j Overview` copy clearly says the target is Snowflake-hosted Neo4j Graph Analytics.
    - `Mismatch Diagnostics` displays bounded samples, not exhaustive diffs.
    - `Refresh metrics` only clears Streamlit cache and reruns read-only helper calls.
    - No new frontend framework, component registry, custom component, icon library, hero layout, or card-heavy redesign is introduced.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render Native App failures without noisy healthy proof</name>
  <files>examples/mdm_graph_dashboard/streamlit_app.py, tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <read_first>
    - edgar_warehouse/mdm/graph_readonly.py
    - edgar_warehouse/mdm/snowflake_graph.py
    - .planning/workstreams/neo4j-snowflake/phases/04-dashboard-hosted-graph-migration/04-UI-SPEC.md
  </read_first>
  <action>Add a failure-only Native App detail section. When the graph helper returns failing Native App checks, render a table with `Check`, `Status`, `Detail`, and `Remediation`. Include compute pool, `GRAPH_INFO`, `BFS`, and `WCC` failures when present. When all checks are healthy, do not add always-prominent Native App proof to the overview.</action>
  <verify>
    <automated>python3 -m py_compile examples/mdm_graph_dashboard/streamlit_app.py</automated>
    <automated>uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Failed Native App checks are visible and actionable from the dashboard.
    - Healthy Native App checks remain secondary or omitted.
    - Remediation copy points to `edgar-warehouse mdm verify-graph` and `infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql` as text only, not buttons or executable controls.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-04-02-01 | Information Disclosure | Streamlit copy | Remove external credential prompts and suppress raw connector errors; show only fixed safe guidance and setting names. |
| T-04-02-02 | Tampering | Dashboard controls | Keep controls limited to navigation, row limits, filters, and cache refresh. |
| T-04-02-03 | Repudiation | Dashboard status | Render last-refreshed timestamps and make clear the dashboard is not the acceptance gate. |
| T-04-02-04 | Denial of Service | Dashboard refresh | Preserve cached reads and row limits; no live polling or automatic AWS/Native App execution controls. |
| T-04-02-05 | Elevation of Privilege | Native App readiness | Render failures only; never grant roles, activate compute pools, or deploy infrastructure from Streamlit. |
</threat_model>

<verification>
Run `python3 -m py_compile examples/mdm_graph_dashboard/streamlit_app.py` and `uv run pytest tests/architecture/test_dashboard_foundation_boundaries.py tests/mdm/test_graph_readonly.py -q`.
</verification>

<success_criteria>
- Operators can inspect MDM, Snowflake-hosted graph counts, and bounded mismatches from the existing dashboard.
- The dashboard remains read-only and Streamlit-native.
- External Neo4j/Bolt credential copy no longer appears in active dashboard UI.
</success_criteria>

