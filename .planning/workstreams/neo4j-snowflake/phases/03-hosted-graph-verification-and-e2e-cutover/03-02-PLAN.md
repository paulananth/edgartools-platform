---
phase: 03-hosted-graph-verification-and-e2e-cutover
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - edgar_warehouse/mdm/cli.py
  - edgar_warehouse/mdm/snowflake_graph.py
  - infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql
  - tests/mdm/test_cli_snowflake_graph.py
  - tests/mdm/test_snowflake_graph_migration.py
requirements:
  - SYNC-04
  - SNOW-03
  - VERIFY-01
  - VERIFY-03
must_haves:
  truths:
    - "`verify-graph` fails hard when required Snowflake Native App proof is missing or failing."
    - "Native App proof includes `GRAPH_INFO`, `BFS`, and `WCC` against the hosted graph path."
    - "Least-privilege database-role and application grants are repo-managed and validated before algorithm execution."
    - "Missing grants, app roles, compute prerequisites, or algorithm failures produce exact remediation output."
---

<objective>
Extend strict SQL parity verification with Snowflake Native App grant validation and query-level graph proof.
</objective>

<context>
@.planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-CONTEXT.md
@.planning/workstreams/neo4j-snowflake/phases/01-snowflake-native-app-feasibility-and-architecture-decision/01-NATIVE-APP-RUNBOOK.md
@.planning/workstreams/neo4j-snowflake/SNOWFLAKE-GRAPH-ANALYTICS-AGENT-INSTRUCTIONS.md
@edgar_warehouse/mdm/cli.py
@edgar_warehouse/mdm/snowflake_graph.py
@tests/mdm/test_cli_snowflake_graph.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add grant automation and validation tests</name>
  <files>infra/snowflake/sql/neo4j_graph_analytics_app_grants.sql, tests/mdm/test_snowflake_graph_migration.py</files>
  <action>Create a repo-managed SQL/runbook script for `EDGARTOOLS_DEV.NEO4J_GRAPH_MIGRATION` Native App access. Use a dedicated database role, grant database/schema usage, select on current/future graph tables/views, required create-table output privilege where algorithms write results, and grant the database role to application `Neo4j_Graph_Analytics`. Tests should assert least-privilege intent and reject broad `ALL PRIVILEGES` patterns.</action>
  <verify>
    <automated>uv run --extra mdm-runtime pytest tests/mdm/test_snowflake_graph_migration.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Native App smoke checks to verify-graph</name>
  <files>edgar_warehouse/mdm/snowflake_graph.py, edgar_warehouse/mdm/cli.py, tests/mdm/test_cli_snowflake_graph.py</files>
  <action>Add Native App validation to the verifier. Validate app installation/application roles, database-role grants, graph schema access, compute-pool availability, and run `GRAPH_INFO`, `BFS`, and `WCC` smoke SQL by default. Allow opt-out only for local/offline tests via an explicit flag that cannot satisfy live Phase 3 acceptance. Return structured remediation for missing roles, grants, compute pools, and failed algorithm calls.</action>
  <verify>
    <automated>uv run --extra mdm-runtime pytest tests/mdm/test_cli_snowflake_graph.py tests/mdm/test_snowflake_graph_migration.py -q</automated>
  </verify>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-03-02-01 | Elevation of Privilege | Native App grants | Use dedicated database role and least-privilege grants; no `ALL PRIVILEGES` or ownership grants. |
| T-03-02-02 | Denial of Service | Native App algorithms | Use bounded smoke graph inputs and low-cost compute selector `CPU_X64_XS` unless live account reports another supported selector. |
| T-03-02-03 | Information Disclosure | Job logs/remediation | Do not print Native App job logs by default; remediation output must avoid secrets. |
</threat_model>

<verification>
Run focused tests, then a live dev smoke with `SNOW_CONNECTION=snowconn` and `DBT_SNOWFLAKE_DATABASE=EDGARTOOLS_DEV` after operator-safe grants are applied.
</verification>

<success_criteria>
- `verify-graph` proves SQL parity and Native App query-level graph execution.
- Missing app prerequisites fail with exact remediation.
- Repo-managed grants are least-privilege and reviewable.
</success_criteria>

