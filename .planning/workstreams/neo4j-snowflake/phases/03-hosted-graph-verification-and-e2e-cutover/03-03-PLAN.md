---
phase: 03-hosted-graph-verification-and-e2e-cutover
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - infra/scripts/run-aws-mdm-e2e.sh
  - .planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-LIVE-DEV-RUN.md
  - tests/architecture/test_dashboard_foundation_boundaries.py
requirements:
  - SYNC-04
  - VERIFY-05
must_haves:
  truths:
    - "AWS MDM E2E success uses Snowflake `sync-graph` plus strict hosted `verify-graph`."
    - "External Neo4j connectivity is not a success requirement."
    - "Lingering `NEO4J_*` references are warnings unless they block the Snowflake-hosted path."
    - "Final acceptance includes Step Functions execution validation and non-secret live dev outputs."
---

<objective>
Cut AWS MDM E2E validation over to the Snowflake-hosted graph path and document the live dev proof.
</objective>

<context>
@.planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-CONTEXT.md
@infra/scripts/run-aws-mdm-e2e.sh
@edgar_warehouse/mdm/cli.py
@tests/architecture/test_dashboard_foundation_boundaries.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update AWS MDM E2E script semantics</name>
  <files>infra/scripts/run-aws-mdm-e2e.sh, tests/architecture/test_dashboard_foundation_boundaries.py</files>
  <action>Rename script messaging from MDM/Neo4j to MDM hosted graph validation, remove `mdm_check_connectivity` as a required success step, keep `mdm_sync_graph`, `mdm_verify_graph`, and Step Functions execution polling, and add warnings for lingering `NEO4J_*` task-definition/script references. Preserve `--status-only` behavior.</action>
  <verify>
    <automated>bash -n infra/scripts/run-aws-mdm-e2e.sh</automated>
  </verify>
</task>

<task type="manual" tdd="false">
  <name>Task 2: Capture live dev validation output</name>
  <files>.planning/workstreams/neo4j-snowflake/phases/03-hosted-graph-verification-and-e2e-cutover/03-LIVE-DEV-RUN.md</files>
  <action>Run the live dev validation using `SNOW_CONNECTION=snowconn`, `DBT_SNOWFLAKE_DATABASE=EDGARTOOLS_DEV`, and the AWS dev deployment. Capture non-secret outputs: graph node/edge counts, relationship parity by type, Native App smoke status, Step Functions execution ARN/status, and any remediation messages.</action>
  <verify>
    <manual>Operator reviews `03-LIVE-DEV-RUN.md` for non-secret evidence and confirms no external Neo4j connectivity gate remains.</manual>
  </verify>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-03-03-01 | Repudiation | AWS E2E evidence | Capture execution ARNs/statuses and non-secret command outputs. |
| T-03-03-02 | Information Disclosure | Live run artifact | Redact secrets; do not paste DSNs, tokens, passwords, or full job logs. |
| T-03-03-03 | Denial of Service | AWS Step Functions run | Keep bounded MDM and graph limits for e2e validation. |
</threat_model>

<verification>
Run `bash -n infra/scripts/run-aws-mdm-e2e.sh`, focused architecture tests if updated, and the live dev e2e run.
</verification>

<success_criteria>
- AWS hosted graph E2E succeeds without external Neo4j connectivity as a success gate.
- Step Functions validation is captured.
- Non-secret live dev evidence is committed for Phase 3 closeout.
</success_criteria>

