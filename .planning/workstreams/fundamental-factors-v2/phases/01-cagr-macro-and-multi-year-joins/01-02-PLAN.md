---
phase: 01-cagr-macro-and-multi-year-joins
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml
autonomous: true
requirements: [GROW-01, GROW-02, GROW-03]
tags: [dbt, snowflake, gold, cagr, unit-tests]

must_haves:
  truths:
    - "A dbt unit test proves 3yr and 5yr CAGR compute correct non-zero values for revenue, net_income, total_assets given exact fiscal_year-N FY predecessors (GROW-01)."
    - "A dbt unit test proves CAGR is null when insufficient history exists (no fiscal_year-N row) (GROW-01)."
    - "Dbt unit tests prove CAGR nulls for: negative-to-negative endpoints (D-02), negative current endpoint, and negative prior endpoint (GROW-02)."
    - "A dbt unit test proves a fiscal-year gap (no exact fiscal_year-N FY row) nulls CAGR, and that the 3yr and 5yr offsets are independent (GROW-03, D-03)."
    - "The existing quarterly-exclusion test asserts all 6 CAGR columns are null on a Q1 row (D-01)."
  artifacts:
    - "infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml (new CAGR unit test cases + extended quarterly test)"
  key_links:
    - "Happy-path fixture has an obviously non-zero expected CAGR (e.g. revenue doubling) so Pitfall 1 (integer-division-to-zero) cannot pass undetected."
    - "Negative-to-negative fixture (both endpoints negative) asserts null — the literal D-02 scenario most likely to be silently wrong with a same-sign guard."
    - "Expected CAGR float values are verified against a real dbt build/test run, NOT hand-arithmetic (Open Question 2)."
---

<objective>
Add comprehensive dbt unit-test coverage for the 3yr/5yr CAGR factors created in plan
01-01, proving GROW-01 (compute + insufficient-history null), GROW-02 (negative-endpoint
null in all three forms), GROW-03/D-03 (fiscal-year-gap null), and D-01 (quarterly rows
never receive a CAGR).

Purpose: ROADMAP Phase 1 success criterion 5 requires dbt tests covering at least one real
multi-year company fixture for each of 3yr and 5yr CAGR. Unlike Phase 2's single-period
margin tests, CAGR factors depend on a working multi-row self-join — so fixtures MUST
establish realistic multi-year `cik` histories at the right `fiscal_year` offsets.

Output: New unit test cases in `_financial_factors_unit_tests.yml` reusing the existing
`&factor_row_defaults` anchor, plus an extension of the existing
`financial_factors_quarterly_cross_year_factors_are_null` test. Verified via
`dbt test --select financial_factors` where the environment permits; otherwise via
`dbt parse` + `dbt compile` plus the explicit environment-blocker note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fundamental-factors-v2/ROADMAP.md
@.planning/workstreams/fundamental-factors-v2/REQUIREMENTS.md
@.planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-CONTEXT.md
@.planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-RESEARCH.md
@.planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-PATTERNS.md
@.planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-VALIDATION.md
</context>

<environment_risk>
Same Phase-2-carried blocker as plan 01-01 (STATE.md Blockers): live
`dbt test --select financial_factors` may fail because this dev Snowflake account's
deployed `SEC_FINANCIAL_DERIVED` source table is missing columns that the SIBLING
`financial_derived` model selects, and `dbt test --select financial_factors` pulls
`financial_derived`'s own unit test into the run (the existing
`financial_factors_complete_fy_ratios` test reproduces the failure on UNMODIFIED code).
This phase's own input columns (`revenue`, `net_income`, `total_assets`) are unaffected.

ACHIEVABLE bar if the blocker is still open: `dbt parse` + `dbt compile --select
financial_factors` succeed, AND the new test cases parse as valid unit-test YAML. If a real
`dbt test` run IS available (synced source schema), run it to confirm green AND to capture
the EXACT floating-point expected CAGR values (Open Question 2 — Snowflake `POWER()`
precision must not be assumed from hand arithmetic). Do NOT mark a failed live `dbt test`
caused by the pre-existing `financial_derived` source gap as a defect of this plan.
</environment_risk>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GROW-01 happy-path and insufficient-history CAGR unit tests</name>
  <files>infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml (the file being modified — &factor_row_defaults anchor lines 10-36, financial_factors_complete_fy_ratios multi-row shape lines 4-93, expect-block granularity lines 64-93)
    - infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql (the new CAGR columns from plan 01-01 these tests assert on)
    - .planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-RESEARCH.md (Pattern 3 fixture items 1 + GROW-01 insufficient-history; Code Examples happy-path; Pitfall 1 + Pitfall 5; Open Question 2)
    - .planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-PATTERNS.md (anchor-merge fixture + expect-granularity + naming conventions)
  </read_first>
  <behavior>
    - Test `financial_factors_cagr_happy_path` (GROW-01): `cik: 10`, FY rows at
      `fiscal_year` 2019, 2021, 2024 (gives 2024 an exact `-3` match at 2021 AND an exact
      `-5` match at 2019), all revenue/net_income/total_assets strictly positive at every
      endpoint. Pick the 2024-vs-prior values so the expected CAGR is OBVIOUSLY non-zero
      (e.g. revenue 2019=100 -> 2024=200, a 5yr doubling -> CAGR ~0.1487; 2021=130 ->
      2024=200 over 3yr -> ~0.1548) so Pitfall 1 (integer-division-to-zero) cannot pass
      undetected. Expect `revenue_cagr_3y`/`revenue_cagr_5y` (and the other two metrics'
      3y/5y) non-null and non-zero on the 2024 row; null on the 2019/2021 rows (no deeper
      history). Expected float values are PLACEHOLDERS to be confirmed against a real
      `dbt build`/`dbt test` run (Open Question 2) — do not commit hand-computed values as
      final without that confirmation; if no live run is available, leave a comment flagging
      the value as unverified.
    - Test `financial_factors_cagr_insufficient_history` (GROW-01): a `cik` with only a
      single FY row (or rows closer together than 3 years) -> all 6 CAGR columns null on the
      most-recent row (no `fiscal_year - N` predecessor exists -> `left join` yields null).
  </behavior>
  <action>
    Append two new unit test cases to `_financial_factors_unit_tests.yml`, reusing the
    existing top-level `&factor_row_defaults` anchor with `<<: *factor_row_defaults` override
    rows (do NOT redefine the anchor). Override only `cik`, `accession_number`,
    `fiscal_year`, `period_end`, and the specific revenue/net_income/total_assets values per
    scenario. Follow the `financial_factors_<scenario>` snake_case naming and the
    `description: >` block-scalar convention, citing GROW-01. Per Pitfall 5 / expect-block
    granularity: list only the columns each test verifies (the relevant `*_cagr_3y`/
    `*_cagr_5y` columns plus row keys `cik`/`accession_number`/`fiscal_year`) — omitted
    columns are NOT asserted. For the happy-path expected values, if a live `dbt test` run is
    available, capture Snowflake's exact `POWER()` output; otherwise mark them unverified per
    the environment note.
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt parse 2>&1 | tail -20 | grep -qiE "(Wrote|Performance info|Found)" && python -c "import yaml; d=yaml.safe_load(open('models/gold/_financial_factors_unit_tests.yml')); names=[t['name'] for t in d['unit_tests']]; assert 'financial_factors_cagr_happy_path' in names, names; assert 'financial_factors_cagr_insufficient_history' in names, names; print('OK', names)"</automated>
  </verify>
  <acceptance_criteria>
    - Both test cases `financial_factors_cagr_happy_path` and `financial_factors_cagr_insufficient_history` exist and the YAML parses (Python assert above exits 0).
    - The happy-path fixture has >= 3 FY rows for one `cik` at offsets producing exact `-3` and `-5` matches. Source assertion: the happy-path `given` block lists three rows with the same `cik` at `fiscal_year` 2019/2021/2024 (or equivalent exact-offset triple).
    - The happy-path expected CAGR values are non-zero (guards against Pitfall 1). Source assertion: the expect block's `revenue_cagr_5y` value is not `0` and not `null` on the most-recent row.
    - The insufficient-history test expects all 6 CAGR columns null on the latest row.
    - `dbt parse` succeeds. (If a synced-source environment is available, `dbt test --select financial_factors` passes these two cases; otherwise the environment blocker is documented in the SUMMARY.)
  </acceptance_criteria>
  <done>
    GROW-01 is covered by a happy-path test (non-zero 3yr/5yr CAGR for all three metrics)
    and an insufficient-history null test; both parse as valid unit-test YAML.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GROW-02 negative-endpoint CAGR null tests (all three forms)</name>
  <files>infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml (the file being modified — financial_factors_negative_equity_nulls_roe lines 131-159 as the negative-value precedent; anchor reuse)
    - infra/snowflake/dbt/edgartools_gold/macros/cagr.sql (the strict-positive guard these tests exercise)
    - .planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-RESEARCH.md (Pattern 3 fixture items 2/3/4; Pitfall 2 — same-sign-check anti-pattern; Common Pitfalls)
    - .planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-CONTEXT.md (D-02 wording)
  </read_first>
  <behavior>
    - Test `financial_factors_cagr_negative_to_negative_nulls` (D-02, GROW-02): `cik: 13`,
      both current and prior `net_income` negative (e.g. prior 2021 = -100, current 2024 =
      -50, an "improving but still negative" trend) -> `net_income_cagr_3y` MUST be null.
      This is the single most important case — a naive same-sign guard would emit a
      misleadingly positive CAGR here (Pitfall 2).
    - Test `financial_factors_cagr_single_negative_endpoint_nulls` (D-02, GROW-02): two
      sub-rows / two ciks proving BOTH operands are checked, not just one:
        - `cik: 11`: current-year `net_income` negative, prior `net_income` positive ->
          `net_income_cagr_Ny` null while `revenue_cagr_Ny`/`total_assets_cagr_Ny`
          (positive at both ends, same row) compute normally — proves the guard is
          per-column, not all-or-nothing row-level.
        - `cik: 12`: current-year positive, N-years-prior value negative for the same field
          -> null — proves the guard checks the PRIOR operand, not just the numerator.
  </behavior>
  <action>
    Append the negative-endpoint test cases, reusing `<<: *factor_row_defaults`. Each
    multi-year fixture needs a current row and an exact `fiscal_year - 3` (or `-5`)
    predecessor row for the same `cik` so the self-join actually fires (a single row would
    null trivially and wouldn't test the guard). Cite D-02 and GROW-02 in each
    `description: >`. For the `cik: 11` per-column case, the expect block must assert
    `net_income_cagr_3y: null` AND the unaffected `revenue_cagr_3y`/`total_assets_cagr_3y`
    NON-null in the same row — this is what distinguishes a correct per-column guard from a
    blunt row-level null. Keep expect blocks scoped to the relevant CAGR columns + row keys.
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt parse 2>&1 | tail -20 | grep -qiE "(Wrote|Performance info|Found)" && python -c "import yaml; d=yaml.safe_load(open('models/gold/_financial_factors_unit_tests.yml')); names=[t['name'] for t in d['unit_tests']]; assert 'financial_factors_cagr_negative_to_negative_nulls' in names, names; assert 'financial_factors_cagr_single_negative_endpoint_nulls' in names, names; print('OK', names)"</automated>
  </verify>
  <acceptance_criteria>
    - Both negative-endpoint test cases exist and parse (Python assert exits 0).
    - The negative-to-negative case has both endpoints' `net_income` negative and expects `net_income_cagr_3y: null`. Source assertion: the case's `given` rows both carry a negative `net_income`; its `expect` asserts the CAGR null.
    - The per-column case (`cik: 11`) expects `net_income_cagr_3y: null` AND `revenue_cagr_3y` non-null in the same row (proves per-column guard).
    - The prior-endpoint case (`cik: 12`) has the prior-period value negative and expects null (proves the prior operand is guarded).
    - `dbt parse` succeeds.
  </acceptance_criteria>
  <done>
    GROW-02/D-02 is covered in all three forms — negative-to-negative, negative current,
    negative prior — with the per-column-guard assertion proving the macro is not an
    all-or-nothing row null.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add GROW-03 fiscal-year-gap test and extend the D-01 quarterly-exclusion test</name>
  <files>infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml (the file being modified — existing financial_factors_quarterly_cross_year_factors_are_null lines 95-129 to EXTEND; anchor reuse)
    - infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql (exact `= l.fiscal_year - N` joins these tests rely on for D-03)
    - .planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-RESEARCH.md (Pattern 3 fixture items 5 + 6; GROW-03 exact-equality join; D-01 quarterly exclusion)
    - .planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-CONTEXT.md (D-03 + D-01 wording)
  </read_first>
  <behavior>
    - Test `financial_factors_cagr_fiscal_year_gap_nulls` (D-03, GROW-03): `cik: 14`, a
      fixture that ISOLATES the two offsets so independence is proven — e.g. rows at
      `fiscal_year` 2019, 2024 only (so 2024 has an exact `-5` match at 2019 BUT no `-3`
      match at 2021) -> on the 2024 row, `*_cagr_3y` columns all null (gap, no fuzzy match)
      while `*_cagr_5y` columns compute. This proves the exact equi-join nulls a gap AND
      that a gap in the 3yr offset does not null the independent 5yr offset.
    - Extend the EXISTING `financial_factors_quarterly_cross_year_factors_are_null` test
      (D-01): add all 6 CAGR columns to its `expect` row, asserting each is null for the
      `Q1` row — quarterly periods never receive a CAGR (the outer FY-gating `case`
      guarantees this). Do NOT create a new test case for this; extend the existing one.
  </behavior>
  <action>
    Append the fiscal-year-gap test case (reusing `<<: *factor_row_defaults`, citing D-03 and
    GROW-03 in `description: >`), with a fixture that makes the 3yr offset gap while keeping
    the 5yr offset present (or vice versa) to prove offset independence. Then edit the
    existing `financial_factors_quarterly_cross_year_factors_are_null` test's `expect.rows`
    entry to additionally assert `revenue_cagr_3y: null, net_income_cagr_3y: null,
    total_assets_cagr_3y: null, revenue_cagr_5y: null, net_income_cagr_5y: null,
    total_assets_cagr_5y: null` for the Q1 row — preserving its existing assertions
    (`current_ratio`, `asset_growth_yoy: null`, etc.), do not remove any. Keep expect blocks
    scoped per Pitfall 5.
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt parse 2>&1 | tail -20 | grep -qiE "(Wrote|Performance info|Found)" && python -c "import yaml; d=yaml.safe_load(open('models/gold/_financial_factors_unit_tests.yml')); tests={t['name']:t for t in d['unit_tests']}; assert 'financial_factors_cagr_fiscal_year_gap_nulls' in tests, list(tests); q=tests['financial_factors_quarterly_cross_year_factors_are_null']; row=q['expect']['rows'][0]; need=['revenue_cagr_3y','net_income_cagr_3y','total_assets_cagr_3y','revenue_cagr_5y','net_income_cagr_5y','total_assets_cagr_5y']; missing=[c for c in need if c not in row]; assert not missing, ('quarterly expect missing CAGR cols:', missing); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Test `financial_factors_cagr_fiscal_year_gap_nulls` exists and parses (Python assert exits 0).
    - The gap fixture isolates the offsets: a gap in one of (3yr, 5yr) nulls only that offset while the other computes. Source assertion: the `given` block omits the row at one exact offset and includes the row at the other; the `expect` asserts the gapped-offset CAGR null and the present-offset CAGR non-null.
    - The existing `financial_factors_quarterly_cross_year_factors_are_null` test's `expect` row now asserts all 6 CAGR columns null AND retains its original assertions (Python assert confirms all 6 CAGR keys present in that row).
    - `dbt parse` succeeds.
  </acceptance_criteria>
  <done>
    GROW-03/D-03 is covered by an offset-isolating fiscal-year-gap test, and D-01 quarterly
    exclusion is proven by the extended existing test asserting all 6 CAGR columns null on a
    Q1 row.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in phase scope) | Test-only changes over already-validated SEC XBRL data inside a Snowflake dynamic table. No untrusted input, no auth/session/network surface. RESEARCH.md Security Domain: no ASVS category applies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | under-fixtured CAGR tests masking a join-key bug | medium | mitigate | A 2-row exactly-N-apart-only fixture wouldn't catch a self-join key bug (e.g. joining on `fiscal_period`). Mitigated by multi-year fixtures spanning >= 3 FY rows (Task 1) and offset-isolating gap fixtures (Task 3). |
| T-01-04 | Information disclosure | unverified hand-computed expected CAGR values | low | mitigate | Hand-arithmetic CAGR values could disagree with Snowflake `POWER()` float precision, producing a false-pass/false-fail. Mitigated by flagging expected values as requiring confirmation against a real `dbt build`/`dbt test` run (Open Question 2). |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — test-YAML-only change, zero dependency-manifest edits. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `dbt parse` succeeds with all new test cases present.
- New cases cover: GROW-01 happy-path (non-zero 3yr/5yr), GROW-01 insufficient-history,
  GROW-02 negative-to-negative, GROW-02 single-negative (both directions), GROW-03
  fiscal-year-gap (offset-isolated); existing quarterly test extended for D-01.
- `git diff --stat` shows ONLY `_financial_factors_unit_tests.yml` touched in this plan.
- KNOWN ENVIRONMENT RISK (not a plan defect): live `dbt test --select financial_factors`
  may fail at the `financial_derived` model level due to the pre-existing dev source-schema
  gap (STATE.md Blockers). If a synced-source environment is available, run `dbt test` to
  confirm green AND capture exact `POWER()` float values; otherwise document the blocker and
  treat `dbt parse`/`dbt compile` success as the achievable bar.
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion 5 met: dbt tests cover at least one multi-year company
  fixture for each of 3yr and 5yr CAGR.
- ROADMAP success criteria 3 and 4 behaviorally verified: sign-change/negative-endpoint
  null (GROW-02) and fiscal-year-gap null (GROW-03) each have a dedicated passing-or-parsing
  test.
- D-01 quarterly exclusion proven by the extended existing test.
- No production-code change in this plan — test YAML only.
</success_criteria>

<output>
Create `.planning/workstreams/fundamental-factors-v2/phases/01-cagr-macro-and-multi-year-joins/01-02-SUMMARY.md` when done.
</output>
