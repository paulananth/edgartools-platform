---
phase: 02-profitability-and-returns-factors
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml
  - infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql
  - infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03]
must_haves:
  truths:
    - "A consumer querying FINANCIAL_FACTORS can read gross_margin (gross_profit/revenue), operating_margin (ebit/revenue), and net_margin (net_income/revenue)."
    - "A consumer can read return_on_equity (net_income/total_equity) and return_on_assets (net_income/total_assets)."
    - "return_on_equity is null when total_equity is negative (D-01), while return_on_assets and the margins still compute from the same row."
    - "All six new factors compute for every fiscal_period value (FY and quarterly), not FY-only (D-02)."
    - "roic appears in FINANCIAL_FACTORS as a pass-through of financial_derived.roic, not recomputed (D-03)."
  artifacts:
    - infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql
    - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml
    - infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml
  key_links:
    - "financial_factors.sql references l.gross_profit, l.ebit, l.roic — already available via base's `select d.*` from financial_derived, no upstream model change."
    - "return_on_equity uses the safe_ratio_signed macro from Plan 01; the four other ratios use the existing safe_ratio macro; roic is a plain pass-through column."
    - "New unit tests assert the negative-equity ROE null guard and the non-FY (quarterly) margin path."
---

<objective>
Extend the `FINANCIAL_FACTORS` gold model with six profitability/returns factors —
`gross_margin`, `operating_margin`, `net_margin`, `return_on_equity`, `return_on_assets`,
and a `roic` pass-through — add unit-test coverage for the new factors (including the
negative-equity ROE null guard and a quarterly row), and document the ROIC pre-tax
simplification in `gold.yml`. Pure dbt gold-layer SQL — zero changes to silver, parsers,
or any loader/runtime file.

Purpose: Satisfies PROF-01 (margins), PROF-02 (ROE/ROA with D-01 negative-equity guard),
and PROF-03 (surface existing roic per D-03). All input columns already exist in
`financial_derived` and flow through `financial_factors.sql`'s `base` CTE via `select d.*`.
Output: 6 new columns on `FINANCIAL_FACTORS`, new+extended unit tests, and a `gold.yml`
ROIC simplification note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fundamental-factors-v2/ROADMAP.md
@.planning/workstreams/fundamental-factors-v2/REQUIREMENTS.md
@.planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-CONTEXT.md
@.planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-RESEARCH.md
@.planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-PATTERNS.md
@infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql
@infra/snowflake/dbt/edgartools_gold/models/gold/financial_derived.sql
@infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml
@infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml
@infra/snowflake/dbt/edgartools_gold/macros/safe_ratio.sql
@infra/snowflake/dbt/edgartools_gold/macros/safe_ratio_signed.sql
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 portion)

| Symbol | Kind | File |
|--------|------|------|
| `gross_margin` | new column (`safe_ratio(gross_profit, revenue)`) | `financial_factors.sql` |
| `operating_margin` | new column (`safe_ratio(ebit, revenue)`) | `financial_factors.sql` |
| `net_margin` | new column (`safe_ratio(net_income, revenue)`) | `financial_factors.sql` |
| `return_on_equity` | new column (`safe_ratio_signed(net_income, total_equity)`) | `financial_factors.sql` |
| `return_on_assets` | new column (`safe_ratio(net_income, total_assets)`) | `financial_factors.sql` |
| `roic` | new column (pass-through `l.roic`) | `financial_factors.sql` |
| `l.gross_profit`, `l.ebit` | new entries in "Base accounting inputs" select block | `financial_factors.sql` |
| `financial_factors_negative_equity_nulls_roe` | new unit-test case | `_financial_factors_unit_tests.yml` |
| extended `expect` assertions for new factors on existing FY + quarterly test cases | unit-test additions | `_financial_factors_unit_tests.yml` |
| `roic` column description (ROIC pre-tax simplification note) | new `columns:` entry under `financial_factors` | `gold.yml` |
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit-test cases for the new factors (RED first)</name>
  <files>infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml (the two existing test cases and the `&factor_row_defaults` anchor — model new cases on these)
    - .planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-PATTERNS.md (section "_financial_factors_unit_tests.yml (test, modified)" — exact new test case shape and anchor-merge guidance)
    - .planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-RESEARCH.md (Pitfall 3 — dbt unit tests compare the full result row; Code Examples — expected values)
    - .planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-CONTEXT.md (D-01, D-02)
  </read_first>
  <behavior>
    - Test (negative equity): a row with net_income=-30, total_equity=-20, total_assets=300, revenue=150, gross_profit=60, ebit=-10 yields return_on_equity=null (D-01 guard), return_on_assets=-0.1, net_margin=-0.2, gross_margin=0.4, operating_margin=-10/150.
    - Test (FY positive): the existing FY row (revenue=150, total_assets=300, total_equity=180, net_income=20, plus added gross_profit/ebit/roic) yields gross_margin, operating_margin, net_margin, return_on_equity, return_on_assets computed > 0, and roic equal to the row's input roic value.
    - Test (quarterly, D-02): the existing Q1 row produces non-null margins/returns (proving the new factors are NOT FY-gated) — at minimum assert one new factor (e.g. net_margin or return_on_assets) is non-null on the Q1 row given non-null inputs.
  </behavior>
  <action>
    Extend `_financial_factors_unit_tests.yml` with coverage for the six new factors. (1)
    Add a NEW test case named `financial_factors_negative_equity_nulls_roe` modeled on the
    PATTERNS.md example: a single `given` row built by merging `<<: *factor_row_defaults`
    with overrides cik=99, accession_number="0099", fiscal_year=2024, net_income=-30,
    total_equity=-20, total_assets=300, revenue=150, and per-row `gross_profit: 60` /
    `ebit: -10`; the `expect` row asserts return_on_equity=null, return_on_assets=-0.1,
    net_margin=-0.2, gross_margin=0.4, and operating_margin (verify the exact decimal of
    -10/150 against a real dbt run before committing — do not hard-code -0.0667 without
    confirming dbt's returned precision; see RESEARCH.md A3). (2) Extend the EXISTING
    `financial_factors_complete_fy_ratios` test: add `gross_profit`, `ebit`, and `roic`
    values to its `given` rows (the `factor_row_defaults` anchor and the second merged row
    currently lack these keys) and add `gross_margin`/`operating_margin`/`net_margin`/
    `return_on_equity`/`return_on_assets`/`roic` assertions to the FY-2024 `expect` row.
    (3) Extend the EXISTING `financial_factors_quarterly_cross_year_factors_are_null` test:
    add the inputs needed (revenue, net_income, total_equity, gross_profit, ebit, roic) to
    its Q1 `given` row and assert at least one new factor is non-null in its `expect` row,
    proving D-02 (margins/returns compute for non-FY periods).

    Prefer specifying `gross_profit`/`ebit`/`roic` per-row over editing the shared anchor,
    UNLESS adding them to the anchor keeps existing assertions passing — if you add them to
    the anchor, re-check both existing test cases still pass (RESEARCH.md Pitfall 3:
    omitted `expect` columns are not asserted, but every `given` row needs a matching
    `expect` row). Do NOT gate any new factor on `fiscal_period = 'FY'` in the expected
    values (D-02). This task is RED-first: the model does not yet emit these columns, so
    `dbt test` for these cases is expected to fail until Task 2 lands.
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && grep -q 'financial_factors_negative_equity_nulls_roe' models/gold/_financial_factors_unit_tests.yml && grep -q 'return_on_equity' models/gold/_financial_factors_unit_tests.yml && grep -q 'gross_margin' models/gold/_financial_factors_unit_tests.yml && uv run --with dbt-snowflake dbt parse 2>&1 | grep -iv error | tail -2 && echo TESTS_PARSED</automated>
  </verify>
  <acceptance_criteria>
    - A new unit-test case `financial_factors_negative_equity_nulls_roe` exists with `return_on_equity: null` and `return_on_assets: -0.1` in its `expect` row.
    - The negative-equity case's `given` row includes `gross_profit` and `ebit` values (model selects them as of Task 2).
    - The existing `financial_factors_complete_fy_ratios` test has new assertions for all six new factors on its FY-2024 expect row, including a non-null `roic` assertion.
    - The existing quarterly test asserts at least one new factor is non-null on the Q1 row (D-02).
    - `dbt parse` succeeds (YAML is valid dbt unit-test spec).
    - `grep -c 'fiscal_period.*FY' ` against any NEW expect block for the new factors does not appear as a gate on margin/return values (D-02 — new factors are not FY-restricted).
  </acceptance_criteria>
  <done>
    New + extended unit tests exist covering negative-equity ROE nulling, positive FY
    factor values, quarterly (non-FY) factor values, and roic pass-through; `dbt parse`
    validates the YAML. Tests are expected RED until Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the six profitability/returns factors to financial_factors.sql (GREEN)</name>
  <files>infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql (the model being extended — the `base`/`line_items`/final-select chain, the existing safe_ratio usage block at lines 71-82, the "Base accounting inputs" block at lines 49-67, and the FY-only gate at lines 83-92 which is the anti-pattern NOT to copy)
    - infra/snowflake/dbt/edgartools_gold/models/gold/financial_derived.sql (confirm gross_profit, ebit, roic are selected upstream so they flow through base's `select d.*`)
    - infra/snowflake/dbt/edgartools_gold/macros/safe_ratio.sql and macros/safe_ratio_signed.sql (the two macros used)
    - .planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-PATTERNS.md (section "financial_factors.sql" — exact insertion point and Base-inputs-block additions)
    - .planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-RESEARCH.md (Architecture Patterns 1-3, Anti-Patterns, Pitfall 1)
  </read_first>
  <action>
    In the final `select` of `financial_factors.sql`, append a new block immediately after
    the `shares_outstanding_yoy_change` factor (currently ending around line 92) and before
    `l.parser_version` (line 94). Add, in order, with a leading comment `-- V2 profitability
    and returns factors (Phase 2).`: `{{ safe_ratio('l.gross_profit', 'l.revenue') }} as
    gross_margin`, `{{ safe_ratio('l.ebit', 'l.revenue') }} as operating_margin`,
    `{{ safe_ratio('l.net_income', 'l.revenue') }} as net_margin`,
    `{{ safe_ratio_signed('l.net_income', 'l.total_equity') }} as return_on_equity`,
    `{{ safe_ratio('l.net_income', 'l.total_assets') }} as return_on_assets`, and `l.roic`
    (a plain pass-through — NO macro, NO recomputation, per D-03). Use `safe_ratio_signed`
    ONLY for return_on_equity (D-01); use plain `safe_ratio` for the other four ratios; do
    NOT add a sign guard to return_on_assets (CONTEXT.md Claude's Discretion — negative
    total_assets is not a realistic balance-sheet state). Do NOT wrap any of these six
    columns in `case when l.fiscal_period = 'FY'` — they compute for every period (D-02);
    copying the asset_growth_yoy FY gate onto them is the explicit anti-pattern.

    Also append `l.gross_profit` and `l.ebit` to the "Base accounting inputs retained for
    coverage and factor debugging" block (currently lines 49-67) for debugging-visibility
    consistency with the other ratios' inputs (revenue, total_equity, total_assets are
    already there). These columns are already available on `l.` via `base`'s `select d.*`
    from `financial_derived` — do NOT add a new CTE or modify `financial_derived.sql`
    (Pitfall 1; a task that touches financial_derived.sql to "expose gross_profit" is the
    warning sign of this mistake). Never write a raw `/` anywhere in this file — Snowflake
    raises a hard division-by-zero error (100051) on unguarded division, which fails the
    whole dynamic-table refresh; all division goes through the macros.

    Column naming follows the file's existing convention: no `_pct` suffix (matches
    `current_ratio`, `working_capital_to_assets`).
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && grep -q 'as gross_margin' models/gold/financial_factors.sql && grep -q 'as operating_margin' models/gold/financial_factors.sql && grep -q 'as net_margin' models/gold/financial_factors.sql && grep -q 'safe_ratio_signed' models/gold/financial_factors.sql && grep -q 'as return_on_assets' models/gold/financial_factors.sql && grep -Eq 'l\.roic( as roic)?,' models/gold/financial_factors.sql && uv run --with dbt-snowflake dbt test --select financial_factors 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `financial_factors.sql` selects `gross_margin`, `operating_margin`, `net_margin`, `return_on_equity`, `return_on_assets`, and `roic`.
    - `return_on_equity` uses `safe_ratio_signed` (the only use of that macro in the file); the four other ratios use `safe_ratio`.
    - `roic` is a plain column reference (`l.roic`), not wrapped in any macro or `case`.
    - None of the six new columns is wrapped in a `case when ... fiscal_period = 'FY'` gate (D-02).
    - `return_on_assets` has NO sign-check guard (uses plain `safe_ratio`).
    - `l.gross_profit` and `l.ebit` are added to the "Base accounting inputs" select block.
    - `financial_derived.sql` is NOT modified (`git diff --name-only` does not list it).
    - `grep -c ' / ' models/gold/financial_factors.sql` shows no raw division added in the new factor lines (all division is inside macros).
    - `dbt test --select financial_factors` passes (all unit tests from Task 1 now GREEN, including the negative-equity ROE null and quarterly non-null assertions).
  </acceptance_criteria>
  <done>
    Six new factors are in the model; `safe_ratio_signed` guards ROE; roic passes through;
    no FY gate on the new columns; `dbt test --select financial_factors` is fully green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document the ROIC pre-tax simplification in gold.yml (D-03)</name>
  <files>infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml (the `financial_factors` model entry, currently lines 111-122 — only PK/grain columns have `columns:` entries today, none have a `description:`)
    - .planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-PATTERNS.md (section "gold.yml" — both valid approaches, Option 2 = column-level recommended)
    - .planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-RESEARCH.md (Pitfall 4 — no existing per-column description precedent; A2 assumption)
    - .planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-CONTEXT.md (D-03)
    - edgar_warehouse/parsers/financials_derived.py line 279 ONLY (read the existing ROIC simplification comment to mirror its wording — do NOT modify this file)
  </read_first>
  <action>
    Document the ROIC pre-tax simplification (D-03) in `gold.yml` under the
    `financial_factors` model. Use the column-level approach (RESEARCH.md recommendation,
    Option 2): add a new entry to the existing `columns:` list for `financial_factors` with
    `name: roic` and a `description:` explaining that this is a simplified pre-tax ROIC
    (EBIT / Average Invested Capital) that does NOT apply a NOPAT tax adjustment, because
    `financials_derived.py` has no parsed `income_tax_expense`/effective-tax-rate field
    today (reference `edgar_warehouse/parsers/financials_derived.py` line 279 as the source
    of this deliberate simplification). Do NOT add a `not_null` test to the `roic` column
    (roic is nullable). Do NOT modify `financials_derived.py` — only reference it in prose.
    Keep the existing `cik`/`accession_number`/`fiscal_period` column entries and the
    model-level `description:` unchanged. This is the explicit YAML-shape decision the plan
    makes (column-level, not model-level) so execution does not have to re-litigate it.
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && grep -A4 'name: roic' models/gold/gold.yml | grep -iq 'tax\|pre-tax\|NOPAT\|simplif' && uv run --with dbt-snowflake dbt parse 2>&1 | grep -iv error | tail -2 && echo GOLD_YML_OK</automated>
  </verify>
  <acceptance_criteria>
    - `gold.yml` has a `name: roic` column entry under the `financial_factors` model with a `description:` mentioning the pre-tax / NOPAT simplification.
    - The description references the absence of a parsed tax field (the reason for the simplification, per D-03).
    - The `roic` column entry has NO `not_null` test.
    - The model-level `description:` for `financial_factors` and the existing PK column entries are unchanged.
    - `financials_derived.py` is NOT in `git diff --name-only`.
    - `dbt parse` succeeds (gold.yml is valid).
  </acceptance_criteria>
  <done>
    `gold.yml` documents the ROIC pre-tax simplification at the column level under
    `financial_factors`; `dbt parse` validates; no Python/silver file touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None applicable. This plan extends a read-only dbt gold model with derived ratio columns
over data already materialized in `financial_derived` (sourced from already-validated SEC
XBRL filings). No external input, no authentication/session/crypto surface, no untrusted
data crossing any boundary, no new ingestion path.

## STRIDE Threat Register

ASVS level 1, block_on severity "high". Per 02-RESEARCH.md "Security Domain": this phase is
read-only financial-ratio computation in a dbt gold model with no input
validation/auth/session/crypto surface. The explicit determination is **no applicable ASVS
categories** — recorded here rather than omitted, per the security contribution guidance.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | N/A | financial_factors.sql ratio columns (SQL transform) | low | accept | No trust boundary crossed; ratios computed over already-validated XBRL data inside Snowflake. Division-by-zero is not a security threat but a refresh-stability concern, mitigated by mandatory use of safe_ratio/safe_ratio_signed (no raw `/`). |
| T-02-03 | N/A | gold.yml documentation | low | accept | Documentation-only change; no executable surface. No mitigation required. |

No package installs in this phase (`T-{phase}-SC` not applicable — RESEARCH.md "Package
Legitimacy Audit": zero changes to any dependency manifest).
</threat_model>

<verification>
- `dbt test --select financial_factors` passes — all unit tests green, including the negative-equity ROE null (D-01) and the quarterly non-null factor (D-02) assertions.
- `dbt parse` succeeds for the model, test yml, and gold.yml changes.
- `git diff --stat` shows ONLY files under `infra/snowflake/dbt/edgartools_gold/**` touched — no `financials_derived.py`, no `silver_store.py`, no loader/runtime file (Phase 2 success criterion #5; ROADMAP success criterion #5; D-03 / Pitfall 1).
- Note (CLAUDE.md "dbt gold model SQL changes — smoke test convention"): because `financial_factors` is a `materialized='dynamic_table'` model, deploying the SQL body change to Snowflake requires `dbt run --select financial_factors --full-refresh` (config-only diffing means a plain `dbt run` is a silent no-op for body changes). This deploy step is a phase-verification/handoff concern; unit-test verification above does not require the live warehouse deploy.
</verification>

<success_criteria>
- All five Phase 2 ROADMAP success criteria satisfied: (1) gross/operating/net margin via safe_ratio; (2) ROE/ROA via safe_ratio / safe_ratio_signed; (3) roic surfaced as pass-through; (4) unit tests cover negative net income / negative equity; (5) only gold-layer SQL touched.
- PROF-01, PROF-02, PROF-03 all consumer-queryable on FINANCIAL_FACTORS.
- D-01 (ROE nulls on negative equity), D-02 (all fiscal periods), D-03 (roic surfaced as-is + documented) all implemented and test-covered.
</success_criteria>

<output>
Create `.planning/workstreams/fundamental-factors-v2/phases/02-profitability-and-returns-factors/02-02-SUMMARY.md` when done.
</output>
