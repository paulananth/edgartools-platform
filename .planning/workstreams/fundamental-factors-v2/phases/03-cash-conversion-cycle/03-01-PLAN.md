---
phase: 03-cash-conversion-cycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/snowflake/dbt/edgartools_gold/macros/days_outstanding.sql
  - infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql
  - infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml
  - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml
autonomous: true
requirements: [CCC-01]
must_haves:
  truths:
    - "A consumer querying FINANCIAL_FACTORS receives a days_sales_outstanding column computed as accounts_receivable / revenue * 365 for every fiscal_period row (FY and quarterly alike)."
    - "days_sales_outstanding is null when accounts_receivable or revenue is null, or when revenue is zero — never a division error, never a spurious value."
    - "The days_outstanding() macro is reusable by DIO/DPO in Wave 2 (plan 02) without modification."
  artifacts:
    - "infra/snowflake/dbt/edgartools_gold/macros/days_outstanding.sql (new macro file)"
    - "days_sales_outstanding column in financial_factors.sql select list"
    - "days_sales_outstanding description block in gold.yml"
    - "DSO happy-path + null-guard dbt unit test cases in _financial_factors_unit_tests.yml"
  key_links:
    - "days_outstanding() macro -> invoked in financial_factors.sql for days_sales_outstanding"
    - "financial_factors.sql reads l.accounts_receivable / l.revenue (both already selected in the Base accounting inputs block) — no financial_derived.sql or silver change"
---

<objective>
Ship Days Sales Outstanding (DSO) to FINANCIAL_FACTORS: create a reusable `days_outstanding()` dbt macro and add a `days_sales_outstanding` column computed from the already-present `accounts_receivable` and `revenue` columns. Zero new silver fields, zero parser/Terraform change (per CONTEXT.md D-02: DSO ships in Wave 1 independent of the DIO/DPO coverage decision).

Purpose: DSO is the only universal CCC factor (applies to any company with credit sales regardless of inventory/COGS applicability) and doubles as a Beneish M-Score DSRI earnings-quality signal — so per D-03 it gets Wave 1 priority and the heaviest verification investment of the three factors.

Output: A new `days_outstanding.sql` macro, a `days_sales_outstanding` column in `financial_factors.sql`, a `gold.yml` doc block, and DSO dbt unit tests (happy-path + null-guard). The macro is the shared dependency Wave 2 (plan 02) builds DIO/DPO on.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-CONTEXT.md
@.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-RESEARCH.md
@.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-PATTERNS.md
@.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-VALIDATION.md
@infra/snowflake/dbt/edgartools_gold/macros/safe_ratio.sql
@infra/snowflake/dbt/edgartools_gold/macros/cagr.sql
@infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql
@infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the days_outstanding() dbt macro</name>
  <files>infra/snowflake/dbt/edgartools_gold/macros/days_outstanding.sql</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/macros/safe_ratio.sql — the exact null-guard shape to replicate (case / when balance is not null and flow is not null and flow <> 0 / then ... end). This is the structural analog per 03-PATTERNS.md.
    - infra/snowflake/dbt/edgartools_gold/macros/cagr.sql — the leading `--` doc-comment convention every non-obvious macro in this project carries (formula + design-decision + precedent citation). Match its comment density.
    - 03-RESEARCH.md Pattern 2 — the already-drafted macro body and the ending-balance-only rationale (matches this codebase's ROA/ROE "single-period approx" precedent, A4).
  </read_first>
  <action>
    Create a new macro file `days_outstanding.sql` in the macros directory. Define `days_outstanding(balance_col, flow_col, days=365)` returning a `case when {{ balance_col }} is not null and {{ flow_col }} is not null and {{ flow_col }} <> 0 then {{ balance_col }} / {{ flow_col }} * {{ days }} end` expression — the same null-guard shape as safe_ratio.sql (null when balance/flow is null or flow is zero), with an added `days` parameter defaulting to 365. Follow safe_ratio's guard shape per the phase_goal pattern-mapper correction: this is a SAME-PERIOD ratio like current_ratio/gross_margin. Do NOT copy cagr()'s strict-positivity (`> 0`) guard and do NOT wrap it in any fiscal_period='FY' gating — DSO/DIO/DPO are computed for every fiscal_period row, unlike CAGR which is FY-only by design. Add a leading `--` doc-comment block (cagr.sql style) stating: the formula (balance / flow * days), an example (DSO = days_outstanding('accounts_receivable', 'revenue', 365)), and the ending-balance single-period-approximation choice with a cross-reference to this codebase's ROA/ROE precedent and Phase 3 CONTEXT.md D-04 / RESEARCH.md A4.
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt parse</automated>
  </verify>
  <acceptance_criteria>
    - File `infra/snowflake/dbt/edgartools_gold/macros/days_outstanding.sql` exists and defines a macro named `days_outstanding` with parameters `(balance_col, flow_col, days=365)`.
    - The macro body contains the three-clause guard `is not null` (balance), `is not null` (flow), and `<> 0` (flow) — verify with: `grep -c 'is not null' infra/snowflake/dbt/edgartools_gold/macros/days_outstanding.sql` returns at least 2, and `grep -c '<> 0' infra/snowflake/dbt/edgartools_gold/macros/days_outstanding.sql` returns at least 1.
    - The macro body does NOT contain a `> 0` strict-positivity guard: `grep -c '> 0' infra/snowflake/dbt/edgartools_gold/macros/days_outstanding.sql` returns 0.
    - `dbt parse` succeeds (macro is syntactically valid Jinja and registered).
  </acceptance_criteria>
  <done>days_outstanding.sql exists, follows safe_ratio's null-guard shape with a days parameter, carries a cagr-style doc comment, and dbt parse succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add days_sales_outstanding column to financial_factors.sql and document it in gold.yml</name>
  <files>infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql, infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql — the "V2 profitability and returns factors (Phase 2)" block (lines ~130-136) and the existing `l.accounts_receivable` / `l.revenue` passthrough selects (lines 84, 95). Confirm both inputs are already in the select — DSO needs no new passthrough.
    - infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml — the `financial_factors` `columns:` doc blocks for the CAGR columns (lines ~129-188) to match description density and the decision-ID cross-reference convention.
    - 03-PATTERNS.md `financial_factors.sql` and `gold.yml` sections — the exact insertion style and the note that DSO needs NO fiscal_period='FY' wrapper.
  </read_first>
  <action>
    In `financial_factors.sql`, add a new comment block `-- V3 cash conversion cycle factors (Phase 3).` immediately after the existing "V2 growth factors: N-year CAGR (Phase 1)" block (after the `total_assets_cagr_5y` column at line ~162, before `l.parser_version`). Under it, add exactly one column for this plan: `{{ days_outstanding('l.accounts_receivable', 'l.revenue') }} as days_sales_outstanding,`. Use `revenue` (NOT cost_of_revenue) as the DSO flow denominator — this is the standard DSO formula per 03-RESEARCH.md Pattern 2. Do NOT wrap it in a `case when l.fiscal_period = 'FY'` gate (unlike the CAGR block) — DSO is a same-period ratio computed for every row, per the phase_goal pattern-mapper correction. Do NOT add DIO/DPO here — those are plan 02 (Wave 2).

    In `gold.yml`, under the existing `- name: financial_factors` -> `columns:` list, after the `total_assets_cagr_5y` entry (line ~188), add a `- name: days_sales_outstanding` entry with a `description: >` block matching the CAGR blocks' density: state the formula (accounts_receivable / revenue * 365), the ending-balance single-period approximation (not average of beginning+ending, consistent with the ROA/ROE precedent), and that DSO is universal across industries — the only CCC factor with near-100% coverage — cite Phase 3 D-03.
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt compile --select financial_factors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'days_sales_outstanding' infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql` returns at least 1, and the line invokes the `days_outstanding(` macro with `l.accounts_receivable` and `l.revenue` as arguments.
    - financial_factors.sql does NOT add a `days_inventory_outstanding` or `days_payable_outstanding` column in this plan: `grep -c 'days_inventory_outstanding\|days_payable_outstanding' infra/snowflake/dbt/edgartools_gold/models/gold/financial_factors.sql` returns 0.
    - The DSO line is NOT wrapped in a `case when l.fiscal_period = 'FY'` gate (inspect the surrounding lines; the DSO column is a bare macro invocation like current_ratio, not gated like the CAGR columns).
    - `grep -A6 'name: days_sales_outstanding' infra/snowflake/dbt/edgartools_gold/models/gold/gold.yml` shows a description block citing the formula and D-03.
    - `dbt compile --select financial_factors` succeeds and the compiled SQL (target/compiled/.../financial_factors.sql) contains `accounts_receivable / revenue * 365` (the expanded macro).
  </acceptance_criteria>
  <done>days_sales_outstanding column compiles into financial_factors via the days_outstanding macro using revenue as denominator, is ungated across fiscal periods, and is documented in gold.yml with D-03 citation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add DSO dbt unit tests (happy-path + null-guard)</name>
  <files>infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml</files>
  <read_first>
    - infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml — the `&factor_row_defaults` anchor (lines 10-36, which already sets accounts_receivable: 25 and revenue: 125) and the `financial_factors_complete_fy_ratios` expect block (lines 62-93) showing the `<column>: <value>` assertion style, plus the `financial_factors_quarterly_cross_year_factors_are_null` test (lines 95-138) showing the null-assertion style.
    - 03-VALIDATION.md Per-Task Verification Map rows 03-01-03 and 03-01-04 — the two DSO test behaviors this task must cover.
  </read_first>
  <behavior>
    - DSO happy-path: a FY row with accounts_receivable=30, revenue=150 produces days_sales_outstanding = 30.0 / 150 * 365 = 73.0.
    - DSO null-guard (null AR): a row with accounts_receivable=null (revenue non-null) produces days_sales_outstanding = null.
    - DSO null-guard (zero revenue): a row with revenue=0 (accounts_receivable non-null) produces days_sales_outstanding = null (the `<> 0` guard prevents division-by-zero).
  </behavior>
  <action>
    Add DSO assertions to `_financial_factors_unit_tests.yml`. The existing `financial_factors_complete_fy_ratios` test's second expect row (accession "0002", fiscal_year 2024) already has accounts_receivable=30 and revenue=150 in its given row — add `days_sales_outstanding: 73.0` to that expect row (line ~74-93 block) as the happy-path assertion (73.0 = 30 / 150 * 365, an exact terminating value; do not use a rounded approximation). Then add a NEW named unit test `financial_factors_dso_null_guards` (mirroring the structure of `financial_factors_negative_equity_nulls_roe`) with two given rows deriving from `<<: *factor_row_defaults`: one row overriding `accounts_receivable: null` (keep revenue non-null) and one row overriding `revenue: 0` (keep accounts_receivable non-null), each with a distinct cik/accession_number; assert `days_sales_outstanding: null` for both. Give the test a `description:` explaining it proves DSO nulls on null balance and on zero flow (D-03's heaviest-verification mandate for DSO). Do NOT add cost_of_revenue/accounts_payable keys here — those columns do not exist until plan 02.
  </action>
  <verify>
    <automated>cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt compile --select financial_factors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'days_sales_outstanding: 73.0' infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml` returns at least 1 (happy-path assertion present with the exact computed value).
    - A new unit test named `financial_factors_dso_null_guards` exists: `grep -c 'financial_factors_dso_null_guards' infra/snowflake/dbt/edgartools_gold/models/gold/_financial_factors_unit_tests.yml` returns at least 1.
    - The null-guard test asserts `days_sales_outstanding: null` for both an accounts_receivable-null row and a revenue-zero row (verify both `accounts_receivable: null` and `revenue: 0` appear within the new test block).
    - `dbt compile --select financial_factors` succeeds (fixtures parse; unit-test YAML is structurally valid). NOTE: live `dbt test` is expected to be blocked by the pre-existing dev SEC_FINANCIAL_DERIVED source-schema gap (see verification section) — compile success is the achievable bar for Wave 1.
  </acceptance_criteria>
  <done>DSO happy-path (73.0) is asserted in the complete-ratios test and a new financial_factors_dso_null_guards test asserts null on both null-AR and zero-revenue paths; dbt compile succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dbt model -> Snowflake dynamic table | SQL compiled from macros runs against warehouse-owned data; no external/untrusted input crosses here — inputs are already-vetted silver columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | days_outstanding() division-by-zero on zero revenue | low | mitigate | Macro's `{{ flow_col }} <> 0` guard nulls the result rather than erroring; covered by financial_factors_dso_null_guards test (Task 3) |
| T-03-02 | Information Disclosure | Malformed/extreme XBRL revenue producing an absurd DSO | low | accept | Same class of risk as every existing _pick()-derived numeric factor (gross_margin, current_ratio); no new mitigation — a data-quality follow-up if observed post-ship, not a security control (per 03-RESEARCH.md Security Domain) |

No package installs in this plan (dbt macros + SQL + YAML only) — no supply-chain threat (T-03-SC not applicable; 03-RESEARCH.md Package Legitimacy Audit: "Not applicable — this phase installs no new packages").
</threat_model>

<verification>
- Per task commit: `cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt parse` (macro task) / `dbt compile --select financial_factors` (model + test tasks).
- Per wave: `cd infra/snowflake/dbt/edgartools_gold && uv run --with dbt-snowflake dbt compile --select financial_factors`.
- `git diff --stat` must touch ONLY the 4 files in `files_modified` — no bronze/loader/silver/Terraform file (DSO is gold-SQL-only per D-02).
- **Environment caveat (carried from Phase 1/2, per 03-VALIDATION.md):** live `dbt test --select financial_factors` may be blocked by the pre-existing dev `SEC_FINANCIAL_DERIVED` source-schema gap (missing `current_assets` etc.). `dbt compile` success is the achievable verification bar for Wave 1; live `dbt test` is held-open per the Phase 1/2 precedent — do NOT mark the phase complete on compile-only without carrying the same explicit held-open caveat STATE.md uses for Phase 1/2.
</verification>

<success_criteria>
- `days_outstanding.sql` macro exists, follows safe_ratio's null-guard shape (no strict-positivity guard, no FY gating), and dbt parse succeeds.
- `days_sales_outstanding` column added to financial_factors.sql via the macro using revenue as the flow denominator, ungated across fiscal periods, compiling to `accounts_receivable / revenue * 365`.
- `days_sales_outstanding` documented in gold.yml with formula + ending-balance rationale + D-03 citation.
- DSO happy-path (73.0) and null-guard (null AR, zero revenue) dbt unit tests present; dbt compile --select financial_factors succeeds.
- No silver/parser/Terraform change (D-02: DSO ships gold-only).
</success_criteria>

<output>
Create `.planning/workstreams/fundamental-factors-v2/phases/03-cash-conversion-cycle/03-01-SUMMARY.md` when done.
</output>
