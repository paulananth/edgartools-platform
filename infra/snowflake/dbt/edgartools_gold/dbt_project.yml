name: edgartools_gold
version: 1.0.0
config-version: 2

profile: edgartools_gold

model-paths:
  - models

analysis-paths:
  - analyses

test-paths:
  - tests

seed-paths:
  - seeds

macro-paths:
  - macros

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  edgartools_gold:
    silver:
      +schema: "{{ env_var('DBT_SILVER_SCHEMA', 'EDGARTOOLS_SILVER') }}"
      +materialized: dynamic_table
    gold:
      +schema: "{{ env_var('DBT_GOLD_SCHEMA', 'EDGARTOOLS_GOLD') }}"
      +materialized: view
