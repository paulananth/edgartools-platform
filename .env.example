# Local Atlas Decision Projection (ticket 04). Gitignored via .env — this
# example is the only committed template. Never add these to GitHub Actions.
MONGO_SRV_HOST=
MONGO_PUBLISHER_URI=
MONGO_AGENT_URI=
